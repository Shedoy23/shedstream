# 21.09.2026 15:00 — Endorphine13 и AnalniyShekotyn

Статус: диагностика и исправление cooldown выполнены локально. По прежнему
указанию владельца ничего не выкатывалось и установленная DLL не менялась.

## Endorphine13 — создание королевства

Игровой журнал точно фиксирует заявку `e7017d0a55464b01a44a01ac228c7878`:

- 14:48:25 — `hero.create_kingdom`;
- герой найден;
- отказ: у клана нет ни одного владения, королевство было бы пустым.

Причина найдена в старом условии от 31.07: обработчик требовал хотя бы одно
поселение. Это расходилось с актуальным решением владельца — разрешить пустое
королевство. Условие удалено: `KingdomManager.CreateKingdom` вызывается и для
клана без феодов; цена 5 млн, лидерство клана и остальные проверки сохранены.

Отдельная ошибка интерфейса: backend запускал 20-минутный cooldown сразу после
постановки заявки, а `action.failed` возвращал плату/показывал причину, но
cooldown не снимал.

Исправление освобождает точный cooldown действия при любом подтверждённом отказе
игры, включая повторную доставку `action.failed`, `power.activate` и раздельные
ключи призыва по стороне. Отдельный исполняемый тест подтверждает создание
королевства без феодов, списание 5 млн, ACK успеха и публикацию состояния.

## AnalniyShekotyn — кинжал

Свежий журнал не подтверждает успешную перековку кинжала:

- 14:52:17 куплен `celtic_dagger` за 20 210 динаров в настоящий инвентарь отряда;
- 14:52:39 он надет в `weapon1`;
- 14:54:00 он снят из `weapon1`, то есть штатно возвращён в инвентарь отряда;
- строк `reforge_quality` для AnalniyShekotyn в журнале 21.09 нет.

Read-only production snapshot подтвердил текущее состояние: `Кинжал горцев`
(`celtic_dagger`) находится в настоящем инвентаре отряда, `source=party`,
`count=1`, без модификатора. Серверная история также не содержит
`hero.reforge_quality` для него: только покупка, надевание и снятие.

Есть более старый похожий эпизод: 17.09 предмет «Сакс» в `weapon0` реально
прошёл Common -> Fine -> Masterwork -> Legendary в 16:43:33–16:43:48. В 20:17
того же дня в `weapon0` был явно надет другой предмет. Но это была кампания
`save_id=stM2D376Kvhj`; текущая кампания — `save_id=zADs4xCeR8Op`. Старый «Сакс»
не обязан присутствовать в другом сохранении и к сегодняшнему `celtic_dagger`
не относится.

Текущая локальная реализация сохраняет вытесненный предмет с точным модификатором
в реальном инвентаре отряда; тесты подтверждают перековку одной вещи без создания
копии, возврат вытесненной экипировки и снятие точного предмета. Это доказывает
механику текущего кандидата. Данные игрока не менялись: сегодняшний кинжал уже
на месте, а старый «Сакс» принадлежал другому сохранению.

Проверки:

- `test_bannerlord_failed_action_cooldown.py`: exit 0;
- `test_action_ack_refund_order.py`: 36/36, exit 0;
- `CreateKingdomHarness`: королевство без феодов, exit 0;
- `EquipmentShopHarness`: 48 проверок, exit 0;
- `NativeInventoryHarness`: 16 проверок, exit 0.
