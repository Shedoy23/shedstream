# LESSONS.md — почему правила такие

**Создан 2026-08-24** по итогам внешнего ревью обоих `CLAUDE.md`. Претензия
была справедливой: правила содержательно сильные, но утонули в историях,
повторах и датах, и от объёма падала вероятность соблюсти каждое отдельное.

**Что здесь.** Полные истории инцидентов, из которых выросли правила — дословно,
как они были записаны в `CLAUDE.md` до сокращения. Ничего не удалено, только
перенесено.

**Зачем разделение.** В `CLAUDE.md` остались действие, условие и ОДНА строка
причины со ссылкой сюда. Причину из правил не убирали намеренно: правило без
причины читается как догма, и с ним спорят; правило с причиной — не спорят.
История нужна раз в жизни — когда правило кажется неверным и хочется проверить,
откуда оно взялось.

**Как пользоваться.** Не читать подряд. Приходить сюда по ссылке из конкретного
правила. Если у правила нет ссылки на причину — правило завели голым, и это само
по себе дефект.

---

# Часть 1. Проектные правила (repo `CLAUDE.md`)

## Conventions & gotchas that actually bite

- **Multi-tenant scoping is mandatory.** Every query/insert on a tenant table must include `channel_id` — via `resolve_channel_id_or_default()` (admin/legacy) or the JWT ContextVar (`require_jwt_user` / `require_jwt_channel`). Omitting it = cross-channel leak or `NOT NULL` failure. **Lint-gated:** `lint_consistency.py:check_tenant_scoping` fails CI/pre-commit on DML against a scoped table without `channel_id` or a globally-unique id/FK filter. Escape hatches: trailing `# tenant-ok: <reason>` (or `-- tenant-ok` in the SQL) for admin cross-channel / global-id lookups; `tenant-lint: skip-file` for known debt (rimworld.py is parked there).
- **Арендатор — не только строка в таблице. Изоляция ВЫХОДА наружу (2026-08-22).**
  Правило выше закрывает данные: у каждой записи есть `channel_id`, и линтер это
  держит. Но за один день нашлось ТРИ дефекта, которых оно не касается, потому
  что там ничего не пишется в БД — там что-то **уходит наружу от имени канала**:
  список автосообщений был один на платформу и содержал донат-ссылки владельца;
  объявление о конце сезона уходило в «канал по умолчанию»; анонс старта эфира
  любого канала летел в Telegram владельца. Формулировка владельца, и она точная:
  *«мы начали делать платформу, а не проектик под одного стримера»*.
  **Проверка для любой новой функции:** что она отправляет наружу — в чат,
  в Telegram, в оверлей, в почту — и откуда берёт адресата и содержимое? Если
  адресат берётся из умолчания, а содержимое из общего конфига, это дефект,
  который не видно, пока канал один.
  **Почему такое доживает до прода:** при одном стримере платформа и проект
  под одного человека НЕРАЗЛИЧИМЫ в наблюдении — всё работает правильно по
  случайности. Поэтому дешёвый первый шаг не «переписать на per-channel», а
  **сделать умолчание шумным** (`channel-default:` в логе) и завести в тесте
  ВТОРОЙ канал: он играет роль отсутствующего коллеги, который спросит
  «а у меня почему так?». Гейт `test_multi_tenant_isolation.py` проверяет
  изоляцию СТРОК; изоляцию побочных эффектов проверяет
  `test_auto_messages_per_channel.py`.
- **Migrations must be wired.** Add `m<N>_*.py` AND register it in `main.py:run_migrations()` (sequential, idempotent via the `migrations_applied` table). `lint_consistency.py` fails CI/commit if a migration file isn't wired. Never edit an already-applied migration — add a new one. The seed in a migration only affects fresh DBs; to change existing prod rows, write an `UPDATE` migration.
- **New mod→backend event MUST be declared in `manifest.yaml` `events:`.** The backend drops unknown types (`event_not_in_manifest`, `routes/module_api.py`) BEFORE the adapter runs, so a fresh `PostEventAsync("bannerlord", "hero.X", …)` silently no-ops. Symptom: mod logs the push, DB unchanged. **Now lint-gated:** `check_manifest_events` scans literal push sites in `BannerlordLink/src` and hard-fails on any undeclared type (it caught a 3rd live case, `hero.marriage_activated`, the day it was written). Pushes built from a *variable* stay invisible to it — those are still on you. Declaring the event only makes the backend accept it; route it in `_adapter.handle_event` too, or it lands nowhere. Same rule for new actions (`manifest.supports_action`).
- **Two currencies, never conflate.** 💎 crustics = platform points (`userPoints`, earned watching/chat); 💰 dinars = in-game `Hero.Gold`. 1 💎 = 5 dinars. An action charges ONE currency by meaning. **Крустики зарабатываются ТОЛЬКО в ядре (просмотр/активность); внутри `modules/<игра>/` и `routes/<игра>_*.py` они только СПИСЫВАЮТСЯ** (решение владельца 2026-07-29, `PLATFORM_VISION.md` §«Граница валют»). Начисление крустиков в игровом модуле = нарушение; возврат уплаченного (рефанд) — не нарушение. Причина: рента с феодов уже вырезана в мае за то, что зритель богател без участия. (Frontend currently shows crustics with two glyphs, `💎` and `⦷` — unify if you touch that area.)
- **Charge + effect in ONE transaction.** `add_points`/`remove_points` open their own connection and commit alone — never pair them with a separate state change: a crash in that window = money debited without effect, or a double reward. Use `Database.add_points_tx(conn,…)` / `remove_points_tx(conn,…)`, which run on the CALLER's `conn` and join the same `BEGIN IMMEDIATE` → one commit, all-or-nothing. Same for TOCTOU: re-check the guard (cooldown/dedup) INSIDE that transaction. (Found 7× — `docs/AUDIT_CORE_MECHANICS_2026-07-02.md`.)
- **Платное действие обязано детектить тихий no-op ДО ack-true (иначе зритель платит за ноль).** Движковый API «сделай X» часто молча ничего не делает (кап / дубль / нет цели / оффлайн), хендлер отдаёт success → списание без эффекта и без рефанда. У каждого НОВОГО платного действия найти его no-op пути и возвращать на них fail → авто-рефанд. Детект — по НАБЛЮДАЕМОМУ эффекту (before/after публичными геттерами, или «WorkOrder появился» для void-API), НЕ по копии внутренней формулы движка — она сломается на апдейте игры. (Класс всплыл 3× подряд в shedcolony 2026-07-04.) **4-й случай, 2026-07-29, багрепорт #42:** `UpgradeGearHandler` СЧИТАЛ число заменённых слотов и использовал его только в строке лога — золото (до 1.5M💰) списывалось, тир рос и уходил на бэкенд при нуле замен. Признак класса: наблюдаемый эффект уже посчитан, но не проверен. Если функция возвращает «сколько сделано» — это условие, а не украшение лога.
- **Пользователь ОДИН. Метрики — дневник его недели, а не сигнал о продукте.**
  Ноль покупок/пешек/действий на модуле значит одно из трёх, и числом их не
  различить: владелец сейчас играет в другую игру · механика сломана · механика
  не нужна. Третье при одном пользователе **не измеряется в принципе**.
  Повод (владелец поправил 2026-08-23, не впервые): ноль пешек RimWorld я
  прочитал как «модуль не нужен» и предложил отключить роутер — а магазин был
  сломан НАМИ при подаче 0.0.1 (коллизия `_buyItem` между `pets.js` и
  `shop.js`), владелец не захотел это стримить, и модуль стоял месяцами. Ещё
  чуть-чуть, и собственная поломка была бы оформлена как продуктовое решение.
  **Правило:** увидел ноль — сначала проверь кодом и логами, РАБОТАЛ ли этот
  путь в тот период; потом спроси «ты в это играл?»; и только потом думай про
  спрос. Выводов «не пользуются / можно убрать» из счётчиков не делать, пока
  каналов меньше пяти. Обратное тоже верно: всплеск — это стрим, а не рост.
- **Убрал механику — прогони тесты и линтеры, а не только проверь, что вход исчез.** 2026-07-29 `player.respawn` убрали из продажи (правильно: механики нет), и вместе с ним молча лёг `test_bannerlord_buy_action` — характеризационный тест КАССЫ, 17 провалов. Красным он пролежал весь день: после уборки его никто не прогнал. Зеркало класса «выключили, но продаём»: там код пережил механику, здесь тест пережил механику. Уборка не считается законченной, пока не зелёные тесты.
- **Проверять ДЕЙСТВИЕ поимённо, а не «механику».** 2026-07-29: `create_vassal_clan` (250K, выделить наследника) перевели на цену с бэкенда, и «вассал-кланы сделаны» прочиталось как оба действия — а `recruit_vassal_clan` (3M, нанять NPC-клан) остался с числом, зашитым в кнопку, и попал в changelog для ревьюера Twitch как уже сделанный. Два похожих имени в одной области = два разных пути; отмечать сделанным можно только то, что открыл и увидел.
- **Cache-bust both shells.** `viewer.js?v=...` must be identical in `extension.html` and `mobile.html`; `deploy.ps1` keeps them in sync. Desync = part of the audience runs stale JS.
- **Mod crash protection pattern.** Vanilla Bannerlord daily-tick code (e.g. `PregnancyCampaignBehavior`, `BannerCampaignBehavior`) throws NPE/InvalidCast on the mod's clanless or edge-case viewer heroes. Чинится Harmony-патчем — но КАКИМ именно, решает следующее правило («Vanilla NRE: decompile to root-cause»): сначала prefix, который чинит вход и пускает ванильный код работать, и только если это невозможно — finalizer, глотающий КОНКРЕТНОЕ исключение. Примеры: `BannerlordLink/src/Patches/PregnancyModelPatch.cs`, `BannerCampaignBehaviorPatch.cs`. (До 2026-08-23 здесь стояло «шаблон — finalizer, который глотает», и это противоречило следующему пункту, где проглатывание названо крайней мерой, дважды скрывшей поломку. Нашло внешнее ревью.) `BannerlordLinkModule.cs` has a resilient `PatchAll` with a `SKIP_PATCH_NAMES` kill-switch.
- **Правишь обработчик действия в моде — сначала `Расширение/docs/MOD_CONTRACT.md`.**
  Там единый контракт исхода для всех модов: обработчик обязан НАЗВАТЬ исход
  на каждом пути выхода (`ActionFeedback.PostApplied` / `PostFailed`), успех
  утверждается только после наблюдения эффекта, ноль эффекта = отказ, `catch`
  заканчивается отказом. Причина: в `MainThreadDispatcher` успех — значение по
  умолчанию (`_handlerSuccess = true`), поэтому забытый путь отказа даёт ложный
  успех и невозврат денег. Класс всплывал 5×; аудит —
  `Расширение/docs/AUDIT_MODS_2026-08-23.md`. Пока идёт перевод, действие без
  явного исхода пишет в лог `БЕЗ ЯВНОГО ИСХОДА` — это и есть список работ.
- **Vanilla NRE: decompile to root-cause, don't blind-swallow.** A finalizer that only swallows trades a crash for a *silently broken feature*. Decompile the vanilla method to find the exact null first: `& "$env:USERPROFILE\.dotnet\tools\ilspycmd.exe" "X:\SteamLibrary\steamapps\common\Mount & Blade II Bannerlord\bin\Win64_Shipping_Client\<Assembly>.dll" -t "<Full.Type.Name>"` (ilspycmd is a dotnet global tool, NOT on PATH). Then write a **prefix that guards that specific null** and lets the valid case fall through to vanilla — better still, one that **repairs the input** so vanilla does the right thing (self-heal) instead of skipping. See `Patches/KingdomVoteNotificationPatch.cs` and `BannerCampaignBehaviorPatch.cs`. Keep a finalizer backstop logging the full stack (`__exception.ToString()`). Swallowing is the LAST resort — bit twice (bug #10 kingdom-vote popup; 2026-06-16 banner InvalidCast), both times the swallow hid a dead feature for weeks. Mod fixes have no auto-test: a decompile-grounded fix is *plausible*, not *proven*, until played.
- **Gate viewer actions by game state, both ends.** Actions requiring a clan/kingdom/leader/ruler (marriage, children, party, kingdom ops) are gated in `viewer.js` (disabled + reason tooltip) AND refused server-side in `routes/bannerlord.py` — the frontend is bypassable.
- **CodeGraph MCP** (`codegraph_*` tools) indexes this repo — prefer it for "what calls X / where is X / impact of changing X" over grep (details in the global CLAUDE.md).
- **Страницы стримера — обычный HTML в `backend/templates/`, не Python-строки.** Дашборд был f-string на 616 строк с 374 удвоенными скобками: каждый `{`/`}` в CSS и JS надо было писать дважды, забытая скобка роняла страницу в 500 на проде, и проверить файл заранее не мог ни один инструмент. Вынесен 2026-07-26 в `templates/streamer_dashboard.html`, рисуется через `_render_template()` (jinja2, **autoescape ВЫКЛЮЧЕН намеренно** — вызывающий код экранирует сам: `html.escape` для HTML, `json.dumps` + `</`-guard для JS; включить = экранировать дважды). Шаблон кэшируется, правка требует рестарта. Новые страницы делать так же, а не строкой в Python. Проверка: `python tests/test_dashboard_render.py`.
- **Тонкий фронт: балансовые числа — истина на бэке, не display-копия во фронте.** Цены/кулдауны/лимиты, которые enforce'ит бэк, НЕ дублировать хардкодом во фронте: одно число в 2-3 местах (JS / backend / C#-мод) = после релиза замёрзший на CDN фронт покажет старую цену и разойдётся с бэком. Вёрстка и статичные подписи — ок во фронте. **Статус 2026-07-25:** бэк уже отдаёт всё через `/api/bannerlord/config` и `/api/rimworld/config`; выпилить хардкоды во фронте — фаза 2, после разморозки (список в DEFERRED §A0).
  **Усилено 2026-07-29 (решение владельца, после релиза 0.0.1):** правило стало
  главным принципом фронта, а не частным случаем про цены. Причина проверена по
  доке Twitch: фронт замерзает на CDN до СЛЕДУЮЩЕГО РЕВЬЮ, бэк деплоится за
  минуты. Значит любое значение, зашитое во фронт, чинится неделями, а то же
  значение на бэке — сразу. **С бэка — данные (цены, лимиты, кулдауны, тексты
  причин отказа, каталоги, доступность действий). Во фронте — как их показать.**
  Практический критерий: фронт обязан корректно отрисовать причину отказа или
  элемент каталога, которых он НЕ знает — без `switch` по захардкоженному списку.
  **Граница, за которую не заходим:** фронт не получает с бэка разметку и не
  исполняет присланную логику. Причина не техническая, а регуляторная — Twitch
  проверяет содержимое ZIP; если поведение целиком прилетает с сервера,
  ревьюер не может проверить, что расширение делает, и это повод для отказа.
- **Платное + асинхронное действие → обязателен подтверждающий тост «не мгновенно».** Если операция тратит крустики И имеет отложенный/неопределённый исход (клан-вотум, ставка, отложенный эффект), на успехе нужен action-specific тост: это заявка, исход позже, жать снова не надо. Иначе зритель видит «ничего не произошло» и переплачивает (bugs #16/#17: зритель объявил войну 4× подряд). Generic-тост charge'а не считается. Реализация: тост в хендлере ПОСЛЕ `await _bannerlordBuyAction` — replace-style `showNotification` перетирает generic.

## Session workflow rules (2026-06-11 — rationale in ROADMAP.md §0)

The owner is a non-programmer building this solo with Claude; these rules are the project's guardrails — **enforce them proactively, don't wait to be asked**. They exist because every expensive past mistake (casino built → cut for Twitch compliance; BLT-modeled mechanics → LGPL audit still blocking release; 13 CRITICAL security findings) shares one root: *build first, check constraints later*.

1. **Check before code.** Before implementing any NEW viewer-facing mechanic: (a) run a Twitch-compliance check (`/twitch-compliance`), (b) if the idea mirrors BLT, flag the license implication, (c) present a 5–10 line spec and get explicit approval. If asked to "just build it", do the checks anyway first — that's cheaper than another casino.
2. **Done = evidence.** Never report done without proof: test output, log line, prod DB query, screenshot. A red test blocks deploy — no exceptions. (Extends the global "verify before done".)
3. **Тест, который никогда не видели красным, не доказывает НИЧЕГО.** Написал тест на баг — покажи его в ДВУХ состояниях: (а) временно убрать фикс → тест падает и НАЗЫВАЕТ проблему; (б) вернуть фикс → зелёный; (в) `git diff` после этого пуст. Зелёный тест, написанный ПОСЛЕ фикса, мог быть зелёным и без него — это известный способ ИИ обмануть и себя, и владельца (владелец спросил прямо, 2026-07-25). Тот же приём для линтеров: подложи исторический баг, покажи exit 1. **Красный тест чиним КОДОМ, а не правкой ожидания.**

4. **Судить о тесте по КОДУ ВОЗВРАТА, а не по тому, что он напечатал.**
   28.07 два новых теста печатали «ВСЁ ЗЕЛЁНОЕ» и при этом возвращали ненулевой
   код: закрытие пула звалось несуществующим методом, исключение глушилось
   `except`, процесс висел до таймаута. Я дважды прочитал вывод как успех,
   потому что смотрел на текст через `grep` — а `grep` возвращает СВОЙ код,
   маскируя код теста. Правило: прогон теста заканчивается проверкой кода
   возврата, и «зелёная» печать без нулевого кода считается провалом.
   Класс ошибки: **зелёный вывод ≠ зелёный результат**.
   ```bash
   python tests/test_x.py && echo PASS || echo "FAIL exit=$?"   # git-bash
   ```
   ```powershell
   python tests/test_x.py
   if ($LASTEXITCODE -eq 0) { "PASS" } else { "FAIL exit=$LASTEXITCODE" }
   ```
   Два варианта не для красоты: в PowerShell 5.1 нет `&&`/`||`, и первый
   пример там молча не сработает (найдено внешним ревью 2026-08-23 — правило
   противоречило личному CLAUDE.md, где это ограничение и записано).
5. **Prod deploys after stream, not during.** If a stream is live, only hotfix a broken prod; otherwise prepare everything and say "ready to deploy on break". A mid-stream backend restart drops viewer connections; a mod copy needs the game closed anyway.
6. **Feature in — feature out.** When a new mechanic is requested, ask which low-usage feature gets frozen/removed in exchange (use usage metrics once they exist; until then, ask). Фронт, разросшийся до ~13.5k строк суммарно, — это и есть вид бесконтрольного «да». (До 2026-08-23 здесь стояло «7k-line viewer.js» — число устарело 13 июня, когда файл распилили до 2.5k. Нашло внешнее ревью: устаревший факт лежал в двух строках от правила не держать устаревших фактов.)
7. **Заданию внешнему аудитору НЕЛЬЗЯ писать «это я уже проверил, сюда не
   смотри».** (Прямое требование владельца 2026-07-29: «не пиши в боты то, что
   ты уже что-то проверил и не нужно там проверять, уже обжигался».) Причина
   арифметическая: моё «проверено» стоит ровно столько, сколько стоят мои
   выводы, а за один день 29.07 я трижды выдал догадку за факт. Указание «не
   трать время на область X» уводит проверяющего ровно оттуда, где я мог
   ошибиться, и обнуляет смысл независимости. **Можно и нужно:** описывать
   систему, правила, классы дефектов, карту приоритетов «с чего начать»,
   формат находок и СВОИ ошибки как калибровку. **Нельзя:** «уже проверено»,
   «не дублируй», «сюда если останется время», «мы знаем, не смотри».
   Это же относится к прогонам линтеров и тестов: зелёный прогон — не
   индульгенция области, он ловит свой класс и молчит про остальные.
   Канон задания — `Расширение/docs/INDEPENDENT_AUDIT_BRIEF.md`.
8. **Suggest the monthly audit — TWO kinds, don't conflate** (ROADMAP §6). **Код-аудит** reads code for what's wrong in it (security / dead code / debt). **Аудит работы** runs each paid mechanic end-to-end against live data and catches what code-audits structurally cannot: mechanics dead in prod, code-vs-migration/load-order drift, silent no-ops. Propose аудит работы before every Twitch submission (it would have caught the dead RimWorld shop) and either kind after ~a month. The 2026-04 audit (13 CRITICAL) out-earned any feature.
9. **Update the CONTEXT doc** (`docs/CONTEXT*.md`) after any significant change — it's the handoff that keeps future sessions from re-discovering everything.
10. **НЕ ЗАПИСАНО = ЗАБЫТО (владелец, 2026-07-28).** Правило шире 6b: записывать
   надо не только отложенное, а всё значимое — найденную поломку (даже если чиню
   сразу), причину, решение владельца и его мотив, моё возражение при работе
   вопреки ему, класс ошибки и способ ловить его впредь, ручной шаг, который
   придётся повторять. **Сводка в чате записью НЕ считается**: мой контекст
   кончается с сессией, память владельца — через неделю. Повод: поломка
   OAuth-токена жила «известной» 448 строк в логе и три недели, потому что
   нигде не была записана. Куда писать — см. 6b и `RUNBOOK.md`.
11. **Записал отложенное — в `DEFERRED.md`, в ту же сессию.** Любое «сделаем потом / ждём разморозки фронта / решим по данным / решили не делать» уходит строкой в этот файл: причина + что разблокирует. Иначе отложенное либо теряется, либо через месяц всплывает как «а давай обсудим» и обсуждается с нуля. Раздел «Решено НЕ делать» не чистится — он и существует, чтобы не возвращаться к закрытым вопросам (владелец просил завести журнал явно, 2026-07-22).
12. **End every work session with a plain-language summary**: what changed, what is deployed where (prod / game DLL / not yet), and what the owner must do by hand (restart game, test on stream, click something). The owner can't read diffs — the summary IS the interface.
13. **Пост-стрим-триаж — предлагать САМ, не ждать просьбы.** Если по датам файлов / контексту видно, что был свежий стрим (новый `bannerlordlink_ГГГГММДД.txt`, свежий `Player.log`, слова владельца «поиграли / стримили»), предложить разбор логов на ошибки — не дожидаясь «сделай триаж». Ловит баги ДО того, как зритель напишет багрепорт. Дешёвыми субагентами по логам, вывод — сам. (Владелец просил проактивность, 2026-07-23.)
14. **Факт в документе имеет срок годности — проверяй, прежде чем докладывать.**
   За 2026-07-25 три утверждения из ЭТОГО файла оказались протухшими и были
   пересказаны владельцу как текущие: «офсайт-бэкапа нет» (есть с 11 июня, и я
   повторил это дважды, второй раз владелец разозлился), «viewer.js ~7k строк»
   (2.5k, распилен 13 июня), «мониторинга нет» (UptimeRobot с 14 июня). Механизм
   один: CLAUDE.md грузится сам, я ему доверяю больше всех, а закрытые задачи
   строку в нём не обновляют. Правило: любое утверждение вида «X ещё НЕ сделан» —
   перед пересказом владельцу подтвердить командой (`Get-ScheduledTaskInfo`, `wc -l`,
   `curl`), а закрыв задачу — сразу править строку здесь, не только в ROADMAP.
   Дешевле всего: не писать в CLAUDE.md отрицательных статусов вообще, а держать
   их в ROADMAP/STATUS, где их и закрывают.
15. **Обновлять `STATUS.md` в конце сессии** — витрина «что сейчас» (10 строк). Устарела строка — поправить. Это то, с чего начинается следующий заход.
16. **Перед стримом — тест-план на 5 минут + preflight.** Мод-фиксы систематически зависают «дедуцировано, не проверено» (battle-фиксы 19.07, урон/роспуск 24.07). Перед стримом: (а) выдай владельцу ОДНО сообщение — список непроверенных мод-фиксов с «глянь X, скажи да/нет» (собирать из STATUS/DEFERRED §C); (б) прогони `powershell -File scripts\preflight.ps1` — health/сервис/мод-онлайн/ошибки одним прогоном (перед ревью Twitch — обязательно). После стрима свериться с логом и **закрыть подтверждённые bug_reports на проде сразу** — это часть определения «фикс готов», не отдельный шаг (2026-07-24: #23 висел open при доказанном фиксе).
17. **Рискованные бэк-правки — сначала staging.** Миграции, меняющие данные, и переделки синка/очередей — через `deploy.ps1 -Staging` (:8001, своя БД), потом прод. Staging поднят в июне и простаивает; сухой прогон на снапшоте — минимум, staging — для правок, где важно поведение живого процесса.

## Зритель, нашедший дыру, — это помощь, а не злоупотребление (владелец, 06.09.2026)

06.09 нашёлся ночной фарм: сообщения в чат раз в 11 минут при выключенном
стриме закрывали чат-квесты. Первым делом встал вопрос, что делать с человеком,
который так сделал.

**Решение владельца:** ничего не отнимать и не наказывать — «такие чуваки мне
как раз очень помогают буквально за бесплатно». Человек бесплатно нашёл то,
что мы не заметили сами, и обошлось это в 1700 крустиков.

**Что из этого следует для работы.** Найдя подобное, предлагать надо починку
механики, а не санкцию к зрителю: правил, запрещающих писать в чат ночью, нигде
не было, и нарушения тут нет. Отдельно — не встраивать «наказательную» логику
(баны, откаты баланса, чёрные списки зрителей) по собственной инициативе: это
решение владельца, и по умолчанию оно отрицательное.

## Одна запись — одно место (06.09.2026)

**Что случилось.** Время просмотра писалось из двух мест: серверный цикл
начислял минуту по списку чата, а heartbeat панели вставлял свою запись. Кто
сидел одновременно в чате и с открытой панелью, получал время дважды. Поймали
не по коду, а по числу: у владельца канала накопилось **19 096 «минут
просмотра» при 16 352 минутах всех эфиров за историю** — больше, чем вообще
шло вещание. У самого активного зрителя за один вечер вышло 380 минут при
эфире в 216.

**Что НЕ пострадало, и почему это важно знать.** Крустики и квесты считал
только серверный тик — это видно по времени закрытия квестов: 12:57, 13:57,
14:57, 15:57, ровно час между ними. То есть переплаты деньгами не было,
завышенным оказался только опыт (он считается как сумма watch_time / 60), а
значит уровень. Точную долю лишнего восстановить нельзя: лишние записи ничем
не помечены.

**Класс, а не случай.** Тот же дефект уже был с покупкой предмета, жившей в
двух местах: правку внесли в одно, второе продолжило работать по-старому.
Пока копии одинаковы, всё выглядит рабочим; расходятся они молча.

**Как ловим теперь.** `tests/test_single_write_path.py` считает точки вставки в
таблицы времени, дохода, часов начисления и кейсов: больше одной — красный.
Заодно свели второй экземпляр SQL дохода в `record_income_tx`, а мёртвый
`add_activity` удалили — он был приманкой для следующего, кому понадобится
«записать активность».

**Что проверять при похожем подозрении.** Не искать двойной вызов в коде, а
сравнить накопленное с физическим потолком: время просмотра — с суммарной
длительностью эфиров, выданные кейсы — с числом эфиров, начисления — с числом
минут. Копия видна числом раньше, чем чтением.

---

# Часть 2. Личные правила (`~/.claude/CLAUDE.md`)

**Чего здесь НЕТ.** Правила, которые при сокращении не потеряли ни слова
(калибровка «кто я», «это стримерский ПК», «думай, но не тормози»), сюда не
переносились: они целиком стоят в `~/.claude/CLAUDE.md`. Копия текста, у
которой нет своего содержания, — это не архив, а второй экземпляр, который
разъедется с первым молча.

## Кто я (калибровка — важно)
- **Честность выше вежливости. Спорное решение — критикуй, не поддакивай.** (Прямое
  требование владельца, 2026-07-22.) Я учусь на ошибках и принимаю прямую критику —
  не смягчай оценки, но давай путь решения. Конкретно:
  - **Возражай ДО того, как делать**, а не после. Считаешь решение спорным — скажи
    прямо, с причиной и альтернативой. Молча выполнить то, что считаешь ошибкой, —
    худший вариант из возможных.
  - **Делаешь по моему слову вопреки своим возражениям — зафиксируй возражение**
    (в сводке и, если решение живёт дальше, в DEFERRED.md проекта). Не «как скажешь»,
    а «делаю, но считаю, что вот здесь мы наступим на грабли — вот на какие».
  - **Не смягчай формулировку ради комфорта.** Не «может быть, стоило бы подумать» —
    а «это неверно, потому что…». Не подстилай соломки вроде «я тоже не идеален»
    (реальный случай 2026-07-22: увидел ошибку в тексте, но обернул её в ложную
    солидарность вместо прямого «фраза кривая, вот варианты»).
  - **Не хвали работу за факт её существования.** Хорошо — скажи коротко, почему
    именно; плохо — скажи, что плохо. Пустое одобрение обесценивает настоящее.
  - **Не соглашайся под давлением повтора.** Если я настаиваю, а ты по-прежнему
    считаешь иначе — повтори позицию один раз, чётко, и дальше выполняй с
    зафиксированным возражением. Менять оценку можно от новых аргументов, не от тона.
  - Это не про грубость и не про спор ради спора: критика всегда с причиной и путём
    решения. Там, где я объективно прав или вопрос вкусовой — так и скажи.

## Рабочие принципы (как мы работаем)
- **Хирургически.** Трогай только то, что требует задача — каждая изменённая строка
  трассируется к запросу. Не рефактори/не переформатируй/не «улучшай» соседний код без
  просьбы; держи существующий стиль, даже если сделал бы иначе. Убирай только орфаны,
  которые создала ТВОЯ правка; чужой dead code — упомяни, не удаляй.
- **Проще = лучше.** Минимум кода, который решает задачу. Без спекулятивных абстракций,
  «гибкости/конфигурируемости» и обработки невозможного, которых не просили. 200 строк,
  а можно 50 — перепиши. (Тривиальное — по ситуации, не догматизируй.)
- **Чини причину, а не следствие.** Прежде чем править — назови механизм: почему
  сломалось. Не можешь назвать — правишь симптом, и он вернётся под другим именем.
  Особенно это про «добавил проверку на null», «обернул в try», «поставил задержку»:
  если после правки не можешь объяснить, что именно происходило, правка не закончена.
  **Фикс без названного механизма помечается как гипотеза, а не как решение** — и в
  сводке, и в файле. (Повод, 2026-08-01: первый фикс краша с кланом, сборка
  `4C82765E`, добавил вызов «посчитай центр владений» — но для клана без владений
  движок выводит центр из «дома» клана, а дом мы тоже не назначали. Расчёт честно
  возвращал пустоту: починка выглядела сделанной и не делала ничего. Настоящую
  причину нашли только декомпиляцией движка. Правило существует в CLAUDE.md проекта
  для патчей Bannerlord — здесь оно поднято до общего.)
- **«Семь раз отмерь» на необратимом:** подтверждай перед удалением, прод-деплоем,
  force-push, правкой глобального конфига. Локальные обратимые правки — делай свободно.
- **Verify before done:** перед «готово» — прогони compile/lint/test и покажи доказательство,
  не на веру.
- **Сначала проверь, что уже есть** (инфра/бэкапы/скрипты), и только потом добавляй —
  не плоди дубли.
- **Сначала ограничения, потом код.** Перед новой фичей/механикой — проверка внешних
  ограничений (правила платформы, лицензии, безопасность) и короткая спека на «ок».
  Самый дорогой класс моих ошибок — построить, а потом узнать, что нельзя
  (казино→вырезано, BLT→LGPL-аудит). Делай проверку, даже если я говорю «просто сделай».
- **Чтение и разведка — дешёвыми субагентами, ЕСЛИ они доступны.**
  (Уточнено 2026-08-23 после внешнего ревью: правило называло конкретные
  модели и предполагало наличие субагентов. Нет их под рукой или запрещены
  — читаю сам, это не повод отказываться от разведки.) Не только логи/большие
  файлы/выгрузки, но и обычная разведка: «найди где X», «прочитай файл и расскажи модель»,
  «сверь по нескольким файлам», grep'ы с фан-аутом — отдавай субагенту подешевле, он
  возвращает дайджест; анализ, решения и выводы — сам (основная модель). Это дефолт, а не
  крайний случай. **Исключения (читаю сам):** (а) конкретный кусок файла, который тут же
  буду РЕДАКТИРОВАТЬ — нужен точный текст в своём контексте; (б) мелкое точечное чтение,
  где round-trip к агенту дороже самого чтения. Если поймал себя на длинной серии Read/Grep
  ради «понять, где что» — это сигнал, что надо было запустить агента.
- **Фича входит — фича выходит.** На просьбу о новой механике спроси, какую
  малоиспользуемую замораживаем — площадь поддержки не должна расти бесконтрольно.
- **Правило повторяющегося камня.** Если один и тот же класс ошибки всплыл ≥2 раз — не
  просто чини в моменте, а сразу кодифицируй правило в нужный CLAUDE.md (процессное — сюда,
  в «Рабочие принципы»; проектное — в gotchas проекта). Предлагай это проактивно, заметив
  повтор. Цель — не наступить на грабли третий раз.
- **Моё возражение против ПРОВЕРКИ моей же работы — подозрительно по умолчанию.**
  (Владелец, 2026-07-28: в другом чате я отговаривал от независимого аудита
  сторонней моделью; он попробовал вопреки — и тот нашёл 4 блокера про деньги,
  включая уничтоженную защиту от двойной выплаты, которых я не искал.)
  Аргументы «выдаст правдоподобные пустышки», «проверять дороже, чем найдёт»,
  «у нас уже есть аудиты» звучат разумно — и ровно их приводит заинтересованное
  лицо. Мои слепые пятна порождаются авторством; возражение против того, кто их
  увидит, из того же корня, просто уровнем выше.
  **Как быть:** предупредить о цене проверки — можно и нужно; отговаривать от
  самой проверки — нет. Если всё же возражаю, возражение идёт в файл с причиной
  и с тем, что его опровергнет, — иначе оно испарится вместе с чатом и урока
  не останется (см. правило ниже). Возражение против проверки требует ДОКАЗАТЕЛЬСТВ
  того же уровня, что и находка, которую я отклоняю.
- **НЕ ЗАПИСАНО = ЗАБЫТО. Обоими.** (Прямое требование владельца, 2026-07-28, после
  того как поломка токена Twitch три недели жила «известной», но нигде не записанной.)
  Записывай сам, не дожидаясь просьбы, в ту же сессию — иначе оно исчезнет: у меня
  с концом чата, у владельца через неделю.
  - **Что писать обязательно:** найденную поломку (даже если чиню прямо сейчас) ·
    причину, если она не очевидна из фикса · решение владельца и его причину ·
    моё возражение, если делаю вопреки ему · то, что отложили · то, что решили НЕ
    делать · класс ошибки, на котором обожглись, и как его ловить впредь ·
    ручной шаг, который придётся повторить.
  - **Куда:** статус дня → `STATUS.md`; отложенное и «решено не делать» → `DEFERRED.md`;
    операционные ловушки и «как доказать, что работает» → `RUNBOOK.md`; правило
    поведения → CLAUDE.md; факт о владельце/проекте, нужный в новых чатах → память.
  - **Признак, что записать НАДО:** я собираюсь сказать «потом сделаем», «стоит
    завести», «известная проблема», «не забыть» — это и есть момент записи, а не
    заметка на будущее.
  - **Проверка перед концом сессии:** пробежаться по тому, что нашёл и решил за
    сессию, и убедиться, что каждый пункт лежит в файле, а не только в переписке.
    Сводка в чате — не запись: чат закончится.

## Git / деплой / стрим
- **«коммить»/«коммит» = локальный commit.** Коммиты — conventional
  (`fix(scope): ...`), по смыслу, не вали в один.
- **Push — в конце каждой сессии ПО УМОЛЧАНИЮ** (решение владельца 2026-07-24:
  коммиты копились по 6-10 штук только на этом ПК — риск потери при смерти диска).
  НЕ пушить без команды только если в сессии есть причина держать (незавершённый
  эксперимент, спорная правка, ждём решения) — тогда назови причину в сводке.
  «Запушь» остаётся для внеочередного пуша посреди сессии.
  **Условия, без которых push не делается** (добавлено 2026-08-23 после
  внешнего ревью: правило «пушить всегда» может отправить чужое или
  недоделанное):
  - пушатся ТОЛЬКО коммиты, сделанные в этой сессии и мной; чужие или
    неизвестные коммиты впереди — сначала показать их владельцу;
  - рабочее дерево не должно содержать НЕ моих незакоммиченных правок;
  - тесты и линтер зелёные (иначе push закрепляет красное на remote);
  - ветка ожидаемая (`git status` показывает upstream), push без `--force`
    и без `--force-with-lease`; если обычный push отвергнут — разбираться,
    а не давить.
- **После стрима — лог-триаж:** прогон логов агентами на ошибки/косяки — дефолтная
  пост-стрим сессия (я часто прошу сам, но предлагай, если вижу что был стрим).

---

# Часть 3. Истории из RUNBOOK

Перенесено 2026-08-24: в рунбуке остались инструкции, здесь — то, как
мы до них дошли. Рунбук читают в проде и в кризис, там нужен ответ, а
не рассказ.

### История: заморозка деплоя перед первым ревью Twitch

> Период ниже завершён 2026-07-28 после approval и Release `0.0.1`.
> `$FreezeUntil` должен быть удалён из `deploy.ps1`. Для следующих версий
> используем version-specific gate из
> `Расширение/docs/TWITCH_UPDATE_RELEASE_PLAYBOOK.md`, а не вечную заморозку
> всего проекта.

`deploy.ps1` сам не пустит на прод: `-Backend`/`-Frontend` выходят с кодом 1 и
печатают, что делать вместо этого. Владелец попросил это прямо своими словами —
«не дай мне сломать прод до аудита», а обещание не механизм.

**Что считается КРИТИЧНЫМ** (только ради этого лезем в прод):
прод лежит · деньги теряются или списываются дважды · данные текут между
каналами · само ревью не может состояться.
Всё остальное — на локальной копии, катим после вердикта.

Разрешено и во время заморозки:
```bash
./scripts/deploy.ps1 -DryRun      # показать, что было бы
./scripts/deploy.ps1 -Staging     # staging на :8001, не прод
./scripts/deploy.ps1 -Mod         # DLL в игру, прод не участвует
python scripts/local-setup.py     # полная копия на своём ПК
```

Настоящая авария — одним флагом, но с письменной причиной:
```bash
./scripts/deploy.ps1 -Backend -CriticalReason "что именно горит"
```
Причина обязательна намеренно: пока формулируешь, становится видно, авария это
или нетерпение. Все обходы пишутся в `scripts/deploy-freeze-overrides.log`.

**После вердикта** — снять `$FreezeUntil` из `deploy.ps1` (или сдвинуть дату
под следующее окно). Заморозка, про которую забыли, однажды остановит нужный
хотфикс.


### Два прогона, повисших с надписью «OK» (26.07 и 30.07)

Обе истории целиком — из них выросла одна запись в рунбуке.

Симптом: тест печатает `PASSED: N FAILED: 0` и **висит вечно**. В CI это хуже
падения — деплой-гейт ждёт бесконечно, красного никто не видит, а по ощущениям
«всё тормозит». Именно так 2026-07-26 съелся час: тесты отрабатывали за секунду
и потом висели, а я ждал.

Причина: пул соединений держит потоки, которые интерпретатор обязан дождаться
перед выходом. Прод закрывает пул сам (`main.py` на shutdown), тестам этого
никто не делал.

Лечение и обе команды проверки — в `RUNBOOK.md`, «Тест печатает «OK» и
висит». Здесь намеренно только история: рецепт нужен в проде, и он должен
быть в одном месте, иначе одну из копий однажды поправят, а вторую нет.

---

✅ **Починено 30.07** (`await db._pool.close()` в `finally`): было 12 минут без
кода возврата, стало `exit=0` за секунду. Ловушка оставлена как образец класса.

`Расширение/backend/tests/test_shedcolony_money.py` (30.07): проходил все 13
проверок, печатал `OK — all ShedColony money/compliance invariants hold` —
и **не выходил**. Провисел 12 минут, потом убит; кода возврата нет вообще, то
есть `$?` не наступает. Утверждения при этом действительно зелёные: инварианты
денег держатся, дефект в завершении прогона (не закрывался пул БД — `asyncio.run`
не может закрыть цикл, пока живы фоновые соединения), а не в проверяемом коде.

**Чем опасно:** прогон «до конца» никогда не наступает. В CI или в скрипте,
который ждёт выхода, это вешает пайплайн; человек, читающий вывод, видит `OK`
и считает тест пройденным. Это тот же класс, что 28.07 (зелёная печать при
ненулевом коде), в самой злой форме — кода нет совсем.

**Как проверять этот и любой standalone-тест:** запускать с жёстким лимитом и
судить по факту выхода, а не по тексту:

```powershell
$p = Start-Process python -ArgumentList "tests/test_shedcolony_money.py" -PassThru -NoNewWindow
$p | Wait-Process -Timeout 120 -ErrorAction SilentlyContinue
if (-not $p.HasExited) { "HUNG"; $p.Kill() } else { "exit=" + $p.ExitCode }
```

Проверено 30.07: `test_action_ack_refund_order.py` и
`test_special_action_refund.py` — честный `exit=0`; висит только shedcolony.

---

# Часть 4. Истории, записанные по ходу

### Ломать для проверки

**2026-08-24, дважды за одну сессию.** У нас есть правило: проверка, которую не
видели красной, ничего не доказывает. Практика простая — временно портишь то,
что проверка должна поймать, смотришь exit 1, возвращаешь как было.

Первый раз: написал линтер, который держит `STATUS.md` витриной на десять
пунктов. Чтобы показать его красным, подложил старую версию файла на 448 строк —
линтер честно назвал «75 пунктов при пределе 10» и вернул 1. После чего я
«вернул как было» командой `git checkout -- STATUS.md`. Она вернула файл к
последнему коммиту — то есть к той самой старой версии, а новая витрина, которую
я час собирал и ещё не коммитил, исчезла. Восстановил из своего черновика.

Второй раз, через несколько часов: та же схема на `mobile.html`. Готовая правка
под отказ ревью Twitch (убраны три карточки и три скрипта) не была закоммичена;
после красного прогона `git checkout -- mobile.html` снёс её целиком. Восстановил
повторным прогоном того же скрипта правок.

**Механизм.** `git checkout <файл>` не знает, какая часть изменений — подложенная
поломка, а какая — работа. Он откатывает файл целиком к коммиту. Пока правка не
закоммичена, для git её не существует, и «отменить эксперимент» означает
«отменить всё».

**Что делать.** Коммит до эксперимента — тогда откат безопасен и возвращает ровно
то, что нужно. Если коммитить рано (правка недоделана) — копировать файл в
сторону и восстанавливать из копии, а не из git.

**Третий случай, 2026-09-04 — тот же корень, другой повод.** Владелец положил в
`Расширение/docs/` присланный извне план укрепления (421 строка, автор — Codex) и
попросил сделать ревизию. Файл был **untracked**: лежал на диске, в git его не
было. Я переписал его на месте через Write. Ревизия получилась, оригинал исчез —
восстанавливать нечего, потому что для git его никогда не существовало. Уцелел
только мой пересказ требований и копия у владельца в чужом чате.

**Механизм тот же.** «Файл на диске» и «файл в git» — разные вещи, и второе
восстановимо, а первое нет. Правило про коммит перед экспериментом я применял
только к порче ради красного прогона, хотя оно шире: любая перезапись файла, не
лежащего в коммите, необратима.

**Что делать.** Первым действием над чужим или новым файлом — `git add` и
коммит «as received», и только потом правки. Дёшево, занимает секунду, и делает
разницу «что было прислано» / «что я из этого сделал» видимой в диффе.

**Из той же сессии, тот же корень — доверие к тому, что не проверено машиной.**
Дважды собрал код линтера строкой в Python и получил управляющие символы вместо
регулярных выражений: `` в обычной строке — это backspace (0x08), `
` —
перевод строки. В первом случае линтер выглядел установленным и был мёртв: он
молча ничего не находил, и только обязательный красный прогон это вскрыл. Во
втором файл перестал компилироваться. Вывод общий: машинная правка кода
проверяется компиляцией и красным прогоном, а не чтением получившегося текста.

### Один признак — два смысла (23.08 и дважды 28.08)

Три раза подряд один и тот же класс, каждый раз в другом месте.

**23.08.** Крустики за просмотр начислялись по `viewers.last_seen`. Поле
записывает ровно одно: «панель прислала heartbeat». Использовалось оно как
«зритель смотрит». Разница между этим — человек, открывший вкладку и ушедший
на кухню. Починили: завели `last_interaction_at` и отдельное окно
`ENGAGED_WINDOW`, полная ставка требует ОБОИХ признаков.

**28.08, находка первая.** Розыгрыш кейсов остался на старом определении.
Замер через две минуты ПОСЛЕ конца эфира: десять зрителей с
`last_seen = сейчас`, последнее их действие — за 5-8 часов до того, двое не
взаимодействовали ни разу. То есть фикс 23.08 прошёл мимо соседней функции, где
жила та же ошибка: чинили место, а не класс.

**28.08, находка вторая.** `colony.snapshot` с пустым списком колонистов
означал в коде «колония ещё не загружена» — и сверка выключалась. Но мод не
шлёт снимок для незагруженной колонии, он выходит раньше. Пустой список значит
«колония загружена и пуста». Из-за склейки четверо зрителей, заплативших по
1000 крустиков, остались привязаны к колонистам, которых нет.

**Что общего.** Во всех трёх случаях в коде было ОДНО значение, а смыслов —
ДВА, и различить их по самому значению нельзя в принципе. Ноль элементов не
отличается от «не знаю»; свежий пинг не отличается от внимания.

**Правило.** Перед тем как использовать поле в условии — назвать вслух две
вещи: что оно ЛИТЕРАЛЬНО записывает и что нужно проверить. Совпало — можно.
Не совпало — нужен второй признак, и брать его надо у того, кто знает
(источник события), а не выводить из размера или свежести. И, найдя такую
склейку, сразу искать её же в соседних функциях: она почти наверняка там.

## Фикс из двух частей выкатывается целиком, иначе половина живёт локально (2026-09-03)

Живым прогоном платных кнопок нашёлся дефект: призыв «против стримера» за 100💎
в бою, где герой зрителя уже дерётся, не делал ничего — свита пропущена, сторону
живому герою мод не меняет, — но доходил до подтверждения успеха. Деньги
списывались вчистую.

Починка вышла из двух частей: отказ с возвратом в моде (C#) и фраза для зрителя
в бэкенде (Python). Мод я собрал, поставил в игру, сверил содержимое DLL — и
доложил «починено».

**Чего не заметил.** Бэкенд остался лежать локально. В игре отказ срабатывал,
деньги возвращались, но зритель получал заглушку «Игра отказала в этом
действии» вместо объяснения. Половина фикса не уехала, а отчёт был уверенным.

**Что спасло.** Решил посмотреть своими глазами, что видит зритель. В панели
тост уже погас, а запрос уведомлений вернул пустой список — и это чуть не стало
«доказательством»: пустой список одинаково означает и «показали и погасили», и
«ничего не отправляли». Ответ дала только прод-база: две строки возврата по
100💎 с текстом-заглушкой.

**Правило.** Считая фикс сделанным, перечислить ВСЕ места, куда он должен
уехать, и для каждого назвать доказательство, что он там. Мод и бэкенд
деплоятся разными командами: `deploy.ps1 -Mod` не везёт Python, `-Backend` не
везёт DLL. И проверять по тому, что видит ПОЛЬЗОВАТЕЛЬ, а не по тому, что
сработал наш код: код сработал, а пользователь получил не то.

