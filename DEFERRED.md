## 24.09 — багрепорты зрителей (разбор `docs/BUG_REPORTS_TRIAGE_2026-09-24.md`)

- #42 закрыт на проде (устарел). **25.09 починены** (владелец: «чини, только
  возраст пока не трогай»), DLL `DE7A018C` в игре, живьём НЕ проверены — после
  подтверждения закрыть на проде: #66 (вещь уходила в инвентарь чужого отряда),
  #61 (роспуск королевства теперь выводит все кланы), #62 (мёртвый клан не
  предлагает войну/мир). Компенсировано 25.09 (решение владельца): analniyshekotyn 60 000 + 60 000
  (две булавы), slopkom_nyi_item 240 000, igotpaws 120 000 (смерть от старости
  с перекованным; владелец: «у него тоже были проблемы с этим» — разовое
  решение, не правило). Потеря надетого при смерти — по задумке
  (владелец 25.09, см. «Решено НЕ делать»); сундук переходит наследнику. #57 не воспроизводится — ждём повтора
  со скриншотом. #58/#63 починены 22.09, ждут живого подтверждения. #65 —
  вопрос владельцу (детали), #59 — решено не делать, #60 (возраст) — владелец
  25.09: «пока не трогай».

## 25.09 — выплаты за бой: владелец «пока оставляем»

Разбор за день новой кампании: 120 боёв, медиана выплаты — малый бой 4,2 тыс.
(активные 5,7), средний 17,9, крупный 32,6; типичный зритель ~790 тыс. за
стрим (~100–120 тыс./ч), топ 3,7 млн; покупка снаряги — медиана 17,7 тыс.
Предложено точечно убрать множитель 0,8 для боёв <50 врагов — не делаем.

## 25.09 — жалобы igotpaws #67–69, сундук для всех, замок стороны (выкачено 25.09: DLL `B3CBCA2B…` + бэкенд)

- #68 арбалет (46 190💰) и, вероятно, #67 топор (80 600💰): у igotpaws свой отряд,
  покупки шли в инвентарь отряда, а игра при заходе отряда лорда в город
  распродаёт его инвентарь, кроме еды (`PartiesSellLootCampaignBehavior`,
  ТОЧНО по коду; в логе после покупки — скачки золота +3 551 / +2 547 без
  действий). В #67 ещё и плен (мод захват не логирует). Решение владельца:
  защиту от распродажи НЕ делать (откачено), а покупки без надевания и всё
  снятое с героя (включая перекованное) — **всегда в личный сундук**, 10 мест
  для всех; инвентарь отряда — только источник («надеть» из него можно).
  Мод: `EquipmentShopHandlers`; бэкенд: `stash_slots` в `equipment_shop.py`.
  Тесты: EquipmentShopHarness 48, NativeInventoryHarness 45 (правила «в багаж»
  переписаны по решению), `test_bannerlord_personal_stash.py` (+ свой отряд и
  полный сундук; мутант пойман). Компенсация igotpaws 126 790💰 (топор + арбалет): владелец 26.09 — **нет**.
- #69 «Знамя клана»: куплено (bannerlord_clan_upgrades_owned 13:54:25). Панель
  перечитывает список сразу после «заявка отправлена», когда игра ещё не
  провела покупку, и больше не обновляет — выглядит «деньги ушли». Первая
  попытка в 13:30 — отказ, не хватило 594 динаров, не списано. Правка — в
  следующей версии панели (перечитать список после ack). Зрителю: переоткрыть вкладку.
- Замок стороны (владелец: «люди меняют стороны во время боя»): первый удачный
  призыв в бою (MapEvent) закрепляет сторону, другая — отказ `side_locked` с
  возвратом; в обе стороны. `ViewerSideLock`, SideLockHarness 9/9, мутант пойман.

## 25.09 — сервер: подбор пароля SSH закрыт (владелец сделал: только ключ + fail2ban); диск и обновления системы — после стрима

Проверено 25.09 (чтение): за час 2 616 неудачных входов по SSH (~44/мин, боты
с разных IP), `PermitRootLogin yes`, `PasswordAuthentication yes`
(`sshd_config.d/50-cloud-init.conf`), fail2ban НЕТ. Процессор это ест мало
(~1–2%), но риск взлома root по паролю — высокий. Менять настройки безопасности
сервера Claude не может (правило) — шаги для владельца: вход только по ключу
(у Claude ключ уже работает), fail2ban. Запасной вход при ошибке — консоль
Timeweb. Диск 82%: `/var/log/journal` 1,3 ГБ, `btmp` (лог неудачных входов)
206 МБ, nginx 302 МБ, бэкапы 497 МБ (по плану), база 168 МБ. Безопасно
освободить: `journalctl --vacuum-size=200M`, ротация btmp. Бэкап по cron в 05:00
UTC (10:00 местного) — во время стрима, если стрим с утра.

## 25.09 — расширение тормозит на стриме: сервер упирается в 1 ядро (после стрима)

Зрители в чате: «лагает расширение жёстко». Timeweb: 1 ядро, во время стрима
80–100% (до стрима 20–40%); ОЗУ 961 МБ; диск 82% (2,7 ГБ свободно). nginx: до
209 тыс. запросов/час (~58/с), ~46 зрителей; 21–22.09 пики те же (200–250 тыс.) —
не регрессия, просто упёрлись. Панель (заморожена на ревью Twitch) опрашивает:
battle-status 2 с, my-buffs 2,5 с, tournament 3 с, основной цикл 8 с (hero,
shop, status, classes, build + ~10 вкладок, даже закрытых); отстаёт на скрытой
панели уже. В логе ошибок `TimeoutError` пула БД (16). `tournament` читает БД на
каждый запрос; `my-buffs`, `battle-status` — из памяти. Время ответа по
эндпоинтам не пишется — самый тяжёлый не назван (ГИПОТЕЗА: оверхед на запрос +
tournament + события мода). План после стрима: (1) владелец — Timeweb 2 ядра/2 ГБ;
(2) `$request_time` в log_format nginx, профиль за стрим; (3) кэш на канал
1–2 с для турнира/общих статусов; (4) след. версия панели — опрос только
открытой вкладки и реже; (5) диск: сжатие access.log, не логировать частые
опросы (access.log ~200 МБ за стрим). Автопилот: цель патруля скачет каждые
3–5 с между городами с близкой оценкой — разобрать.
Оптимизация без потери качества (владелец спросил 25.09, найдено в коде):
uvicorn 0.24 без `uvloop`/`httptools` (на проде стоит только uvicorn) — ставить
`uvicorn[standard]`; `security_headers` сделан через `@app.middleware("http")`
(BaseHTTPMiddleware — известный медленный путь Starlette) → чистый ASGI с теми
же заголовками; кэш на канал 1–2 с для общих данных (турнир, статус,
каталоги), личное — отдельно; кэш проверки JWT; не логировать частые опросы.
Порядок: замер → uvloop+middleware+логи → staging → прод → CPU на стриме →
решать про 2 ядра. `Cache-Control: no-store` для /api/ оставить (инцидент).
**Подготовлено 25.09, НЕ выкачено (стрим):** `main.py` — заголовки чистым ASGI
(замер в процессе: 2,53 → 1,16 мс на запрос; старый код проходит новый тест —
заголовки те же; мутант пойман); `requirements.txt` — uvloop 0.21 (не Windows)
и httptools 0.6.4; `deploy/nginx/twitchbot.conf` — keepalive к приложению +
время ответа в логе. Тесты бэкенда 115/120 (5 старых), критический 107/107.
Выкат после стрима: staging → прод ПОИМЁННО (`main.py`, `requirements.txt` +
`pip install uvloop==0.21.0 httptools==0.6.4` в venv) — полный deploy.ps1 нельзя,
пока на проде незакоммиченный код Codex; nginx — по RUNBOOK (`nginx -t` на проде
ещё не делали). Проверка: CPU в Timeweb на следующем стриме, `rt=` в логе.
**Выкачено 25.09 ~14:45 UTC** (владелец: «давай, перерыв, откат сохрани»):
staging (код синхронизирован с продом, заголовки, uvloop, M132 — ок) → прод
поимённо (прод-версии сверены с git: отличия только наши) → nginx (`nginx -t`
ок). Бэкапы и `rollback.sh`: `/root/twitch-extension/backups/perf-20260925/`.
Результат в перерыве: load 0,42, CPU ~20%, p50 3 мс / p95 17 мс / p99 89 мс;
«database is locked» были у старого процесса (3), у нового 0. Медленные по
новому логу: `/api/bannerlord/party-orders` до 1,1 с, `/api/viewer/perks` до
1,1 с, equipment-shop ~0,4–0,5 с — разобрать. Диск: `journalctl --vacuum-size`
освободил 0 (не трогает активный журнал) — нужно `--rotate`, после стрима.
**Главный виновник найден 25.09 (замер):** запрос недавних возвратов в
`/my-hero` перебирал все ~27 тыс. платных действий канала и сортировал —
153 мс на проде, 201 мс на копии базы; /my-hero ~2 раза/с на всех зрителей →
~40% ядра, долгие запросы держали соединения пула (ВЕРОЯТНО источник
TimeoutError). M132 — индекс (channel_id, module_id, created_at): 0,01 мс,
результат тот же (`test_recent_refunds_index.py`, красный до миграции).
Замер обработчиков на копии базы с индексом: my-hero 5,9 мс, build 3,2 мс,
equipment-shop 31,8 мс (редко), прочие 0,7–1,3 мс, battle-status/my-buffs/status
из памяти. Выкатывать вместе с ASGI-мидлварем: `main.py` (+M132 в run_migrations),
`routes/bannerlord.py`, `migrations/m132_*.py`, `requirements.txt` — поимённо,
сверив прод-версии с базой в git. Оставшиеся находки аудита (после замера на
проде): `player.state_update` делает до 5 UPDATE одной строки без проверки
«не изменилось»; equipment-shop каждый раз декодирует весь каталог (кэш на
канал — осторожно: ответ собирается мутацией словарей каталога); кэш JWT;
busy_timeout 10 с держит слот пула при конкурирующей записи.

## 26.09 — обмен слабых на сильных на экране трофеев (владелец), собрано, не установлено

Владелец (скрин: отряд 366/366, слева освобождённые тир 2–4): «научи на этом экране
заменять освобождённых из плена войск высокого тира нашими слабыми». Раньше
`PollPrisonerScreen` брал освобождённых только в свободные места. Теперь: освобождённых
берём от сильных к слабым; при полном отряде, если слева есть боец строго выше
тиром, чем самый слабый наш, — отпускаем влево столько самых слабых наших, сколько
сильных, и на следующем опросе берём сильных. Герои не трогаются. Проверено по 1.4.8:
в режиме трофеев любой не-герой переносим в обе стороны (`PartyScreenHelper.IsTroopTransferable`),
список наших — `PartyVM.MainPartyTroops` (в контракте, 497/497). Сбой отпуска — только
«обмен прекращён» на этом экране, автопилот работает. Стенд 695 ok (+3 теста), мутант
«без обмена» пойман. **В игре не проверено:** что отпущенные влево бойцы после «Готово»
просто остаются (их теряем — это и есть цель) и подтверждение не требует игрока.
В пакете `pending-20260926-autopilot-politics2` (DLL `68B37905…`).

## 26.09 — штурм сразу, как готов осадный лагерь (владелец), собрано, не установлено

Владелец: «научить ИИ не тянуть и начинать только с осадным лагерем, без катапульт и
прочих построек». Причина затяжки (ТОЧНО по декомпиляции 1.4.8): автопилот ждал
`BesiegerCamp.IsReadyToBesiege` = лагерь готов **и** `StartingAssaultOnBesiegedSettlementIsLogical`
— случайный бросок ИИ-лордов, шанс которого растёт с тараном/башней/машинами. Кнопка
игрока «Возглавить штурм» доступна уже при `IsPreparationComplete`. Теперь штурм по ней.
Факт 25–26.09: бой у лагеря (подмога) через ~1,3–4,5 игровых дня после начала осады —
лагерь стоял и ждал. Цена: штурм по лестницам без тарана/башни — потери выше; вход
в осаду по-прежнему x1,5 и уровень 3,5, снятие осады при подмоге остаётся.
Стенд 688 ok (+2 теста), мутант «ждать IsReadyToBesiege» пойман (4 FAIL); контракт с
игрой 496/496 (свойство есть в 1.4.8). В пакете `pending-20260926-autopilot-politics2`
вместе с политикой и радиусом подмоги 50 (владелец: «уменьши радиус поиска угрозы до 50», было 100; стенд 689 ok, мутант «100» пойман) — DLL `75E10A3C…`.

## 26.09 — политика: мир с сильным, война со слабыми (владелец), собрано, не установлено

Владелец: «он сейчас в своём королевстве» → наёмничество отпадает. Причина молчания —
правило 23.09 «ровно одна война»: с Вландией (одна война) мир не предлагается, новая
не объявляется, сила врага не учитывалась. Сделано (`PoliticsPolicy`): единственная
война с тем, кто сильнее нас ≥ x2 (`Kingdom.CurrentTotalStrength`), и есть кого бить
слабее → предложить мир; при 0 войн цель — сперва слабее нас, среди них — с большей
поддержкой клана. Мир с сильным без слабой альтернативы не предлагается (иначе круг).
Возможная вторая причина — влияние < 100: покажет новая строка «ПОЛИТИКА: не предлагаем».
Тест PoliticsPolicy (+6 проверок), мутанты «без мира» и «без приоритета слабых» пойманы;
AuditRegression 685 ok. Пакет `D:/shedlink-build/pending-20260926-autopilot-politics2/apply.sh`
(пересобран: + подпись «правитель» в сводке).
**Факты 26.09 14:55 (сводка нового автопилота):** королевство «ShedLink», влияние 4 002 —
значит политика НЕ молчит по условиям, а выбирает «ничего» по правилу «ровно одна
война» (это и чинит пакет). Осада: ближе всех «Замок Невьянск» — стены 524 + лорды
Вландии в радиусе 100 на 1 520 = 2 044, надо x1,5 = 3 066, у нас 778; «армию собрать
не из кого» при 4 002 влияния → в королевстве ShedLink нет лордов со своими отрядами
(кланы зрителей без партий). Без армии осады не будет; путь — NPC-вассалы (покупка
зрителями) или война со слабым королевством (пакет политики).

## 26.09 — осады невозможны в одиночку; политика молчит без объяснения

**Осады (лог 26.09 10:51–11:51):** ближайшие вражеские крепости — стены 418–1 190
(гарнизон + ополчение), у Невьянска подмога 3 664–4 247; наш отряд ~570 силы
(~360 бойцов). Даже голые стены Тирбю (855) и Драпанда (1 186) сильнее отряда —
в одиночку осада невозможна при любом пороге; «армию собрать не из кого». Нужна
армия (королевство + влияние + лорды) или больший отряд — вопрос владельцу.
**Политика (владелец: «странно, что он мир Вландии не предлагает и не нападает на
слабых»):** `AutopilotPolitics.RunPolitics` молча выходит при «клан не в королевстве /
наёмник / влияние < 100»; в логе за день ни одной строки ПОЛИТИКА и «решений
королевства 0». Какое условие — по логу не установить. Сделано: строка
«ПОЛИТИКА: не предлагаем — <причина>» при смене + в недельной сводке «королевство
«X» (наёмник/вассал); влияние N». Стенд 684 ok. DLL `08652074…` в
`D:/shedlink-build/pending-20260926-autopilot-politics/apply.sh` — **установлено 26.09**.

## 26.09 — надбавка за сверхусилие УБРАНА (владелец), установлено 26.09 (`65E77B2E…`)

Владелец спросил «сверхусилие бонус даёт?» и сказал: «думал наоборот, дальше меньше
дают». Надбавка (с 22.09, решение «такие вклады не меньше миллиона»): сверх 6700 очков
героя вторая кривая до +386 тыс. С множителем уровня врага 1,5–2,6 порог стал частым:
бой 11:32 26.09 — slopkom_nyi_item 10 461 очко, 1 021 086💰, из них ~539 тыс. надбавка.
Решение 26.09: «убери надбавку». Личная часть — только с затуханием; потолки: обычный
бой 679 200, большая осада 1 086 720 (было 1,6 и 2,6 млн); тот же бой — ~482 тыс.
Стенд выплат 81/81; тесты «не меньше миллиона» 22.09 заменены на «больше вклад —
больше, но за очко дешевле»; мутант «надбавка вернулась» пойман (8 FAIL).
DLL `65E77B2E…` = всё сразу: без надбавки + призыв на расстановке (ЖИВЬЁМ НЕ ПРОВЕРЕН)
+ множитель 1,5–2,6 → `D:/shedlink-build/pending-20260926-link-final/apply.sh`.

## 26.09 — оверлей: итог боя 30 с вместо 120 (владелец: «новых питомцев не видно»), ВЫКАЧЕНО точечно

`overlay.html`: финальная карточка боя висела 2 мин (`age_sec < 120`) и закрывала
питомцев. **Прод-версия оверлея — из НЕЗАКОММИЧЕННОЙ работы Codex** (новые питомцы
`pet-assets/companions-v1`, рабочая копия `.claude/worktrees/codex-public-release`,
выкачено 25.09 20:15 UTC), её нет ни в одной ветке — поэтому на проде заменено ТОЛЬКО
число 120 → 30 (бэкап `/root/twitch-extension/backups/overlay-final30-20260926/`).
В моей ветке то же как константа `BNR_FINAL_SUMMARY_SEC = 30`. **Для Codex:** при
следующем выкате overlay.html из своей копии перенести 30 с, иначе вернётся 120.
Чтобы OBS подхватил — обновить источник «Браузер» (кэш страницы).

## 26.09 — данные из игры: задел под большие моды (The Old Realms и т.п.)

Владелец: «хочу, чтобы расширение в будущем могло поддерживать даже такие
колоссальные моды… большую часть корректно принимало из игры». Сейчас НЕ делаем
(TOR на Bannerlord 1.3.15, мы на 1.4.8 — отдельная сборка и установка игры;
разведка на день, если решим). Принцип записан в CLAUDE.md. Уже из игры: каталог
снаряжения (все ItemObject), свита (culture.BasicTroop/EliteBasicTroop + дерево
UpgradeTargets), выплаты, призыв. **Прошито (починить при случае):**
1. Панель: выбор культуры при создании героя — список из 6 культур
   (`viewer-bannerlord.js` ~5129, `CULTURES`) → отдавать с сервера из игры (след. версия панели).
2. Панель: список законов королевства (`viewer-bannerlord.js` ~2369, `policy_*`) → с сервера.
3. Мод: 6 классов героя привязаны к ванильным культурам ради стиля снаряжения
   (`Actions/ClassLoadout.cs`, `CultureId = "empire"…`) → запасной путь: культура героя.
4. Сервер: отдельные id культур в `routes/bannerlord.py`, `routes/bannerlord_settlements.py`,
   `migrations/m35_clan_upgrades.py` — разобрать, что из них ломается без ванили.

## 26.09 — призыв на расстановке: КРАШ в игре, ОТКАЧЕНО (`6f7ba7b1`, мод `97662914`)

**Первый же живой призыв на расстановке уронил игру.** 11:57:34 @stepuhatgn, на
расстановке, `isAlarmed=false`, не подкреплением, без позиции → `SpawnTroop` бросил
`InvalidOperationException: Nullable object must have a value` (мод поймал, деньги
вернулись), через 12 с — `AccessViolationException` в `MissionState.TickMission`
(дамп `crashes/2026-09-26_06.57.46`, `scripts/triage-crash.ps1`). Механизм (ВЕРОЯТНО):
на расстановке у формации нет точки появления для нового бойца, движок падает внутри
SpawnTroop, оставив полусозданного агента, — следующий такт миссии читает битую память.
Старый запрет BLT RC22 был по делу; как новый BLT обходит это — НЕ разобрано (вероятно,
другие условия вызова). Откат установлен сразу (игра упала, была закрыта); надбавка
убрана и множитель 1,5–2,6 сохранены. **Вернуться — только** с разбором, как BLT
получает точку появления на расстановке, и прогоном в копии сейва ВНЕ стрима.
**Урок:** правку вызова движка (спавн, миссия) не ставить на стриме без прогона в игре —
тесты вокруг проверяют только наш выбор режима, не движок. Владелец принял риск
осознанно, откат был готов — поэтому минуты, а не часы.

Было (для истории):

ethanenok/владелец: отказы «mission mode Deployment» — 10 на 2 459 призывов 25.09.
Владелец: «почему не спавнить сразу на место? новые BLT как-то смогли». У нас был
запрет из BLT RC22 («SpawnAgent падает на Deployment»); `reference/BLT_lait` этот
запрет снял и призывает на расстановке `isAlarmed=false` и не подкреплением (свиту
тоже). Сделано так же (`Util/SummonModePolicy`, `SummonHeroHandler`): Deployment не
запрет, герой и свита на расстановке — не по тревоге и не подкреплением. Отклонён
вариант «ждать конца расстановки»: `ActionPoller` выполняет заявки по одной
(`foreach … await ProcessActionAsync`) — ожидание одной задержало бы все.
**РИСК:** вылет игры при призыве на расстановке, если старый запрет был по делу —
проверить ДО стрима одним призывом во время расстановки; вылет → откат.
SpawnPlacementHarness PASS, мутант «Deployment снова запрет» пойман. DLL `1C1B8E20…`
(включает множитель 1,5–2,6) в `D:/shedlink-build/pending-20260926-link-deploy/apply.sh`.

## 26.09 — множитель уровня врага в выплате 1,5–2,6 (владелец), УСТАНОВЛЕНО 26.09

Владелец: «может бонус от уровня врага поднять… сделать от 1.5 до 2.6» (было 0,4–1,5:
уровень/26). Сделано сдвигом шкалы на +1,1 (`BattlePayoutPolicy.ThreatShift`):
новобранец 1,5, уровень 26 — 2,1, 39+ — 2,6; элита выгоднее новобранца в 1,7 раза (было 3,7).
**Моё возражение (сказано до правки, владелец настоял):** очки за удар ×1,7–3,7 → выплата
примерно ×2 (личная часть и свита растут с затуханием), вместе с добивкой 30 тыс. того же
дня — в ~2,5–3 раза больше денег, чем 25.09; эффекты двух правок не разделить. Предлагал
сперва проверить добивку, а при нехватке — общий множитель. Проверить после стрима: сумма
выплат за бой/за час на зрителя против 25.09 (18 тыс./бой медиана, 150–400 тыс./ч).
Стенд 79/79, мутант «сдвиг 0» пойман. DLL `D74CF7D9…` в `D:/shedlink-build/pending-20260926-link-threat/apply.sh`
(при запущенной игре отказывает — проверено).

## 26.09 — бои затянуты (RBM): пробуем отключить боевую часть ИИ, УСТАНОВЛЕНО 26.09

**ПОПРАВКА 26.09 (после установки):** RBM в лаунчере ВЫКЛЮЧЕН (`LauncherData.xml`,
`IsSelected=false`), его журнал ИИ последний раз писался 21.09 17:03 — то есть RBM не
работал с вечера 21.09 (кто и почему выключил — не записано; вероятно владелец после
35-минутных боёв 21.09). Значит, замеры 22/25/26.09 ниже — БЕЗ RBM, и мой вывод «бои
затягивает RBM» по ним неверен: бои 500+ 22.09 (медиана 9,5 мин, макс 25 мин) шли без
него. С RBM был только 21.09: 500+ медиана 414 с, макс 2143 с (35,7 мин — тот самый бой).
Настройка config.xml действует, только если RBM включить в лаунчере (решение владельца).

Владелец: «мод на улучшение действий в бою сильно растягивает бой… бои около 600 шли
35 минут; взять часть улучшений (лучники линией сзади) не особо растягивая бой».
Мод — наша сборка RBM AI-only (21.09). Лицензии у RBM нет — код брать нельзя (разбор
`D:/shedlink-build/third-party/COMBAT_AI_STUDY_2026-09-21.md`). Замер
(`scripts/battle-durations.py`): 22.09 бои 500+ — медиана 570 с, макс 1510 с; 25.09 —
232 / 414 с; малые бои ~2 мин как до RBM. Оговорка: бой 41 мин был и 17.09, ДО RBM —
длинные бои не только из-за RBM (размер, волны перепризывов — не проверено).
Что тянет (по исходникам RBM): правка блоков/парирований бойцов (`AgentStats`, при
`VanillaCombatAi=0`) и Frontline (шаги назад/поиск союзника в строю; 21.09 выкручен на
«агрессивно»). Тактика строя (лучники сзади, заходы конницы) — отдельно и остаётся.
Решение владельца 26.09: «давай попробуем, подготовь, скажу когда ставить».
Подготовлено `D:/shedlink-build/pending-20260926/`: `config.xml.new` (VanillaCombatAi=1,
FrontlineEnabled=0, остальное без изменений) + автопилот `DBAECD64…` (снятие осады при
подмоге) + `apply.sh`/`rollback.sh` (проверка запущенной игры по `TaleWorlds.MountAndBlade`
— проверена: при запущенной игре отказ, ничего не тронуто). Проверка после: тот же скрипт
по дню стрима, сравнить 500+ с 22.09/25.09. Если бои всё равно долгие — убрать RBM и
написать свою расстановку в автопилоте (своими руками, без чужого кода).

## 26.09 — пост-стрим триаж 25.09 (агенты sonnet, ключевое перепроверено)

**Деньги:** 539 проваленных платных действий за день — все закрыты уведомлением
(332 возврата + 207 отказов), зависших queued/dispatched 0, `event_not_in_manifest` 0.
«БЕЗ ЯВНОГО ИСХОДА» 0, CRASHED в моде 0 после 22:21, все 412 призывов с агентом.
Единственная выплата мёртвому — stepuhatgn до правки (владелец: не компенсировать).
**Сервер:** после рестарта 14:46 UTC ошибок 0. До него: 5 таймаутов пула (09:00–09:31
UTC) и 4 «database is locked» (12:01 UTC) — до индекса M132. 502 на рестартах:
521 за перерыв 25.09 (два рестарта) и 38 при выкате 26.09 ~19:49 UTC — это
известный SIGKILL при остановке (см. «две трети рестартов добиваются SIGKILL»).
Трафик equipment-shop за 25.09 — 10 ГБ (16 510 ответов по ~636 КБ); со сжатием
26.09 — ~63 КБ на ответ.
**Новые багрепорты (открыты):**
- #72 dzirtdourden «найм свиты невозможен, пока не перезагрузить… деньги есть» —
  ТОЧНО по коду панели: блок свиты (`_renderRetinue`, viewer-bannerlord.js
  ~5590) перерисовывается только при смене СОСТАВА свиты, а надпись «не хватает»
  считается от золота героя на момент отрисовки — золото после боёв растёт, блок
  нет. Панель заморожена. Варианты: (а) след. версия панели — ключ перерисовки с
  золотом; (б) обход с бэка — отдавать в элементах свиты поле с золотом, чтобы
  изменился JSON и блок перерисовался (хак под замороженную панель).
  **Владелец 26.09: «делай обход» — сделано и выкачено 26.09 ~02:00:** `render_key`
  (золото:предел) в каждом бойце свиты в /my-hero; `test_retinue_render_key.py`
  (красный без правки). Откат `backups/retinue72-20260926/rollback.sh`.
  Остаток: при ПУСТОЙ свите обход не работает (нет элементов) — чинить в панели.
  Закрыть #72 на проде после подтверждения зрителем.
- #70 dzirtdourden «надо перезагрузиться, чтобы засчитался апгрейд клана» — тот же
  класс, что #69 (список апгрейдов не перечитывается после исполнения в игре).
- #71 neyrahatomia «на мобильной версии некорректно отображает баланс в апгрейдах
  клана» — не разобрано (нужен скриншот / какая именно цифра).
**Автопилот:** осад 0 (см. раздел про опытный отряд), развороты ~9/ч.
**Ручное для владельца (диск 82%, не горит):** `journalctl --vacuum-size=200M`
(журнал 206 МБ), ротация `/var/log/btmp` (112 МБ, 305 тыс. неудачных SSH-входов
до fail2ban). Claude не удаляет логи на проде сам (правило о безвозвратном удалении).

## 26.09 — добивка выплаты за бой до 30 тыс. в любом бою (владелец), установлено 26.09 ~01:00 (`0BE444A6…`)

Владелец спросил, не маловато ли «~18 тыс. за бой». Мой ответ: по часам не мало
(середина 150–400 тыс./ч, топ ~1 млн/ч; клан за 2–3 ч), мало у слабых: малые бои
медиана 7,5 тыс., большие 34 тыс. Владелец: «добивку до 50к?» — против: 187 из
236 выплат, денег x2, призыв 50💎 ≈ 250💰 окупается стоянием. «Давай до 30к» —
предложил условие ≥100 очков урона (+18%); владелец: «поучаствовать в каждом бою
тоже надо время и следить за стримом» — довод принят, условие по урону снято.
Ровные 30 тыс. всем давали +46%, в основном за стычки с 30–45 бандитами (10
зрителей × 30 тыс. за минутный бой) — поэтому по размеру боя: >=200 врагов 30
тыс., 50–199 — 15 тыс., <50 — 5 тыс. (+16% по логу 25.09). Добивка идёт в
«участие» до бонуса подписчика; мёртвым не платим; в логе `floor=+N`.
Сделано `BattlePayoutPolicy.WithFloor`, стенд 78/78, мутант пойман (5 FAIL).
DLL `3F0CD879…` собрана, НЕ установлена (игра запущена, владелец: не закрывать).
**Итог (владелец 26.09): ровно 30 тыс. в любом бою** — «завтра потестим; раздувания
не жду, некоторые вещи в 6 тире стоят несколько миллионов». Моё возражение: +46%
денег по логу 25.09, львиная доля — стычки с бандитами (10 зрителей × 30 тыс. за
минутный бой). Проверить на стриме 26.09: сумма `floor=+` за стрим и сколько
тратят (снаряжение VI тира, свита). Откат к 30/15/5 — одна строка `FloorGold`.
Стенд 77/77, мутант «30/5 по размеру» пойман. DLL `5004BF4B…`, НЕ установлена.

## 25.09 (вечер) — автопилот: на осады только опытным отрядом (владелец), установлено 25.09 ~22:40 (`0C991F7A…`)

Владелец: «сначала качать отряд, нападая на отряды, потом с очень крепким идти
в осады, а то с новичками дают пизды». Факт того же вечера: 21:15 штурм → в
отряде 37; 22:06 «Замок Ремтойл» — 320 бойцов, набранных за полчаса (37 → 320),
сила 453 против 382 (гарнизон 206 + ополчение 148 + 28 рядом) → 1 боец, герой в
плену. Причина в коде: осада разрешалась, пока защитники не сильнее нас в
1,5 раза (то есть шли и при МЕНЬШИНСТВЕ), про уровень бойцов не спрашивали.
Сделано (`AutopilotConquest.cs`): осада только при своей силе >= 2x защитников
(`SiegeStrengthRatio`) и при среднем тире бойцов >= 3,5 (`SiegeMinAverageTier`);
иначе охота, как раньше, — бойцы растут в полевых боях. Гейт стоит во всех
трёх местах: своя находка крепости, штатное предложение игры, отказ в пути.
В журнале: «ПОХОД: осад пока нет — отряд зелёный: средний уровень бойцов X».
Стенд 669 ok; мутанты «без гейта уровня» (6 FAIL) и «старое 1,5x» (3 FAIL)
пойманы. DLL md5 `aeacaee5…` (в ней и погоня `a229aa52`).
**Числа — допущение, не проверены в игре:** 3,5 может копиться очень долго
(свежий набор тянет среднее вниз). Калибровать по журналу после первого стрима:
как часто «осад пока нет» и какой уровень был. Решение 23.09 «осада важнее
набора до 90%» живо, но теперь под этими двумя условиями.
Рейды деревень не тронуты (владелец про них не говорил).
**Пост-стрим 26.09:** после «отряд окреп» (23:16) войны были (Северная империя,
Вландия), но осад не было ни одной, а журнал не объяснял почему (ВЕРОЯТНО ни одна
крепость в досягаемости не проходит x2). Добавлено: раз в игровые сутки строка
«ПОХОД: крепостей по силам нет …; ближе всех к порогу «X»: защитники D, надо x2 =
2D, у нас O, с армией A» (`AutopilotConquest.cs`), стенд 672 ok, мутант пойман.
Установлено 26.09 ~01:15 (`AFA364F7…`, откат `D:/shedlink-build/dll-rollback/20260926-autopilot-siege-log`).
**Решать по этим строкам на стриме 26.09:** если отряду не хватает немного —
снизить x2 до x1,7; если много — осады только армией (сбор союзников).
**26.09 — почему бьют сразу после начала осады (владелец: «может автопилот заранее
выдаёт задачу сесть в осаду и на неё триггерятся боты?»).** Гипотеза НЕ подтвердилась:
марш к крепости идёт приказом «посетить» (`ApplyDecision`, Besiege → GetActionForVisitingSettlement),
а угрозу крепости ваниль ставит только при старте осады (`Settlement.LastAttackerParty`
в SiegeEvent; AiMilitaryBehavior берёт «оборону» только при нём). Факт по логу 25.09:
в 4 из 5 осад через 8–49 с после `town_besiege` нас атаковали в поле (`team=Defender`,
`siege=False`): 666 врагов (19:38) и 850 (22:07, Ремтойл) при оценке защитников 382.
Причина (ТОЧНО по коду оценки): `SiegeDefenderStrength` пропускал всех, кто в поселении
(лордов внутри крепости), и лордов дальше 35 или вне поля зрения. Исправлено
(`AutopilotConquest.SiegeDefenders`): + лорды внутри, + вражеские лорды в радиусе 100
(видны или нет, в поселении или нет); разбивка в журнале. Стенд 676 ok, мутант пойман.
**ГИПОТЕЗА:** что радиус 100 покрывает реальную подмогу — не доказано (кто именно
атаковал, в логе нет); смотреть разбивку на стриме. Не сделано: снимать осаду, если
во время подготовки идёт подмога сильнее нас — **сделано следом (владелец: «снимать осаду
и уходить, если все бегут давать пизды»; «пока идёт подготовка осады, к нему стягиваются
все, кому не лень»):** пока лагерь ждёт готовности, раз в игровой час пересчёт той же
защиты (стены + лорды внутри + подмога до 100); своя сила < защита x1,2 → «ПОХОД: снимаем
осаду … до удара» с разбивкой, 12 ч к крепости не идём. **Моя ошибка 25.09:** прежнее
снятие осады (24.09, армия от 2x) после «5x везде» требовало x5 — подмога x1,5–2 его не
проходила; тест Hopeless «x4,5 → штурмуем» переписан под новое правило владельца.
Стенд 681 ok, мутант «без снятия» пойман (4 FAIL). **Уточнение (владелец: «точно от
тех, кто идёт к нему, а не от мимо проходящих?»):** во время осады подмога = лорды
внутри + лорды с целью «эта крепость»/«наш отряд» (у армии — цель командира) + любые
ближе 15 к лагерю; проходящие мимо не считаются. При выборе крепости — все лорды в
радиусе 100 (подмогу зовут только со стартом осады). Стенд 682 ok, мутант «все
считаются идущими» пойман. **УСТАНОВЛЕНО 26.09 вместе с настройкой RBM.** **Та же картина 26.09 утром (старая версия `308A2494`):**
09:06:53 осада «Замок Друиммор» — оценка 184 гарнизон + 90 ополчение + 11 рядом = 285
против наших 582 (x2,0, прошло); через 15 с `lord_meet_player` → бой у лагеря в поле,
748 врагов, 357 бойцов → 5, герой в плену; следом рядом армия «Отряд Дертерта» силой
3 503. Добивка работает: shedoy23 при 0 личного урона получил 60 000 (30 000 x2 подписка).
**Собрано `DBAECD64…`, НЕ установлено —
игра запущена** (26.09 первая попытка упала на блокировке файла, .pdb возвращён).
**Владелец 26.09: «измени 2 на 1.5, вдруг пойдёт» — сделано:** `SiegeStrengthRatio = 1.5`
(своих в 1,5 раза больше защитников; уровень >= 3,5 остаётся). Стенд 672 ok, мутант
«x2» пойман. Установлено 26.09 (`308A2494…`, откат `D:/shedlink-build/dll-rollback/20260926-autopilot-siege15`).
**Первая проверка в игре (25.09, 22:21–22:38, 16 мин):** Harmony ok=27 failed=0,
исключений 0; «осад пока нет — отряд зелёный: 2.3 < 3.5» записано; 4 боя с
бандитами, все выиграны. Развороты A→B→A: 7 за 16 мин (~26/ч против 65–77/ч
до правки), все — погоня ↔ поездка за пополнением, когда цель ушла в чужой бой
(«ВСТРЕЧА: чужой бой … не вступаемся») — это правильный отказ, не срыв погони.
Хвосты (не начато): (1) «продолжаем погоню» пишется каждый игровой час —
до 12 строк за 5 с, писать один раз на цель; (2) поездка за пополнением
меняет деревню при каждом выходе (Орфра → Веалос → Самафа); (3) идея: в чужой
бой против бандитов вступать на стороне лорда — ещё бои для зрителей.
**Калибровка порога 3,5 (лог до 00:01 26.09):** «зелёный 2,3» в 22:29 →
«отряд окреп» в 23:16, за 47 минут: ~270 строк прокачки (имперские лучники
T2→T5 массово — зрители дают отряду много опыта). Порог не завышен. Осад после
этого не было — войн не было. Развороты: 15 за ~100 мин (~9/ч). Строк
«продолжаем погоню» 111 — хвост (1) выше подтверждён.

## 25.09 (вечер) — план оптимизации, шаг 2, и находки в бэкенде и моде

Замер после выката (стрим, ~20:00): p50 3 мс, p99 330–415 мс, пул без таймаутов.
Самый тяжёлый — equipment-shop, p50 133 мс. **Разобран на проде по частям:**
каталог 1031 вещь / 578 КБ; SELECT+разбор JSON 26 мс, `jsonable_encoder` FastAPI
67 мс, `json.dumps` 12 мс. **Главное:** ответ ~650 КБ уходит зрителю НЕСЖАТЫМ —
в `/etc/nginx/nginx.conf` `gzip on`, но `gzip_types`/`gzip_proxied`
закомментированы (сжимается только HTML); `$body_bytes_sent` 653 КБ медиана.
Сжатие уровня 4: 650 → ~70 КБ за ~5 мс. Для зрителя на телефоне это и есть «виснет».

| # | Что | Выигрыш | Риск | Статус |
|---|---|---|---|---|
| 1 | gzip JSON в `deploy/nginx/twitchbot.conf` | ответ магазина 675 → 63 КБ | низкий, reload мягкий | **выкачено 26.09 ~01:00** |
| 2 | кэш каталога + ответ без `jsonable_encoder` (`9b5ea444`) | 138 → 28 мс (копия прод-БД) | низкий: тест + 2 мутанта пойманы | **выкачено 26.09 ~01:00**, откат `backups/perf2-20260926/rollback.sh` |
| 3 | `player_state_update`: навыки/атрибуты одним INSERT, JSON-поля клана в общий UPDATE (`_adapter.py` ~1230–1294, до ~30 запросов на снимок) | миллисекунды | низкий, та же транзакция | **решено НЕ делать сейчас** |
| 4 | `/my-hero`: одно соединение вместо двух; не читать `bannerlord_equipment`, если есть снимок инвентаря | <1 мс | нулевой | **решено НЕ делать сейчас** |
| 5 | общий `aiohttp.ClientSession` для resolve-twitch-token | редкий путь | низкий | **решено НЕ делать сейчас** |
| 6 | следующая версия панели: опрос только открытой вкладки, battle-status реже вне боя, 5 GET главного цикла → 1 | на порядок меньше запросов на зрителя | средний (ревью Twitch) | следующий релиз |

Почему 3–5 не делаем (замер на стриме 25.09 ~16:00 UTC, 61 зап/с): время
СЕРВЕРА у событий мода p50 3 мс / p95 93 мс, у /my-hero p50 14 мс; 0,3 с у
событий — дорога от ПК стримера до сервера, её серверная правка не сокращает.
Вернуться, если серверное время (`urt=` в логе nginx) вырастет.
**Выкат шага 2 (ждёт слова владельца):** прод-версии `equipment_shop.py` и
`routes/bannerlord.py` сверены с базой `ddd15f29` — совпадают (отличие только
CRLF). Порядок: бэкап обоих файлов и конфига nginx → nginx: конфиг из репо,
`nginx -t`, `systemctl reload nginx` (соединения не рвутся) → бэкенд: два файла,
`supervisorctl restart twitchbot` (~5 с без связи) → проверка: `$body_bytes_sent`
магазина ~70 КБ, `urt` магазина ~40 мс.
Кэш JWT — решено НЕ делать: HS256 — микросекунды, а кэш даёт окно для
протухшего токена. `base64` секрета на каждый запрос — микросекунды, не стоит правки.

**Находки (проверены):**
- Автопилот дёргается: «ОХОТА» ставит EngageParty на грабителей, ближайший
  часовой пересчёт возвращает PatrolAroundPoint, потом снова охота. Пар A→B→A за
  ≤30 с: 43/ч в 14:00, 65 в 16:00, 77 в 17:00 — после правки «гулять по карте»
  не ушло. Причина (лог 16:14): охота бросает цель, как только та на миг
  быстрее нас («не догнать»), и шестичасовой пересчёт уводит в патруль.
  **Исправлено `a229aa52`: погоня держится до 6 игровых часов после
  подтверждения, пока цель жива, враждебна, не в поселении/бою и в радиусе;
  пересчёт её не перебивает.** Стенд 662 ok, два мутанта пойманы. Собрано
  (вошло в `BannerlordAutopilot.dll` md5 `aeacaee5…`), в игру НЕ установлено.
  Не сделано: патруль↔патруль (Жакулан→Остикан→Галенд каждые 6 игровых часов,
  когда текущий город выпадает из предложений движка) — отдельный разбор.
- Экран «попытаться уйти» выключал автопилот в 12:16 и 12:20 — ДО правки;
  после 12:24 таких выключений нет (лог до 20:30), но и строки `try_to_get_away`
  после правки нет — экран, похоже, больше не выпадал. Проход не доказан.
- `hero_id` после смены кампании: `player.linked` у neyrahatomia/dssardg в 12:07
  отклонён (`UNIQUE hero_id`, номер ещё держала старая строка), а заявка
  подтвердилась успехом, не дождавшись привязки (`AdoptHeroHandler.cs` ~302–306).
  Сейчас у обоих привязка верная (прод-БД, 20:25) — починилось следующим
  снимком. Вреда нет; нарушение «успех — утверждение». Низкий приоритет.
- Текст панели «Вещь попадёт в инвентарь его отряда»
  (`viewer-bannerlord-equipment.js:196`) врёт после «сундук для всех»; у героя
  со своим отрядом вещи сундука видны под заголовком «Старое хранилище мода».
  Панель заморожена — исправить текст (лучше отдавать его с бэка) в следующей версии.
- `direct_purchase` (купить и сразу надеть) с новым модом недостижим: нужен
  только для старого мода без сундука. Убрать вместе со следующей версией панели.
- `player.spawn:ally` 12:03 `InvalidOperationException` (Nullable) — деньги
  вернулись (PostFailed); тип миссии не опознан, нужен трейс при повторе.
- 210 отказов `power.activate` «герой ещё не на поле» — деньги возвращаются;
  панель не гейтит кнопку по факту появления героя (следующая версия панели).
- 30 `TaskCanceledException` мод→бэкенд — все дошли повтором через outbox.
- Чисто: «БЕЗ ЯВНОГО ИСХОДА» 0, срабатываний finalizer 0, Harmony failed=0.
- **Первая смерть в бою в новом сейве:** stepuhatgn, 21:18 по часам ПК, L25,
  проигранный бой против Империи (635 врагов). Бэкенд смерть получил
  (`bannerlord_events_log` 16:17:31 UTC). **Поправка к моему первому ответу
владельцу («клан распущен»): это было допущение, неверное.** В клане жила жена
Эйра (свадьба за 50 000💰 в 17:07, беременна), знатная, то есть лорд, поэтому
ваниль передала лидерство ей (`ChangeClanLeaderAction.ApplyWithoutSelectedNewLeader`),
а распускает клан только если других лордов нет. Тот же вызов переводит всё
золото старого лидера новому: так 107 861💰 и ушли в ноль (декомпиляция;
в игре не проверено — посмотреть в энциклопедии клана ShedoyGayclub). Теперь
клан с Эйрой и нерождённым ребёнком — NPC: зритель пересоздал героя без клана,
и защита `IsViewerClan` на этот клан больше не действует. **Дефект:** выплата за тот же
бой (21:22, 16 370💰) начислена УЖЕ МЁРТВОМУ герою и пропала: через 20 с
  зритель создал нового (1 500💰). Чинить в `BattlePayout`: мёртвому не платить,
  а платить текущему живому герою зрителя или наследнику. **Сделано минимально:**
  мёртвому не платим и пишем в игре «герой погиб — награда не начислена»
  (нового героя в момент выплаты ещё нет — платить некому). Собрано
  (`BannerlordLink.dll` md5 `9abf8949…`), не установлено, в игре не проверено.
  О компенсации 16 370💰: владелец 26.09 — нет (см. «Решено НЕ делать»).
- Защита «зритель без клана не исчезает» за день сработала 5 раз: dzirtdourden,
  stepuhatgn, igotpaws, nikita__g345, banderaslv (`[ViewerWanderer] BLOCKED`).

## 25.09 — скрытые правила игры против героев зрителей: 1–3 исправлены (владелец: «делай все три»), DLL `04A7AFE3…` установлена 25.09

Разбор по декомпиляции 1.4.8 + логам 21–25.09 (два агента, выводы сверены).
Подтверждено:
1. **Клан без королевства и без владений распускается через 28 игровых дней**
   (`FactionDiscontinuationCampaignBehavior`: таймер ставится при выходе клана из
   королевства, распаде королевства или потере последнего владения вне
   королевства) → `DestroyClanAction` → `ApplyByRemove` всех героев клана.
   С автопилотом 28 дней ≈ 10–15 мин. Защита `ViewerWandererRemovalPatch` не
   спасает (у героя клан ещё есть). Вероятно, так погиб fikoos418 24.09 (его
   клан уничтожен, kingdom=—); 21.09 до выключения бессмертия таких блокировок
   было 18. Распад королевства (потеря последнего поселения) сам кланы НЕ
   убивает — выводит их и ставит на тот же 28-дневный таймер.
2. **Игра сама женит героев зрителей** (`RomanceCampaignBehavior.CheckNpcMarriages`,
   база 0,2%/день) и, если зритель не лидер клана, переводит его в клан супруга
   (`GetClanAfterMarriage`). 25.09 dssardg: вступил в «Бану Сарран» за 50 000💰,
   игра женила на Чанбадар — теперь «Тигриты»/Хузаит (своего действия брака нет).
3. **Смена правителя королевства переписывает снаряжение** старому и новому
   правителю по шаблону (`NPCEquipmentsCampaignBehavior.OnRulingClanChanged`) —
   перековка и купленное пропадают. Случаев в логах не найдено (ВЕРОЯТНО по коду).
4. Мастерская зрителя уходит нотаблю при банкротстве (капитал и золото владельца
   меньше дневного расхода) — `WorkshopsCampaignBehavior.HandleDailyExpense`.
5. Дневные финансы клана (жалованье) списывают золото лидера молча — экономика
   принята владельцем как есть; плен зрителя мод не логирует вообще.
Опровергнуто: «защита героев не работает с 22.09» (агент по логам) — это
намеренно выключенное бессмертие; новый патч грузится (ok=24).
Сделано (`ViewerFactionGuardPatches.cs`): клан зрителя, его вассалы и чужой клан
со зрителем не распускаются по 28-дневному таймеру (и снимаются с него); игра
сама не женит зрителей и родню их кланов (свадьба по заказу — через MarryHandler,
мимо модели); смена правителя не переодевает зрителя-короля. `ViewerGuardHarness`
11/11, три мутанта пойманы; все стенды мода зелёные. В игре — искать
`[ViewerGuard] BLOCKED`.

## 25.09 — зрители без клана исчезали на дневном тике: исправлено, DLL `EF086CC5…` установлена 25.09

Вопрос kuro_gothic: «от чего я умер, если не было надписи смерть». Лог:
`HeroKilled: @kuro_gothic by unknown (Lost)` в 14:54:37 — на дневном тике, через
10 с после боя, clan=—, 966 052💰. Так же logistick1 (14:11, clan=—) и,
вероятно, ethanenok 24.09. Механизм (декомпиляция): героя зрителя мод создаёт из
шаблона странника (`AdoptHeroHandler`, Occupation.Wanderer); без клана ваниль
считает его ненанятым странником — `CompanionsCampaignBehavior.DailyTick →
TryKillCompanion` раз в игровой день с шансом 10% → `ApplyByRemove` (Lost, без
сообщения). До 22.09 это скрывало бессмертие (`AdoptedHeroDeathPatch`, выключено).
`WandererHome` («прописка» HomeSettlement) от этого пути НЕ защищает —
TryKillCompanion домашний город не проверяет; его обоснование неполное.
Фикс: `ViewerWandererRemovalPatch` — не даёт «убрать» героя зрителя без клана;
бой, старость, казнь, уход с уничтоженным кланом — как раньше.
`ViewerWandererHarness` 6/6, мутант пойман. В игре проверить строкой
`[ViewerWanderer] BLOCKED` и что без-клановые зрители не пропадают.
fikoos418 24.09 — другое: его клан уничтожен (ваниль убирает героев клана,
по решению 22.09 так и задумано). Решение владельца 25.09: kuro_gothic — откат сейва на
момент, когда он жив (без компенсации); logistick1 — +100 000💎 (начислено,
уведомление #2536).

## 25.09 — экономика зрителей: исследование (`docs/ECONOMY_RESEARCH_2026-09-25.md`)

Открыто: (1) расход ванили на клан/королевство зрителя не логируется — не
измерить «сколько стоит клан в день»; предложена строка лога с разбивкой
дневного баланса клана. (2) Документы врут про конверсию доходов владений и
мастерских в крустики (100:1/200:1) — в коде `credit_* → 0`: докстринги
`bannerlord_fiefs.py`, `migrations/m55_workshops.py`, комментарий
`FiefTributeSyncBehavior.cs`. (3) Свита теряется навсегда с шансом 3% за
гибель: у топ-зрителя до 12 бойцов за день (до нескольких млн💰).
**Решение владельца 25.09: «по идее нормально»** — экономику не трогаем:
свита 3%, осадный множитель 1,6, выплаты как есть; строку лога баланса клана
тоже не заказал. Вернуться, если жалобы на бедность новичков или раздувание
(признак — зрители выше 10 млн💰).

## 25.09 — автопилот тёрся у «Диатмы»: гуляет по нейтральной карте, установлено 25.09 (`249BE152…`)

Причина (декомпиляция `AiPatrollingBehavior.CalculateDefensivePatrollingScores`):
у фракции без поселений патруль вокруг города оценивается как
k·(ср.расстояние·5 / расстояние от ДОМА отряда до города) — домашний город x5;
штраф автопилота за долгий патруль (до −0,8) его не перебивал (3,50 против
0,3–0,9). Владелец: «пока нет королевства и территорий пусть гуляет по карте,
где нейтральные территории». Сделано: пока у фракции отряда нет поселений,
домашняя дробь снимается; города враждебных фракций для патруля — 0. С
поселениями у фракции ничего не меняется. AuditRegression 656/656, два мутанта
пойманы. Автопилот `249BE152…` (вместе с правкой раненого героя) ждёт закрытия
игры. В игре не проверено: смотреть, что «НОВАЯ ЦЕЛЬ … PatrolAroundPoint» меняет
города.

## 25.09 — «5x везде» у раненого героя: исправлено (владелец: «раненый даже в бой зайти не может»), установлено 25.09 (`249BE152…`)

Лог 25.09 12:37: после убежища герой ранен, напали бандиты; по правилу 5x
отхода нет → «Послать воинов» (авторасчёт) → из 24 бойцов остался 1, герой в
плену (mno_continue 12:37:57). В авторасчёте миссии нет — зрителям некуда
призываться, бой им ничего не даёт. Моё возражение (было и до установки):
раненому герою вернуть отход при силах хуже 0,8x, здоровому оставить 5x.
Предложено владельцу, ждём ответа.

## 25.09 — автопилот: отступление только от 5x везде + экран «попытаться уйти» (установлено 25.09, DLL `67501EB2…`)

Владелец на стриме: «слишком часто начал съёбываться, даже если врагов на 10–30
больше» → «отступление только при 5х перевесе везде». Было: 2x по силе игры
(карта, бой, снятие осады), 5x за свой замок, 0,8x у раненого героя. Теперь 5x
везде; у раненого героя ниже 5x — авторасчёт, как до 24.09 (риск потерять отряд
в авторасчёте вернулся — осознанно). Попутно: после «Попытаться уйти» игра
открывала `try_to_get_away` → `try_to_get_away_debrief`, автопилот их не знал и
стоял на паузе — теперь «Продолжайте» и «Продолжить». AuditRegression 652/652,
мутант «порог 2x» → 4 FAIL. **Старая поломка, не чинил:** стенды
`ContractBlindSpots`, `ContractGetter`, `ContractScoresGetter` не собираются с
~17.09 (`EngineContract.cs` использует `TaleWorlds.CampaignSystem.ComponentInterfaces`,
которого нет в их заглушках) — то есть эти проверки контракта с движком
молча не работают больше недели.

## 25.09 — главный герой погиб в новой кампании на 26-й минуте (ждёт решения владельца)

Новая кампания 25.09 (save `GzijZ3JXkjVd`, старт ~11:28, 1 боец, 1000💰).
Автопилот за 26 мин провёл 11 боёв; в бою 11:53–11:54 (выигран, грабители)
герой, переданный штатному ИИ, погиб — игра загрузила
`scn_cutscene_main_hero_battle_victory_death` (rgl_log 11:54:46). Наш патч
0,02% действует только на героев зрителей, главного НЕ касается. Ваниль
(`DefaultPartyHealingModel.GetSurvivalChance`): смерть за нокаут =
1/(51·(1 + лекарь + ур.·0,02 + броня·0,01 − возраст·0,01)); свежий герой в
обмотках ≈ 2% за каждый нокаут, одоспешенный 30-го уровня ≈ 0,7%. Опция кампании
«Смерть в бою» стоит не на «Лёгкой» (иначе игрок бессмертен). Варианты: опция
«Смерть в бою: Лёгкая» в меню кампании (без кода); либо автопилот не пускает
слабого героя в ближний бой (но в авторасчёте зрителям некуда призываться).

## 25.09 — «Защитник»: сделан строго по тексту (владелец: «сделать как написано»), установлено 25.09, DLL `7EADF777…`

Проверка сборок и оружейных способностей по просьбе владельца (стрим 25.09):
10 из 11 обещаний панели совпадают с кодом и логом (значения по рангу, 45 с,
КД 90 с, условия оружия). Расхождение: «Защитник» — «+15% здоровья в полевых
боях», а код (`PowersMissionBehavior.cs`, гейт только `IsArenaOrTournamentMission`)
даёт ×1.15 во всех боях, кроме арены/турнира — и в осадах, и в убежищах (лог
22.09: hp×1.15 у shedoy23 в бою с `siege=True`). Варианты: поправить текст в
`HeroBuildRuntime.cs` (рекомендую) или сузить код до `IsFieldBattle`. Оба — DLL,
после стрима. Урон на каждом попадании пишется только в verbose-лог (выключен),
поэтому поштучное применение проверено кодом, не логом.

## 25.09 — на проде незакоммиченный код Codex (разблокирует: коммит Codex)

`deploy.ps1 -Backend` остановлен проверкой дрейфа: `routes/admin.py`,
`routes/bannerlord_admin.py`, `routes/promo.py`, `routes/streamer.py` на проде
(23.09) нет ни в одной ветке; совпадают с НЕЗАКОММИЧЕННОЙ рабочей копией
`.claude/worktrees/codex-public-release` (сверено по `promo.py`). Сундук выкачен
поимённо тремя файлами (прод = база до правки, сверено), резерв
`/root/twitch-extension/backups/stash-20260925/`. До коммита Codex полный выкат
из любой ветки откатит выбор канала в админке и проверку промокодов.

## 25.09 — личный сундук героя без отряда: СДЕЛАН (`docs/BANNERLORD_PERSONAL_STASH_2026-09-25.md`)

Решение владельца: 10 мест, переходит наследнику, панель не трогать. Отложено
до следующей версии панели (Twitch): подписи «Инвентарь отряда (багаж)» и
«Вещь попадёт в инвентарь его отряда» у героя без отряда → «личный сундук».
Не сделано сознательно: новый герой через `hero.create` (не наследник) сундук
не получает — владелец сказал «наследнику». **Моя ошибка в записи 25.09:**
писал, что надевание из хранилища без отряда падает NRE — неверно, раньше
срабатывал отказ `no_inventory` (строка-гард в начале обработчика).

## 24.09 — оверлей и чат-бот (разбор `docs/OVERLAY_CHATBOT_REVIEW_2026-09-24.md`)

- **Выкачено 24.09 ~18:10 UTC** (владелец: «делай всё»): дроп на оверлее по
  своему каналу, у чиров свой лимит приветствий (`002e870b`); 6 файлов
  (`dependencies.py`, `routes/misc.py`, `main.py`, `bot_core.py`, `eventsub.py`,
  `frontend/overlay.html`), откаты рядом `*.rollback-20260924`. Перед заменой на проде
  файлы совпадали с версией до правки. Health 200, IRC переподключился.
- **Автосообщения 20 → 60 мин** (UPDATE 7 строк канала 98319857 на проде; было
  20 у всех) — строка рекламы раз в ~8–9 мин вместо ~3. Стример может менять
  частоту сам в дашборде (редактор есть, агент ошибся, сказав, что нет).
- **Старые дефекты тестов (не мои):** `test_m109_migration_ledger`,
  `test_m110_manager_credentials`, `test_manager_pairing_core`,
  `test_rimworld_child_tenant_migration` падают в обычной консоли Windows на
  печати эмодзи (`UnicodeEncodeError`), с `PYTHONIOENCODING=utf-8` зелёные;
  `test_frontend_module_lifecycle` — «cache-bust всех viewer-модулей — одна метка»
  красный и до правки.
- **Второй бой сразу после победы — исправлено 24.09** (решение владельца:
  «если первый бой проебали, второй автоматически тоже»). `BattleContinuePatch`
  (BannerlordLink): победа в полевом бою в сцене засчитывается сразу, без второго
  боя с бежавшими. Наш проигрыш, осады, убежища, море — как в игре. Выключатель —
  имя в `SKIP_PATCH_NAMES`. DLL `D0C4177D…`, откат (`13E9597A…`) в
  `D:/shedlink-build/dll-rollback/20260924-link-battle-continue`. В игре не
  проверено: после победы над лордом с бежавшими — строка `[BattleContinue]` в
  логе мода и никакого «Атаковать» заново.

## 24.09 — странности автопилота по логам (разбор `BannerlordAutopilot/review/ODDITIES_2026-09-24.md`)

- **Раненый герой, безнадёжный бой, мигающая цель осады — исправлено 24.09**
  (владелец: «Делай»), DLL `DFFEC6A7…`, разбор в `ODDITIES_2026-09-24.md`.
  Проверить на эфире: `autopilot-metrics.py` — строки «отступлений», «авторасчётов»,
  «снятых осад»; армия не должна обваливаться до 1 бойца, кроме плена.
- **Не сделано:** штатная инициатива игры (`TryApplyNearbyAttack`, перевес > 1)
  может напасть на бандитов крошечным отрядом — «Охота» и «Отход» это в основном
  закрывают, живьём не проверено.

## 23.09 — хвосты сверки аудита 0.0.5 (Codex)
- **Поданный в Twitch ZIP `6f8e07f5` не лежит ни в одном коммите.** Собран из
  `5dc8a5bd` + незакоммиченных правок worktree `codex-public-release` (36 файлов,
  ~1200 строк, там же неподтверждённая запись в `RELEASE_RECORD.md`). От живой
  ветки фронт ZIP отличается метками кэша, `shedlink-version` и строкой бонуса
  вассалов. Байт-копия теперь в `backend/tests/fixtures/frozen_client_0.0.5_review/`.
  Разблокирует: решение, чья это работа (Codex) — закоммитить или выбросить;
  чужой незакоммиченный код не трогал.
- **На проде два осиротевших файла** `event_manager.py`, `routes/event.py` (от
  02.07, механика вырезана 29.07, в `main.py` не подключены). Вреда нет — выкат
  не удаляет файлы. Убрать при случае руками.

## 23.09 — автопилот: цель владельца и хвосты

- **Цель владельца (23.09):** автопилот должен сам довольно умно играть, пока
  владельца нет дома, а зрителей надо развлекать. Шаги 1–2 (не ронять игру, не
  выключаться на ложной тревоге прокачки) сделаны. Прежде чем строить «эфир без
  стримера» — проверить правила Twitch про стримы без присутствия (`/twitch-compliance`).
- **Охота 0,8x — установлена, возражение зафиксировано.** Владелец выбрал
  «безбашенный». Моё возражение: при 0,8x чаще проигрыши с лордами, а плен героя
  = нет отряда, зрителям некуда призываться. Смягчено режимом «Восстановление»
  (нет охоты при заполнении < 50%). Проверить на эфирах: доля поражений и
  случаи плена; если плен частый — поднять порог (`HuntMinRatio`).
- **Мотив владельца к 0,8x (23.09):** в начале боя присоединяются зрители со
  своей свитой — реальная сила на поле больше, чем видит карта. Возражение про
  плен остаётся проверяемым: `autopilot-metrics.py`, строка «герой в плену».
- **Сделано 23.09:** метрики (`scripts/autopilot-metrics.py`) и приоритет осад
  (на войне при заполнении >= 70% осада важнее набора до 90%; охота по дороге на
  осаду в радиусе 10). Осталось: подстройка порогов по эфирам.
- **Отход (23.09, решение владельца):** бежим от 2x; оборону своего замка
  бросаем только перед 5x — «со зрителями отбивали и такое». Подтверждено логом
  22.09 (защитники: победы при ~5,2x по числу бойцов, поражения от 6,4x).
  Проверить на эфирах: `autopilot-metrics.py` — строки «отходов» и «отсидок».
- **Политика (23.09, решение владельца):** ровно 1 война; без защиты платных
  кнопок 20 дней — владелец считает, что игра не даст мир сразу после войны.
  Декомпиляция: верно для войны после мира (20 дней у штатного ИИ), для мира
  после войны — только «враг не готов к переговорам», не таймер. Следствие:
  платная война зрителя поверх текущей = 2 войны → автопилот ищет мир (не с
  самой новой, а где нужнее). Если на эфире мир будет перечёркивать оплаченное —
  вернуть защиту. Призыв союзника и торговые соглашения — сделаны 23.09.
- ~~**Самовыключения 21.09, не разобраны**~~ — **ошибка записи (24.09):** все три
  вида починены Codex ещё 21.09 (`93efc2d5`, `8ba6c252`, `be806029`, `64817ba9`);
  22.09 ни одного. Я записал их открытыми, не проверив историю коммитов. Вместо
  них сделан общий **автовозврат** (после самовыключения — сам включается на
  свободной карте, до 3 раз за 30 мин). Остаток риска: выключение в меню, которое
  само не закроется, — автовозврат ждёт свободной карты; в логах 20–22.09 не было.
- **Не сделано, предлагалось:** тесты автопилота не в CI; `ContractBlindSpots`,
  `ContractGetter`, `ContractScoresGetter` не собираются с 16.09.
- **Идея:** оборона с 1 бойцом едет к осаждённому замку, получает «недостаточно
  людей» и откатывается на 6 ч. Можно проверять штатную модель потерь при прорыве
  (`TroopSacrificeModel.GetLostTroopCountForBreakingInBesiegedSettlement`) ДО выбора
  цели и сразу ехать нанимать. Не делал: не просили, поведение безопасное.

## 23.09 — триаж эфира 22.09: хвосты (разбор `docs/POSTSTREAM_TRIAGE_2026-09-22.md`)

- **Бессмертие выключено — первый час: РЕШЕНО оставить как есть (владелец,
  23.09).** Мотив владельца: герои массово умирали, потому что за время
  бессмертия им набежало по ~90 лет; голый призыв после смерти погибает в
  текущем бою, а в следующем бою всё нормально. Моя поправка к фактам, для
  следующей проверки: по логу от старости умерли 2 из 11 (igotpaws,
  nikita__g345), остальные 9 — в бою, и новые герои L5 были в возрасте 27–34.
  Если после волны стариков смерти в бою не станут реже, вернуться к вопросу.
- **Шанс смерти в бою: один для всех зрителей — 0,02% за падение** (сначала
  0,1%, после подсчёта призывов владелец снизил; решение 23.09, `docs/BANNERLORD_DEATH_CHANCE.md`). Заменил правило Codex
  «5% от ванильного», у которого герой без отряда получал 5% за падение.
  DLL установлена, **в бою не проверено**: на следующем эфире смерти в бою
  должны стать редкостью (ожидание: самый активный зритель ≈4% за эфир).
- **Мёртвый герой шлёт состояние под ником зрителя** (23:15:25, L36 после
  смерти в 23:08) — на один ник два героя. Не разбиралось.
- **#64 (раздача трофеев) и #66 (перекованная вещь пропала при смене)** —
  доказать нечем: мод не пишет раздачу трофеев и не пишет, в чей инвентарь
  ушла снятая вещь (`equip_owned` кладёт её в `hero.PartyBelongedTo.ItemRoster`
  — если герой в чужой партии, вещь уезжает чужому). Разблокирует: строка в
  логе мода на обе точки.
- **Краш 14:15 22.09 без дампа** — папка `crashes` удалена целиком. При
  следующем краше не чистить её до разбора.

## 23.09.2026 — редкая гибель зрителей в бою (локально)
По просьбе владельца после отключения бессмертия fe40bb5: ViewerBattleMortalityPatch уменьшает вероятность гибели зрительского героя до 5% от штатной: survival=1-(1-survival)*0.05. Основа ветки882086d, сохранены отключённые кампанийная защита и боевая конверсия, защита коней прежняя. Старость, казни, Lost и наследование не меняются.
Движок1.4.8: SandBox AgentStatCalculateModel использует PartyHealingModel.GetSurvivalChance для вероятности гибели; CampaignSystem simulation также использует этот метод. Патч DefaultPartyHealingModel, дополнительного RNG нет. Мод, полностью заменяющий эту модель без вызова base, может обойти поправку. В просмотренном списке классов установленного RBM Healing-модели не обнаружено.
Тест: ViewerMortalityHarness красный5/3 exit1 (без нового патча), зелёный8/0 exit0; Release0/0. Проверены исходные риски100%,50%,2%,0% и отсутствие изменений NPC/обычных солдат/null. Файлы DLL/PDB: D:/shedlink-build/viewer-battle-mortality/BannerlordLink/bin/Win64_Shipping_Client. Не установлено, живой бой не проверен.
## 22.09 — бессмертие зрителей ВЫКЛЮЧЕНО (решение владельца)

Зрители теперь гибнут по ванильным правилам. Выключено двумя выключателями,
оба возвращаются одной строкой:

- `KillCharacterAction_ApplyInternal_Patch` добавлен в `SKIP_PATCH_NAMES`
  (`BannerlordLinkModule.cs`) — это была кампанийная защита от смерти;
- `HeroBattleImmortality = false` в `Mission_OnAgentRemoved_Patch` — боевая
  конверсия «убит → без сознания» для героев.

**Защита КОНЯ оставлена намеренно:** конь оплачен отдельно и к бессмертию героя
отношения не имеет. Если надо и его — снять там же.

Побочный эффект, ради которого это и делалось: исчезает источник кланов
«уничтожен, но живой». Ваниль помечала клан и убивала героев, мы смерть глушили,
флаг оставался — отсюда и краш 22.09, и пропавшее королевство у `neyrahatomia`.
Теперь ваниль доводит уничтожение до конца.

**Чего это стоит зрителям, назвать вслух:** адоптированный герой — платный
контент. Смерть теперь настоящая: вложенные крустики в перековку, классы,
снаряжение уходят вместе с героем. Ни предупреждения в панели, ни компенсации
не предусмотрено — этого разговора ещё не было.

Проверки: сборка Release 0/0, 15 гейтов мода и 3 гейта автопилота зелёные.
В игре НЕ проверено — доказательство будет в логе первого запуска:
`Harmony patch SKIP (safe-rollback): ...KillCharacterAction_ApplyInternal_Patch`
и отсутствие строк `[DeathProtect] BLOCKED`.

## 22.09 — личная часть и участие в боевой выплате подняты (решение владельца)

Повод: в последней осаде (1650 врагов) зрители выдали по 8–10 тысяч личных
очков, а получили 377–616 тысяч — вклад перестал различаться, потому что кривая
`P/(P+4000)` на таких числах плоская, а участие упиралось в потолок 8000 очков.

Решение владельца дословно: «личную часть надо поднять, чтоб такие вклады
оценивались не меньше миллиона», «такие осады не часто и чтоб так заработать
надо чтоб зритель прям попотел», «поднять потолок участия с 8000».

Сделано так, чтобы обычные бои не изменились ВООБЩЕ:

- прежняя кривая личной части не тронута; выше **6700 очков** (это ровно
  максимум, наблюдённый 21.09 — 59 убийств человека) включается вторая кривая
  «сверхусилие», потолок 386 000 до множителя и масштаба;
- участие растёт тем же наклоном, но теперь до **20 000** очков вклада вместо
  8000; всё до 8000 платится ровно как вчера.

Пересчёт на фактических входах той осады:

| зритель | личных очков | было | станет |
|---|---|---|---|
| slopkom_nyi_item | 10 502 | 616 650 | **1 338 930** |
| logistick1 | 9 448 | 525 606 | **1 129 238** |
| analniyshekotyn | 8 978 | 518 068 | **1 055 918** |
| dzirtdourden | 7 750 | 469 832 | 777 950 |
| igotpaws | 4 575 | 399 748 | 399 748 (без изменений) |
| omaryn42 | 5 402 | 432 776 | 432 774 (без изменений) |

Обычный бой на 172 врага: было 30 048, стало 30 050 — то есть не изменился.

**Моё возражение, зафиксировано:** это умножает самый быстрый источник динаров.
Возражение против ×2 от 21.09 в этом же файле ещё открыто, а теперь верх кривой
поднят ещё втрое. Владелец ответил, что такие осады редки и требуют реального
усилия — принято, сделано. Если динары начнут обесцениваться, крутить надо
порог 6700 (выше = реже срабатывает), а не множитель.

**Ожидания гейта `BattlePayoutHarness` пришлось переписать:** три проверки
держали прежние абсолютные потолки (499 200 / 798 720 / 119 808). Это была
калибровка, а не правило, и владелец её сменил; новые потолки
1 605 600 / 2 568 960 / 385 344 закреплены теми же проверками. Характеризация
«ровно вдвое против выплат 21.09» продолжает держать участие и свиту.

**В игру НЕ поставлено.**

## 22.09 — панель `neyrahatomia`: «нет отрядов и не в королевстве» — разбор

**Отряд.** Данные отставали, сейчас на месте: он купил отряд в 19:52 по игре,
зеркало получило снимок в 19:58 — 26 бойцов, стоит на месте. Не дефект, задержка
в минуты.

**Королевство — дефект, и корень глубже панели.** Его клан `[BLink] Рой пчёл`
помечен в игре как **уничтоженный**, хотя живой: 5 членов, свой отряд, 24 430
известности, лидер — он сам (`clan_info_json`). Игра при этом по-прежнему
считает его в Вландии (`kingdom=Вландия` в каждом снимке мода до самого выхода).

Зеркало теряет королевство, потому что `HeroStateSync.BuildKingdomInfo` отдаёт
`null`, если клан помечен уничтоженным:

    var kingdom = hero?.Clan?.Kingdom;
    if (kingdom == null || kingdom.IsEliminated || hero.Clan.IsEliminated) return null;

Бэкенд честно записывает пустоту, фронт честно показывает «не в королевстве».

**Моя вина рядом:** сегодняшний `ValidKingdomSupportersPatch` фильтровал
голосующих в том числе по `IsEliminated` — и в 14:27 выкинул ЕГО клан из выборов
(строка `[voter-guard] клан '[BLink] Рой пчёл' убран из голосующих`). Ваниль на
таком клане не падает, падает только на `Leader.Clan == null`. Условие убрано,
гейт видел красным. В игру ещё не ставил.

Остаётся владельцу:

- **Откуда берётся «уничтожен, но живой»** — это тот же класс, что уронил игру:
  ванильный `DestroyClanAction` помечает клан и убивает героев, а наша защита
  смерти зрителей оставляет их живыми. Клан остаётся с флагом. Нужно решить:
  снимать флаг при живых членах или не доводить до `DestroyClanAction`.
- **`BuildKingdomInfo` не должен молчать про королевство живого зрителя** —
  но чинить это до ответа на пункт выше нельзя: сначала надо понять, что такое
  «уничтоженный клан» в нашей игре.
- `is_clan_leader` в зеркале стоит 0, а `clan_info_json` говорит `is_leader:true`
  — расхождение двух путей записи одного факта, отдельная мелочь.

## 22.09 — «деньги пропадают»: не дефект, но зритель не видит содержания

Разбор по жалобе `neyrahatomia`. За игровой день 22.09 у него пришло
+3 589 195 динаров, ушло −1 964 357, итог **+1 624 838** (было 71 653, стало
1 696 491). То есть он в плюсе, а видит только списания.

Куда уходит: **44 из 49 крупных списаний происходят на суточном тике**
(−1 746 991, 89% всех потерь) — это ванильное жалованье войскам его отрядов.
Средний убыток за тик вырос в 2,4 раза сразу после покупки отряда в 10:48:
до — 14 304, после — 34 778. Всего у него 8 покупок отряда и 13 наймов войск.
Вне тика списаний всего пять, и все — его собственные покупки (свадьба 50 000,
отряд 200 000).

Проверено, что это НЕ мы: королевский налог мода ни разу не сработал (в логе
только строки регистрации), как вассал он никому не платит (ноль строк
`VassalIncome`), других списаний динаров расширение не делает.

Отдельно: за день у него погибло **8 караванов** (11 записей — три шаблона
возродились и погибли снова; это ванильное поведение, дубля событий нет).
Больше, чем у всех остальных вместе взятых. Каждый куплен за 4 000 крустиков,
и вместе с караваном пропадают вложенные динары и весь будущий доход.

**Что стоит сделать (решение владельца):** зритель не видит ни строки про
содержание. В панели есть баланс, но нет ответа на вопрос «сколько я плачу в
день». Дешёвый вариант — показывать в карточке героя суточный баланс
(доход минус жалованье) и число живых отрядов/караванов. Без этого каждый, кто
купит второй отряд, придёт с тем же вопросом.

22.09 12:21+05: Autopilot f40d4c9 с удержанием центра камеры УСТАНОВЛЕН при закрытой игре по команде владельца. SHA256D2AB7C9192C613F3C25C3616010553037B3783F3776924BFB2ADF03ED5BA2FD9, MD5112825DC287C79EE2C259B0827BF5493. DLL/PDB совпали со сборкой overview-stable-20260922. Откат D:/shedlink-build/dll-rollback/20260922-122100-heirs-camera. Живой повтор ожидается. Свежий краш12:09 находится в DefaultDiplomacyModel.GetRelationScore при суточном голосовании за мир, не исправлен; разбор в poststream-fixes-20260922/docs/CRASH_PEACE_2026-09-22.md.

22.09: удержание центра камеры ГОТОВО ЛОКАЛЬНО. Далёкая группа должна превышать оценку текущей в1.5раза+5 и сохраняться12 последовательных опросов (6секунд); краткие волны подкрепления сбрасывают кандидата, пустой старый центр отпускается сразу. Перемещение по-прежнему плавное. Красный c7f7a99:43/1; после43/0, Release0ошибок/0предупреждений. DLL D:/shedlink-build/overview-stable-20260922/bin. НЕ установлена; живой повтор ожидается.

22.09: исправление отрисовки камеры6ab4e29 УСТАНОВЛЕНО 2026-09-22T11:29:07.7909256+05:00 по команде владельца, при закрытой игре. Наклон40°, высота80м. DLL/PDB SHA256 совпали со сборкой; DLL SHA2564CFDCE290A50A9C7BF7D5C9679F9F990A97A4274D0F5593FDF7ECFA9DCA20BFD; MD5EB911A0F526A788012A6E49102C9E71F. Откат: D:/shedlink-build/autopilot-dll-rollback/20260922-112906-before-camera-render-fix. Живой повтор ожидается.

22.09: исправление высокой камеры ЛОКАЛЬНО. Добавлены SetFovVertical (65°, near0.1/far12500) и Mission.SetCameraFrame каждый кадр: override пропускал штатный UpdateCamera целиком. Наклон40° вместо60°, настройка25–80°, высота80м сохранена. Красный тест848a15d:40 проверок/5 отказов; после40/0, Release0ошибок/0предупреждений. DLL D:/shedlink-build/overview-camera-fix-20260922/bin, SHA2564CFDCE290A50A9C7BF7D5C9679F9F990A97A4274D0F5593FDF7ECFA9DCA20BFD. НЕ установлена, игра не закрывалась. Устранение исчезновения моделей требует живого повтора; native renderer стендом не воспроизводится.

22.09: по команде владельца новая камера УСТАНОВЛЕНА при закрытой игре 2026-09-22T11:10:49.6013944+05:00. Сборка20bcc3b; DLL SHA256154E56CE0F9AFC5F46AEC0A88C91B537F1B916F0473A6F2479C7E02899FE0855; MD5030C90EE1BC924EC9C4D23C835F0313E. DLL/PDB сверены. Откат: D:/shedlink-build/autopilot-dll-rollback/20260922-111048-before-overview-camera. Прежний запрет установки снят командой «ставь новую длл». Живой обзор ещё не проверен.

## 22.09 — высокая камера: не устанавливать

Владелец прямо запретил пока ставить. Готовая сборка overview-camera-20260922;
живая проверка высоты/осад/подписей/F9 после разрешения на установку.
См. BannerlordAutopilot/review/OVERVIEW_CAMERA_2026-09-22.md.

## 22.09 — живой повтор Hammer of the Sun

После локального исправления повторной проверки очереди требуется повтор последнего save: событие должно обработаться до выхода из города. Дамп доказывает null CurrentSettlement и оставшееся событие, но не точный callback постановки. Не объявлять живой краш устранённым по одному стенду. См. BannerlordAutopilot/review/ALLY_THANKS_AND_INCIDENT_2026-09-22.md.

Автопилот 21.09: DLL с починкой двух пауз (be80602, 2f8fab4) собрана, но НЕ установлена — игра была запущена, владелец просил её не закрывать. Разблокирует: закрытая игра, копия DLL в `Modules/Shedoy23.BannerlordAutopilot/bin/Win64_Shipping_Client/` с откатом и сверкой md5. Живой прогон: убежище, где окно отряда не открылось (ждём в журнале «уходим штатной кнопкой» вместо выключения), и своя крепость под осадой (ждём нажатия обороны или прорыва).

Автопилот 21.09: механизм несовпадения книг при улучшении «Новобранца имперской виглы» НЕ НАЗВАН. Сработало трижды за день (19:55:34, 20:15:53, 21:18:07) и один раз стоило замка. Починка сделана только в реакции: прокачка захода прекращается, автопилот живёт, в журнал пишутся числа сравнения. Разблокирует: первая строка «ПРОКАЧКА ОСТАНОВЛЕНА: … не подтверждено: <числа>» в живом журнале — она назовёт, какое именно сравнение разошлось. Подозрение (не проверено): посторонний подписчик `OnPartySizeChanged` правит ростер прямо посреди обмена бойца — в сборке стоят RBM и CoherentWarAI.

Автопилот 21.09: почему штатный `assault` в убежище изредка не открывает окно выбора отряда — не доказано. Штатное последствие делает единственный вызов `Handler?.OnOpenTroopSelection`, то есть без обработчика меню кнопка молча не делает ничего; живым кадром это не подтверждено. Починка сделана в реакции (уход штатной кнопкой), а не в причине. Разблокирует: повтор в игре с новой DLL — в журнале будет полное описание меню на момент таймаута.

Уточнение 18.09 (вечер) к трём находкам выше, после разбора с владельцем.

По еде владелец сказал: «это как раз потому что он вставал и думал думал думал» — проверено, он прав, и цифра такая. За день 79 эпизодов, где позиция И поведение не менялись шесть и больше часовых записей подряд; из них 55 — под приказом осады (самый длинный 74 часовые записи), 24 — Hold, и эти почти все законные: разговор или встреча, мод их и не трогает. То есть еда уходила в ноль не из-за маленькой цели запаса, а потому что партия стояла, а время на ускорении шло. Причину чинит сторож движения из этой же смены. Цель запаса НЕ трогаю: сначала живой прогон с новой DLL, потом мерить. Второй, независимый вклад в голод — рынок: 40 раз за день штатная модель закупки не нашла в поселении подходящей еды («нужно было 123, запас пополнен на 54, на складе 0»); это не потолок трат мода, а маленькие поселения против отряда в 218 бойцов. Решать после того, как стояние уйдёт.

По меню join_encounter уточняю, чей это путь. Разбор в моде ЕСТЬ, но только для случая «наша осаждённая крепость» (AutopilotOperations, case join_encounter → help_defenders при FriendlySiege). Путь, на котором мод выключился 18.09 в 14:32:24, другой: прерывание рейда в поселении, FriendlySiege ложно, обработчик возвращает false, и меню доезжает до ветки «неизвестное меню». Код с этим разбором был установлен (7490aa0, 14:28) — то есть остановка случилась ПРИ нём, а не до него. Нейтральная починка — нажимать join_encounter_leave, как мод уже делает в чужих полевых боях; ждёт слова владельца, потому что альтернатива (помогать стороне) — продуктовое решение.

Сводку после загрузки сейва («бойцов 1, еда 1079 дней») владелец пометил как неважное — не чиним, строка остаётся тут как известная помеха при триаже.

Автопилот 18.09, полный триаж журнала за день (07:23–18:59, 107 165 строк). Ни одного исключения, ни одной строки «не удалось»/«БЕЗ ЯВНОГО ИСХОДА»; в игровых rgl_log_errors за день только безобидные строки про озвучку. Найдено три вещи сверх уже записанных.

1. Меню `join_encounter` выключает автопилот, и это хвост починки 64a5526. 18.09 14:32:24 в «Сестадайм»: мод нажал `encounter_interrupted_raid_started_leave` (та самая починка 17.09) и попал в меню `join_encounter [help_attackers, help_defenders, abandon, leave]`, которого в этом состоянии не знает, — «ВЫКЛЮЧЕНИЕ: открыто меню, которое автопилот не знает». То же 17.09 20:44:51 в «Линдорн». Обе остановки ПОСЛЕ той починки: она переводит игру в меню, чей разбор есть только на пути «чужой полевой бой» (`IsForeignFieldBattle`), а из поселения этот путь не срабатывает. Нужно решение владельца: помогать ли стороне вообще. Нейтральное и совпадающее с текущим поведением — `join_encounter_leave` (мод так и уходит из чужих боёв, 5 раз за день штатно). Пока не сделано.

2. Запас еды рассчитан на человеческий темп, а не на автопилотный. За день 16 недельных сводок с едой меньше трёх дней, из них три с ровным нулём (11:58:58, 11:59:15, 15:22:01) — при 590 тысячах золота в кошельке. Причина арифметическая: цель запаса 12 дней (ServiceLimits, MaxFoodSpendPerPass 5000), а на ускоренном времени 12 игровых дней проходят примерно за минуту реального: между 15:15 и 15:22 счётчик недель ушёл с 13351 на 13363. Мод при этом честно пишет «еда: хватает (на складе 330, цель 12 дн.)» — он не врёт, он меряет не тем. Тот же класс, что и вылет 14:34: наш темп ломает расчёт, сделанный под человека. Чинить — поднять цель запаса и снять потолок трат, пока денег кратно больше расхода; разблокирует живой прогон, чтобы увидеть, сколько запаса реально хватает между поселениями.

3. Недельная сводка сразу после загрузки сейва пишет чепуху: «бойцов 1, жалование 0, еда 1079.8 дней» (18:57:03, при этом через минуту в бою четыре формации). Мелочь, но она портит триаж: по этим строкам день выглядит как двенадцать эпизодов потери 40+ бойцов, из которых настоящих боевых потерь только две (11:59 и 16:21). Чинить — не писать сводку, пока ростер после загрузки не заполнен.

Автопилот 18.09, сторож движения: живого подтверждения нет. Почему движение не началось после штатной смены Hold → BesiegeSettlement, по журналу не видно — мод такого состояния не писал. Сторож теперь пишет полное состояние движка при застревании; первая строка «ЗАСТРЯЛИ: …» в живом прогоне назовёт причину, и тогда можно будет чинить её саму, а не только последствие. Разблокирует: установка DLL b42da5d9… и игровой сеанс с приказом осады. Заодно проверить, не разбудит ли сторож ложно после боёв (расстройство исключено по IsDisorganized, но бывают и другие штатные стоянки).

Автопилот 18.09, хрупкость стенда: заменители движка не двигают партию, поэтому любой тест с приказом «ехать» и более чем шестью часовыми тиками подряд теперь разбудит сторож движения. Два существующих теста проходят впритык (ровно шесть тиков). Правильное решение — двигать позицию в таких тестах; сделать при следующей правке этих тестов, отдельно трогать чужие тесты сейчас не стал.

Автопилот 18.09, триаж живого прогона 14:40–18:31 (лог autopilot_20260918.txt, DLL 7490aa0, исправления краша в нём ЕЩЁ НЕТ). Три наблюдения, все с доказательством в журнале.

1. ЕДИНСТВЕННЫЙ ЗАХВАТ ПОТЕРЯН — это прямо про цель «завоевать карту». 14:56:26 взят Замок Фрактори, выбрана милость, карта 1/120, плюс две деревни. Держался до 16:10:42 (259 бойцов, 0 раненых, еда 6.9 дней). В 16:21:27 уже 0 феодов, 190 бойцов из них 187 РАНЕНЫХ, еда 0.8 дня, жалование выросло до 1513 в день. То есть отряд принял бой за замок и был разбит, а снабжение к этому моменту кончилось. Автопилот держать захваченное не умеет: гарнизона не оставляет, на осаду своего феода не возвращается, еду под большой отряд не запасает. Решение продуктовое и за владельцем: оборонять (гарнизон + возврат на осаду) или не брать крепости вовсе, пока отряд не потянет оборону. До решения новые крепости будут теряться так же.

2. Отметки ПРОСТОЙ в основном ложные. 10-12 секунд «время кампании не идёт» мод пишет, пока сам же перебирает послебоевые экраны по одному стеку за опрос (17:03:27 простой → следом 13 строк ПОПОЛНЕНИЕ и 8 строк ПЛЕННЫЕ, всё за те же секунды). Сторож считает простоем работу самого мода, и из-за этого настоящие простои (14:42:32 — окно поверх карты, поведение BesiegeSettlement, ничего не происходит) тонут среди ложных. Починка: не засчитывать в простой опросы, в которых мод что-то сделал.

3. За 3,5 часа владелец 20 раз выключал автопилот F12. Причины в журнале не видно — F12 пишется как «выключено игроком». Стоит спросить владельца, что именно он каждый раз доделывал руками: это и есть список следующих работ, и он дешевле любого моего анализа.

Автопилот 18.09: живой прогон исправления очереди событий не выполнен — игра была запущена, DLL в игру не ставилась. Разблокирует: закрытая игра, установка DLL, заезд в пустынный асерайский город с отрядом 50+ смешанного состава. Ждём в журнале «СОБЫТИЕ в очереди: выход … придержан до показа» и следом обычную запись о показанном событии. Также не измерено, сколько опросов держится очередь на самом деле (предел стоит 20 с запасом).

Автопилот 18.09, журнал мода растёт: 1,3 МБ (16.09) → 7,4 МБ (17.09) → 11 МБ (18.09) за день. Основной объём — строка «та же цель продолжается, приказ не перевыдаю (N-й пересчёт)» на каждый опрос, дважды в секунду. Триаж по такому файлу дорог, а грепы по нему медленные. Решать после живого прогона: сворачивать повторы в одну строку со счётчиком. Не делал сейчас — это правка того же файла, что и исправление краша, и смешивать их в одной смене не стоит.

Уточнение 18.09 к строке от 16.09 про ContractBlindSpots: гейт сломал 81f3794 (14.09), добавив в контракт MobilePartyAIModel. Одной строкой не чинится — за первым ComponentInterfaces идут GameModels, Vec2, InquiryData, Army, Clan и ещё десяток типов (проверено: дописал namespace, получил следующую волну ошибок, правку откатил). Это ОТРИЦАТЕЛЬНАЯ проверка «контракт обязан отвергнуть несовместимый API», и с 14.09 она не выполнялась ни разу. Разблокирует: дописать второй набор заменителей движка (~200 строк) или собрать ContractBlindSpots поверх заменителей AuditRegression.

Автопилот 16.09 22:26: 61e4549 установлен после приёмки остановленной смены Claude. Сбор бандитов, осады/армии/рейды, снабжение, плен и встречи реализованы; остаётся живой прогон на отдельном сейве. Самостоятельное объявление войны и морские операции не добавлялись. Поздние естественные подкрепления не блокируются потолком добора бандитов. Старые ContractGetter/ContractScoresGetter/ContractBlindSpots требуют обновления stubs (ComponentInterfaces), не считаются зелёными. См. BannerlordAutopilot/review/CONQUEST_IMPLEMENTATION_2026-09-16.md.

Случайные окна 16.09: MapIncident и неизвестные разговоры сохраняют разрешённый владельцем случайный режим. Мирные дорожные встречи и поддержанные боевые разговоры с 61e4549 имеют фиксированные переходы. Нужен живой повтор; неизвестные модальные экраны могут требовать ручного вмешательства.

## 22.09 — автопилот сливает добычу в опыт отряда: сделано, в игру НЕ ставил

Реализовано в `BannerlordAutopilot/src/AutopilotLoot.cs`: при золоте от
1 000 000 автопилот забирает из экрана добычи только коней и припасы, а
снаряжение оставляет — ваниль превращает его в опыт бойцов (75–300 за предмет
по тиру). Механика и числа: `docs/BANNERLORD_TROOP_XP_2026-09-22.md`.

Решение принимается по ДВУМ числам самой игры, а не по своей формуле:
`XpGainFromDonations` (ноль без перков `Steward.GivingHands` /
`Steward.PaidInPromise`) и `_donationMaxShareableXp` (сколько отряд способен
принять). При нулевом потолке добыча забирается как раньше — иначе опыт сгорает,
а вещи пропадают зря.

Гейт `AuditRegression`: новый тест видел красным, сейчас 238 ok / 0 FAIL; две
мутации (снять проверку потолка, снять проверку перков) роняют его поимённо.
Сборка Release 0/0.

Осталось:

- **Установить и проверить в игре** — НЕ ставил, играют зрители (запрет
  владельца 22.09). Разблокирует: закрытая игра и слово владельца.
- **Сверить числа вживую:** открыть экран добычи после боя и сравнить подпись
  «получено опыта» с таблицей тиров. Всё посчитано по коду 1.4.8; если RBM или
  другой мод подменил `ItemDiscardModel`, цифры будут другие.
- **Порог в миллион — константа в коде** (`LootDonationGoldThreshold`). Если
  захочешь крутить его на ходу, нужна настройка, это отдельная работа.
- Не проверено, что именно попадает в фильтр «разное» у твоей сборки: расчёт
  на то, что еда и товары там, а оружие/броня/щиты — в своих фильтрах.

## 22.09 — вассалы общего клана: ВЫКАЧЕНО 23.09

`@antitail` и `@ksiandil` в одном клане `[BLink] хвост`. Мод шлёт список
вассалов по клану (`BuildSnapshotForMaster(hero.Clan)`), поэтому оба члена
присылают одни и те же clan_id, а уникальный ключ зеркала — на канал. Снимок
второго ронял ВСЮ транзакцию `player.state_update` (за весь эфир 22.09 — 426 раз, 126 ниже — счёт посреди эфира): у ksiandil замерло зеркало
героя (в базе уровень 20 и 882 587 динаров, в игре 24 и ~9 600). 126 отказов за
22.09, 148 за 21.09.

Починено в ветке: вассал остаётся за первым владельцем, чужой clan_id
пропускается со строкой `[VAS-RECONCILE]` в логе. Гейт
`tests/test_vassal_shared_clan.py` видел красным.

**Выкачено 23.09 ~16:55 UTC** по слову владельца: один файл
`routes/bannerlord_vassals.py` (md5 `51986966…`), откат рядом —
`bannerlord_vassals.py.rollback-20260923`. Рестарт штатный, health и страницы
200, гейт на коде прода 3/3. Остальной бэкенд прода уже совпадал с веткой;
фронт (`extension/mobile.html`, `viewer-bannerlord.js`) расходится с продом и
НЕ выкатывался — владелец велел не трогать. На следующем эфире проверить: отказов больше
нет, его уровень и золото догнали игру.

Не сделано намеренно: уникальный ключ `(channel_id, vassal_clan_id)` не менял и
модель владения не переписывал — по-хорошему вассал принадлежит КЛАНУ, а не
нику, но это миграция и правка мода, отдельная задача.

## 22.09 — прокачка отряда автопилотом: разведка, реализации нет

Карта возможностей с цифрами из декомпиляции: `docs/BANNERLORD_TROOP_XP_2026-09-22.md`.
Коротко: у отряда игрока суточного опыта нет вообще (ваниль явно исключает клан
игрока), весь опыт — из боя по формуле, где он пропорционален СИЛЕ цели; фарм
лутеров почти бесполезен; авторасчёт стоит всего 0.9 от обычного боя; арена и
турниры для бойцов — мусор (0.0625 и 0.33).

**Найдена ванильная механика «не забирать добычу → опыт отряда»** (владелец
был прав): перки `Steward.GivingHands` (оружие) и `Steward.PaidInPromise`
(броня, засчитывается от компаньона-интенданта), 75–300 опыта за оставленный
предмет, только в экране добычи после боя, потолок — сколько отряду не хватает
до апгрейдов. Это самый быстрый источник из найденных и прямо ложится в
`AutopilotLoot.cs`.

Что разблокирует: три замера в игре (реальный опыт за бой по разным целям,
потери своих в симуляции против отыгранного боя, какие из суточных перков вообще
есть у стримера и компаньонов в ролях). Без них цикл прокачки настраивается
наугад.

## 22.09 — безопасный спавн: сделано, ждёт установки и прогона в игре

Работа забрана у смены Codex по решению владельца. Их красные гейты
`SpawnPlacementHarness` и `InvalidSupporterHarness` влиты как контракт и закрыты
своей реализацией (`87b4990`): `Util/SpawnPointSearch.cs`,
`Util/SafeSummonPlacement.cs`, `Patches/ValidKingdomSupportersPatch.cs`.

**Их некоммиченные `SafeSummonPlacement.cs` и `SpawnPointSearch.cs` в
`D:/shedlink-build/poststream-fixes-20260922` я не брал и не открывал** — моя
реализация написана под их же опубликованный тест. Если они успели доделать
своё, сравнивать надо два решения осознанно, а не сливать вслепую: имена файлов
совпадают намеренно, чтобы merge не дал двух копий молча.

Осталось:

- **Установлено 22.09** (`scripts/deploy.ps1 -Mod`, игра и лаунчер закрыты):
  сборка из `76b9e0b`, SHA-256 `98C8F26F…`, MD5 `5287EEE6…`, источник и копия
  в игре сошлись. Откат — `BannerlordLink.dll.rollback-2026-09-22-2210611D`
  в той же папке (сборка смены Codex, что стояла до этого).
  **Осталось проверить в игре после перезапуска:** в логе призыва должно быть
  `проверенная точка X/Y` вместо прежнего `offset N.Nм`, а в тесноте —
  `места нет: … → зона подкреплений`. Плюс `[clan-repair]` на загрузке сейва и
  суточный тик без падения.
- **Поднять норму патчей.** `scripts/mod-log-baseline.json` держит `ok: 19`;
  добавились два класса патчей (`DiplomacyRelationScorePatch`,
  `ValidKingdomSupportersPatch`), то есть ожидается 21. Число брать из живого
  запуска, а не из подсчёта файлов — иначе норма подстроится под патч, который
  не применился.
- **Не проверено:** пороги подобраны рассуждением, не замером — свободный радиус
  1.2 м пешему и 2.5 м конному, допуск по высоте 3 м, кольца 2/3/4.5 и 4/5.5/7.
  Если в бою герои всё равно будут появляться впритык или слишком далеко от
  своих, крутить надо эти числа, и менять их по НАБЛЮДЕНИЮ в игре.

22.09 12:21+05: backend df8c21a и DLL BannerlordLink УСТАНОВЛЕНЫ по команде владельца. Критические тесты прошли; PID2585473, health200 status/db ok, страницы200; SHA256 четырёх изменённых backend файлов совпал. Backup /root/shedlink-deploy-backups/heirs-20260922-071802 (SQLite integrity ok). DLL SHA256E17FAF3120129D193DD9CC6CE29B8B715FB48448BC8BE4F569149A134A8C22F5; DLL/PDB сверены. Обе игровые DLL поставлены при закрытой игре, rollback D:/shedlink-build/dll-rollback/20260922-122100-heirs-camera. Фронт не менялся; восстановление наследников в живой кампании после загрузки ещё не проверено.

22.09: наследники/вассалы исправлены ЛОКАЛЬНО. Полный снимок взрослых детей восстанавливает bannerlord_heirs; clan_info.vassal_heirs отдельно передаёт живую доступность (свой взрослый ребёнок, жив, не пленник, тот же действующий клан, не другой зритель). Проверка повторяется перед созданием в игре. Backend учитывает занятых/ожидающих вассалов, сохраняет совместимость старых клиентов и tenant isolation; snapshot внутри существующей транзакции state_ts. Фронт не менялся. Тест snapshot/eligibility PASS, lifecycle6/0, C#eligibility10/0, Release0/0. DLL D:/shedlink-build/heir-snapshot-20260922/bin. Production/DLL НЕ обновлены, живой повтор kuro_gothic ожидается.

## 22.09 — краш от `hero.leave_clan`: починен в коде, ждёт установки и прогона

Полный разбор с доказательствами: `docs/BANNERLORD_CRASH_2026-09-22.md`.
Класс — «платное действие обязано детектить тихий no-op»: проверка смотрела на
собственную запись (`hero.Clan == null`), а не на результат чужого вызова
(`clan.Leader != hero`).

Все три пункта сделаны 22.09 в ветке `claude/poststream-2026-09-21`
(`f3737c2` красный гейт → `78d9d9d` фикс). Осталось:

- **Установить DLL и прогнать в игре.** Пересобрана из слитой ветки:
  SHA-256 `6C0650F3…`, MD5 `CD1DF9DD…` — содержит и фикс наследников смены
  Codex, и этот фикс краша. В игре с 12:21 стоит их `E17FAF31…`.
  **Не ставлю по своей воле:** у смены Codex в их worktree лежит НЕкоммиченная
  работа по безопасной точке призыва (`SafeSummonPlacement.cs`,
  `SpawnPointSearch.cs`, правки `SummonHeroHandler.cs`), её в моей ветке нет и
  быть не может. Поставить мою DLL сейчас — заморозить их работу до следующей
  их сборки. Разблокирует: слово владельца (игра должна быть закрыта), либо
  их коммит этой работы — тогда влить и пересобрать одним артефактом.
  Откат класть рядом с их `D:/shedlink-build/dll-rollback/20260922-122100-heirs-camera`.
- **Поднять норму патчей.** `scripts/mod-log-baseline.json` держит `ok: 19`,
  новый бэкстоп-патч даёт 20. Менять только после живого запуска, где видно
  `Harmony patched … ok=20` — иначе норма подстроится под несработавший патч.
- **Приёмка в игре:** загрузить текущий сейв и убедиться, что в логе есть
  `[clan-repair] … висящий лидер снят`, а суточный тик проходит без падения.

Не проверено: сколько зрителей проходили этот путь в прошлые дни (в логе 22.09
он виден у `endorphine13` дважды, по другим дням не искал). Связь с
багрепортами #61/#62 вероятна по симптомам, отдельно не доказана.

## 22.09 — хвосты после исправления триажа 21.09

**Обновление после разрешения владельца:** backend и общая DLL/PDB установлены
22.09 около09:31 +05; прежний запрет на поставку общей сборки снят уточнением
«и длл выкати». Исторические записи ниже описывают состояние до этого разрешения.
Production archive включён, health/db/hash подтверждены. Остаётся живая приёмка
и исторические компенсации; подробности в записи установки отчёта.

Исправления новых покупок перековки, дейлика, каталога, членства, tier, мира и
телеметрии прошли локальные/staging проверки. Они ещё не на production.
См. [полный разбор 14 жалоб](docs/POSTSTREAM_FIXES_2026-09-22.md).

- Старые потери качества: собрать привязанные к реальным action_id/мод-логу
  квитанции, вычесть возвраты и уцелевшие права, затем согласовать восстановление
  либо компенсацию. Не начислять оценочные 920k общей суммой.
- Старые покупки с ложным max_vassals_bonus и пропущенные дейлики: решение по
  компенсации отдельно; фиксы не переписывают исторические деньги/даты.
- #50: нужен конкретный неучтённый параметр с предметом и значением before/after.
- #61: нужен native RulingClan/Clan/isEliminated в момент выхода; имеющиеся строки
  показывают обычный выход, не ветку распуска правителем. Не уничтожать по UI.
- #52/#60 (иконки/возраст), #56 (50% за discard), часть #55 (платный rename своего
  клана) — предложения, а не разрешение изменить экономику под видом багфикса.
- Живая приёмка после согласованной установки: reforge→load→restore без списания,
  повтор дейлика, выход/политика, прямой мир; замер объёма снимков и обрывов.
- Архив LOG_ARCHIVE_DIR включён и проверен только staging; при разрешённом выкате
  включить production. Он хранит logging, не все legacy print(); утраченные логи
  прошлых эфиров не восстановлены. Не обещать, что все 839 обрывов устранены.
- Нужна отдельная согласованная поставка: baseline этой ветки содержит боевые
  приказы с прежним запретом. Production/CDN/игра не обновлялись в этом триаже.

## 21.09 — боевые приказы: локальный фикс готов, выкат запрещён владельцем

Выбор целей пешего Charge, Gate, обработка отказов/rollback и статусы исправлены
локально; 43 проверки + UI/backend + Release + save/load прошли. Живое движение
и медленная ходьба требуют игрового повтора после отдельного разрешения на
установку. Полноценное использование осадных объектов — следующий этап.
См. docs/BANNERLORD_BATTLE_ORDERS_LOCAL_2026-09-21.md. Ничего не выкатывать.

## 21.09 — боевые приказы зрителей: найдены дефекты, требуется отдельный фикс

Аудит без разрешения на реализацию: ворота могут выбирать лестницу; осадный
отказ после EnsureDetached уже включает Hold; ошибки движения скрываются от
PostApplied. Сначала исправить эти три дефекта и статус приказа; полноценное
использование осадных объектов — отдельный этап с живыми проверками.
Разблокирует запрос владельца на исправление. Сохранение и вассалы не трогать.
Подробности: docs/BANNERLORD_BATTLE_ORDERS_AUDIT_2026-09-21.md.

## 22.09 — класс «оплачено, но живёт только в сейве» закрыт НЕ весь

Разбор эфира: `docs/POSTSTREAM_TRIAGE_2026-09-21.md`. Четыре находки триажа
(перековка, дейлик, `max_vassals_bonus`, поток снимков) исправлены сменой Codex
в этой же ветке и ждут согласованной поставки — см. блок выше.

Остаётся открытым сам КЛАСС. Право на перековку теперь хранит бэкенд, но это
единственное платное действие с таким правом. Все прочие покупки за крустики,
чей эффект живёт только в игровом сейве, по-прежнему исчезают при откате без
возврата и без уведомления:

| действие | цена | за эфир 21.09 |
|---|---|---|
| `hero.enact_policy` | 1500💎 | 66 |
| `kingdom.propose_peace` | 3000💎 | 29 |
| `kingdom.propose_war` | 2000💎 | 24 |
| `hero.add_skill` | 500/1000/5000💎 | 1 платная + дейлики |
| `hero.army_create`, `hero.respec_child_skills`, `hero.pay_ransom`, `hero.propose_marriage`, `hero.change_child_looks`, `hero.rename_child` | 50–1000💎 | десятки |

Порядка 250 000💎 за один эфир лежит в этом классе. Проверено по коду ветки
`codex/poststream-fixes-20260922`: из бэкенда после загрузки мод перезапрашивает
только апгрейды клана и теперь права на перековку — ни политики, ни дипломатия,
ни XP не восстанавливаются (`git grep OnGameLoadFinishedEvent` по
`BannerlordLink/src`).

Что разблокирует: решение владельца по дешёвому общему детектору вместо реестра
прав на каждое действие — мод знает `save_id` и время кампании, и если после
загрузки игровое время ушло НАЗАД, все покупки за крустики после этой точки
считать несостоявшимися и возвращать. Один детектор закрывает весь класс;
реестр прав остаётся там, где эффект надо именно восстановить, а не вернуть
деньги.

**Моя ошибка в разборе 22.09:** открытых багрепортов 14, а не 13 (и 12 из них
с 18 и 21.09, а не 11) — посчитал строки на глаз, смена Codex пересчитала по
базе и оказалась права. Исправлено в `STATUS.md` и в разборе.

**Решено НЕ делать пока:** багрепорт #59 («Совет лидеров скамит на 400к») —
не скам: деньги списаны честно, апгрейд записан, эффекты применяются на
суточном тике. Смена Codex добавила объяснение момента применения; вопрос
витрины эффекта или запрета покупки в бою остаётся за владельцем.

**Не проверено:** эфиры 16–19.09 (только деньги; логи бэкенда вытеснены
ротацией). `hero.make_peace` — 2 попытки, 0 успехов; поиск королевства
исправлен, живой повтор нужен.


## 21.09 — вассальные кланы: аудит, исправление отложено

Production зеркало: 0 строк при 13 подтверждённых созданиях 18.09. В текущем
save наличие этих кланов не доказано. Нет game -> backend resync вассалов;
rename оптимистично меняет БД без отката; уничтожение не очищает зеркало;
самостоятельный переход вассала не сверяется с сюзереном. Пока только аудит.
Подробности и доказательства: docs/BANNERLORD_TOURNAMENT_VASSALS_2026-09-21.md.

## 21.09 14:38 — очередь турнира расходится между игрой и расширением

Подтверждено: мод восстановил 13 участников из сейва; повторные заявки
AnalniyShekotyn/omaryn42/dryg00/dzirtdourden и других отклоняет как уже
записанных. После banderaslv, shedoy23 и nikita__g345 в игре 16 участников.
Публичный GET /api/bannerlord/tournament/queue-usernames?channel_id=98319857
показал только этих трёх. Причина: загрузка мерджит backend -> game, но не
публикует восстановленную очередь обратно; JoinTournamentHandler на дубле
выходит до tournament.joined. Очередь игры не очищать. Исправлено локально: полный снимок очереди,
идемпотентная запись, fencing и сохранение ожидающих следующего турнира.
Тесты и сборка прошли; НЕ выкачено по указанию владельца. Живой старт
с новой версией не проверен. См. docs/BANNERLORD_TOURNAMENT_VASSALS_2026-09-21.md.

## 21.09 — установка исправления ActionPoller

DLL установлена после закрытия игры. Осталось после запуска проверить
ActionPoller started и живой призыв. См. docs/BANNERLORD_POLLER_STARTUP_2026-09-21.md.

## 21.09 — выкат исправления синхронизации героев

Production-выкат подтверждён владельцем и выполнен 21.09 14:09 +05.
Живой повтор после 14:11 подтверждён: 26 героев; у AnalniyShekotyn и
shedoy23 сборки готовы. Задача восстановления записей закрыта.
Twitch ZIP/CDN пока не обновлены. См. docs/BANNERLORD_HERO_SYNC_2026-09-21.md.

21.09 — настоящий инвентарь Bannerlord: M127/backend/frontend/DLL выкачены
по подтверждению владельца; ждём живого прогона плен/освобождение/замена вещи.
До выката подтверждено: в production вообще 0 inventory snapshots, герой
shedoy23 в плену. Причина отсутствия снимков всё ещё не установлена:
после запуска проверить публикацию/приём для текущего save/hero/session.
См. docs/BANNERLORD_NATIVE_INVENTORY_2026-09-21.md.

Случайные окна 16.09: ручной выбор MapIncident заменён разрешённым владельцем рандомом, RandomDialogsEnabled включён по умолчанию (bdb5982 установлен). Проверить живое событие и знакомство с лордом; предел 16 реплик, ограничения неизвестных боёв/армий остаются.

Добыча 16.09, 17:37: ложная остановка после Готово исправлена и установлена (8d27395). Остаётся живой прогон; точный обработчик, изменивший количество в эпизоде 16:23:57, не установлен. Новая версия пишет до/после/разницу; перенос проверяет до закрытия.

Уточнение 16.09, добыча автопилота: прежнее ограничение «предметы вручную» для своего послебоевого InventoryState/Loot снято кодом 4701cf9; DLL установлена. Осталось проверить в игре обычный сбор и полную вместимость. Корабли/морской экран не автоматизированы.

# ShedLink — отложенные решения

**Повторный разбор шести замечаний Claude, после установки 61e4549:** остаются
неустановленный источник +1/+2 предметов после закрытия добычи, вводящий в
заблуждение счётчик формаций убежища и риск формаций, появившихся после первой
передачи управления. События выбираются случайно, это не оптимизация выгоды.
Старый журнал заканчивается раньше установки новой DLL. Подробности:
BannerlordAutopilot/review/CLAUDE_SIX_FINDINGS_RECHECK_2026-09-16.md.

**17.09, Bannerlord — переход от классов к снаряжению:** магазин, инвентарь и
переодевание реализованы локально. Для новых героев реализованы специализации,
оружейные активки и формации по экипировке. Перенос старых героев не нужен
по решению владельца: запускать новую кампанию.
17.09 frontend /dev и backend опубликованы и проверены по запросу владельца.
Новый мод установлен 17.09; остался живой тест сохранений, покупок, миссий
и обрывов связи. Старый CDN-клиент
не содержит магазина и теряет классовую выдачу при новом моде, поэтому
одиночная установка мода не является готовым выпуском. На серверный /dev
фронт и backend изменения уже выкачены; Twitch ZIP/CDN прежние.
Спецификации: `docs/BANNERLORD_EQUIPMENT_SHOP_2026-09-17.md`,
`docs/BANNERLORD_FREE_BUILDS_2026-09-17.md`.

**16.09, уточнение пленных:** экран Loot после сдачи преследуемых бандитов или
поддерживаемого боя теперь принимает доступных пленных до текущего лимита и
закрывается через «Готово». Синхронное подтверждение оставшихся обрабатывается
только для своего VM/callback. Своих бойцов не переводит, гарнизоны/торговлю и
обычный экран отряда не меняет. Экран вещей/добычи ещё ручной. Нужен живой
повтор после исправления расстановки: review/PRISONERS_CRASH_2026-09-16.md.

**16.09, осады/убежища автопилота:** по запросу владельца реализована помощь
обороняющимся (прорыв со штатными потерями → ожидание обороны), а также выбор
ближайшего известного населённого убежища вместо патруля/отсутствия цели.
Штурм дневной, ночью штатное ожидание утра; главарь — бой всем отрядом.
Живой прогон остаётся непроверенным. Ночной скрытный подход и морские бои не
добавлены. Наступательные осады/армии/рейды добавлены в 61e4549; добыча и
пленные после поддержанного боя автоматизированы последующими правками.
Три старых отрицательных стенда ContractGetter/ContractScoresGetter/
ContractBlindSpots не компилируются: в их старых stubs отсутствует
ComponentInterfaces; этот импорт и обращения уже были в EngineContract до
текущей правки. Выводы сохранены в evidence/operations-Contract*.txt.

**Решение владельца 15.09 о диалогах:** сдавшихся бандитов брать в плен;
для остальных разговоров разрешён отдельно включаемый случайный доступный
вариант. Реализовано, требуется игровой прогон. Прежние записи ниже о ручной
судьбе сдавшихся описывают состояние до этого решения.

**15.09, уточнение автопилота:** выход завершённой полевой миссии и ультиматум
преследуемой партии бандитов реализованы; требуется живой прогон. Разговоры
с лордами, судьба сдавшихся, экраны добычи/пленных остаются ручными.
См. `BannerlordAutopilot/review/BATTLE_FLOW_2026-09-15.md`; историческая запись
ниже о полном отсутствии выхода описывает состояние до этой правки.

Обновлено: 2026-09-12
## Четыре теста всё ещё не запускаются руками на Windows (12.09)

`test_m109_migration_ledger.py`, `test_m110_manager_credentials.py`,
`test_manager_pairing_core.py`, `test_rimworld_child_tenant_migration.py`
падают `UnicodeEncodeError` до первой проверки: миграции `m109` и `m110`
печатают «✅», а консоль Windows по умолчанию cp1251. Правка `config.py`
(12.09) чинит это для всех, кто импортирует `config` — этих четверых в его
цепочке импорта нет. В CI они зелёные: там `PYTHONIOENCODING=utf-8`.

Было 19 таких, стало 4 — замерено прогоном без переменной окружения.

**Почему не чинится сейчас:** правильное место — сами миграции, а их
редактировать нельзя («применённую миграцию не редактировать»); обходной путь
— защита вывода в четырёх тестах, но это правка чужих файлов ради косметики
вне текущей задачи. **Разблокирует:** ближайшая работа в этих тестах, либо
решение завести общий стартовый импорт для тестов.

## Диск C: заполнен под ноль (12.09)

`df` показывает 150 ГБ из 150 ГБ занято, свободно **190 МБ**. Замечено, когда
распаковка 68-мегабайтного архива менеджера оборвалась «write error (disk
full?)». Для стримерского ПК это риск не только сборок: запись OBS, сейвы игры
и обновления могут упасть без внятной ошибки.

В рабочем дереве проекта чистить нечего — `dist/` и весь сборочный мусор
`bin`/`obj` вместе занимают 9 МБ. Значит место держит что-то вне репозитория
(записи эфиров, игры, кэши).

**Почему не сделано:** удаление чужих файлов — решение владельца, наугад тут
можно снести запись эфира. **Разблокирует:** его взгляд на то, что занимает
место (WinDirStat или «Хранилище» в параметрах Windows).

## Сутки квестов идут по UTC — решение продукта за владельцем (12.09)

Багрепорт #48 (kuro_gothic, 13.08) подтверждён: квесты обновляются в 00:00
UTC, то есть в 03:00 МСК. Границу задаёт `date.today()` в шести местах
(`bot_core.py:954`, `database.py:842/916/977/1400`, `routes/viewer.py:312`),
прод живёт в UTC.

**Почему не чиню сам:** сутки сдвигаются ровно один раз, и в день перевода
кто-то либо потеряет день квестов, либо получит два — а квесты дают крустики.
Выбор между «считать по Москве», «считать по часовому поясу канала» и «оставить
UTC, но написать это в интерфейсе» — продуктовый, разбор в
`docs/BUG_REPORTS_TRIAGE_2026-09-12.md`. **Разблокирует:** слово владельца.

## Идея зрителя: шкала вайпа за крустики или динары (12.09)

Багрепорт #24 (bapah1_1, 17.06) — не поломка, а предложение: копить общую
шкалу (например, 100к) и по достижении устраивать вайп маунта. Багрепорт
закрыт как «не баг», идея сохранена здесь, чтобы не потерялась.

**Разблокирует:** решение владельца, нужна ли механика; по правилу «фича
входит — фича выходит» взамен придётся заморозить малоиспользуемую.

## Идемпотентный ключ заявки не привязан к зрителю (12.09)

Свойство платформы, а не одного модуля: уникальный индекс из миграции M46 —
`(channel_id, module_id, client_action_id)`, и поиск повтора в
`routes/shedcolony.py` и `routes/bannerlord.py` идёт по нему же, без имени
зрителя. Прислал бы второй зритель тот же ключ,
сервер ответил бы ему «Действие уже принято» с `success: true` и чужим
`action_id`: денег не списали бы, эффекта бы не было, а панель сказала бы
«принято». Найдено случайно — контрактный тест замороженного клиента послал то
же тело от другого зрителя и получил успех там, где ждал отказ.

**Почему не чинится:** ключ генерирует клиент через `crypto.randomUUID()`,
столкнуться нельзя, подобрать чужой UUID — угадать 122 бита. Денежной дыры
нет, а правка сейчас означала бы миграцию уникального индекса под замороженный
фронт. **Разблокирует:** ближайшая правка этой области — тогда `username` в условие
поиска и новый индекс миграцией; чинить надо ОБА маршрута и индекс, починка
одного оставит дыру под тем же именем.

## На проде два мёртвых файла от «рулекциона» (11.09)

`backend/event_manager.py` и `backend/routes/event.py` лежат на проде с 02.07,
а из git удалены 29.07 (`fb6f706`, механика переписана в голосование). Ни
текущий код, ни `main.py` на проде их не подключают — проверено поиском. Вреда
нет; мешают только сверке ревизии: манифест чистый, а лишние файлы торчат.

Остались, потому что выкат распаковывает архив поверх и удалённое не стирает
(`RUNBOOK.md` §7 «Ловушки, на которых мы уже спотыкались»).

**Почему не удалено сразу:** это изменение на проде, а сверка по плану frontend
freeze велась только на чтение. **Разблокирует:** слово владельца; удаление —
`rm /root/twitch-extension/backend/event_manager.py /root/twitch-extension/backend/routes/event.py`
без рестарта, раз их никто не импортирует.

## Решено: тех-режим RimWorld на проде оставлен (11.09)

На проде в `backend/.env` стоит `RIMWORLD_REQUIRE_STREAM_LIVE=false`. Это
переключатель «технический прогон с запущенной игрой без Twitch-эфира», и он
остался включённым: платные действия RimWorld не проверяют, идёт ли эфир. До
11.09 при нём не проверялось вообще ничего — ни эфир, ни игра, — и зритель мог
заплатить в любой момент суток, а команда ложилась в очередь, которую некому
вычерпать. Так 10.09 и зависли 550 💎 (`docs/POSTSTREAM_TRIAGE_2026-09-10.md`).

С 11.09 игра проверяется и в тех-режиме, а зависшие команды возвращает сторож,
так что денежной дыры больше нет. Остаётся вопрос продукта: пока RimWorld
запущен, зритель может покупать его действия и вне эфира — например, пока ты
играешь без стрима. Если так и задумано, ничего не делать. Если нет —
выставить `true` и перезапустить службу.

**Решение владельца 11.09: оставить.** Мотив — режим нужен ему для тестов без эфира; что зрители при запущенной игре могут покупать и вне стрима, принято осознанно. Денежной дыры после 11.09 нет: без игры покупка отклоняется до списания, а невзятая команда возвращается сторожем через 30 минут. Прод не трогали — флаг и так стоял `false`. **Не «чинить» этот флаг, увидев его в `.env`:** это не забытый тестовый режим, а решение.

## «Вылечить» не проверяет, есть ли у зрителя пешка (11.09)

`/api/rimworld/heal-pawn` сверяет вход, кулдаун и баланс, но не наличие пешки:
10.09 `laitru` заплатил 150 💎 за лечение, не имея пешки вовсе. Денег это
больше не теряет: при игре не на связи откажет вход, при игре на связи откажет
мод, и возврат придёт с объяснением. Но касса при успехе ставит 15-минутный
кулдаун лечения, а ни один путь возврата его не снимает —
`rimworld_heal_cooldowns` только пишется и читается, проверено поиском по коду.
Зритель, который следом создаст пешку, четверть часа не сможет её лечить.

**Почему не сделано сразу:** к деньгам не относится, а правка обработчика —
отдельный заход со своим тестом.
**Разблокирует:** заход по RimWorld — проверка пешки перед кассой и отказ
«сначала создай пешку».

## 23 теста падают в обычной консоли Windows, а не в прогоне (10.09)

`tests/test_rimworld_channel_isolation.py` и ещё 22 файла из 90 не выставляют
кодировку вывода. Под `run-backend-tests.py` они зелёные (раннер задаёт
кодировку сам), а запущенные руками — падают `UnicodeEncodeError` на первой
же кириллической строке, ДО того как что-либо проверят. Владелец увидит не
результат теста, а трассировку.

Класс знакомый: ровно так 09.09 проверка кандидата была зелёной у меня и
падала у человека, потому что мои прогоны шли с `PYTHONIOENCODING=utf-8`.
Правило про это уже записано («гейт хотя бы раз запустить так, как его
запустит человек») — не хватает исполнения.

**Почему не сделано сразу:** 23 файла правки, к сегодняшней задаче
отношения не имеют, и правка каждого по отдельности — это 23 шанса задеть
чужой тест. Разумнее один заход: либо добавить `reconfigure` шапкой во все
разом скриптом, либо перестать поддерживать ручной запуск и сделать
`run-backend-tests.py` единственным входом.

**Разблокирует:** решение владельца, какой из двух путей (правка шапок или
единственный вход). Пока не сделано — тест, запущенный руками и упавший на
кодировке, НЕ означает регрессию: перепроверить через раннер.

## ~~Фронт на проде стоит на коде, которого нет ни в одном коммите~~ — ЗАКРЫТО 10.09

**Факт, проверенный хешами, а не предположение.** `shop.js`, `viewer.css`,
`extension.html` и `mobile.html` на проде побайтно совпадают с рабочим деревом
worktree `codex-public-release`, где эти четыре файла лежат НЕЗАКОММИЧЕННЫМИ.
Выкачено 10.09 в 09:27 UTC. Содержимое — раскрытие карточки товара в магазине
плюс экранирование подставляемых значений; рядом ещё неотслеживаемые
`docs/VIEWER_UI_PLAN_2026-09-10.md` и `scripts/fixtures/`.

**Авторство не моё** — работу ведёт параллельная сессия, файлы правились за
девять минут до этой записи. Поэтому НЕ трогал: не коммитил, не откатывал, не
пересобирал кандидата. Правило «сохранять чужие изменения».

**Чем это опасно, в порядке вероятности.**
1. Любой `deploy.ps1 -Frontend` или `-All` из чистого чекаута **молча вернёт
   прод на последний коммит** — интерфейс магазина исчезнет, и никто не
   свяжет это с деплоем. Мои сегодняшние выкаты были `-Backend`, фронт они не
   трогают, так что пока не задето.
2. Смерть диска = потеря опубликованной работы: её нет ни в одном коммите и
   ни на одном remote.
3. То, что показано на `/dev`, нельзя привязать к ревизии — при разборе
   «почему у зрителя так» нечего открыть.

**Побочно:** из-за этих правок кандидат `shedlink-0.0.5.zip` отстал от дерева
на два файла (`scripts/verify-candidate.py` даёт exit 1). Заливать архив в
кабинет по-прежнему можно — он валиден как «0.0.5, каким его тестировали», —
но тогда магазин в него не попадёт. Пересобирать до готовности той работы
нельзя: упакуем незаконченное.

**Закрыто 10.09.** Владелец передал работу по магазину сюда и сказал
пересобрать архив. Код закоммичен (`81aa332`) после проверок: `node --check`,
совпадение наборов скриптов в обеих оболочках, одна метка кэша. Кандидат
пересобран, `verify-candidate.py` зелёный. Механизм, из-за которого запись
появилась, остаётся верным и записан навсегда в `RUNBOOK.md` §7 «Ловушки, на которых мы уже спотыкались»: деплой
заменяет в развёрнутой папке всё, что не исключено, поэтому опубликованное
без коммита однажды уезжает назад молча.

## Порядок снимков Bannerlord висит на настенных часах (10.09)

Внешний обзор нашёл два дефекта в защите порядка (M123). **Первый —
неатомарность проверки и записи — починен в тот же день** (`b04495f`, гейт
`test_state_ts_atomicity.py`, виден красным). **Второй остаётся открытым по
существу и здесь именно поэтому.**

**Что не так.** Ключ порядка — `env.ts`, то есть обычные настенные часы на
машине игрока. Часы врут: коррекция NTP, севшая батарейка CMOS, разъезд
UTC/локального времени при двойной загрузке. Порядок, выведенный из них,
неверен по построению — сервер не может отличить «снимок старше» от «часы
переставили».

**Что сделано как смягчение, а не как решение.** Метка дальше пяти минут
вперёд от часов сервера (те под NTP) теперь означает «порядок неизвестен»:
снимок применяется, но `state_ts` им не отравляется. Это убирает худший исход
— молчаливую заморозку зеркала до перезапуска игры, — но не даёт верного
порядка. Гейт: `test_state_ts_clock_guard.py`.

**Чем чинится по-настоящему.** Монотонным счётчиком отправок из самого мода.
`BannerlordLink/src/Util/StateSyncTracker.cs` уже ведёт `Seq` (растёт на каждую
отправку) и `Epoch` (растёт на `ResetForNewSession`), но `BuildStateJson` их не
кладёт в payload — счётчик есть, до сервера не доезжает. План: мод шлёт
`state_seq` + идентификатор сессии процесса, бэкенд при их наличии сортирует по
ним и полностью игнорирует `ts`; старые моды продолжают ехать по `ts`.

**Почему не сделано сразу.** Три причины, в порядке веса:
1. Требует новой DLL и рук владельца (закрыть игру, поставить, перезапустить) —
   а фикс мода без прогона в игре правдоподобен, но не доказан.
2. Нужна миграция под новые колонки и решение краевого случая: конверт из
   УМЕРШЕГО процесса, долетевший после первого снимка нового. Порядковый номер
   сессии придётся выдавать серверу, иначе после перезапуска игры счётчик
   стартует с нуля и бэкенд отвергнет живую игру навсегда — это был бы новый
   дефект того же класса.
3. Худший исход уже снят смягчением выше, то есть пожара нет.

**Разблокирует:** свободный вечер владельца с закрытой игрой + прогон
`docs/TESTING_PLAYBOOK.md` по зеркалу состояния. До тех пор — не считать
порядок снимков доказанным: он работает, пока часы на игровой машине идут
ровно.

## `loadStats()` осталась без защиты от гонки (09.09)

`loadUserData()` получила счётчик поколений `_stateSeq` — она красит баланс,
доход, квесты, кейсы и может показать карточку входа, поэтому чинилась первой.
Рядом живёт `loadStats()` (`viewer.js`, три запроса через `Promise.all`:
stats/achievements/streak) — у неё та же форма и той же защиты нет.

Последствия мягче: она рисует время просмотра, достижения и серию, денег не
касается и вход не гасит. Поэтому отложено, а не сделано заодно.
Разблокирует: отдельный счётчик (общий с `loadUserData` брать нельзя — запросы
разные, они бы гасили друг друга) плюс сценарий в
`scripts/test-frontend-state-race.mjs`.

## Решено: `player.modify_attribute` закрыт как легаси-двойник (09.09)

Действие объявлено в манифесте и стоило 50 крустиков, но ветки в
`_prepare_action` не имело вовсе: `points` приезжал из тела запроса, а
`ModifyAttributeHandler.cs:28` берёт его как есть — проверяет только
`points != 0`, потолка нет — и зовёт `AddAttribute(attr, points)`.

Рядом живёт настоящий путь `hero.add_attribute`: зажимает количество в 1..10,
проверяет имя характеристики и берёт `ATTRIBUTE_COST = 50 000` динаров **за
каждое очко**. То есть двойник давал тот же эффект за 50 крустиков вместо
50 000 динаров и в неограниченном количестве одним вызовом. Фронт его не
вызывает нигде — попасть можно было только прямым запросом.

**Моё решение — отказывать, а не зажимать**, и его стоит перепроверить
владельцу. Даже с потолком в одно очко это остаётся путём в тысячу раз дешевле
настоящего, а назначать ему цену — продуктовое решение, не инженерное.
Разблокирует возврат: собственная цена + валидация имени характеристики +
потолок, как у `hero.add_attribute`.

## Проверка остальных полей, доезжающих до мода (09.09, частично)

Метод: мод читает 80 полей из `data`; `payload = dict(data)` — прямая копия
тела запроса зрителя, поэтому любое поле, которое бэкенд не задал сам, задаёт
клиент. Прогнал сверку «что читает мод» против «что присваивает бэкенд».

Закрыто: `max_alive_children`, `duration_s`/`value` у `power.activate`,
`points` у `player.modify_attribute`.

Снято как безопасное — с доказательством, а не осмотром:
`reward_boost` (ставит `_resolve_perks`, строка 2574), `gear_tier` у
`set_class` (берётся из БД перед отправкой), `power_key` (валидируется по
`POWER_PRICES`, неизвестный — отказ), `_action_id` (перезаписывает поллер мода,
`ActionPoller.cs:221`), `amount`/`item_type` у `player.give_item` (пресеты),
`prefix`/`text`/`color` у `world.broadcast_message` (зрителю не продаётся).

**НЕ проверено, ждёт своей очереди** — поля покупаемых действий, которые
бэкенд не задаёт: `slot` (`hero.discard_item`, `hero.reforge_quality`),
`stance` (`hero.set_combat_stance`), `kind`/`distance` (`world.trigger_event`,
1000 крустиков), `skill_key` (`hero.add_skill`), а также поля
`kingdom.propose_war/peace` и семейных действий (`hero.rename_child`,
`hero.change_child_looks`, `hero.respec_child_skills` — они backend-only, но
payload им собирают другие файлы). Часть из них почти наверняка законные
параметры выбора зрителя, а не правила, — но это осмотр, а не проверка.
Разблокирует: для каждого назвать «правило или параметр» и для правил —
исполняемый тест с враждебным телом.

## Багрепорт зрителя 09.09: «не у всех отображает бонусы к параметрам»

`kuro_gothic`, 2026-09-09 14:07, статус `open`. Зритель сообщает, что бонусы к
параметрам показываются не для всех. Не разбирался — нашёл в постстрим-триаже
(`docs/POSTSTREAM_TRIAGE_2026-09-09.md`).

Разблокирует: уточнить у зрителя, о каких именно параметрах речь (пешка
RimWorld? навыки? импланты?) и на каком экране — по одной фразе диагноз не
ставится. Закрывать в проде как resolved только после фикса.

## Часовой кейс и дроп эфира неразличимы в данных (09.09)

Оба пишутся с `source='drop'` (`bot_core.py:1173`); отличает их только
`trigger_key` в таблице `case_triggers_fired`. Из-за этого «сколько раздала
часовая механика» нельзя ответить запросом к `cases`, и я на этом построил
ложную гипотезу «часовые не работают», сняв её лишь чтением кода.

Не дефект — механика работает (5 раундов за эфир 09.09). Разблокирует: свой
`source` для часовых (например `hourly`) — но это меняет данные и требует
миграции существующих строк, поэтому отложено.

## Хвосты дыры с лимитом детей (09.09)

Сама дыра закрыта в тот же день: клиент присылал `max_alive_children: 20`, и
предел поднимался впятеро, потому что бэкенд поле не ставил, а мод принимал
1..20 из payload'а. Теперь бэкенд присваивает его безусловно
(`MAX_ALIVE_CHILDREN` в `routes/bannerlord.py`), гейт исполняет настоящую
`_prepare_action` с враждебным телом и виден красным. Осталось три хвоста.

- **Три остальных «твёрдых лимита» Bannerlord не проверены поимённо** —
  5 вассалов, 10 в пачке, кулдаун дипломатии 300с. Накануне заведённый гейт
  объявлял их защищёнными, но проверял наличие текста в исходнике, а не
  поведение, и ровно так пропустил четвёртый. Считать неизвестными, пока
  каждый не проверен ИСПОЛНЕНИЕМ с враждебным payload'ом.
  Разблокирует: три теста по образцу `_spoofed_payload_is_overridden`.

- **Мод доверяет payload'у на слово.** `MakeBabyHandler.cs:44-49` принимает
  любой `max_alive_children` в 1..20. Сейчас это безопасно: единственный
  источник payload'а — бэкенд, а он всегда шлёт 5. Укрепление второго слоя
  (мод сам зажимает предел) требует пересборки DLL и установки владельцем, то
  есть стоит эфира. Отложено сознательно: одного слоя достаточно, пока бэкенд
  единственный отправитель. Разблокирует: ближайшая плановая пересборка мода.

- **Ещё шесть полей мод читает, а бэкенд не ставит** — `attribute`,
  `body_code`, `distance`, `prefix`, `random_category`, `_action_id`. Я
  проверил из них два: `_action_id` перезаписывает сам поллер мода
  (`ActionPoller.cs:221`), а `broadcast` зрителю не продаётся, то есть `prefix`
  этим путём недостижим. Оставшиеся четыре выглядят законными параметрами
  выбора зрителя, а не правилами, — но **это осмотр, а не проверка**.
  Разблокирует: для каждого назвать, правило это или параметр; правило —
  ставить на сервере.

## НАЙДЕНО 07.09: перезапуск бэкенда обнуляет панель у всех, кто её уже открыл

**Симптом** (скриншот владельца, 18:29 UTC): панель показывает правильный ник,
но баланс 0, доход +0, квесты 0/0, кейсы 0 и бейдж «Зритель». В базе прода у
`shedoy23` на канале 98319857 в этот момент **11 058 327💎**.

**Механизм назван, не гипотеза.** Логин зрителя резолвится через
`dependencies.resolve_jwt_login`, который для opaque-токена лезет в
`_twitch_login_cache` — словарь **в памяти процесса**, БЕЗ таблицы в базе.
Заполняется он вызовом `/api/user/resolve-twitch-token` при загрузке панели.
Значит: рестарт бэкенда → кэш пуст → у всех уже открытых панелей
`require_jwt_user` возвращает None → эндпоинты отвечают **HTTP 200** с
`{"status": "unauthorized"}` без поля `points`. А фронт (`viewer.js:1003`)
делает `data.points || 0` и рисует НОЛЬ; слова `unauthorized` во всём JS архива
нет ни разу. Ноль и «не знаю, кто ты» склеены в один пиксель — то самое правило
«один признак — один смысл».

**Триггер этого случая:** прод перезапускался сегодня при выкате фикса TTS,
скриншот сделан через ~25 минут после. Проверка занимает пять секунд:
перезагрузить панель — баланс возвращается.

**Почему это важнее одного скриншота:** каждый деплой молча обнуляет панель
каждому зрителю, у кого она открыта, пока он не перезагрузит страницу. Это же
объясняет давнее «то заходит, то не заходит».

**Две починки, разной цены:**
- (A) бэкенд, без пересборки ZIP: хранить маппинг opaque→логин в базе, а не в
  памяти процесса. Убирает причину. Деплоится сегодня.
- (B) фронт: отличать «неавторизован» от нуля и звать войти заново. Требует
  НОВОГО архива в Twitch, то есть делать это надо ДО подачи, иначе поедет в
  следующую версию через ревью.

**Риск для ревью:** ревьюер, открывший панель в неудачный момент, видит
уверенные нули вместо ошибки и пишет «расширение не работает».

## ~~Озвучка без решения стримера~~ — ЗАКРЫТО 07.09, выкачено на прод

Решение владельца 07.09: **срок жизни заявки 30 минут**, дальше автовозврат.
Выбор из трёх вариантов (срок / показывать статус в панели / ничего не делать)
— взят первый: показ статуса требует правки фронта, а фронт замерзает на CDN до
следующего ревью.

Опасение про накопленный хвост снято замером ПЕРЕД выкатом: заявок в очереди на
проде **0**, то есть первый прогон подметалки никого не задел и вопрос о
возврате задним числом отпал сам. Заодно видно, почему дыра не всплывала: из 61
озвучки за всё время прозвучали все, отклонённых нет ни одной, а после
появления гейта 01.08 через него прошло всего три.

Осталось наблюдение, а не работа: у зрителя в панели по-прежнему **нет статуса
своей заявки** — он узнаёт исход уведомлением о возврате. Полноценный статус
это правка фронта, её место — следующая версия расширения, не текущая подача.

Этот файл содержит только решения, сознательно отложенные относительно текущего
product roadmap. Полная история до 2026-08-06 находится в
`docs/archive/DEFERRED_THROUGH_2026-08-06.md`.

## Триаж 06.09 — фантомная сессия и интеграции

- **Фантомная stream session вернулась.** Один эфир дал настоящую сессию
  `316792161493` и пяти минутный огрызок `2026-09-06`; посещаемость разделилась.
  EventSub ставил live-cache, но не записывал `event.id` в `_stream_ext_id`,
  поэтому polling 300 секунд использовал дату. **Исправлено и выкачено 06.09:**
  поведенческий тест видел красный результат и зелёный 6/6; на следующем эфире
  проверить, что в `stream_sessions` появилась ровно одна строка.
- **Лёркер выпал из случайных дропов и watch-time квестов.** Продовый код
  исключал зрителей без свежего взаимодействия из `_process_drop`, а квесты
  обновлял только в engaged-ветке, вопреки принятому правилу «половина ставки,
  но не ноль и без потери целей дня». **Исправлено и выкачено 06.09:** оба
  дефекта воспроизведены поведенческими тестами; после выката три целевых теста
  на самом проде завершились с exit 0.
- **Telegram-анонс старта не доставлен:** TimeoutError; VPS по-прежнему не
  соединяется с Telegram. Нужен отдельный сетевой выход/relay.
- **PubSub `Client not found`** повторился на online/offline. Polling смягчает
  влияние, но мгновенная смена состояния панели не доказана.
- Репорт №49 закрыт: живой сценарий «смерть → исчезновение трупа» сохранил
  `is_alive=0`, HP=0, имя и мир у обеих пешек.

Полные доказательства: `docs/POSTSTREAM_TRIAGE_2026-09-06.md`.

## Цены во фронте переехали на сервер (05.09, ночь) — в два захода

`/api/rimworld/config` существовал с 24.07 и не был подключён; за ним нашлись
копии во всех остальных модулях. Закрыто в один заход:

| Файл | Было чисел | Источник |
|---|---:|---|
| `pawn.js` + оболочки (RimWorld) | 7 | `/api/rimworld/config` |
| `viewer-bannerlord.js` | 24 | `/api/bannerlord/config` |
| `viewer-shedcolony.js` | 28 | новый `/api/shedcolony/config` |
| `voting.js`, `guilds.js`, `family.js`, озвучка в оболочках | 12 | новый `/api/core/config` |

В оболочках осталось по 4 запасных числа — их перерисовывает JS сразу после
загрузки конфига; это осознанный fallback, а не копия.

**Второй заход, по итогам внешнего обзора.** «Все цены переехали» продержалось
недолго: в ShedColony нашлись ещё 17 кнопок, где цена написана как
«Покормить · 75» — без значка валюты, поэтому храповик их не видел вовсе.
Переведены на `SC.<действие>.price`; на этот формат добавлен отдельный гейт в
`scripts/test-frontend-rimworld-prices.mjs` (видел красным).

**Чему научил заход.** Мой первый проход пропустил четыре места, и все четыре
были невидимы линтеру: два клиентских гейта баланса (`if (amt < 100)`) при уже
переведённом тексте ошибки, «100k💎» и «5,000,000💰» — регулярка требовала цифры
впритык к значку. Нашёл внешний обзор. Линтер починен (разделители, суффиксы
k/M, HTML-комментарии больше не считаются ценой) и сразу показал ещё три места
в Bannerlord. Гейты: `PRICE_LITERAL_BASELINE` на нуле везде, кроме оболочек,
`scripts/test-frontend-rimworld-prices.mjs` и `backend/tests/test_price_configs.py`.

## Аудит совместимости: два кандидата на починку (найдено 06.09)

Из первого прохода `docs/MOD_COMPAT_AUDIT.md`. Оба — не гипотезы, а места в
коде с известным поведением; чинить по правилу «денежные дефекты не ждут конца
аудита».

- **Свита призыва редеет молча, действие остаётся успешным.**
  `SummonHeroHandler.cs:685` резолвит бойцов по сохранённым в базе
  идентификаторам (`bannerlord_retinue.troop_id`, на проде лежат ванильные
  `aserai_skirmisher` и подобные). Нет дефа — `continue`, и `PostApplied`
  ставится всё равно. Зритель платит 50💎 за «героя со свитой 5/5» и может
  получить героя одного; в логе это видно как `retinue: 0/5 spawned`.
  Возвращать нечего — формально действие выполнено, поэтому обычный критерий
  «отказ без потери денег» такое не ловит. Разблокирует: сравнить `spawned` с
  `retinueIds.Count` и сказать зрителю, что свита неполная.

- **Чёрный список товаров RimWorld живёт в моде, а не на сервере.**
  `ShopManager.cs:27`. Чтобы снять с продажи мёртвый товар (сабля
  `W_Weapon_Melee_Sheathe`: 5 покупок по 5520💎, 0 успехов), нужен релиз DLL и
  перезапуск игры у стримера — при том что цену того же товара мы меняем на
  бэкенде за минуты. Разблокирует: серверный фильтр каталога по имени предмета,
  применяемый при приёме каталога от мода.

## Вечерний триаж 05.09 — четыре хвоста (разбор: `docs/POSTSTREAM_TRIAGE_2026-09-05-evening.md`)

Деньги за вечер сошлись полностью (630 действий, 27 отказов, 103 367💎 возвращены,
разрывов журнала нет). Отложено следующее:

- ~~**RimLink пишет только в общий `Player.log` — чужой мод глушит наш лог.**~~
  **СДЕЛАНО в тот же вечер 05.09:** у мода свой файл
  `ModLogs\rimlink_ГГГГММДД.txt` (`RimLink/Source/RimLinkLog.cs`); 165 вызовов
  переведены на него, порядок «сначала файл, потом `Verse.Log`». Держит
  `RimLink/tests/LogFileHarness` в CI (видел красным). DLL с логгером собран
  и установлен в игру, SHA-256 `6BD8866C…`, предыдущий сохранён как
  `RimLink.dll.rollback-2026-09-05-013FA64F`. Осталось увидеть файл глазами
  после первого запуска игры.
- **`W_Weapon_Melee_Sheathe` (сабля за 5520💎) — причина найдена, чинить не у нас.**
  Разобрано 05.09 по декомпиляции: падает `MVCF.VerbManager.AddVerb` — берёт
  `verb.Managed()`, получает null (у врождённого верба расы «Вольфейн» нет
  ManagedVerb, MVCF сам это печатает: «Attempted to get ManagedVerb … which does
  not have one»), и СЛЕДУЮЩЕЙ строкой разыменовывает: `managedVerb.Notify_Added(...)`.
  То есть NRE внутри чужого мода, наш обработчик отрабатывает верно — откатывает
  и возвращает деньги. Гипотеза по коду, стеком пока не подтверждена: наш `catch`
  логировал только сообщение. С 05.09 логируем полный стек
  (`PawnManager.cs`, EquipItem) — первая же попытка покажет, верна ли она.
  **Развилка для владельца:** (а) Harmony-prefix из RimLink на `MVCF.VerbManager.AddVerb`,
  пропускающий вербы без ManagedVerb — чинит и покупку, и ручную экипировку, но
  тащит в RimLink зависимость Harmony и патч чужого мода; (б) выдавать такое
  оружие в инвентарь колониста вместо рук — без зависимостей, но это не совсем
  «экипировать»; (в) оставить отказ с возвратом, как сейчас. Пока не решено —
  сделан только стек в логе.

- **Деревянные протезы продаются без учёта того, что уже стоит.** `kot_cmetah`
  за 23 минуты купил PegLeg/WoodenHand/WoodenFoot 10 раз подряд, все 10 — отказ
  «уже стоит», 500💎 возвращены. Разблокирует: фронт должен знать занятые части
  тела (это данные с бэка, а не логика во фронте) — или бэк обязан отказывать
  ДО списания.
- ~~**ShedColony: три платные кнопки не сработали ни разу.**~~ **РАЗОБРАНО 05.09,
  вывод был неверный.** `colony.donate` не сломан — он переименован в
  `colony.supply` ещё 02.07 (45 успехов), а 29 строк отказов — исторические, от
  26.06; такой кнопки сегодня нет ни во фронте, ни в манифесте. `colony.spy_boost`
  и `colony.spawn_visitor` работают, но отказывали ЗАКОННО: первому нужен
  активный рейд, второму — таверна в колонии (`ColonyOps.spyBoost` / `spawnVisitor`).
  **Настоящий дефект был в другом:** мод отвечал одним словом `op_failed`, и
  «нет таверны» было неотличимо от «мод сломан» — ни зрителю, ни триажу.
  Починено: мод шлёт `op_failed: <причина>` (jar 0.1.2), бэкенд переводит её
  словарём и кладёт зрителю в уведомление той же транзакцией, что и возврат
  (`modules/shedcolony/refusals.py`, тесты в `test_viewer_notices.py`).
  **Выкачено 05.09:** бэкенд на проде (`refusals.py` побайтно совпал с локальным),
  jar 0.1.2 опубликован по прямой ссылке, подписанный ZIP лежит в `/releases/`,
  манифест `shedcolony-0.1.2.json` подписан и проходит валидатор.
  **Осталось:** Manager носит манифесты установки внутри себя — 0.1.2 он
  увидит только со следующей сборкой Manager (сейчас у него 0.1.1);
  владельцу заменить jar в своей игре на 0.1.2 и проверить живой отказ.

- **Сохранение игры RimWorld падало 4 раза за вечер** из-за нулевого байта в
  письме мода YART: одно письмо валит запись всего файла. Файл сейва на диске
  цел (XML разбирается целиком), прогресс не потерян. Владельцу решать, трогать
  ли чужой мод; наш код ни при чём, но риск потери эфира реальный.

## Триаж 05.09: №49 и доставка Twitch — перед продолжением R3

Подтверждены потеря полей пешки при минимальном death payload в bulk-sync и
устаревшее alive после смерти. Backend исправлен и выкачен, DLL установлена;
**живой ретест 06.09 пройден, №49 переведён в resolved**.
Следом проверить follow 10:46:02 UTC (Helix 1, EventSub 0) и PubSub 404.
Полный разбор без дублирования доказательств: `docs/POSTSTREAM_TRIAGE_2026-09-05.md`.
Работы по ссылке Manager и схеме манифестов остаются локальными.

## До завершения R0–R3

- CK3 integration;
- marketplace/community integrations;
- публичный Integration SDK;
- сложная plugin-система Manager;
- billing и Pro trial;
- viewer monetization;
- масштабный frontend redesign, не подтверждённый activation/retention данными;
- добавление игр ради количества.

## ~~R3/R4: переключить канал загрузки Manager~~ — ЗАКРЫТО 06.09

Постоянная ссылка `/download/manager` теперь редиректит на **alpha.16**
(константа `MANAGER_RELEASE_VERSION` в `routes/streamer.py` — одно место, где
живёт номер). Backend выкачен, редирект проверен ответом сервера. Запись про
alpha.12 к этому моменту уже устарела: ссылка вела на alpha.15, а не на
alpha.12 — очередной случай отрицательного статуса, пережившего свою правду.

Заодно закрыт разрыв, из-за которого менеджер ставил вчерашние моды:
**alpha.16 знает shedcolony 0.1.4, bannerlord 0.1.3 и rimworld 0.1.3**
(опубликованы, sha256 сверены после скачивания, метки правок в бинарниках
проверены). До этого он ставил ShedColony 0.1.1 и моды 0.1.2 — то есть работу
двух дней пользователь бы не получил.

**Остаётся:** чистая установка R3 (матрица 0 из 10) — проверка менеджера на
пустой машине.

## Мелочь, но записать: test_eventsub мигает (найдено 2026-09-01)

При полном прогоне `test_eventsub.py` один раз дал exit 1, при повторном прогоне
и при запуске в одиночку — зелёный (34/34). То есть тест не воспроизводимо
падает в общей пачке, скорее всего из-за таймингов или занятого порта.

**Почему это не «ерунда»:** гейт, который иногда краснеет без причины, приучает
перезапускать вместо разбираться — и настоящую регрессию в нём однажды
пролистают. **Что разблокирует:** отдельный заход — воспроизвести с
`--repeat`, посмотреть, не поднимает ли тест сервер на фиксированном порту.

## Найдено при этой же проверке: канат не сбрасывал рейтинги между сезонами (02.09)

Копируя сезонную логику крестиков «дословно», я потерял из неё ровно одну
строку — сброс `duel_stats` на старте нового сезона. Последствие: рейтинг копится
через сезоны, топ-3 замирает на первых игроках, приз каждый сезон уходит одним и
тем же, новичок не догоняет никогда.

**Почему это не поймал первый тест.** Он проверял ОДНО закрытие сезона: приз
выдан, повторный проход не платит. Следующего сезона тест не видел вовсе.
Класс ошибки: **скопировал форму, потерял часть содержимого, а тест проверял
форму**. Починено и закрыто новой проверкой «новый сезон начинается с чистых
рейтингов» — показана красной на убранном сбросе.

## Платный путь на 1.4.8 работает; вскрылся старый долг «БЕЗ ЯВНОГО ИСХОДА»

Владелец купил действия при живой игре. **Обратный путь пройден целиком:**

| Действие | Списано | Что сделал мод |
|---|---|---|
| `player.give_item` | 10 000💎 | `gold 378439 → 478439 (+100000)` |
| `hero.add_skill` | 2 500💎 | `Throwing +1000xp (×2.0 boost)` |
| `player.give_item` (ежедневный) | — | `gold 478439 → 578439 (+100000)` |
| `hero.army_create` | 500💎 | армия собрана у «Замок Флинтолг» |

Все четыре `acked`, `ack_success=1`, ошибок нет. Журнал крустиков сходится:
`balance_after` предыдущей строки равен `balance_before` следующей.

**Ложная тревога, которую я чуть не объявил дефектом.** У третьего действия нет
цены и нет строки в журнале денег — выглядело как «дали 100 000 динаров даром».
В payload оказалось `"_daily": true`: это ежедневная бесплатная награда, её
создаёт бэкенд, а не покупка зрителя. Признак «цены нет» имел два смысла —
«не списали» и «бесплатное по замыслу», и разделяет их только payload.

### Настоящая находка: 4 действия завершились БЕЗ ЯВНОГО ИСХОДА

`[action] БЕЗ ЯВНОГО ИСХОДА — успех проставлен умолчанием; обработчик не
подтвердил эффект`. Типы: `player.give_item` (×2), `hero.army_create`,
`hero.add_skill`.

**Это НЕ регресс 1.4.8.** Маркер завели 28.08, а игру с модом после этого не
запускали до сегодня (последний лог до переезда — 20.08). То есть сегодня он
впервые ПОКАЗАЛ уже существовавший долг, а не поймал новую поломку.

**Чем это опасно:** эффекты сегодня произошли — золото выросло, опыт начислен,
армия собралась. Но если бы они НЕ произошли, зритель всё равно увидел бы
«успех», и деньги не вернулись бы. Ровно класс, ради которого написан
`docs/MOD_CONTRACT.md`.

**Список работ (он же список этих обработчиков):** перевести `give_item`,
`add_skill`, `army_create` на явный `PostApplied` после НАБЛЮДЕНИЯ эффекта
(золото до/после, xp до/после, армия существует) и `PostFailed` на всех прочих
выходах. Гейт уже есть: `scripts/triage-mod-log.py` считает этот маркер
фатальным и валит вердикт.

**Исправлено и подтверждено живым прогоном 02.09 после аудита.** Все три обработчика теперь
ставят `PostApplied` только после необратимого игрового эффекта. Поздний сбой
синхронизации или логирования не превращается в возврат за уже выданное золото,
XP или созданную армию; сбой до эффекта даёт `PostFailed`. Диспетчерный harness
проверяет, что явный успех захватывается и не печатает маркер. Повторные покупки
`give_item`, `add_skill`, `army_create` прошли без маркера, на проде все три
`acked/ack_success=1`.

Тем же прогоном найден следующий старый долг: успешно применённый
`hero.party_order_set` (`patrol`) написал `EXIT-OK`, но остался «БЕЗ ЯВНОГО
ИСХОДА». В рабочей копии `party_order_set` теперь вызывает `PostApplied` после
регистрации sticky-order, а соседний `party_order_release` получил явные успехи
и отказы вместо немых выходов. DLL установлена; требуется один живой приказ,
чтобы подтвердить именно новый исход в игровом логе.

Полный статический аудит кнопок после переезда: 60 покупаемых action type, 56
имеют C#-обработчик и 4 намеренно остаются backend-only; дыр между фронтом,
кассой, manifest и registry нет. IL-проход по обработчикам разрешил 1318/1318
ссылок на методы и поля установленных TaleWorlds 1.4.8. Reflection-проход нашёл
один настоящий drift приватного API: `Clan._heroes` стал `_heroesCache`.
Fallback выхода героя из клана обновлён с совместимостью назад и установлен
(MD5 `5CD9DA51…`). Это доказывает связность API, но не заменяет живые проверки
состояний кампании для всех 56 мутирующих действий.

Отдельно проверен неуспешный `kingdom.propose_peace`: мод вернул
`proposer_clan_against`, action стал `failed`, а журнал денег содержит подряд
`-1500` и `+1500` в одну секунду. Возврат доказан балансами, не предположен.

**Побочно:** `army_create` собрал армию, но `созвано партий=0`. Исключений в моей
новой ветке `CanLordCreateArmy` не было, то есть метод отработал и вернул пусто.
Норма это (некого звать рядом) или поведение отличается от старого
`GetMobilePartiesToCallToArmy` — по одному запуску не различить. Проверять, когда
рядом с героем будут свои партии.

## Вылет 02.09 — ПОДТВЕРЖДЕНО: виноват BanditBlackHole (проверено запуском)

Владелец выключил `BanditBlackHole` и продолжил ТО ЖЕ сохранение от 1.3.15.
**Вылета нет.** То есть из двух подозреваемых виноват был чужой мод, а старый
сейв сам по себе игру не роняет — по крайней мере на этом пути.

Что при этом отработало в игре (не «патчи встали», а механики сработали):

| Признак в логе | Что доказывает |
|---|---|
| `[tournament:patch] FIRED` ×2 (town_B2, town_V2) | патч турнира выстрелил на живых городах |
| `[ClanUpgrades] daily tick: renown→6 heroes, influence→6` | клановые апгрейды начисляются |
| `hero.fief_tribute_sync → ACK success:true` | доход с феодов уходит на сервер и принят |
| `hero-stats mirror: pushed 7 changed hero(es)` | статистика героев синхронизируется |
| `[party_order] HourlyTick: reissued=1 active=1` | приказ отряду живёт и переиздаётся |
| `[HeroResolver] взят HeroObject '[BLink] slopkom_nyi_item'` | герои зрителей резолвятся по сейву 1.3.15 |

На проде: 16 героев живых, 5 105 807 золота суммарно, 13 феодов, ошибок в
`twitchbot.err.log` ноль. Ошибок в логе мода тоже ноль (четыре совпадения по
маске — это строки РЕГИСТРАЦИИ финализаторов, где слово Exception стоит в
описании; сам триаж их не считает фатальными, и правильно).

**Единственное, что осталось непроверенным:** платное действие ЗРИТЕЛЯ целиком —
от кнопки в панели до эффекта в игре и списания. Всё выше показывает, что мод
шлёт события НАРУЖУ; обратный путь (сервер → очередь → мод → игра) на 1.4.8 ещё
не проходили.

## Вылет 02.09 в 22:05 — виноват НЕ наш мод (разобрано по дампу)

**Исключение:** `System.MissingMethodException` (HResult 0x80131513).

**Стек целиком ванильный, ни одного нашего кадра:**

```
FieldBattleEventComponent.CreateFieldBattleEvent(PartyBase, PartyBase)
DefaultEncounterModel.CreateMapEventComponentForEncounter(...)
StartBattleAction.ApplyInternal(...)
EncounterManager.StartPartyEncounter(...)
MobileParty.OnPartyInteraction(...)
EncounterManager.HandleEncounters(Single)
Campaign.Tick() → MapState.OnMapModeTick(...)
```

То есть падение на **старте полевого боя** (встреча отрядов на карте).

**Что такое MissingMethodException здесь.** Код вызвал метод, которого в
загруженной сборке нет. Ровно то, что бывает у мода, собранного под ДРУГУЮ
версию игры. Наш пересобран сегодня под 1.4.8 и в стеке не появляется.

**Кто остаётся.** В аргументах запуска, кроме нашего, был ровно один сторонний
мод — **`BanditBlackHole` v1.0.8**, и он про встречи с бандитами, то есть про
этот самый путь. Под 1.4.8 он не пересобирался.

**Вторая независимая причина:** сохранение сделано на **v1.3.15.110062**, игра
**v1.4.8.119303**, `Game Integrity is Achieved: False`. Старый сейв на новой
версии падает и сам по себе.

**Проверка на две минуты:** выключить `BanditBlackHole`, начать НОВУЮ игру,
встретить любой отряд на карте. Упало — виноват сейв или мы; не упало — виноват
`BanditBlackHole`.

**Правило, которого у нас не было:** после обновления игры пересобирать надо не
только СВОИ моды. Чужие собраны под старую версию и падают в ванильных стеках,
где наших кадров нет вовсе — то есть выглядят как «игра сломалась сама».

### Побочно: шумный старт сессии (не причина краша)

Раньше лог содержал 176 событий `hero.equipment_changed` подряд: 11 отдельных
запросов на каждого из 16 героев. Исправлено пакетной отправкой 11 событий на
героя. Свежий запуск подтвердил `ACK ALL` 16/16, ноль отказов; старт теперь
делает 16 запросов вместо 176.

## Переезд на 1.4.8 СОСТОЯЛСЯ на уровне «мод жив» (2026-09-02, вечер)

Второй запуск после починки: **19 патчей из 19**, совпадает с нормой 1.3.15.
Доказательства, а не только цифра:

- `[IsSideDepleted] postfix registered на TaleWorlds.MountAndBlade.DefaultBattleMissionAgentSpawnLogic`
  — патч встал именно на НОВЫЙ класс, а не «число сошлось случайно»;
- `event module.session_start → ACK: {"status":"ok"..."success":true}` — сервер
  подтвердил приём, то есть связь двусторонняя;
- `heroes_snapshot pushed: 16 adopted heroes`, и в боевой базе ровно **16 героев,
  все живые** — данные зрителей пережили обновление игры.

**Чего это НЕ доказывает и что осталось сделать руками:** патчи нашли цели, но
не проверено, что они делают то же, что делали. Нужен прогон по
`docs/TESTING_PLAYBOOK.md`: создать героя зрителя, купить действие, увидеть
эффект в игре. До этого прогона переезд считается недоказанным, а мод-фикс —
правдоподобным, но не подтверждённым (`LESSONS.md`, «Vanilla NRE»).

## Итог первого запуска на 1.4.8 (2026-09-02, вечер)

Скрипт триажа лога отработал на первом же реальном случае и дал три находки:

1. **18 патчей из 19.** Не встал `IsSideDepletedPatch`: класс
   `MissionAgentSpawnLogic` в 1.4.8 **переименован** в
   `DefaultBattleMissionAgentSpawnLogic`, метод `IsSideDepleted(BattleSideEnum)`
   тот же. Патч теперь перебирает оба имени — новое первым, старое следом, чтобы
   мод пережил и откат игры на 1.3.15.
   **Ложный след по дороге:** грep по бинарнику находил строку
   «MissionAgentSpawnLogic» и говорил «тип на месте» — это была ПОДСТРОКА нового
   имени. Список классов сборки показал правду. Ровно правило «один признак —
   один смысл»: подстрока в бинарнике не равна существующему типу.

2. **`config.json` мода потерян вместе с папкой.** Он не в git (там токен) и не
   в релизном архиве, поэтому восстановление папки его не вернуло: мод достучался
   до бэкенда, но рукопожатие не прошло и опрос заданий не запустился — в логе
   `module_token не задан в config`. Токен перевыпущен, конфиг восстановлен.
   **Что это значит на будущее:** после любой пропажи папки мода надо
   восстанавливать ТРИ вещи, а не две — SubModule.xml, DLL и config.json.

3. **Копия DLL падала «файл занят» при закрытой игре** — держал лаунчер.
   Проверка добавлена в `deploy.ps1 -Mod` (`RUNBOOK.md` §7 «Ловушки, на которых мы уже спотыкались»).

**Не проверено и требует прогона в игре:** встали ли патчи ПО СМЫСЛУ. Сборка
зелёная и 19 патчей на месте — это значит «нашли цель», а не «делают то, что
делали». Нужен короткий прогон: создать героя зрителя, купить действие,
посмотреть эффект (`docs/TESTING_PLAYBOOK.md`).

## ИНЦИДЕНТ 2026-09-02: игра обновилась до 1.4.8, папка мода исчезла

Владелец: «в меню баннера даже выбрать наш мод не могу». Причина не в меню —
**папки `Modules/Shedoy23.BannerlordLink` в игре больше нет.** Проверено:
рядом два других наших мода (`Shedoy23.MassUpgrade`, `Shedoy23.SiegeSanity`)
уцелели, а основной исчез. Файлы игры датированы 02.09 — то есть в этот день
приехало обновление до **1.4.8** (`Modules/Native/SubModule.xml`: `v1.4.8`).

**Интеграция Bannerlord сейчас не работает вообще.** Это флагманская механика:
2146 платных действий от 15 человек за август.

### Что показала сборка против 1.4.8

`dotnet build` — 7 ошибок в 4 местах API. Это ровно та «просадка сигнатур», о
которой предупреждала матрица совместимости:

| Что поехало | Где | Похоже на |
|---|---|---|
| `ArmyManagementCalculationModel.GetMobilePartiesToCallToArmy` — метода нет | `ArmyHandlers.cs:107` | переименование/удаление |
| `MobileParty.SetMoveRaidSettlement` — добавился параметр `isTargetingPort` | `PartyOrderBehavior.cs:459`, `PartyOrderHandlers.cs:170` | **Naval DLC**, как и предсказывала матрица |
| `Clan.CommanderLimit` — свойства нет | `CreatePartyHandler.cs:113,117` | переименование |
| `SpawnTroop(..., forceDismounted:)` — параметра нет | `SummonHeroHandler.cs:416,686` | смена сигнатуры |

**Важно про объём.** Это только ошибки КОМПИЛЯЦИИ — их видно сразу. Вторая
часть переезда начнётся после того, как соберётся: Harmony-патчи могут
скомпилироваться и при этом не найти цель в рантайме. Сколько их — заранее не
скажет никто; покажет `scripts/triage-mod-log.py` при первом запуске
(норма 19 применённых патчей записана с 1.3.15).

### Развилка для владельца

- **Откатить игру на 1.3.15.** Мод восстанавливается из
  `dist/releases/BannerlordLink-0.1.1.zip` (там SubModule.xml, DLL и префаб —
  распаковать в `Modules/`), всё работает как работало. Burning Empires при этом
  не поставить: ему нужен 1.4.8.
- **Переезжать на 1.4.8.** Чинить 4 места компиляции, потом смотреть патчи, потом
  пересобирать и перепубликовывать два публичных мода в Workshop.

**Урок на будущее, независимо от выбора:** папка мода в игре не под git и не под
бэкапом — её снесло обновлением, и заметили это только когда владелец полез
играть. Восстановление возможно только потому, что релизный архив 0.1.1 лежал в
`dist/releases/`. Стоит проверять наличие папки в `preflight.ps1`.

## Версия ИГРЫ не пишется в лог мода (найдено 2026-09-02)

Скрипт триажа лога определяет версию МОДА (`v0.1.1 OnSubModuleLoad`), а версии
Bannerlord в логе нет вообще. При переезде на новую версию это первое, что
хочется знать: «патчи не встали — а на какой версии это вообще происходило?»
Сейчас ответ добывается руками из главного меню игры.

**Что чинит:** одна строка в `BannerlordLinkModule.OnSubModuleLoad` с
`ApplicationVersion.FromParametersFile()` или аналогом. **Почему не сделал
сразу:** это правка мода, а мод — пересборка и копия в игру при закрытой игре;
делать её отдельно от переезда на новую версию нет смысла. **Разблокирует:**
ближайшая пересборка мода.

## Мини-игры мертвы с 18 июня, а канат построен по их модели (02.09)

Разбор аналитики Twitch за 28.07–31.08 вместе с нашей базой. Факты, каждый
проверен запросом к боевой БД:

| Что | Число |
|---|---|
| Платных действий Bannerlord за август | **2146 от 15 разных людей** |
| Действий ShedColony за август | 14 от 1 человека |
| Матчей во ВСЕХ трёх мини-играх за всё время | **33** |
| Последний матч: кубики / крестики / RPS | 18.06 / 18.06 / 09.06 |
| Матчей за август | **ноль** |
| Записей о выплате сезонного приза за всё время | **ноль** |
| Максимальный рейтинг в `duel_stats` сейчас | 1000 у всех, то есть стартовый |
| Уникальных зрителей, видевших панель за 30 дней (данные Twitch) | 138 |
| Установок / удалений за месяц | 2 / 1 (белый список, иначе и не могло быть) |
| Использовано битов | **0 за весь период** — машинное подтверждение нашего аргумента ревьюеру |

**Вывод, который меня касается напрямую.** Канат я построил ровно по модели трёх
мёртвых механик: очередь подбора → нужен ВТОРОЙ человек в очереди в ту же минуту
→ рейтинг → сезонный приз с порогом 1100. На этих данных модель не сработала ни
разу: 33 матча за всё существование и ни одной выплаты приза. Я скопировал
проверенную ФОРМУ, не проверив, работает ли она ПО СУЩЕСТВУ — при том что
предупреждение лежало в комментарии `duels.js:95-99` («за два месяца никто не
перешагнул порог»), я его прочитал и вывода не сделал.

**Что с этим делать (не сделано, нужно решение владельца):**

1. **Режим против бота.** У кубиков он был и работал в одиночку; у каната его
   нет. Темп бота обязан быть ДЕТЕРМИНИРОВАННЫМ и опубликованным (например,
   ровный тап раз в N мс), иначе вернём случайность, из-за которой всё и
   затевалось. За матч против бота рейтинг не начисляем — как было у кубиков.
2. **Порог приза 1100 при старте 1000** отсекает всех, кто сыграл меньше трёх
   удачных матчей. Либо снижать, либо считать приз от числа побед, а не от ELO.
3. **Почему `duel_season_payouts` пуст — ПРОВЕРЕНО 02.09, механизм назван.**
   При ротации сезона все три игры делают `UPDATE duel_stats SET elo=1000,
   win_streak=0, season_id=<новый>` — то есть рейтинги обнуляются вместе с
   началом нового сезона. Матчи были в июне, сезоны с тех пор ротировались
   больше десяти раз, поэтому в таблице у всех ровно стартовые 1000, а
   рейтинги выше порога (1126) сохранились только в `match_rooms` как след
   конкретных матчей. Журнал выплат `duel_season_payouts` заведён миграцией
   M105 позже тех ротаций, поэтому июньские закрытия сезонов в него попасть и
   не могли. **Порог владелец решил НЕ трогать** (02.09).
   **Что осталось непроверенным:** выплачивались ли призы за июньские сезоны
   фактически. Проверяется по `points_income` за июнь-июль, если там есть
   источник сезонной выплаты; отдельной задачей, деньги старые.

**Чего НЕ делать:** выводов «мини-игры не нужны». Правило `CLAUDE.md` про одного
пользователя тут работает наполовину: людей в Bannerlord пятнадцать, и это уже не
N=1, но мини-игры могли умереть и от того, что в них некому играть в одиночку, а
не от отсутствия интереса. Режим против бота как раз и различит эти две причины.

## Правила в канате закрывались сами — починено, но НЕ в поданной 0.0.3 (02.09)

Владелец открыл «Правила» в канате на телефоне: они схлопывались через секунду.
Причина не в `<details>`, а в том, что опрос раз в 2 секунды переписывал
innerHTML ВСЕГО тела модалки вместе с ними. Крестики этого не делают — у них
меняется только `#ttt-content`, а свёрнутые блоки живут в оболочке рядом.

**Почему это не косметика:** блок правил — наш ответ на требование ревью
показывать правила ДО входа в матч. Правила, которые закрываются сами, — это
правила, которых ревьюер не прочитал.

**Починено 02.09** (оболочка отделена от перерисовываемой части, гейт
`tests/test_game_modal_shape.js` показан красным), заодно приведены к одной форме
стартовые экраны трёх мини-игр — владелец заметил, что каждая открывалась
по-своему.

**НО в поданной 0.0.3 этого нет:** архив загружен в кабинет ДО находки, а
пересобирать и подменять его во время ревью нельзя — иначе теряется ответ на
вопрос «то же ли самое видит ревьюер». Правки уедут со следующей версией.

**Что разблокирует:** вердикт по 0.0.3. Если завернут — пересобираем с этими
правками; если одобрят — они уходят в следующую версию обычным порядком.
**Чего НЕ делать:** отзывать 0.0.3 ради этого. Очередь ревью начнётся заново, а
дефект не блокирующий: правила открываются и читаются, просто их приходится
открыть повторно.

## Зрители на публичной 0.0.1 видят кубики, которые больше не запускаются (02.09)

Заморозка кубиков сделана на бэкенде, а на CDN Twitch у зрителей до сих пор
**публичная 0.0.1** — в ней карточка «Кубики» есть, и клик по ней теперь получает
вежливый отказ: «Кубики заморожены — их заменило „Перетягивание каната“».

**Это осознанно, а не недосмотр.** Правило регламента требует, чтобы бэкенд
одновременно обслуживал старый и новый клиент с CDN. Он и обслуживает: старый
клиент не падает и не молчит, он получает названную причину. Альтернатива —
держать кубики живыми до одобрения — означала бы, что ревьюер и зрители играют в
разные игры, а именно кубики и есть причина отказа.

**Что снимет пункт:** одобрение 0.0.3 и Release — тогда у всех появится карточка
каната вместо кубиков. Если 0.0.3 завернут надолго, стоит вернуться и решить:
терпеть отказ на кнопке или временно разморозить.

## Кубики ЗАМОРОЖЕНЫ, их заменил канат (владелец 2026-09-01)

**Сделано, не отложено** — запись здесь, чтобы «морозим» не превратилось в
«кто-то когда-то что-то отключил». Реализовано: `routes/tugofwar.py`,
`frontend/tugofwar.js`, миграция `m120`, гейт `tests/test_tugofwar.py`,
кнопка запуска раунда в кабинете стримера.

**Поправка того же дня:** сначала канат был сделан КОМАНДНЫМ (раунд канала,
фикс за участие, статус за победу). Владелец: «не нужен командный, по принципу
остальных с сезонами и т.д надо 1х1». Переделано в дуэль 1×1 — очередь подбора,
комната, ELO, сезон, призы топ-3, как у крестиков и дуэлей. Командные таблицы и
миграция удалены до выката, кнопка запуска раунда из кабинета убрана. Выигрыш от
переделки не только в единообразии: в командном варианте приз нельзя было вешать
на исход, а в дуэли можно — значит третья сезонная лестница, выключенная вместе с
кубиками, вернулась.

**Что именно заморожено и где:** карточка и скрипт кубиков убраны из ОБЕИХ
оболочек; `play-vs-bot` отвечает отказом (`FROZEN` в `routes/dice.py`); `dice`
убран из `MATCHMAKING_GAME_TYPES`, иначе зритель встал бы в очередь прямым
запросом мимо снятой кнопки. Сезонная таблица и история матчей остаются
читаемыми — это заморозка, а не удаление.

**Что разморозит:** ничего автоматически. Возврат = вернуть строку в
`MATCHMAKING_GAME_TYPES`, снять `FROZEN` и вернуть карточку в обе оболочки
ОДНОВРЕМЕННО. Половинчатый возврат даст «кнопка есть, очередь закрыта» или
наоборот.

**Отменённое этим решением:** ниже стоял выбор владельца от 26.08 — заменять
кубики 1v1-игрой на навык на существующей сезонной награде. Он отменён:
командный канат выбран вместо него. Спека 1v1 (`SPEC_002_1V1_SKILL_DICE_REPLACEMENT_2026-08-29.md`)
остаётся непострoенной альтернативой.

## Замена кубиков (dice) — ОТМЕНЁННЫЙ выбор от 2026-08-26 (см. раздел выше)

Кубики срезаются с мобилы по правилу 3.5 (см. `docs/TWITCH_REVIEW_0.0.2_REJECTION.md`).
**Решение владельца по замене: Путь 2 — новая 1v1 игра на навык на СУЩЕСТВУЮЩЕЙ
сезонной MMR-награде** (как крестики/дуэли), а не командное «Перетягивание каната».
Причина выбора: в 1v1 исход в руках игрока → сезонный MMR-приз стоит на той же
почве, что уже есть; командный вариант потребовал бы отдельной статус-награды
(разбор — переписка 26.08 + `docs/audits/CORE_STANDALONE_IDEAS_CONSOLIDATED_2026-08-26.md`).

**Реализация НАМЕРЕННО отложена до ответа Twitch на письмо по 0.0.2** — не строим,
пока не знаем, что именно ревьюер требует срезать и подтверждают ли они сезонно-💎
модель вообще (на ней и висит 0.0.2). Разблокирует: ответ ревьюера.

**НЕ строить командный вариант** — `docs/specs/SPEC_TUG_OF_WAR_2026-08-26.md`
остаётся как отклонённая альтернатива (casual/статус-призы), помечен как parked.
Конкретную 1v1-механику (реакция / «взломай код» / иная кнопочная) выбираем и
специфицируем ПОСЛЕ ответа Twitch.

**Ориентир по механике (владелец 26.08): КОРОТКАЯ и ЛЁГКАЯ** — ~30 сек, низкий
порог. «Навык» здесь = исход зависит от действий игрока (не пассив, не удача),
а не хардкорный скилл-грайнд; 30-секундный клик напрягом не считается. Так Path 2
сочетает casual-порог и 1v1+MMR без противоречия.

## Требует решения владельца: три риска подачи, найденные 2026-09-01

Полный разбор — `docs/audits/TWITCH_REJECTION_SURFACE_2026-09-01.md`. Здесь то,
что я НЕ трогал, потому что это прод-поведение и вопрос твоего решения.

> **Решение владельца 2026-09-01: автосообщения и награду НЕ трогать.** Оба
> пункта ниже остаются как есть. Возражения я высказал до решения, они записаны
> тут же и остаются в силе; переспрашивать не буду. Что это значит практически:
> если следующий отказ придёт по 4.4/4.6.3 или по «плате за доступ», причина уже
> названа здесь, и разбираться будем не с нуля. Пункт про домен картинок решением
> не закрыт — он остаётся на владельце.

- **Награда «ПОЛУЧИТЬ ДОСТУП К РАСШИРЕНЮ» в баллах канала.** Ничего не выдаёт
  автоматически — регистрирует заявку, доступ выдаётся руками через белый список
  Twitch. Бот при этом рекламирует её словами «купи в наградах канала». Читается
  как плата за вход в расширение, а оно обязано быть бесплатным для всех.
  **Варианты:** переименовать в «Заявка в тестеры» и убрать «купи» из
  автосообщения, либо снять награду, пока расширение не публичное.

- **Автосообщение бота со ссылками DonationAlerts и Boosty.** Включено на проде.
  Сообщение шлёт наш бэкенд, то есть часть расширения, а правило 4.4 запрещает
  расширениям рекламу, 4.6.3 — ссылки на покупку. Серая зона: сообщение в чате, а
  не в панели. **Дешёвый вариант:** выключить на время ревью, потом вернуть.

- **Домен картинок в Developer Console.** Спрайты питомцев грузятся с
  `shedoy23.ru` (отдаются исправно, проверил). Если домена нет в Allowlist for
  Image Domains, ревьюер увидит битых питомцев. **Проверить может только
  владелец:** Developer Console → Extension → Asset Hosting.

**Отдельно, и это самое неприятное:** в уже отправленном ревьюеру письме стоит
«the only random element is which tier drops». Это неверно — случайны ещё выбор
навыка и выбор супруги. Блок раскрытия в оболочке 01.09 сужен до кейсов, письмо
исправить задним числом нельзя. **Разблокирует:** оговорка в следующем письме
либо доведение выбора навыка до конца (правка на десяток строк, бэкенд уже
принимает `skill_key`).

## Решено: «Случайный товар» убран, слово «рандом» снято с интерфейса (владелец 2026-09-01)

**Что сделано.** Механика «🎁 Случайный товар» (оружие/броня/конь из high-tier
пула за 500K–1M динаров, конкретный предмет катал мод) убрана целиком — и из
фронта, и из покупаемых действий на бэкенде. Снаряжение качается тирами:
`hero.upgrade_gear`, T1→T6, фиксированная цена. Снято на бэкенде намеренно, а не
только во фронте: адрес без кнопки — это ровно тот класс дыры, что нашли 27.07 с
атрибутами. Тест `test_bannerlord_target_spoof.py` [7] переписан с «законный путь
жив» на «закрыты оба пути» — он и краснел на этой правке, пока не был обновлён.

Слово «рандом»/«случайный» убрано из видимых строк: платный XP, ежедневная
награда, женитьба, выбор культуры (заодно ушла игральная кость 🎲), подпись про
дроп кейсов. **Механики не менялись** — это правка подачи, и правило Twitch 3.5,
по которому нас отклонили, говорит именно про ориентацию поверхности.

**Моё возражение, зафиксировано.** Переименование не делает механику
детерминированной. Пока слова стояли на кнопках, наш же блок раскрытия
утверждал: «случайность есть ровно в одном месте — тир кейса». Это было неверно
и до правки, а после снятия слов стало бы прямым обманом ревьюера — то есть
находкой хуже, чем сам «рандом» на кнопке. Поэтому фразу я **сузил до кейсов**
(«внутри кейсов случайность ровно одна»), в обеих оболочках. Что осталось
неверным и не в моей власти: **в уже отправленном письме ревьюеру та же фраза
стоит в неограниченном виде** — если он ответит, это надо оговорить.

**Что остаётся случайным после правки:** тир кейса, какой навык получит XP,
какая NPC станет супругой, культура «пусть выберет игра» (бесплатно), броски в
кубиках, авто-ход в крестиках по таймауту. Всё это оплачивается либо ничем, либо
внутриигровым золотом; пути «реальные деньги → ценность» нет нигде (разбор —
`docs/audits/AUDIT_002_REVIEW_2026-09-01.md` §6).

## Веб-панель вместо расширения — отложено (владелец 2026-09-01)

Обсуждали, как дать зрителю удобный вход, если не через расширение Twitch.
**Решение владельца: пока не делаем — «долбим твич, пока можем, и посмотрим».**

Чтобы следующая сессия не расследовала это заново, факты на момент решения:

- **Механизм уже написан и работает.** `routes/dev_login.py`: вход через Twitch
  OAuth → подписанная cookie на 30 дней → тот же фронт, что в расширении
  (`viewer.js` имеет ветку `_devJwt` и в `Twitch.ext` при этом не лезет).
  Ограничение одно — белый список `DEV_LOGIN_WHITELIST` в `config.py`, где стоит
  один ник владельца. То есть «сделать веб-версию» = снять белый список и
  приделать приличную дверь, а не переписать проект.
- **Что теряется без расширения:** наложение поверх видео и попадание в каталог
  Twitch. Обе потери во многом бумажные: мобильное наложение уже отобрано
  правилом 3.5, а в каталог мы и так не попадаем — сидим на белом списке.
- **План, если решим делать:** веб-панель основным путём + ссылка в панелях под
  видео + бот кидает её в чат + 3–5 частых действий продублировать наградами за
  баллы; телеграм-бот вторым шагом ради УВЕДОМЛЕНИЙ («твой герой умер»), чего не
  умеют ни сайт, ни расширение.
- **Опасность, названная заранее:** две витрины — две правды. Держать ОДИН фронт
  с несколькими способами входа, иначе получим класс «оболочки разошлись молча».

**Что разблокирует:** отказ Twitch, который нельзя обойти, либо молчание
ревьюера, после которого владелец решит выйти за пределы расширения.

## Внешний концепт платформы (ChatGPT, 2026-09-01) — что взяли, что нет

Владелец: переписывать проект по нему не будем. Документ архивирован целиком —
`docs/archive/STREAM_GAME_PLATFORM_CONCEPT_CHATGPT_2026-09-01.md`, там же
провенанс и разбор. Кратко: он независимо описал ту архитектуру, которая у нас
уже работает (событийное ядро, адаптеры игр с манифестом, очередь с жизненным
циклом, журнал, дедуп доставки, кабинет, оверлей, тестовый режим).

**Взято и сделано в тот же день:** аварийная остановка интеграции — единственное
из документа, чего у нас не было по делу. Кнопка в кабинете, два затвора (касса
и выдача заданий моду), гейт `tests/test_actions_pause.py`, на проде с 01.09.

**Взято на заметку, НЕ делаем сейчас: движок правил.** Связка «действие зрителя →
эффект в игре» зашита в Python по каждому действию; сущности «правило» (событие +
условия + действия + cooldown + лимиты), которую можно править без программиста,
нет. **Почему не сейчас:** правила некому настраивать, пока стример один, и цены
с кулдаунами тоже пока константы в коде. **Разблокирует:** второй живой стример.

**Отвергнуто:** центральный пример документа «донат вызывает событие в игре» —
внутри Twitch Extension это продажа игрового преимущества мимо платёжной системы
Twitch, а отсутствие такого пути и есть наш самый сильный аргумент в ревью.
Отвергнуто и «чтение памяти игры» (§6.1) — путь к бану, у нас мод и API.

## Решено НЕ делать: компенсация igotpaws 126 790💰 за топор и арбалет #67/#68 (владелец 2026-09-26)

Причина потери устранена (покупки всегда в личный сундук). Владелец: не компенсировать.

## Решено НЕ делать: компенсация stepuhatgn 16 370💰 (владелец 2026-09-26)

Выплата за бой 25.09 ушла уже мёртвому герою (дефект исправлен в моде: мёртвому
не платим). Владелец: возвращать не надо.

## Решено НЕ делать: возврат нового героя зрителя в прежний клан с живой семьёй (владелец 2026-09-26)

После смерти лидера-зрителя клан с женой-лордом переходит ей и становится NPC
(ваниль, `ChangeClanLeaderAction`; случай stepuhatgn / Эйра 25.09). Предлагал дать
новому герою зрителя вернуться туда — владелец: нет. Смерть настоящая,
вложенное в клан остаётся клану.

## Решено НЕ делать: надетое снаряжение наследнику не переходит (владелец 2026-09-25)

При смерти героя зрителя наследнику переходит только личный сундук (вещи без
слота); надетое, в том числе перекованное, уходит вместе с погибшим. Владелец
на предложение переносить надетое в сундук наследника: «ненене, всё верно
сейчас как раз по задумке». Смысл: смерть должна чего-то стоить. Компенсация
igotpaws 25.09 за вещи, ушедшие со смертью, — разовое исключение, не правило.

## Решено НЕ делать: женитьба остаётся случайной (владелец 2026-09-01)

Агенты предложили заменить «поиск супруга» выбором NPC из списка (спека
`docs/specs/SPEC_002_DEGAMBLE_SCOPE_B_2026-08-29.md` §5). Владелец: это лишает
механику смысла, и избавляться от случайности везде подряд не нужно.
**Возражение принято**, потому что подкреплено кодом: женитьба стоит 50 000
динаров у героя и 0 крустиков (`routes/bannerlord.py:2031-2049`), у супруги нет
ни редкости, ни тира, ни ценника — то есть разброса ценности, по которому
читается лутбокс, здесь нет вовсе. При этом по их же спеке это самая дорогая из
пяти правок: новое событие мод→бэкенд, строка в манифесте, зеркало с TTL, новый
эндпоинт, переделка фронта и обработчика мода.

**Цена решения:** если Twitch когда-нибудь назовёт эту механику отдельно —
объясняем, что плата внутриигровым золотом и приза с разбросом ценности нет.
Разбор целиком — `docs/audits/AUDIT_002_REVIEW_2026-09-01.md` §5–6.

## Bannerlord рандом-платные механики — рас-азартить ВЫБОРОМ (владелец 26.08)

Три проверенных мобильных блокера (платный рандом-XP, рандом-снаряга, рандом-
женитьба) азартны только из-за СЛУЧАЙНОГО исхода. Направление владельца: сделать
исход ВЫБИРАЕМЫМ зрителем → детерминированная платная покупка → комплаент →
**оставить на мобиле, не прятать**. Женитьба — выбор NPC из списка; скилл —
выбор скилла (бэк УЖЕ принимает `skill_key`, Jim); снаряга — выбор предмета из
каталога. Разбор и спека — `docs/audits/MOBILE_GAMBLING_AUDIT_CONSOLIDATED_2026-08-26.md`,
секция «FIX DIRECTION». **Что разблокирует:** продолжаем следующей сессией;
всё равно ждём вердикт Twitch перед выкаткой. Потом продолжим.

**Уточнение приоритета 2026-09-01.** Сортировать механики по признаку «есть ли
`random` на платном пути» — неверно. Денежной оси у нас нет вообще: биты дают
только текст благодарности (`eventsub.py:783-817` прямо отказывается начислять),
подписки не начисляют ничего, единственный источник крустиков в EventSub — баллы
канала (`eventsub.py:364,440`), которые зарабатываются просмотром. Пути «реальные
деньги → внутренняя ценность» не существует, а лутбокс-правила Apple и Twitch
§6.2.x привязаны как раз к нему. Остаётся ось ПОДАЧИ (правило 3.5 — как экран
читается на 13+), и по ней порядок такой: кейсы (сундук + разброс 500×) чинить
по-настоящему; «🎁 Случайный товар» переименовать или сделать выбором из каталога;
платный XP — фронтовая правка на десяток строк, бэкенд уже принимает `skill_key`
(`routes/bannerlord.py:1926-1940`), фронт его просто не шлёт; кубики заменяются
1v1-игрой на навык; женитьба не трогается (раздел выше).

## До измеренного infrastructure trigger

- миграция SQLite → PostgreSQL;
- выделенный worker/process;
- object storage/CDN для downloads;
- microservices;
- Kubernetes.

## Намеренно на паузе — семья, гильдии, питомцы

**Это решение владельца, а не забытый хвост** (сказано 2026-08-23). Механики
работают и данные в них есть: 2 гильдии, 20 питомцев, 2 брака на проде. Пауза
не из-за поломки — владелец не видит, куда их развивать.

**Не предлагать их вырезать «как неиспользуемые».** Низкие числа тут значат
«развитие остановлено», а не «никому не нужно» — тот же класс ошибки, из-за
которого я 23.08 чуть не предложил отключить RimWorld, увидев ноль пешек
(а ноль был следом нашей же поломки). См. правило про нулевые метрики в
CLAUDE.md.

**Направление, которое рассматривает владелец:** развить в сторону RPG и
боссов. Разбор — в этом же файле ниже по мере проработки; спека и проверка
правил Twitch обязательны ДО кода (правило 1 в CLAUDE.md: самый дорогой класс
ошибок — построить, а потом узнать, что нельзя).

**Почему развитие встало — рабочая гипотеза (моя, не владельца):** все три
механики производят СОСТОЯНИЕ БЕЗ ПОТРЕБИТЕЛЯ. Питомец куплен — и ничего от
него не зависит; в казне гильдии копятся крустики — тратить не на что; брак
заключён — ни на что не влияет. Механика развивается, когда есть давление:
нечто, что это состояние ПОТРЕБЛЯЕТ и создаёт повод копить. Босс —
классический такой потребитель, поэтому направление выбрано верно.
## Требует продуктового решения

- поведение всей integration-вкладки, когда выбранная игра или мод выключены;
- пересчёт исторических streamer/viewer streaks после исправления двойных
  `stream_sessions`;
- окончательный состав Free/Pro после появления retention data;
- выбор CK3 после подключения второй существующей integration к Manager.

- **Потерял ПК — ключ интеграции живёт ещё до 90 дней, и погасить его нечем.**
  Найдено 2026-08-22 при чистке Manager: `revoke` есть только как
  `DELETE /v1/manager/module-credentials/{id}` — он требует и сессию Manager, и
  сам `id`, а листинга ключей канала в API нет вовсе. То есть стример, у которого
  сдох диск или который переустановил Windows, свои действующие ключи ни увидеть,
  ни отозвать не может: они просто ждут `expires_at` (90 дней). Мне пришлось
  гасить два осиротевших ключа прямой правкой прод-базы — это и есть признак
  дырки в продукте, а не разовое неудобство. Разблокирует: `GET` со списком
  ключей канала + кнопка «отозвать все ключи этого канала» в Manager (либо на
  странице стримера, что даже правильнее — она доступна без Manager).

- **Twitch перестал доставлять `channel.follow` — механизм не назван
  (2026-08-22).** Последнее сработавшее приветствие — 5 августа
  (`@tomsan_126`, секунда в секунду с реальным фолловом). Фолловы 6 и
  20 августа приветствия не получили, и 20-го **nginx не видел ни одного
  запроса** в ту минуту — то есть Twitch не присылал. При этом подписка
  `enabled`, условие верное (`broadcaster=moderator=98319857`), scope
  `moderator:read:followers` у токена есть, токен живой, а другие типы
  (баллы канала, чат-нотисы) в тот же день доставлялись. Рестартов бэкенда
  5-6 августа не было — значит это не наш деплой. Гипотеза: подписка
  протухла на стороне Twitch. **Это гипотеза, а не диагноз.** Проверяется
  пересозданием подписки: заработает — механизм подтверждён, нет —
  копать дальше. Разблокирует: пересоздание + фолловер на живом эфире.

- **ЗАКРЫТО 24.08: биты (`channel.cheer`) зарегистрированы.** Владелец перезашёл, токен получил `bits:read`, подписка создана тем же кодом, что и на старте бэкенда (`register_subscription`, идемпотентно) — без рестарта, зрителей не задело. Проверено списком: 10 подписок, все `enabled`. Ждёт живого чира на эфире — благодарность в чате, без игровой выгоды.

- ~~**Биты (`channel.cheer`) ждут повторного входа владельца в Twitch.**~~
  Обработчик и подписка сделаны 22.08, рейды (`channel.raid`) заработали
  сразу — им scope не нужен. Битам нужен `bits:read`, которого у токена нет:
  Twitch отдаёт 403 при регистрации. Разблокирует: владелец открывает
  `https://shedoy23.ru/api/streamer/auth/start`, после чего подписку надо
  ЗАРЕГИСТРИРОВАТЬ — сама она появится только при следующем рестарте бэкенда
  (регистрация живёт в startup), либо разово через helix, как делали с
  channel.follow 22.08.

- **Событие, доставленное и обработанное молча, не оставляет следа.**
  Во время эфира приходит ~5 событий в час, которые не пишут в лог ничего:
  опознать их постфактум нечем, потому что `eventsub_seen` живёт сутки.
  Из-за этого расследование поломки фолловов пришлось вести по nginx.
  Разблокирует: одна INFO-строка на каждое принятое уведомление (тип +
  канал) на входе webhook.
- **Свои автосообщения стример пока может завести только через SQL.**
  22.08 личные сообщения (донат-ссылки, соцсети, названия наград) переехали
  из общего списка в таблицу `channel_auto_messages` — иначе бот рекламировал
  бы донаты владельца в чате чужого стримера. Но интерфейса для их
  редактирования нет: новый стример получает только нейтральные сообщения и
  добавить свои не может. Это лучше утечки, но половина работы. Разблокирует: блок
  «Автосообщения» на странице стримера (список + добавить/выключить).
- **Telegram-оповещения о старте стрима — один канал на всю платформу.**
  22.08 поставлен гейт: анонс уходит только для канала-владельца этого
  Telegram (`TELEGRAM_NOTIFY_CHANNEL_ID`, по умолчанию `DEFAULT_CHANNEL_ID`).
  До гейта эфир ЛЮБОГО канала анонсировался бы аудитории владельца. Гейт
  чинит вред, но не даёт функции второму стримеру: чтобы он получил свои
  анонсы, нужны per-channel настройки TG (токен + chat_id в дашборде).

- **`DROP_BLACKLIST` по умолчанию содержит ник владельца.** То есть на чужом
  канале зритель с ником `shedoy23` не получал бы дропы. Влияние копеечное,
  переопределяется через .env, поэтому не трогал — но это тот же класс:
  личность владельца в общем конфиге.

- **`resolve_channel_id_or_default()` теперь ЗАМЕТЕН.** 28 мест подставляют
  канал владельца, когда контекст неизвестен; пока стример один, это
  безобидно, со вторым — запись в чужой канал. Молчание было главной
  проблемой: 22.08 добавлено предупреждение в лог (раз в 5 минут на место
  вызова). Список строк `channel-default:` в логе прода = рабочий перечень
  того, что обязано получить явный channel_id до второго стримера.
- **Локально стоит twitchio 3.2.1, а requirements требуют `<3.0`** (прод на
  2.10.0). Значит проверка API чат-бота на этой машине НИЧЕГО не говорит о
  проде: в 3.x нет `join_channels`/`connected_channels`, на которых держится
  дозаход в каналы. Замечено 2026-08-22 при правке late-join. Локальный
  запуск `python main.py` с IRC-ботом здесь работать не будет.
- **Бэкенд не останавливается по-хорошему: две трети рестартов добиваются
  SIGKILL.** По журналу supervisor — 404 SIGKILL на 611 остановок, самый
  ранний 2026-04-11, то есть это давнее поведение, а не следствие правок
  22.08 (проверено именно из-за подозрения на новые фоновые циклы).
  Целостности базы это не грозит — SQLite в WAL переживает жёсткое убийство,
  — но при каждом деплое теряются запросы в полёте и накопленные в памяти
  сообщения для чата, а WAL остаётся без checkpoint. Разблокирует: понять,
  что именно висит на SIGTERM (кандидаты — обработчик `shutdown`, пул БД,
  недемонизированные потоки), либо поднять `stopwaitsecs` у supervisor.
  Замечено 2026-08-22, отдельной работой, не смешивать с онбордингом.
- **Кулдауны Bannerlord живут только в памяти процесса — каждый рестарт их
  обнуляет.** `modules/bannerlord/_adapter.py: _cooldowns` — обычный dict,
  таблицы в базе нет (есть только `rimworld_heal_cooldowns`, для другой
  игры). Значит любой деплой снимает ограничение частоты со ВСЕХ зрителей
  разом; 22.08 деплоев было восемь. Деньги при этом списываются каждый раз,
  то есть это обход ЛИМИТА, а не кража, — но лимиты и ставились затем,
  чтобы мощное действие нельзя было повторять подряд. Найдено 2026-08-22
  при вопросе владельца «где слабое место».
  Усугубляется тем, что бэкенд не гасится по SIGTERM (см. пункт выше):
  состояние в памяти теряется жёстко, без шанса сохраниться на выходе.
  Разблокирует: перенести кулдауны в таблицу (ключ `channel_id + username +
  cooldown_key`, поле `until_ts`), либо сознательно записать в «решено не
  делать» с причиной.
- **Мод молчит на отказе: зрителю сообщают успех, которого не было.**
  Аудит модов 2026-08-23 (`Расширение/docs/AUDIT_MODS_2026-08-23.md`).
  `CreateKingdomHandler` вызывает `PostFailed` в одном месте из семи;
  так же ведут себя `CreatePartyHandler`, `EquipItemHandler`, а в
  `ActivateHeirHandler` и `BroadcastMessageHandler` механизма отказа нет
  вовсе. Денег не теряется (цена в крустиках 0, золото проверяется до
  списания), но это причина багрепортов #16/#17 — зритель жмёт повторно,
  не понимая, произошло ли что-нибудь. Разблокирует: `PostFailed` на
  каждом выходе, а по-хорошему — контракт как в shedcolony, где решение
  об ACK принимает диспетчер, а не каждый из 64 хендлеров.

- **Два заряженных ружья в моде: код живой, вход перекрыт.**
  (а) `disarm_burst` — 250 крустиков, два пути «эффекта нет» без возврата;
  активка исключена из `ACTIVE_POWER_KEYS`, кнопки нет, но цена и
  обработчик на месте. (б) `ModifyAttributeHandler` меряет `before`/`after`
  и пишет их только в лог, возвращая успех безусловно; действие снято с
  продажи 27.07, но цена `50` в таблице осталась. Включение любого из двух
  одной строкой сразу даёт утечку. Решить: чинить или удалять.
- ~~Класс `_buyItem` машиной не закрыт~~ — **закрыто 2026-08-23.** Поставлен
  ESLint с правилом `no-undef` по файлам оболочки, вшит в pre-commit и показан
  красным на историческом баге (переименовал объявление `_rwBuyItem`, вызовы
  оставил — пять ошибок с адресами). Первый прогон нашёл две мёртвые точки в
  `viewer.js` — `onMktSelChange` и `sendActivityData`, обе удалены. Как
  пользоваться и что делать, если правило сыплет, — `RUNBOOK.md` §6 «Проверки».
  Непокрытым осталось то, что не ловит и `no-undef`: общие ИЗМЕНЯЕМЫЕ
  переменные, которые пишет один модуль и читает другой, и неявный контракт
  порядка подключения скриптов.

## Требует продуктового решения — учёт просмотра

- **Крустики начисляются за ПРИСУТСТВИЕ, а не за вовлечённость** (найдено
  2026-08-23 по вопросу владельца). 25💎/мин × бонус уровня получает любой,
  чья панель пинговала за последние 15 минут; 15–30 минут — половина; дальше
  ноль. Это 1500💎/час пассивно при цене каравана 4000💎.
  **Ключевая деталь:** фронт УЖЕ шлёт `active_clicks` и `mouse_moves`
  (`viewer.js:835`), но модель `ActivityRequest` объявляет только `username`,
  `watch_time`, `total_time` — остальное Pydantic молча выбрасывает. Данные
  для различения «смотрит» и «вкладка открыта» долетают и теряются.
  **Что защищено и переделки НЕ требует:** JWT, начисление только в эфире,
  минимальный heartbeat и лимит частоты, потолок `watch_time` в одном пинге,
  выключенный флагом presence-по-чату, чат-бонус за символы с дедупом.
  **Варианты:** (1) полная ставка только при взаимодействии, иначе половина;
  (2) оставить как есть — обычная лояльностная экономика; (3) не трогать
  пассив, усилить активные пути (чат-бонус, квесты).
  **Оговорка к (1):** на мобильном мышью не двигают, условие должно быть
  «взаимодействие ИЛИ сообщение в чате», и понижать до половины, а не до нуля.
  Ровно из-за мобильного перекоса и заводили `PRESENCE_WATCHTIME_ENABLED`.

## Мелкие известные неудобства

- **Два вопроса агентов ждут ответа владельца с 26.08** (задача T1 в
  `hive/tasks.json`, оба гейтятся ответом Twitch по 0.0.2, поэтому не горят):
  (1) готовить ли правку мобильного гейтинга по §7 консолидированного отчёта —
  desktop не трогаем, на мобильном не рисуем кубики/кейсы/платный рандом,
  метка поверхности статическим `<meta name="shedlink-surface" content="mobile">`;
  (2) дуэли «камень-ножницы-бумага» на мобильном — переименовать нейтрально и
  оставить, или спрятать до перепроверки. Второе — вопрос допустимого риска,
  решать владельцу. Разбор: `Расширение/docs/audits/MOBILE_GAMBLING_AUDIT_CONSOLIDATED_2026-08-26.md`.


- **Четверо зрителей всё ещё привязаны к колонистам, которых нет** — это
  данные, а не код. Причина найдена и починена 28.08 (`e7e04b4`): защита
  `if not snap: return` считала пустой ростер признаком «колония не
  загружена», хотя мод физически не шлёт снимок для незагруженной колонии.
  Строки в проде (`bapah1_1`/10, `linewolf51`/18, `ethanenok`/2,
  `slopkom_nyi_item`/39 — все `colony_id=1`, июль) вычистятся САМИ первым же
  снимком после деплоя бэкенда и запуска игры. **Разблокирует** — деплой
  бэкенда. Если через день после деплоя и запуска игры они всё ещё `active` —
  значит снимок не приходит, и это уже другая поломка.

- **ЗАКРЫТО 28.08: `shedcolony_targets` завис на 2 августа** — тот же
  устаревший jar. Гипотезу «в новой колонии просто нечего слать» проверил и
  отбросил: строки `colony.targets` в отгруженном jar нет вообще, а в
  пересобранном из HEAD есть. То есть пикеры Фазы A зрителю показывали цели
  колонии, которой уже нет, не из-за пустоты, а потому что мод их не шлёт.
  Уйдёт вместе с выпуском 0.1.1. Урок общий: «может быть законно» держится
  ровно до первой прямой проверки — байты jar ответили за минуту.

- **ЗАКРЫТО 28.08: ShedColony 0.1.1 выпущен.** Отгруженный 19.08 jar был
  собран до того, как в исходники легли 12 действий и `colony.targets`.
  Мод пересобран из HEAD, подписан, лежит на
  `https://shedoy23.ru/releases/ShedColony-0.1.1.zip` (проверено: отдаётся с
  тем же sha256, что в подписи). Manager alpha.13 с обновлённым каталогом —
  там же. Ворота в упаковщике не дадут отгрузить неполный jar снова.
  **Осталось владельцу:** переустановить Manager, снять 0.1.0 кнопкой
  «Удалить», поставить 0.1.1, проверить в игре одно из ожививших действий.
  До этого 12 кнопок остаются мёртвыми (возврат работает).

- **У журнала денег нет причины («за что»).** Сам журнал заведён 28.08
  (M118, триггер на `viewers.points`) и на проде: движение, дельта, баланс
  до/после. Чего он не знает — за какое действие. Триггер узнать не может, а
  тащить причину через 39 мест (17 вызовов функций + 22 прямых `UPDATE`) —
  отдельная работа. Сегодня причина восстанавливается сопоставлением по
  времени с `module_actions`, где есть тип действия, цена и статус. Годится,
  пока каналов мало и движений в час десятки. **Разблокирует** — первый спор,
  который не удалось разрешить сопоставлением по времени, либо второй активный
  канал (тогда совпадений по времени станет много).

- **Триггеры журнала переживают не всякую миграцию.** `trg_points_ledger_*`
  висят на таблице `viewers`. Любая будущая миграция, которая ПЕРЕСОЗДАЁТ
  `viewers` (m1 ровно это и делала), снесёт их МОЛЧА: деньги продолжат ходить,
  журнал просто перестанет писаться, и узнать об этом будет неоткуда. Пока это
  прикрыто проверкой [8] в `tests/test_points_ledger.py`, но она смотрит свою
  временную базу, а не прод. **Чинится** проверкой существования триггеров на
  старте бэкенда (одна строка в `run_migrations`) или пунктом в линтере: любая
  миграция, где встречается `CREATE TABLE viewers`, обязана пересоздать и
  триггеры. **Разблокирует** — следующая миграция, трогающая `viewers`.

- **Сообщение «Release-каталог недоступен или повреждён» врёт про причину**
  (найдено 2026-08-28 на живом запуске). Тот же `catch` ловит ещё и
  `DirectoryNotFoundException` — случай «папки `Release\` рядом с `.exe`
  вообще нет». Владелец запустил Manager двойным кликом прямо из окна
  архиватора; тот распаковал во временную папку один `.exe`, и программа
  сообщила про повреждение целого каталога. Это НЕ гипотетический сценарий из
  пункта выше: он воспроизвёлся у единственного пользователя с первой попытки,
  значит воспроизведётся и у чужого стримера. Предупреждение в
  `START-HERE.txt` стоит первым пунктом и не сработало — текст рядом с
  программой не защищает, сообщение обязано называть причину само.
  **Чинится** отдельной веткой `catch (DirectoryNotFoundException)`: «рядом с
  программой нет папки Release — распакуйте архив целиком и запускайте из
  распакованной папки». Пять строк в
  `ShedLink.Manager.App/ReleaseConfiguration.cs`.
  **Почему не сделано сразу:** правка ничего не меняет, пока не собран и не
  роздан новый пакет (alpha.13), а `ReleaseConfiguration` лежит в WPF-проекте,
  куда `Core.SelfTest` не дотягивается — красным тестом фикс не покажешь без
  переезда загрузчика каталога в `Core`. **Разблокирует** — решение владельца
  собирать alpha.13 (или любая другая причина пересобрать Manager).
  Операционный обход — `RUNBOOK.md` §7 «Ловушки», подраздел «Manager,
  запущенный ПРЯМО ИЗ ОКНА АРХИВА».

- Тип значения в JSON-конфиге угадывается, хотя манифест мог бы его объявить.
  `ManagedJsonConfiguration.ParseScalar` пишет числом всё, что принял
  `long.TryParse`, и строкой — остальное. Для сегодняшних полей это верно
  (`channel_id` — число, url и токен — строки), но правило глобальное: у
  следующей игры поле-строка со значением из одних цифр (номер сервера, ключ
  вида `007`) молча уедет без кавычек, а это зеркало того самого дефекта,
  ради которого правило и вводилось. В манифесте у каждого поля уже есть
  описание — тип просится туда же. Разблокирует — первое поле, где угадывание
  даст неверный тип; до тех пор дешевле оставить как есть, чем менять схему
  дважды.

## Ждёт вердикта Twitch по 0.0.2

- **ЗАКРЫТО 2026-08-20: цены мастерской и каравана исправлены; компенсации НЕ
  будет — решение владельца.** Форма покупки показывала `💎1000 + 💰20000` и
  `💎1500 + 💰15000`, тогда как кнопка НАД НЕЙ с 02.07 показывала правильные
  `💎2500`/`💎4000`. Обе формы переведены на `/api/bannerlord/config`, поблажка
  `FROZEN_PRICE_LITERALS` снята — возврат хардкода роняет линтер.

  **Поправка к моей же записи, сделанной часом раньше.** Я написал «17 зрителей
  переплатили ≈33 500 крустиков». Обе половины неверны:
  - это **5 человек**, а не 17 (17 — число покупок; одна neyrahatomia сделала 11).
    Я взял количество строк в таблице и назвал его людьми;
  - **переплаты не было**: все 17 покупок прошли 31.07 и 03.08, то есть ПОСЛЕ
    того, как внешняя кнопка стала показывать верную цену. Списано ровно
    столько, сколько было написано на кнопке, по которой человек нажал. Дефектом
    был противоречивый экран, а не скрытая цена.

  **Решение владельца:** ничего не возвращать, «разработка ведь ещё». Причина
  принимается: людям назвали цену, они её заплатили и получили товар.
  **Что изменит ответ:** приход незнакомых стримеров. Для своих зрителей на
  этапе разработки «противоречие в интерфейсе» — терпимо; для чужих такой же
  дефект будет стоить доверия, и тогда ответ должен быть другим.

  **Урок для меня:** прежде чем называть дефект «обманом на деньги», проверить,
  что именно видел человек В МОМЕНТ покупки, а не что написано в коде сегодня.
  Число строк в таблице — не число людей.

- **Шаг 8 инструкции ревьюеру ведёт на страницу, куда ревьюер не войдёт**
  (`/streamer/dashboard` → вход как одобренный стример). Решено не отзывать
  версию ради правки текста: отзыв сбрасывает место в очереди, а модерация
  реально существует. **Что разблокирует:** ответ ревьюера. Если он напишет,
  что не смог проверить модерацию, — отвечаем письмом с записью экрана, а в
  0.0.3 переносим доказательство туда, куда он попадает без входа.

- **Панель Колонии показывает устаревший баланс крустиков после покупки**
  (найдено 2026-08-19 при разборе архитектуры фронта, не чинится — фронт
  заморожен до вердикта по `0.0.2`). `viewer-shedcolony.js:_buy` на успехе не
  зовёт `loadUserData()`, в отличие от `_bannerlordBuyAction` и
  RimWorld-покупок. Панель рисует «Баланс: N 💎» из `_cachedUserPoints`, который
  обновляется только раз в 60 с общим тиком ядра; вдобавок `_renderIfChanged`
  перерисовывает панель лишь при изменении состояния колониста, поэтому
  показанный баланс может врать и дольше минуты. Зритель видит непотраченные
  крустики и жмёт вторую покупку. **Класс:** каждая игра переписывает платёжный
  путь с нуля и теряет защиту, которая уже есть у соседней. **Что
  разблокирует:** вердикт Twitch → фронт-батч `0.0.3`; чинить вместе с выносом
  общего `buyAction` (см. следующий пункт).

- **Единый платёжный путь фронта (`core.buyAction`) — принято как направление,
  начинать после разморозки фронта** (анализ 2026-08-19). Сейчас три копии:
  `_bannerlordBuyAction` (полная — idempotency-UUID, кулдаун с сервера,
  role-gate, обновление баланса), `viewer-shedcolony.js:_buy` (без кулдауна,
  role-gate и обновления баланса), RimWorld (отдельный эндпоинт на каждое
  действие, `client_action_id` не шлёт вовсе — идемпотентности на этом хопе нет,
  в отличие от двух других игр). Каждая новая игра копирует ближайшую версию и
  теряет часть защит; это деньги зрителя, а не стиль кода. **Что разблокирует:**
  вердикт Twitch. **Что считать сделанным:** все три игры ходят через одну
  функцию, `node --check` зелёный, поведение Bannerlord-панели не изменилось.

- **Реестр игровых модулей фронта вместо `if/else` в `switchIntegrationModule`**
  (анализ 2026-08-19, начинать после разморозки). Сегодня добавление игры правит
  три файла в четырёх местах (`viewer.js`, `extension.html`, `mobile.html`), и
  именно отсутствие единого lifecycle уже стоило прод-инцидента 03.08 (панель
  Bannerlord тысячами запросов долбила RimWorld). Решено **не** заводить
  manifest/capabilities во фронте: ни один общий экран не спрашивает «умеет ли
  игра скиллы» — каждая игра рисует свою панель целиком, capabilities было бы
  абстракцией без потребителя. **Что вернёт вопрос на стол:** появление экрана,
  который ядро рисует по данным нескольких игр сразу.

- **`test_frontend_module_lifecycle.py` не проверяет cache-bust ShedColony**
  (найдено 2026-08-19). Регулярка `viewer(?:-bannerlord|-rimworld)?\.js` не
  включает `viewer-shedcolony.js`, поэтому рассинхрон версии колонии между
  `extension.html` и `mobile.html` тест не увидит и останется зелёным. Правка
  теста фронт не меняет и под заморозку не попадает, но делать её надо вместе с
  разбором теста: он строково матчит текущее тело `switchIntegrationModule` и
  сломается о любой реестр.

- **Подпись релиза покрывает только сам архив, но не инструкции установки**
  (найдено 2026-08-20 при починке определения Minecraft). `SigningPayload` — это
  `integration_id + release_version + artifact_id + size + sha256`. Блоки
  `detection`, `installation.target`, `configuration.store` подписью НЕ
  накрыты, а каталог манифестов Manager читает с диска (`Release/*.json` рядом
  с exe), а не из ресурсов сборки. Значит подменённый манифест с валидной
  подписью на настоящий артефакт может изменить, КУДА кладётся файл и куда
  пишется конфиг. **Почему не чиню сейчас:** радиус ограничен — `PathBoundary`
  не даёт выйти за корень игры, а тот, кто может писать рядом с exe, уже может
  больше. **Что закроет:** включить в подписываемую строку хеш нормализованного
  манифеста целиком; это ломает совместимость со всеми выпущенными подписями,
  поэтому делать вместе со следующей сменой ключа.

- **«Закрой игру перед установкой» для Minecraft не сработает** (2026-08-20).
  `process_names` в манифесте — `["MinecraftLauncher"]`, а сторонние лаунчеры
  (Lexplosion, Prism, CurseForge) запускают игру как `javaw`. Добавить `javaw`
  нельзя бездумно: он есть у любого Java-приложения, и ложное «закрой игру»
  заблокирует установку на пустом месте. **Пока живём так:** установка в
  запущенную игру упрётся в залоченный файл и откатится штатно, молчаливой
  порчи не будет. **Что закроет:** проверять не имя процесса, а занят ли
  целевой файл.

- **Временный костыль в папке игры владельца создан и удалён 2026-08-20.**
  Чтобы разблокировать проверку до починки, в
  `E:\lexplosion\instances\minecolonies official` был создан файл-заглушка
  `versions/1.21.1/1.21.1.json`. Владелец попросил сначала починить — костыль
  удалён вместе с папкой `versions`, инстанс вернулся в исходный вид. Записано,
  чтобы никто не искал его следы и не принял за настоящий файл лаунчера.

- **ИСПРАВЛЕНО 2026-08-20 в тот же день. Оставлено как урок: я назвал это
  недосмотром, а это было заявленное решение.** Диалог удаления прямо писал
  «Настройки и защищённый ключ останутся для восстановления», а отзыв ключа в
  продукте существовал — но только внутри «Выйти», где спрашивался Да/Нет.
  То есть возможность была, а на пути «Удалить» её не было.
  **Что осталось верным в претензии:** человек, удаляющий интеграцию насовсем,
  до диалога выхода не доходит, а ключ лежит обычным файлом ВНУТРИ папки игры —
  и папку инстанса Minecraft принято архивировать и передавать друзьям, так что
  ключ уезжает вместе с ней. Об этом не предупреждал никто.
  **Как починено:** удаление спрашивает тем же Да/Нет/Отмена, что и выход. «Да»
  — снести мод, стереть СВОЙ конфиг и отозвать ключ на бэкенде; «Нет» — прежнее
  поведение, и статус честно пишет, что ключ остался файлом в папке игры. Текст
  диалога предупреждает про раздачу папки. Покрыто SelfTest: свой конфиг удалён,
  чужие настройки в `config/` не тронуты, повторное удаление не падает.

- **Каждая сборка Manager добавляет 67 МБ на диск прода, и никто их не чистит**
  (замечено 2026-08-20 при обходе прода). Уже лежат alpha.10, .11 и .12 — 200 МБ
  из 14 ГБ диска; свободно 5.5 ГБ, при этом там же живёт БД (83 МБ) и суточные
  бэкапы. Раз в неделю по сборке — и через полгода это 2 ГБ мусора.
  **Почему не чищу сейчас:** контракт запрещает трогать опубликованные файлы, и
  правильно — на них могут быть ссылки. Но альфы, которые никто не ставил,
  исключение из этого правила, а не нарушение.
  **Что делать:** при выпуске очередной сборки удалять альфы старше двух
  предыдущих, предварительно убедившись по журналу nginx, что их никто не
  качал. **Что напомнит:** свободное место меньше 3 ГБ.

## Внешнее ревью правил (2026-08-23/24) — что приняли, что нет

Друг владельца прочитал оба `CLAUDE.md` и оценил их 7/10 по инженерной части и
4/10 по выполнимости: правила содержательные, но от объёма падала вероятность
соблюсти каждое отдельное. Правки сделаны в тот же день; здесь остаётся то, что
решено НЕ менять, и почему — чтобы через месяц не обсуждать заново.

- **Три названных противоречия — исправлены, не отложены.** (1) Правило про
  проверку кода возврата показывало пример на `&&`, которого нет в PowerShell
  5.1, — теперь даны обе оболочки. (2) Строка про размер `viewer.js` протухла
  во ВТОРОЙ раз (стояло 2.5k, реально 1631), поэтому числа строк из описания
  репозитория убраны совсем: они не влияют ни на одно решение, но гниют молча.
  (3) Про ванильные NRE в моде правило одновременно требовало «глушить только в
  крайнем случае» и описывало глушение как основной приём — оставлено одно
  прочтение: сначала prefix, чинящий вход, глушение последним.

- **Авто-push в конце сессии остаётся** — вопреки замечанию ревьюера, что это
  рискованный дефолт. Причина: решение владельца 2026-07-24 после того, как
  коммиты копились по 6-10 штук на одном ПК без второй копии; названный риск
  (диск умрёт) реализуется молча и целиком, а риск авто-пуша ограничен веткой
  разработки — push не деплоит. В правило добавлено явное «push ≠ деплой».
  Пересмотреть, если появится второй человек, коммитящий в ту же ветку: тогда
  цена мусорного коммита в общей истории вырастет.

- **Обязательное обновление CONTEXT/STATUS остаётся обязательным.** Ревьюер
  предлагал смягчить до «по необходимости». Не принято: владелец не читает
  диффы, и документ — единственный носитель между сессиями; «по необходимости»
  на практике означает «никогда», а именно на этом уже обожглись (токен Twitch
  кричал в лог три недели).

- **Привязка к именам моделей убрана.** Правило про разведку субагентами
  говорило «sonnet/haiku»; имена моделей живут месяцами, правило — годы. Теперь
  «дешёвая модель», критерий поведенческий.

- **Разделение состоялось:** `CLAUDE.md` = что делать + условие + одна строка
  причины + ссылка; `LESSONS.md` = полные истории дословно. Проектный файл стал
  меньше на 36%, личный на 35%, ни одно из 35 правил не выпало (проверено
  механически: все именованные объекты оригиналов присутствуют, ссылки
  резолвятся линтером `check_rule_links`).

## Ждёт ответа ревьюера Twitch (2026-08-24)

- **Что именно в мобильном виде считается «азартной ориентацией» — не знаем.**
  Отказ по правилу 3.5 назвал класс, но не элементы. Механики у нас без ставок
  (проверено по коду: `dice.py`/`duel.py`/`tictactoe.py` — списаний за матч нет,
  приз только сезонный по ELO с порогом 1100), поэтому вырезать вслепую значит
  потерять механики зря. Владелец пишет ревьюеру уточняющее письмо (черновик и
  проверенные факты — `Расширение/docs/TWITCH_REVIEW_0.0.2_REJECTION.md`).
  Разблокирует: ответ Twitch. Заготовка на случай «убрать всё» уже собрана в
  ветке `compliance/mobile-3.5` и проверена линтерами — выкатывать после ответа.

- **Мобильный вид и десктопный РАЗОЙДУТСЯ по составу механик — но пока НЕ
  разошлись.** Поправка 2026-09-01: строка выше утверждала это в настоящем
  времени и ссылалась на линтер `check_mobile_view_age_rating`. И расхождение,
  и линтер существуют только в ветке `compliance/mobile-3.5`; в `main`
  `extension.html` и `mobile.html` совпадают по набору механик и по скриптам
  (машинная проверка Jim'а, аудит 002 §0). То есть механизма расхождения ещё
  нет, и все блокеры 3.5 сейчас стоят и на десктопе тоже. Пересмотреть, если
  Twitch ответит, что дело в подаче (иконка кубика, слово «кейс»), — тогда
  дешевле перерисовать, чем лишать мобильных зрителей игр.

- **Владелец 2026-09-01: «Twitch уже не ответит».** Это меняет цену ожидания:
  весь план 0.0.2 (замена кубиков, рас-азарт Bannerlord-механик) записан как
  «ждём ответа», и если ответа не будет никогда, план не разблокируется сам.
  Развилка, которую надо закрыть решением, а не ожиданием: (а) назначить дату,
  после которой считаем «ответа нет», и строить Scope B по своему разумению;
  (б) подать пересобранный архив без ответа и получить второй отказ как
  источник конкретики. **Что уже сделано и от ответа не зависит:** M1 (правила
  конкурсов), M2 (турнирный трофей), M4 (CI). **Разблокирует:** решение
  владельца по (а)/(б).

## Munder Difflin (харнесс агентов) — найдено при разборе 2026-08-27

Разбирал установленное приложение `E:\MunderDiffin` (чужое, автор
`github.com/chaitanyagiri/munder-difflin`, v0.4.6). Что нашлось и требует
решения владельца:

- **Агент Jim был запущен БЕЗ песочницы — ПОЧИНЕНО 27.08.** В `roster.json` его
  команда была `codex --model gpt-5.6-sol --dangerously-bypass-approvals-and-sandbox`.
  Приложение по умолчанию ставит безопасную позу (`-a never -s workspace-write`
  + `--add-dir` на папку агента), но НЕ добавляет её, если в команде уже стоит
  любой флаг стойки — а `--dangerously-bypass...` именно такой. Итог был:
  автономный агент писал куда угодно на машине, где лежат ssh-ключи к прод-VDS,
  токены Twitch и офсайт-бэкапы. Заменено на `-a never -s workspace-write`
  (автономность сохранена, песочница вернулась). Копия старого файла — в
  scratchpad сессии, `md-config-backup/roster.json.bak`.
  **За чем следить:** песочница codex может резать сеть. Если Jim перестанет
  делать прод-деплой или `git push`, лечится ключом `network_access = true`
  в секции `[sandbox_workspace_write]` личного codex-конфига
  (`~/.codex/config.toml`) — файловая песочница при этом остаётся.
- **Расход не-Claude агентов не считается вообще.** В `hive/cost-ledger.jsonl`
  217 строк и все с `agent_id: god`; Jim за полный день работы (ревью,
  прод-деплой) дал ноль строк. Метрики приходят только по OTel Claude Code;
  для codex такого канала нет. Значит cost cap и circuit breaker по деньгам
  покрывают только Claude-агентов. **Разблокирует:** issue автору либо свой
  учёт расхода codex.
- **Приложение пишет своё состояние в корень нашего репо** (`hive/`, `palace/`,
  `roster.json`, `roster-backups/`, `hallways.json`). Закрыто `.gitignore`
  27.08. `hive/` — вообще отдельный git-репозиторий внутри нашего.

- **Оркестратор (Michael) исключён из circuit breaker.** В коде явно
  `if (id === reg.godId) continue;` перед проверками. То есть единственный агент,
  который реально тратит деньги, не проверяется ни на cost cap, ни на зацикливание.
  Ставить `costCapUsd` в конфиг бессмысленно: он покрыл бы только работников, а
  работник у нас codex, чей расход не измеряется вовсе (см. пункт выше).
- **Доллары в приложении — фикция при подписке (поправка владельца 27.08).**
  Приложение нигде не знает про план: берёт оценку Claude Code по прайсу API и
  рисует как деньги. Оба агента у нас на подписке, счёт по токенам не приходит,
  поэтому `costCapUsd` и цифра `$` в fleet.json — декорация. Что РЕАЛЬНО
  ограничивает: недельная и 5-часовая квота планов. Мерить надо токены, не
  доллары: за ~1.9 часа работы этаж сжёг 910k токенов оркестратора (это
  эквивалент $23.58 по прайсу API, если бы платили по нему).
  Драйвер — `godModel: claude-opus-4-8[1m]`: миллионное окно раздувает вход на
  каждом ходу, а Opus ест недельную квоту быстрее всех. **Решение владельца:**
  оставить или сузить окно. Сначала проверить гейджем контекста в приложении,
  даёт ли подписка 1M на самом деле — если нет, суффикс `[1m]` ничего не делает.
- **Сломанный MCP-сервер отключён 27.08.** В конфиге был включён
  `search-with-key` (Brave) с пустым `BRAVE_API_KEY` — сервер поднимался в
  сессии каждого агента и не мог авторизоваться. Выключен.

Полный разбор (раздутый инсталлятор, стоимость хуков, KG-поиск) — только в
переписке 27.08, отдельным файлом не заводил: чужой код, чинить не нам.

## Призыв «против стримера» списывает 100💎 и не делает ничего (найдено 2026-09-03)

Кнопка «⚔️ Призвать против стримера» в бою, где герой зрителя УЖЕ на поле (движок
заспавнил его как клан-члена), не делает **ничего** — но деньги списывает и
отчитывается успехом.

Путь в `BannerlordLink/src/Actions/SummonHeroHandler.cs`:
- герой уже в Mission → `heroAlreadySpawned = true`, лог обещает «спавним только
  retinue + heal»;
- свита уже спавнилась в этом бою → `retinue SKIP` (строка 627);
- перевод на сторону врага (`forced SetTeam`) и лечение стоят под общим гейтом
  `if (agent != null && !heroAlreadySpawned)` (строка 424) → не выполняются, то
  есть обещанный в логе heal не происходит;
- обработчик доходит до `PostApplied(actionId)` (строка 756) → успех, возврата нет.

**Доказательство** — лог мода 03.09, действие `1ef7312da7f94eccbca56fc83c6eb188`
(22:29:32): `hero уже в Mission … спавним только retinue + heal` →
`SelectEnemyParty … uniqueParties=0` → `enemy party не найдена → fallback на
MainParty` → `retinue SKIP — already spawned this battle` → `→ retinue-only (…,
team=Attacker)`. `team=Attacker` — та же сторона, что и при союзном призыве
минутой раньше; строк `forced SetTeam` и `HP restored` в логе нет. Баланс
зрителя 243 121 → 243 021. Ни `БЕЗ ЯВНОГО ИСХОДА`, ни возврата.

**Второй признак, независимый от лога мода:** `/api/bannerlord/battle-status`
через десять минут после покупки отдаёт `"is_player_side": true` — герой
по-прежнему на стороне стримера. То есть сторона не поменялась не только по
логу спавна, но и по состоянию, которое видит бэкенд.

**Класс:** «платное действие обязано детектить тихий no-op ДО ack-true» — шестой
случай. Особенность: no-op собран не движком, а собственными гейтами, каждый из
которых по отдельности правильный (анти-эксплойт «призови себя, чтобы
подлечиться»; свита один раз за бой).

**Нужно решение владельца по продукту:** должен ли повторный призыв «против»
перебрасывать героя, который уже дерётся за стримера, на сторону врага?
- **да** — вынести `forced SetTeam` из-под гейта `!heroAlreadySpawned` и вызвать
  `KillRewardBehavior.RefreshSide` (иначе награда считается по стороне «до» —
  грабли 31.07, 43 выплаты не в ту сторону);
- **нет** — отказ с возвратом (`PostFailed`) до того, как деньги ушли.

**Что разблокирует:** ответ владельца + закрытая игра (DLL занята процессом).

## Стойка боя: панель говорит «Натиск», мод применил «оборону» (найдено 2026-09-03, НЕ подтверждено)

При призыве 03.09 в 22:28:45 мод записал `[CombatAI] applied @shedoy23 def=1.00
off=0.20 stance=defensive`, тогда как в панели зрителя подсвечен «⚔ Натиск»
(`aggressive`), и подсветка берётся с сервера (`h.combat_stance`,
`viewer-bannerlord.js:3255`).

**Почему это гипотеза, а не дефект:** `[CombatAI] applied` пишется один раз на
героя за миссию (`_aiLogged.Add(user)`), поэтому по логу нельзя отличить
«стойка неверная всю миссию» от «кэш `PowerCache.GetHeroStance` был холодным на
момент спавна и через тик подтянул верную». Механизм не назван — значит, гипотеза.

**Цена ошибки, если подтвердится:** зритель выбирает «Натиск» (атака в потолок,
блок в пол) и молча получает противоположное поведение героя; заметить это в бою
нельзя, а бесплатность стойки не спасает — она влияет на исход платного призыва.

**Что разблокирует:** прогон, где стойка меняется ПОСЛЕ призыва и проверяется
вторым признаком (не логом `CombatAI`, он однократный) — например, разовым
дампом `AgentDrivenProperties` по требованию.

## HP 288/287 и платное лечение в полный HP (закрыто кодом 2026-09-04)

`/api/bannerlord/battle-status` отдавал `"hp": 288, "hp_max": 287`, панель рисовала
`288/287`. Проверка кода уточнила причину: награда за убийство и M1-regen уже были
зажаты `HealthLimit`; расхождение давало разное округление дробного лимита Bannerlord (287.5).
Снимок теперь округляет оба значения одинаково и зажимает `hp` по `hp_max`.

**Почему это не косметика:** «полное HP» перестаёт быть надёжным признаком. Любая
проверка вида «лечить нечего, HP полное» на сравнении `hp >= hp_max` работает,
а на `hp == hp_max` — молча ломается. Это ровно правило «один признак — один
смысл»: показатель `hp` перестал означать то, что означал.

**Что разблокирует:** отдельный заход — зажать прибавку HP потолком
`agent.HealthLimit` в `KillRewardBehavior` и в тике реген­а, затем проверить,
как `heal_burst` ведёт себя при полном здоровье (в этом прогоне лечение так и
не протестировано).

**Закрытие 04.09:** проверка кода показала, что награда и regen уже были
зажаты `HealthLimit`; `288/287` оказалось разным округлением дробного лимита.
Живой тест вскрыл другое: `heal_burst` при полном HP списал 100💎 за нулевой эффект
и вернул `Success=true`. Исправлено на `health_full` + refund; DLL установлена, фраза
отказа выкачена на прод, сборка 0/0, harness 133/133. Остался live-retest после перезапуска игры.

## ~~ 33 кода отказа мода зритель видит безликой фразой (найдено 2026-09-03)~~ — ЗАКРЫТО 2026-09-06

Все 33 кода получили фразы, отвечающие «что мне с этим делать»; денежные
написаны первыми, как и планировалось. Список долга `LEGACY_NO_PHRASE` в
`scripts/lint_consistency.py` теперь пуст — новый код мода без фразы красит
линтер сразу. Отдельный гейт `tests/test_bannerlord_refusal_coverage.py`
сверяет словарь с исходниками мода: 148 кодов, без фразы 0. Два денежных
(`skill_cap_reached`, `skill_xp_not_applied`) нашёл сам линтер — они
динамические, с двоеточием, и мой сборщик их не видел.
## Защита от двойного клика глотает второй призыв молча (найдено 2026-09-03)

`ShedLink.buyAction` (`frontend/viewer-actions.js:90`) держит замок по ключу
`<игра>:<тип действия>` — для обоих призывов это один и тот же
`bannerlord:player.spawn`. Купил «за стримера», через секунду жмёшь «против
стримера» — второй клик отбрасывается, зритель не получает НИЧЕГО: ни тоста, ни
отказа, только `console.warn`. Выглядит как «кнопка не работает».

На бэкенде это два разных действия: цены 50💎 и 100💎, кулдауны раздельные
(`routes/bannerlord.py:2559`, ключ `player.spawn:{side}`). То есть замок во
фронте склеивает то, что сервер намеренно разделил. Комментарий во фронте
(`viewer-bannerlord.js:3590`) утверждает обратное — «кулдаун общий на обе
стороны» — и с сервером разошёлся.

**Как найдено:** два клика подряд в живом прогоне; в лог мода пришло только
одно действие, списалось только 50💎.

**СДЕЛАНО 2026-09-06.** Ключ замка теперь включает параметры действия, а
заблокированный клик показывает «предыдущее действие ещё выполняется». Там же
нашлось третье: фронт читал кулдаун призыва по несуществующему ключу
`player.spawn` — отсчёт не показывался ни разу. Держит
`scripts/test-frontend-action-lock.mjs` (видел красным).

~~**Почему не исправлено сразу:** фронт заморожен, `0.0.3` на ревью Twitch
(`RELEASE_RECORD.md`). Правка однострочная — добавить сторону в ключ замка, —
но уехать может только со следующей версией.

**Цена ожидания:** низкая. Зритель жмёт второй раз и покупает. Хуже другое:
молчание неотличимо от поломки, и такой зритель пишет в баг-репорты.

**Что разблокирует:** снятие заморозки фронта после вердикта Twitch.

## Таймауты `POST /events` у мода Minecraft — безопасны, разобрано 2026-09-03

**Оставлено как ответ на будущий вопрос, а не как задача.** В логе за вечер
трижды: `POST /events failed: HTTP connect timed out`, в том числе сразу после
успешно выполненного платного действия. Выглядит тревожно — но деньги тут ни
при чём, и вот почему, чтобы не расследовать заново:

- по `/events` ездят только `colony.snapshot` и `colony.capacity` —
  периодические снимки. Их потеря безобидна по замыслу: следующий отчёт через
  5 секунд перекрывает предыдущий (`ShedNet.isCriticalEvent`);
- одноразовые `player.linked` / `player.died` считаются критическими и
  буферизуются с повтором;
- **исход платного действия идёт вообще не сюда**, а через `ShedNet.ack` →
  `POST /ack`.

`ack` отправляется без повтора, и это выглядит дырой — но обе ветки
самозалечиваются при повторной доставке действия: успех записан в `ShedSeen`,
поэтому повтор даёт переответ без выполнения (`ShedActions:35`); отказ в
`ShedSeen` НЕ записывается, поэтому повтор просто выполняет действие заново.
Дедуп переживает перезапуск (в логе `dedup loaded 13 action id(s)`).

**Что НЕ проверено:** что бэкенд действительно переотправляет действие, на
которое не пришёл ack. Вся конструкция на это рассчитана, иначе персистентный
`ShedSeen` не нужен, но своими глазами я этого не видел.

**Что остаётся фактом:** сетевой путь этой машины иногда рвёт соединения — в
тот же вечер SSH трижды не поднялся к двум разным серверам. Причина не названа,
подозрение на DNS/VPN (`reference_network_doh`).

## Модовые предметы ATM10 в продажу автоматически НЕ попадают (решено 2026-09-06)

Мод 0.1.3 умеет сверять каталог с реестром сборки и мог бы так же прислать
ВЕСЬ список доступных предметов. Сознательно не делаем: реестр ATM10 — это
тысячи позиций, из них сотни видов еды. Выпадающий список стал бы непроходимым,
а цена за выдачу у нас одна на действие — значит эндгейм-предмет уходил бы за
те же 200💎, что и хлеб.

Это вопрос БАЛАНСА, а не совместимости: какой модовый предмет уместно продавать
и почём — решает владелец. Канал для передачи каталога уже есть, добавить
позиции можно строкой в `_GIVE_ITEM_CATALOG` без релиза чего-либо.

**Разблокирует:** решение владельца, какие модовые предметы и по какой цене.

## Живые проверки, которые остались на владельце (06.09)

- **ShedColony 0.1.4** — поставить jar в сборку, перезапустить сервер: в логе
  должна появиться строка `catalog check: N of M item(s) missing from this pack`,
  а на бэкенде — запись в `shedcolony_catalog_check`. Пока мод не запускали,
  сверки нет и торгуем полным каталогом — это штатное поведение, не поломка.
  **0.1.3 ставить нельзя** и он снят с публикации: в нём сверка отменяла сама
  себя, и скрытые товары возвращались в продажу через раз.
- **Bannerlord** (с 06.09 днём) — перезапуск игры и прогон двух кнопок:
  «Призвать за стримера» (уведомление о неполной свите) и «Нанять войска»
  (элитный рекрут определяется жалованьем, а не именем юнита).
- **RimWorld** — запустить и убедиться, что появился `ModLogs
imlink_ГГГГММДД.txt`.
- **Панель 0.0.5 в Hosted Test** — пройти чек-лист `Расширение/docs/REVIEWER_WALKTHROUGH.md`:
  вход, мобильная оболочка, «Открыть все», вид блоков раскрытия шансов.

## Правило ведения

Новый пункт добавляется только вместе с причиной, условием возврата и ссылкой на
milestone. Выполненный пункт удаляется: история остаётся в Git.

- **Автопилот Bannerlord: модальные экраны игрока блокируют цикл** (12.09).
  Госрешения королевства, выборы политики и любой запрос, требующий ответа
  игрока, держат время на паузе — часовых тиков нет, автопилот выглядит
  зависшим, хотя он просто ждёт. Сейчас такое состояние не распознаётся.
  Разблокирует: научить `PollState` видеть открытый модальный экран и
  останавливаться с причиной. Найдено на прогоне за правителя — у вассала
  всплывает реже.
- **Автопилот Bannerlord: не проверены бой, осада, армия, плен** (12.09).
  Прототип их намеренно не применяет и останавливается с причиной. Осада
  отдельно упирается в барьер 2 (`GetBesiegeBehavior` закрыт для MainParty).
  Разблокирует: отдельный заход после того, как карта отработает стабильно.

- ~~**Автопилот Bannerlord: исправления по независимой проверке не запускались в игре** (12.09).~~ **Закрыто 13.09** прогоном в игре — кроме встречи с осаждающей партией, см. ниже.
  Доказаны заменителями движка и рефлексией по настоящей DLL, но не кампанией.
  Разблокирует: прогон на копии сейва — мирный город, замок снаружи у ворот и
  **встреча с осаждающей партией** (главный сценарий находки 1).
- **Автопилот Bannerlord: тестовые сейвы 12.09 могут хранить снятый флаг** (12.09).
  Прежняя версия снимала `DoNotMakeNewDecisions` и не всегда возвращала; новая
  не трогает флаг и восстановить не может — исходного значения у неё нет.
  Касается только `AUTOPILOT_TEST2` и `saveauto2`. Решено НЕ чинить: копии
  тестовые, флаг для пути автопилота не нужен, оригиналы лежат в бэкапе на D:.
- **Автопилот Bannerlord: встреча с осаждающей партией не проверена в игре** (13.09).
  Это главный сценарий независимой проверки: встреча несёт поселение, которое
  осаждает собеседник, и прежняя версия закрывала бой. Исправление доказано
  стендом, но на карте прогона осады не было. Разблокирует: сейв, где рядом идёт
  чужая осада, — подъехать к осаждающему лорду с включённым F11; ожидается
  остановка «встреча с другой партией», встреча не закрыта.
- **Автопилот Bannerlord: «посетить и побыть» сделано в коде, в игре не проверено** (13.09).
  Коммиты `ee058ae` и `4b0aef8` (исправления по независимой проверке): в
  поселении автопилот нажимает «Подождать», пересчёт раз в 6 часов, уходит
  «Перестать ждать» → выход, когда штатный AI выбрал другую цель; меню трогает
  только на активной карте; время на карте не стоит; сторож простоя. Стенд 60/0,
  контракт 81/81 на 1.4.8. Разблокирует закрытие: прогон на копии сейва (Ревиль —
  там было 89 кругов) и деревня (там была найденная проверкой поломка).
  Риск, который стенд не ловит: движок не кормит, не нанимает и не продаёт
  пленных за партию игрока, поэтому оценка «побыть в городе» может не падать и
  пребывание станет бесконечным. Теперь это видно в журнале («ДОЛГОЕ
  ПРЕБЫВАНИЕ» после пяти суток), но не лечится.
- **Автопилот Bannerlord: к замку партия может подъезжать через `castle_outside`** (13.09).
  Мод считает это меню «здесь ждать нельзя» и уходит. Если к своим замкам игрок
  попадает именно так (просить пустить у ворот), модель NPC «заехать в свой
  замок и побыть» автопилотом не повторяется. Не проверено ни кодом движка до
  конца, ни в игре. Разблокирует: прогон у своего замка.
- **Автопилот Bannerlord: бой, плен, окна, услуги поселения — разведаны, не сделаны** (13.09).
  Пути в движке и опасные места: `BannerlordAutopilot/review/unattended-play-engine-notes.md`.
  Независимая проверка 13.09 плана (оборвалась по лимитам,
  `review/REVIEW_2026-09-13/plan-review.md`, ответ `review/RESPONSE_2026-09-13.md`)
  опровергла два моих решения по умолчанию: «всегда отправлять войска» (есть
  отход, а «отправить» бывает недоступно) и «незнакомые окна — отказ» (у окон с
  одной кнопкой бывают обязательные последствия: возврат отряда, голосование о
  союзе). Остаются: выкуп из плена — отказ (не тратить золото героя); решения
  королевства — «воздержаться» (может потратить влияние, как и кнопка);
  незнакомое окно — остановка с записью. Проверка не покрыла утверждения №2 и №4
  брифа. Разблокирует: следующий кусок — по плану с поправками.
- **Автопилот Bannerlord: обслуживание партии в поселении сделано в коде, в игре не проверено** (14.09).
  Коммит `e14dcd0`: продажа обычных пленных (город), еда и найм (город и деревня)
  штатными расчётами и действиями 1.4.8, резерв и потолки трат — README, раздел
  «Обслуживание партии в поселении». Стенд 102/0, контракт 176/176, мутации: снятие
  каждой защиты (фильтр героев, остаток продавца, резерв, окно, интервал, слот
  добровольца, чтение в контракте) ловится проверками. Разблокирует закрытие P1
  итоговой проверки 13.09: прогон по сценарию из README.
  Проверка 14.09 нашла два пропуска, исправлены `ecbc951`: упавшее событие найма
  оставляло бойца бесплатным; загрузка сейва обнуляла срок прохода, и предел трат
  умножался на число загрузок. Отметки проходов теперь в сейве строкой. Попутно
  пределы найма за проход доведены до срабатывания (`8379995`). Стенд 112/0,
  контракт 177/177. Ответ — `BannerlordAutopilot/review/RESPONSE_2026-09-14.md`.
  Не проверено в игре: загрузка сейва с отметками и сейв без мода — шаги 7 и 10
  сценария README. DLL этой версии поставлена в игру 14.09 в 02:15 по просьбе
  владельца попробовать (md5 `B2152E6C…` сверен); до этого в игре стояла версия
  13.09 (контракт 46, без пребывания и обслуживания) — первый запуск 14.09 в 02:06
  шёл на ней и трижды остановился у Ревиля на «пребывание не поддерживается». Откат —
  `D:\shedlink-build\autopilot-dll-rollback\BannerlordAutopilot-20260913-8D1C0BF5.dll`,
  копия сохранений — `D:\shedlink-build\bannerlord-saves-backup-20260914`. В 04:34,
  после штатного выхода из игры, поставлена следующая — с защитой от окон
  (`5f31d82`, md5 `A9F220C0…` сверен); в игре ещё не запускалась. Откат на прогнанную
  версию — `…\autopilot-dll-rollback\BannerlordAutopilot-20260914-ecbc951-B2152E6C.dll`.
  Решено НЕ сохранять в сейв `_cannotStayUntil` («здесь ждать нельзя», сутки): денег
  не касается, после F11 или загрузки худший случай — один лишний вход и выход.
  Не сделано сознательно, по границам задачи 14.09: наёмники из таверны (NPC их
  нанимает), продажа трофеев и покупка лошадей (у NPC есть свои поведения).
  Разблокирует: журнал прогона, где партия стоит из-за этих потребностей.
- **Автопилот Bannerlord: первый долгий прогон новой версии в игре — работает; одна остановка** (14.09).
  С 02:16 до 04:15 и дальше партия сама: 10 заездов и 10 подтверждённых выходов
  (Ревиль, Устокол, Жемянь, Карбур, замок Устокол), 21 проход обслуживания, 9
  новобранцев наняты (деньги и отряд сошлись), в замке обслуживание пропущено по
  правилу, выключений нет. Не проверено этим прогоном: покупка еды (запас не опускался
  ниже цели), продажа пленных (пленных нет), резерв и потолки (у героя 1,2-1,7 млн).
  **Остановка:** в 02:50 в деревне Жемянь после «Подождать» время «Stop
  (заблокирован)», простой 161 с, снял владелец повторным F11. Причина — окно
  случайного события карты (владелец подтвердил: окно было). Хуже простоя:
  автопилот его не видел и под ним обслужил партию и нажал «Подождать». Исправлено
  в коде (`5f31d82`, в игре не проверено): под 13 окнами поверх карты автопилот не
  действует, запись о простое называет окно. Подробности —
  `BannerlordAutopilot/review/unattended-play-engine-notes.md`, «Окна». **Не сделано:
  выбор варианта события** — у вариантов последствия (деньги, отношения, мораль);
  рекомендация владельцу — сначала собрать по записям, какие события выпадают, и
  писать правила под них. Разблокирует: слово владельца. На патруле тот же приказ
  повторяется при каждом пересчёте (286 раз за 2 часа) — позиция при этом меняется,
  вреда не видно; записано, чтобы не забыть сравнить с NPC. Прогон кончился штатным
  выходом из игры в 04:28 (автосохранение, краш-дампа нет, в журнале ошибок пусто);
  журнал дня — `BannerlordAutopilot/evidence/autopilot-day-2026-09-14.log`.
  Косметика, не чинилось: в ожидании в ЗАМКЕ пишется «ОБСЛУЖИВАНИЕ «Замок …»
  (ожидание)» без действий. Меню ожидания у замка то же, что у города
  (`town_wait_menus`), а «в замке нечего» ServiceBlocked говорит только по меню
  `castle` — признак меню подменяет тип поселения. Денег не касается: еда и найм в
  замке выходят сразу. Разблокирует: следующая правка обслуживания.
- **Автопилот Bannerlord: работа Кодекса 14.09 — бой ведёт к стоянке, решения за владельцем** (14.09).
  Разбор — `BannerlordAutopilot/review/CODEX_REVIEW_2026-09-14.md`. Еда по живой цене
  (`b30568d`) верна и подтверждена игрой. Полноценный бой (`a5ddafc`) в игре не
  проверен: после боя сцена закрывается только кнопкой TAB (`BattleEndLogic.TryExit`),
  а сторож простоя в миссии молчит; атакует всегда, разговор перед боем не обработан,
  боевой код вне контракта. Ротация патруля (`8260ed5`) работает, но это уже свои
  оценки мода, README о «ни одной собственной оценке» неверен. Незакоммиченный
  автовыбор события закрывает окно так же, как интерфейс. Разблокирует: решение
  владельца — оставить бой (и довести выход из сцены, выбор бой/отход, разговор) или
  до этого вернуть «встреча с боем выключает автопилот»; нужны ли свои оценки патруля;
  выбирать ли варианты событий.
- **Автопилот Bannerlord: не нападает на бандитов и не убегает от сильных** (14.09).
  Замечено владельцем в прогоне. Движок считает «напасть или бежать» для NPC
  отдельно от часового пересчёта и для партии игрока выключает
  (`ShouldPartyCheckInitiativeBehavior`, CS 63379); автопилот этот расчёт не зовёт.
  Бегства нет — значит, догнавший враг остановит игру встречей. Предложен порядок:
  бегство → автобой → нападение (нападение без автобоя только выключит автопилот).
  Разблокирует: слово владельца начать кусок «бой».
- **Автопилот Bannerlord: в деревенском ожидании обслуживания нет** (14.09).
  Партия за околицей, пунктов торговли и найма у игрока там нет; обслуживание идёт
  при входе. У NPC еда покупается и рядом с деревней. Долгая стоянка у деревни
  останется без пополнения. Разблокирует: прогон с долгой стоянкой у деревни.
- **Автопилот Bannerlord: покупка еды партиями одного продукта, а не по единице** (14.09).
  NPC выбирает продукт на каждую единицу, видя остатки после покупки; мод
  планирует выбор по остаткам до покупки — иначе движок пишет сообщение о трате на
  каждую единицу. Цены с убыванием остатка, разнообразие еды и мораль стендом не
  моделируются. Пределы трат и `PriceSafety` — моё решение, не расчёт движка.
  Разблокирует: цены и число сообщений из прогона.
- **Автопилот Bannerlord: нет вероятностного допуска смены решения NPC** (13.09).
  У NPC после выбора лучшего решения есть допуск: при CurrentObjectiveValue ≥ 0.05
  (его пишет расчёт обхода врага) новое решение принимается с вероятностью, иначе
  NPC держит прежнее. В обычной обстановке значение 0, движок поднимает допуск до
  1 и принимает лучшее всегда — как мод. Решено НЕ повторять сейчас: редкий
  случай, а повтор добавляет случайность и новые члены контракта без
  доказанного вреда. Разблокирует: журнал прогона, где партия мечется при враге
  рядом. Отличий периода тоже два: нет сокращений NPC (Rethink, армия), а
  стоящая партия пересчитывается каждый час — своё правило.
- **Автопилот Bannerlord: пропуск осады — своё правило выбора, не NPC** (13.09).
  Итоговая проверка: при устойчивом максимуме «осада» партия раз за разом делает
  следующее мирное решение (стенд — 48 часов патруля). Это сознательная цена
  требования «игра не останавливается», описано в коде и документах как
  «штатные оценки + ограниченный набор действий + своё правило выбора».
  Разблокирует: поддержка осад и армий — отдельный большой кусок, владелец
  отложил их на потом.
- **Автопилот Bannerlord: цикл по разным поселениям не перепроверен на новой версии** (13.09).
  Третий прогон 12.09 шёл по трём городам на старой версии; прогоны 13.09 упёрлись
  в Ревиль. Разблокирует: сейв не у своего владения, либо вассал.

- **Автопилот Bannerlord: решение владельца 13.09 — «чтобы игра игралась сама», отдельный тест-мод** (13.09).
  Цель: стример отходит, партия живёт сама. **ShedLink не трогаем** — прямое
  указание владельца: автопилот тестируется отдельно. Порядок работ по стопорам:
  жизнь в своём городе → встречи и бои (автобой) → окна, требующие решения → плен.
  Осады, армии и личный бой героя — позже.
  Возражение, записанное до работы: если когда-нибудь запускать автопилот
  ВМЕСТЕ со ShedLink в эфире без стримера, зрители будут платить крустики за
  действия, пока игра стоит на паузе или окне, и возврата не будет — сигнал «мод
  онлайн» идёт из фонового потока, не из игры (`docs/TWITCH_COMPLIANCE_AFK_STREAM_2026-09-13.md`).
  Правило «фича входит — фича выходит» не применялось: отдельный тест, не
  механика платформы. Twitch-условие совместного эфира — модератор в чате.

## 21.09 — срочная оборона своих феодов: живой повтор
6c8d1fb готов локально: 50% здоровых, приоритет защиты, настоящее движение и штатный прорыв. Не установлен. Нужны установка при закрытой игре и наблюдение подхода/потерь/боя; стенд не доказывает победу. Артефакт и план: BannerlordAutopilot/review/OWN_SIEGE_DEFENSE_2026-09-21.md.

# Осадная расстановка автопилота, 21.09.2026

Исправление готово локально, см. BannerlordAutopilot/review/SIEGE_DEPLOYMENT_2026-09-21.md.
Установка отложена: владелец потребовал не закрывать игру. Разблокировка — закрытая
игра и разрешение на установку; затем живой штурм/оборона с автоматическим переходом
через расстановку. DLL/PDB в D:/shedlink-build/siege-deployment-20260921/bin.

## 21.09 — покупка без багажа: выкат и живая проверка ожидают
Реализация готова локально поверх выплат x2. Нужны согласованный выкат мода/сервера/панели и живая проверка замены, баланса, save/load. Автопилот из переданного комплекта сохранён. Подробности: docs/BANNERLORD_NO_PARTY_EQUIPMENT_2026-09-21.md.

22.09 Endorphine13: снять фактические IsEliminated клана/королевства, согласовать sync/join и исключить уничтоженные цели до поиска. Причина установки флага не доказана; production/game не менять вслепую. docs/ENDORPHINE_KINGDOM_2026-09-22.md.

22.09: диагностика kuro_gothic — создание вассала скрыто из-за пустой bannerlord_heirs (channel98319857), при этом есть один вассал Утелайм и живые дети42/28/17 лет в family_info. clan_info.is_leader=true; лимит5 не достигнут. eligible-heirs читает только таблицу наследников; UI скрывает создание при пустом ответе. Мод заполняет таблицу событием HeroComesOfAge, полного восстановления наследников в HeroStateSync нет. Точная причина отсутствия исторических строк не установлена. Нужен снимок настоящих доступных наследников с game IDs и восстановление зеркала; нельзя вставлять по именам family_info. Кода/production/game не меняли.
`nКраш12:09: NullReferenceException в расчёте поддержки мира, конкретный evaluatingFaction/null не определён. Не исправлен. docs/CRASH_PEACE_2026-09-22.md.

Отдых после боя 22.09: установка запрещена владельцем; живой прогон 12 часов ожидает отдельной установки. Сохранение таймера через загрузку сейва и отдельный маршрут к месту отдыха не добавлены.

22.09: новая формула 16000/x0.8 только локально; установка и живой прогон ожидаются.

22.09: установка формулы16000/x0.8 выполнена по новой команде владельца; остаётся живой прогон.

22.09: осадная надёжность — географическая граница (3 ближайших/100), не поиск сухопутного фронта. Установка и живой повтор не выполнены. Срочная оборона по-прежнему феодов клана, не всего государства.
