# Надёжность транспорта модов ShedLink — план и журнал (2026-09-09)

Ветка `codex/public-release-readiness`. Точка отсчёта — `a598476`
(рабочее дерево чистое на старте задачи).

Задача: довести **существующий** транспорт до состояния, пригодного для
ближайшего Twitch Review. Архитектуру не переписываем, логику между backend и
модами не переносим, цены/баланс/состав действий не трогаем.

## Ограничения инструментов

**CodeGraph недоступен.** `codegraph_status` отвечает «CodeGraph not
initialized for this project. Run 'codegraph init' first» — каталога
`.codegraph/` в этом worktree нет. Структурные вопросы («кто вызывает X»,
«что сломается») решались адресным чтением и `rg`, а не графом символов. Это
ограничение, а не выбор: связи строились руками, поэтому в колонке «оставшаяся
проверка» честно указано, что не пройдено.

**Игровые DLL и dotnet на месте** — `X:\SteamLibrary\...\Mount & Blade II
Bannerlord` доступна, `dotnet 10.0.104`. Значит мод Bannerlord собирается
локально; CI его не собирает (нужны те же DLL).

`AGENTS.md` в репозитории нет — применимые правила взяты из корневого
`CLAUDE.md` и личного.

## Состояние пунктов 2 и 3 на входе

Задание описывает их как открытые дефекты, но они были закрыты ранее в тот же
день, до постановки задачи:

* п.2 — `28589aa` «предел детей ставит сервер, а не тело запроса»;
* п.3 — `d117f13` «проверки печатают по-русски в консоли Windows».

Поэтому по ним работа другая: **перепроверить по актуальному коду и усилить до
требований задания**, которые строже сделанного (п.2 требует проверять
фактический payload В ОЧЕРЕДИ и значения 20/1/некорректное; п.3 требует
матрицу запусков, включая перенаправление вывода и заведомо испорченного
кандидата на временной копии).

## Журнал

| Пункт | Статус | Evidence | Тесты | Commit | Оставшаяся проверка |
|---|---|---|---|---|---|
| 1. Bannerlord: потеря обновления состояния | исправлено, локально | снимок кэшировался ДО отправки; результат отправки выбрасывался; `PostEventAsync` считал ACK'ом общий `status:ok`, хотя отказ конверта лежит в `acks[].success`; тот же нестрогий разбор — в **денежном** пути `SendDurableItemAsync`, где «успех» УДАЛЯЕТ событие из надёжной очереди | `BannerlordLink/tests/StateSyncHarness` — 8 сценариев; на прежнем поведении красные 6 из 8 (a, c, d, e1, e3, e4), зелёные b и e2 — они работали и раньше | `8018743` | прогон в игре: зеркало после обрыва сети и после загрузки другого сейва |
| 2. Bannerlord: обход лимита детей | закрыто | клиентское поле доезжало до мода через `payload = dict(data)`; прежний гейт искал строку в исходнике мода и был зелёным при живой дыре | `tests/test_bannerlord_child_limit_payload.py` — покупка через настоящий `_bannerlord_buy_action_locked` с настоящей БД, проверяется payload в `module_actions`; красный со снятым присваиванием и называет фактическое значение (`got 20` / `got 1` / `got 'много'` / `got None`) | `28589aa` + `4f11cd9` | прогон в игре: мод действительно применяет предел 5 |
| 3. Windows: проверка кандидата | закрыто | падало `UnicodeEncodeError` на первой строке до единой проверки ZIP; дефект был замаскирован моими прогонами с `PYTHONIOENCODING=utf-8` | матрица прогонов 09.09: (1) обычный `python` без `-X utf8` → exit 0, кириллица видна; (2) stdout в файл → exit 0, кириллица читается, stderr пуст; (3) в pipe → работает; (4) заведомо испорченный кандидат на ВРЕМЕННОЙ копии → exit 1 и три названные причины, включая совпадение метки кэша; (5) отсутствующий архив → exit 2. Боевой архив `513ff646…` не изменился | `d117f13` | — |
| 4. Пять платных действий: подтверждённый результат | проверено статически, дефектов транспорта не найдено | транспортные гарантии доказаны по коду (см. §4 ниже): исход пишется ДО ACK, повторная доставка отвечается ТЕМ ЖЕ результатом без повторного применения, потеря ACK не теряет исход | существующие: `test_action_ack_refund_order.py`, `test_rimworld_refund*.py`, `test_special_action_refund.py`, `test_module_delivery_gaps.py` — все зелёные | — | **живой прогон каждого из пяти действий в игре** — без него «ранний return без эффекта» не закрыт |
| 5. ShedColony: найти и проверить транспорт | исходники найдены, транспорт проверен статически | `D:\sheddev\shedcolony` (путь указан в `OVERVIEW.md`, `RUNBOOK.md`); 11 java-файлов, транспорт в `ShedNet.java` / `ShedActions.java` / `ShedSeen.java`. Дедуп выполненных `action_id` персистентный и переживает перезапуск мира; повтор доставки пере-ACK'ается БЕЗ повторного выполнения; запись только при успехе и ДО ACK | своих тестов не добавлено — дефектов не найдено | — | полный прогон одного платного действия на живом сервере Minecraft |
| 6. Проверки и кандидат | выполнено, кроме Hosted Test | backend suite 89/89; `lint_consistency` exit 0; `StateSyncHarness` зелёный; сборка мода 0 ошибок/0 предупреждений; фронт НЕ менялся → ZIP не пересобирался (кандидат `513ff646…` в силе) | см. выше | — | **DLL в игре устарел**: установлен `ad517d85` от 06.09, собран `aeeae79d` — фикс в игру не попал; Hosted Test не прогонялся |

**Правило заполнения.** Пункт не отмечается завершённым, если выполнена только
статическая проверка (чтение кода, поиск строки, разбор синтаксиса). Для
«готово» нужен прогон, который падал до правки и проходит после, либо живое
наблюдение в игре. Всё, что проверено лишь локально, помечается отдельно от
проверенного в игре.

## Что в пакет НЕ входит

Полная унификация модов, миграция экономики, массовое переписывание
обработчиков, механизм резервирования крустиков, поддержка отката старых
сейвов. Падение процесса между игровым эффектом и записью результата и
загрузка старого сейва — фиксируются как фактическое поведение с отдельным
предложением, без обещаний exactly-once.

---

# Пункт 1 — разбор до правки

Перепроверено по актуальному коду (не по прошлому отчёту).

**1.1. Хэш пишется в кэш ДО отправки.** `HeroStateSync.PushIfChanged`
(`BannerlordLink/src/Util/HeroStateSync.cs:52-65`):

```csharp
if (hashCache != null) hashCache[username] = hash;   // ← записали
PostStateUpdate(username, json);                     // ← только теперь шлём
return true;
```

Отправка не удалась — кэш уже утверждает, что этот снимок доставлен. Пока
состояние героя не изменится снова, `PushIfChanged` будет возвращать false, и
актуальный снимок НЕ уйдёт. Зритель видит замороженные золото/уровень/навыки.

**1.2. Результат отправки не используется вовсе.** `PostStateUpdate`
(`:67-84`) — `Task.Run` без возврата: `await PostEventAsync(...)` вызывается,
но его `bool` отбрасывается. Ошибка попадает только в лог.

**1.3. Успех определяется неверно даже там, где его смотрят.**
`BackendClient.PostEventAsync` (`BannerlordLink/src/Net/BackendClient.cs:218-221`):

```csharp
bool ok = response.Contains("\"status\":\"ok\"") || response.Contains("\"status\": \"ok\"");
```

А бэкенд (`Расширение/backend/routes/module_api.py:413`) возвращает
`{"status": "ok", "acks": [...]}` ВСЕГДА, и отказ отдельного конверта лежит
внутри: `{"id": ..., "success": false, "error": "event_not_in_manifest"}` либо
`"error": "<Тип исключения>: ..."` при падении обработчика. То есть
отклонённое событие мод считает подтверждённым. Это ровно тот случай, о
котором предупреждает задание: HTTP 200 и общий `status=ok` недостаточны.

**1.4. Нет защиты от обгона.** Отправки идут через `Task.Run` без
упорядочивания; при двух параллельных подтверждение старой могло бы затереть
состояние более новой (после того, как подтверждение вообще начнут учитывать).

**1.5. Загрузка сейва чистит кэш, но не отменяет старые отправки.**
`MainCampaignBehavior.ResyncAdoptedHeroes` (`:715-718`) делает
`_heroStateHashes.Clear()`. Сейчас это безвредно (кэш пишется синхронно), но
как только подтверждение станет асинхронным, callback от ОТМЕНЁННОЙ сессии
сможет записать хэш в кэш новой — и снимок нового сейва не уйдёт.

**Вывод.** Все пять требований задания к пункту 1 обоснованы актуальным кодом.
Правка должна быть точечной: подтверждение по конкретному конверту, кэш только
после подтверждения, поколение отправок против обгона, эпоха против старых
callback'ов, и никакого обращения к объектам движка из фонового потока (JSON
строится на главном потоке уже сейчас — это свойство сохранить).


---

# Пункт 4 — что доказано и что нет

Пять действий и то, чем платят:

| Действие | Валюта | Путь |
|---|---|---|
| Bannerlord: найм свиты (`hero.recruit_troops`) | **динары** (`Hero.Gold`), крустиков 0 | mod-bound |
| Bannerlord: мастерская (`hero.buy_workshop`) | **крустики** 2 500 | backend-only + отдельный enqueue |
| Bannerlord: караван (`hero.buy_caravan`) | **крустики** 4 000 | backend-only + отдельный enqueue |
| RimWorld: создание пешки | **крустики** `SPAWN_COST` = 200 | `_charge_and_enqueue` |
| RimWorld: покупка гена | **крустики**, прогрессивная `1000 × (куплено+1)` | `_charge_and_enqueue` |

**Доказано по коду (транспорт).**

* Исход пишется ДО ACK: `ActionPoller.cs:310` — «Store the outcome before ACK».
* Повторная доставка одного `action_id` не применяет эффект второй раз и
  отвечает ТЕМ ЖЕ результатом, включая текст отказа: `ActionPoller.cs:178-192`
  (`TryGet` → `repeat ACK success=... without re-apply`).
* Потеря ACK: исход сохранён, ответ не доставлен — пишется в лог, исход
  остаётся для повтора (`:329-330`). На стороне бэкенда повторный ACK не
  запускает второй возврат (`database.ack_action`: переход только из
  `queued`/`dispatched`, иначе `False`), а маркер `REFUNDED:` не перезаписывается.
* Залипшие действия подметают два сторожа: `dispatched` старше 10 минут →
  переочередь, `queued` старше 30 минут → возврат.
* Найм свиты не «слепой успех»: при сбое постановки события игровое золото
  возвращается (`HeroGoldCharge.Refund`) и ставится `PostFailed`.

**НЕ доказано (нужна запущенная игра).** Ни одно из пяти действий не
прогонялось в этой задаче вживую. Вопрос «возможен ли ранний return без
эффекта, который считается успехом» статически закрыть нельзя: счёт вызовов
`PostApplied`/`PostFailed` в файле — это не поведение, и задание прямо
предупреждает не выдавать такой подсчёт за надёжность. Постановку в очередь
выполнением игрового действия здесь не считаем.

# Пункт 5 — ShedColony против Bannerlord/RimWorld

Исходники: `D:\sheddev\shedcolony` (найдены по `OVERVIEW.md` и `RUNBOOK.md`,
у владельца не запрашивались).

ShedColony оказался не слабее, а местами понятнее остальных:

* `ShedSeen.java` — персистентный журнал ВЫПОЛНЕННЫХ `action_id`, LRU на 2048,
  переживает перезагрузку мира; запись **только после успешного выполнения**,
  поэтому действие, полученное но не выполненное (краш), будет повторено.
* `ShedActions.java:35` — повторная доставка отвечается `ack(true)` без
  повторного выполнения; `:49-50` — запись исхода ДО ACK; `:52` — явный отказ
  с текстом.
* `ShedNet.java` — буфер повторов только для КРИТИЧЕСКИХ одноразовых событий
  (`player.linked`/`died`), ограниченный по размеру; периодические события
  самозалечиваются следующим отчётом. Это осознанная и записанная в коде
  граница, а не упущение.

**Отличие от Bannerlord:** Bannerlord хранит в журнале ещё и ТЕКСТ отказа и
отвечает им на повторную доставку, ShedColony при повторе отвечает просто
`success=true`. Практической разницы для зрителя нет (деньги уже вернулись по
первому ответу), но у Bannerlord диагностика богаче.

**Дефектов транспорта в ShedColony не найдено** — правок не вносил.

# Нерешённые границы (не прятать)

1. **Падение процесса между игровым эффектом и записью журнала исходов.**
   И Bannerlord (`ActionOutcomeStore.Record`), и ShedColony (`ShedSeen.record`)
   пишут журнал ПОСЛЕ применения эффекта. Если процесс умрёт ровно между ними,
   повторная доставка применит эффект второй раз. Exactly-once здесь не
   обещается и не достигнута. Предложение отдельной задачей: писать намерение
   ДО применения (intent-запись) и снимать её после — цена в лишней записи на
   каждое действие.
2. **Загрузка старого сейва.** Зеркало состояния теперь корректно
   переустанавливается (эпоха в `StateSyncTracker`), но действия, выполненные
   в более новом сейве и записанные в журнал исходов, останутся помеченными
   выполненными и после отката к старому сейву. Полноценная поддержка отката в
   этот пакет не входит.
