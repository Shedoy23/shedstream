# Осадная расстановка — 21.09.2026

Запрос владельца: починить расстановку; игру НЕ закрывать. Установка не выполняется.

## Причина и движок

В журнале autopilot_20260921.txt: 23:34:13 подключена миссия штурма,
23:34:30 управление передано AI; между ними нет автоматического FinishDeployment.
Скриншот владельца показывает экран расстановки. Кто завершил его в этом прогоне,
журнал не устанавливает.

PollDeployment искал только BattleDeploymentMissionController.
Декомпиляция установленной TaleWorlds.MountAndBlade.dll показывает:
SiegeDeploymentMissionController — отдельный наследник DeploymentMissionController.
Поэтому штурм пропускал весь путь завершения расстановки.

Вызовы повторяют DeploymentControllerVM.DeployFormationsOfPlayer из игровой
ViewModelCollection: AutoDeployTeamUsingTeamAI(team, false),
OnPlayerTeamDeployed, AutoAssignDetachmentsForDeployment; затем FinishDeployment.
Орудия уже размещены штатным SiegeDeploymentMissionController.OnSetupTeamsOfSide.
Завершение остаётся в опросе приложения: вызов из Mission.OnTick удаляет
обработчики из обходимого списка (инцидент 16.09, LESSONS.md).

Осадный контроллер выбран явно; прочие неизвестные типы миссий не подключаются.
Ждём TeamSetupOver, героя, команду и осадный handler. Модальное окно, выключение
и конец миссии запрещают действие. При частичном исключении — выключение без повтора.

## Разбор предыдущих изменений

HEAD до работы 13de464, установленная сборка записана в 67b6ef8/13de464.
Проверены затрагиваемые границы 6c8d1fb: IsOwnedOperationBattle допускает
поддерживаемые осадные миссии, но не завершает их расстановку. ee42bca/d31fe4f
в BattleAutopilotMission меняют камеру и управление уже начавшимся боем;
не устраняют поиск неверного контроллера. Эти ветви сохранены и покрыты
прежними BattleMission-проверками. Полный аудит защиты феодов не проводился.

## Проверки и артефакт

- Красный коммит d3c38ae: две проверки FAIL, код возврата 1 на старом коде.
- После исправления BattleMission: 77 проверок, код возврата 0.
- AuditRegression: 542 ok / 0 FAIL, код возврата 0.
- ContractCheck: 493 ожидания, код возврата 0 (существующий контракт).
- Release по установленным DLL: 0 ошибок / 0 предупреждений, код возврата 0.
- DLL/PDB: D:/shedlink-build/siege-deployment-20260921/bin.
- DLL SHA256: D86A93C4D358BF84056C50E663D95B318D588C22429CF32478FF9B3D81718507.

НЕ установлено, живой штурм/оборона не проверены. После разрешённой установки
при закрытой игре проверить строки «ОСАДА: штатное авторазмещение и назначение
расчётов выполнены», «БОЙ: штатная расстановка готова; начинаем бой» и реальный
переход к бою без ручного «Готово». Размещение выбирает штатный AI игры/RBM.
