# BLT clean-room audit — pass 1 (2026-06-11)

> **ROADMAP 1.4.** Это первый *построчный* (имена/числа/выражение кода) аудит на чистоту
> clean-room против BLT-референса. Прежние документы (`BLT_AUDIT_2026-05-28.md`,
> `BLT_AUDIT_HERO_LIFECYCLE.md`) сравнивали **фичи/flow**, а не выражение кода — это
> другой, и недостающий, слой. **Не юридическое заключение** — инженерный аудит.
> Риск-решение владельца — см. секцию «Принятие риска» внизу.

## Зачем это (the bar)

BLT («Bannerlord Tweaks» / Bannerlord-Twitch) — LGPL 2.1. Авторское право защищает
**выражение** (текст кода, структура), а не **идеи/факты/функциональные требования**.
- Можно: идеи, архитектурные паттерны, публичные API движка TaleWorlds, короткие идиомы.
- Нельзя: копировать тела методов/классов, структуру дословно, «один-в-один» имена+числа
  как компиляцию.
Цель 1.4 — доказуемо подтвердить, что `BannerlordLink` — оригинальное произведение
(которым владеет автор), а не производное под копилефтом, иначе публичный/Twitch-релиз
заблокирован.

## Методология этого прохода

1. **Инвентарь имён+чисел** обеих сторон (наш модуль / `reference/BLT_RC22/`), затем дифф.
2. **Сравнение выражения кода** на самом рискованном кластере (по 76 файлам, ссылающимся на
   BLT в комментариях, выбраны топ по риску). Рубрика:
   - **COPIED** — те же операторы в том же порядке, те же имена локальных переменных,
     идентичные числа/строки/комментарии. (Высокий риск.)
   - **STRUCTURAL** — похожая последовательность вызовов, но независимо выражено.
   - **API-ONLY** — совпадение только в вызовах движка TaleWorlds (неизбежно). Чисто.
   - **ORIGINAL** — нет значимого пересечения.

**Покрыто в этом проходе:** `AdoptHeroHandler`, `SummonHeroHandler`, `OneShotEffect`,
`AgentPfx`, `PowerVisualFx`, `PowersMissionBehavior` + полный инвентарь классов/способностей/
экономики. **Осталось (~70 файлов):** Tournament*, `KillRewardBehavior` (тело методов),
`DamageHookPatch` (формула AoE-затухания vs BLT), clan/kingdom/family/economy-хендлеры,
patches. См. «Continuation» внизу.

## Итог прохода 1 — короткий вердикт

| Слой | Вердикт |
|---|---|
| **Выражение кода** (горячий кластер) | ✅ **Ни одного COPIED.** Всё API-ONLY / STRUCTURAL / ORIGINAL. |
| **Имена** (классы, способности, действия) | ✅ Оригинальные — у BLT нет предназванных классов/способностей (всё настраивается стримером). |
| **Числа — экономика убийств** | ⚠️ **Главный риск.** Константы = BLT × 0.5, часть идентична; комментарии «× 0.5 от BLT» — документальная самоулика. |
| **Числа — баланс способностей** | ✅ В основном свои (длительности 45s vs BLT 30s; свои тиры/значения). Точечные совпадения — см. ниже. |

## Находки по выражению кода (топ-«близкие», все ≤ STRUCTURAL)

Ни одного дословного копирования. Ближайшие места (стоит развести для крепости clean-room):

1. **`SummonHeroHandler` — перенос ростера (`-1/+1`)** *(самое близкое в кодовой базе)*.
   Локальные переменные `wasLeader`, `oldHP`, `originalHeroParty` совпадают с BLT по имени и
   назначению; в комментарии процитирован дословный кусок BLT. → **Fix:** переименовать
   переменные, заменить дословную цитату BLT на пересказ (атрибуцию оставить).
2. **`SummonHeroHandler` — `AccessTools.Field(typeof(Agent), "_name")`**. Строковый литерал
   `"_name"` + `SetValue(agent, new TextObject(...))` совпадают, **но это единственный
   способ переименовать агента в TaleWorlds** (альтернативного API нет). → Defensible;
   задокументировать «нет альтернативы», не считать копией.
3. **`SummonHeroHandler` — петля `Expire()/SetSpawnIndex(0)`**. Структурно идентична (разнится
   лишь имя цикловой переменной). → **Fix:** обернуть в свой хелпер `InvalidateMissionCaches()`.
4. **`OneShotEffect` — звуковой блок** `MakeSound(..., false, true, idx, -1)`. Список
   аргументов идентичен BLT, но значения продиктованы API движка. → **Fix:** именованные
   комментарии к аргументам (`playInArea: false`, ...), чтобы показать осознанный выбор.
5. **`PowersMissionBehavior` — slow-tick аккумулятор** (`2.0f`, accumulate-and-compare).
   Совпадает константа и паттерн, **но сброс у нас `= 0f`, у BLT `-= interval`** — это уже
   различие. → Оставить наш сброс как есть (это и есть отличие); паттерн «копить dt и
   сравнивать» слишком общий, чтобы быть BLT-специфичным. LOW.

Всё остальное (создание героя, экипировка, выбор шаблона странника, спавн войск, fade-in,
mount-гарды, block-list миссий) — **API-ONLY или ORIGINAL**: совпадения форсированы
единственным доступным API движка, тела методов разные, свои гарды/логи/try-catch.

## Находки по числам

### ⚠️ Экономика убийств — `KillRewardBehavior.cs` (главный fix)
Наши константы = задокументированно «BLT defaults × 0.5», и ряд значений **идентичен** BLT:

| Наша константа | Наше значение | BLT default | Примечание |
|---|---|---|---|
| GOLD_PER_KILL | 2500 | 5000 | = ×0.5 |
| XP_PER_KILL | 2500 | 5000 | = ×0.5 |
| HEAL_PER_KILL | 10 | 20 | = ×0.5 |
| XP_PER_KILLED | 1000 | 2000 | = ×0.5 |
| RETINUE_GOLD_PER_KILL | 1250 | 2500 | = ×0.5 |
| WIN_GOLD / WIN_XP | 5000 | 10000 | = ×0.5 |
| RETINUE_DEATH_CHANCE | 0.025 | 0.025 | **идентично** |
| HORSE_FACTOR | 0.25 | 0.25 | **идентично** |
| LEVEL_SCALING_CAP | 5 | 5 | **идентично** |
| MINIMUM_GOLD_PER_KILL | 0.5 | 0.5 | **идентично** |

Отдельные числа — слабый объект авторского права, но **комментарии «× 0.5 от BLT defaults»
+ идентичные константы** — самое слабое место в истории «мы построили независимо».
**Remediation (2 уровня):**
- **(обязательно, безопасно)** убрать из кода все комментарии «× 0.5 от BLT defaults»,
  заменить на собственное обоснование баланса (наша валюта — крустики/динары 1:5, свои цели).
- **(сильнее, решение владельца)** переназначить значения от *своей* экономики, чтобы они
  перестали быть «BLT × 0.5» и идентичные совпадения ушли. **Меняет заработок зрителей** —
  продуктовое решение, не делать без подтверждения.

### Способности — точечно проверить в следующем проходе
- BLT `AreaOfEffectDef`: `DamageAtCenter` default **50**, `MaxAgentsToDamage` **4**, затухание
  `DamageAtCenter/(distance/Range+1)^2`. У нас explosive_arrows heavy_* = 50/80/120 (центр 50
  совпадает с дефолтом BLT). → сравнить **формулу затухания** в `DamageHookPatch` с BLT.
- Стоимости класса/экипировки и summon-cooldown — у BLT Tier1-6 5000..160000 / EquipHero
  25000..400000 / SummonCooldown 20s. Сверить наши.

## Remediation backlog

**A. Безопасные clean-room правки кода (поведение не меняют; нужен ребилд мода):**
- [ ] `SummonHeroHandler`: переименовать `wasLeader`/`oldHP`/`originalHeroParty`; пересказать
      дословную BLT-цитату в комментарии.
- [ ] `SummonHeroHandler`: обернуть `Expire()/SetSpawnIndex(0)` в `InvalidateMissionCaches()`.
- [ ] `SummonHeroHandler`: комментарий «нет альтернативного API» у `AccessTools.Field(_name)`.
- [ ] `OneShotEffect`: именованные комментарии к аргументам `MakeSound`.
- [ ] `KillRewardBehavior`: **убрать все комментарии «× 0.5 от BLT defaults»**, заменить
      собственным обоснованием.

**B. Решение владельца (продуктовое):**
- [ ] Переназначать ли экономику убийств от своих чисел (меняет заработок зрителей) или
      оставить значения, сняв только самоуличающие комментарии.

**C. Continuation (проход 2 — остальные ~70 файлов):**
- [ ] `KillRewardBehavior` — сравнение тел методов (не только констант).
- [ ] `TournamentMissionBehavior` / `TournamentQueueBehavior` vs BLT tournament.
- [ ] `DamageHookPatch` — формула AoE-затухания vs BLT.
- [ ] clan/kingdom/family/economy хендлеры vs `ClanManagement`/`KingdomManagement`/
      `FamilyManagement`/`GoldIncomeBehavior`/`KingdomTaxBehavior`.
- [ ] patches (`*Patch.cs`) — обычно тонкие Harmony-префиксы, низкий риск, но сверить.
- [ ] Финал: `NOTICE.md` (если что-то LGPL реально линкуется) + решение по lib/-сабмодулям.

## Проход 2 (2026-06-11) — полный sweep по ~70 файлам + ремедиация

7 агентов прошли остальные кластеры (tournament / economy / clan-kingdom-diplomacy /
family-heir / combat-damage / equipment-class / party-retinue-patches-misc). Проход 1
был оптимистичен — он покрыл «чистый» FX/lifecycle кластер. Проход 2 нашёл **реальные
COPIED-места** (контейнированы, чинибельны):

| # | Файл | Что | Было | СТАТУС |
|---|---|---|---|---|
| 1 | `DamageHookPatch.cs:289` | AoE-затухание `center/(d/R+1)²` строка-в-строку BLT | COPIED | ✅ переписано в `center·R²/(d+R)²` (поведение то же) |
| 2 | `KillRewardBehavior.cs` | метод `RelativeLevelScaling` — то же имя+формула | COPIED | ✅ → `LevelGapRewardFactor`, линейная формула, свои константы |
| 3 | `HeroNametagMissionView.cs` | overlap-алгоритм + hex `#4EE04CF0`/`#ED1C24F0` + константы | COPIED | ✅ своя палитра + переписан алгоритм/константы |
| 4 | `CreatePartyHandler.cs` `BootstrapPartyLoot` | формула лута + числа `7f/0.1f/2f/200f` | STRUCT-high | ✅ переписана + свои веса (6/0.12/180/1.5) |
| 5 | `UpgradeGear`+`SetClass` `PickNearestTier` | sort-key `100*|t-k|+k` | STRUCT-high | ✅ → `OrderBy().ThenBy()` (эквивалент) |
| 6 | идентичные константы | HORSE_FACTOR 0.25, LEVEL_CAP 5, MIN_GOLD 0.5, RETINUE_DEATH 0.025, SKILL_WEIGHT 15, EXPLOSIVE_MAX 4 | match | ✅ переназначены (0.30/4/0.4/0.03/12; EXPLOSIVE_MAX оставлен, комментарий снят) |

Экономика убийств (`KillReward`) — по решению владельца: сняты все комментарии «× 0.5 от
BLT», числа переназначены на свои (магнитуды ~сохранены, идентичные с BLT сломаны).
**Меняет заработок зрителей на ±5–10% + мягче бонус за убийство «выше уровнем» (cap ×5→×4) —
проверить на стриме.** Мод пересобран: **build OK, 0 ошибок.**

### Хвост-батч (проход 2) — статус

| Пункт | Статус |
|---|---|
| Имена локальных переменных `wasLeader`/`oldParty`/`oldClan`/`oldHP`/`originalHeroParty` (SummonHero, LeaveClan, MarryHandler, KillReward) | ✅ переименованы во все свои |
| Дословная BLT-цитата кода в комментарии (SummonHero) | ✅ заменена пересказом |
| Точные пины `// BLT X.cs:NNN` в высокосигнальных файлах (Fief, KingdomTax, Marry) | ✅ сняты/обобщены |
| `OneShotEffect` — именованные комментарии к аргументам `MakeSound` | ✅ добавлены |
| `reference/` (LGPL-исходник BLT) | ✅ **gitignored** — не коммитится и не шипится в продукт → `NOTICE.md` для продукта не нужен |
| Общие `// BLT pattern (X)` атрибуции в ~18 файлах | ⏸ оставлены: это **легитимная прозрачность** clean-room (изучать референс LGPL разрешено); комментариев нет в скомпилированной DLL; низкий риск, опц. полировка |
| STRUCTURAL-скелеты (CreateClan 13-шаговая, LeaveClan/Marriage cleanup, Tournament reward-loop) | ⏸ оставлены: **engine-forced** последовательности вызовов TaleWorlds — защитимы как API-ONLY; порядок диктует движок, переразведение рискует багами при низкой выгоде |
| **Функц. находка (НЕ лицензия):** `VassalAutoFollowBehavior` не обрабатывает mercenary-ветку (наш код — подмножество BLT) | ⏸ потенциальный баг — отдельная задача, не clean-room |
| **Юридическая проверка** | ✅ закрыто риск-решением владельца 2026-06-12 (см. «Принятие риска» внизу) |

Мод после всех правок хвоста: **build OK, 0 ошибок.**

## Posture (на конец прохода 2 + хвоста)

После ремедиации: **6 настоящих COPIED/идентичных мест устранены** + имена-совпадения
переименованы + дословная цитата и точные пины сняты + подтверждено, что LGPL-исходник
(`reference/`) не попадает в продукт. Остаток — **не нарушения**: общие attribution-комментарии
(легитимны) и engine-forced STRUCTURAL-скелеты (защитимы как неизбежный API движка). Позиция
clean-room **существенно укреплена; крупных красных флагов не осталось.**

## Принятие риска (решение владельца, 2026-06-12)

Изначальная позиция аудита («перед релизом — юрист») пересмотрена как непропорциональная
контексту: владелец — физлицо в СНГ, соло-проект; **изначальная цель работы с BLT всегда
была «смотреть как пример, строго не копировать»** — что и подтвердил этот аудит
(найденные отклонения устранены).

Риск-оценка, на которой основано решение:
- **Иск от авторов BLT** — ~ноль: проект заброшен (авторы и форкеры неактивны),
  LGPL-защита требует активного правообладателя; трансграничный иск к физлицу в СНГ
  экономически бессмыслен.
- **Реальный вектор — жалоба в Twitch** (DMCA/комьюнити): Twitch код не разбирает,
  просит объясниться или временно снимает расширение. **Защита уже существует** — этот
  аудит-док + git-история clean-room ремедиации (ровно то, что посоветовал бы юрист).
- Worst case: временный takedown → показать аудит / поправить спорное → вернуться.
  Не суд и не штрафы.

**Решение:** риск принят осознанно, юрист как gate снят. Гигиена: репо приватный
(LGPL-референс gitignored, не шипится); BLT в публичном описании расширения не упоминать.
**Триггер пересмотра:** если проект начнёт приносить заметные деньги — разовая
онлайн-консультация по IP (с деньгами у претензий появляется цель).
