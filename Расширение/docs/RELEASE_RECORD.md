# Release record — какой ZIP чем был

Регламент выпуска требует: «каждый загруженный и каждый Released ZIP сохранять
неизменяемым с номером, датой, commit SHA и контрольной суммой SHA-256»
(`TWITCH_UPDATE_RELEASE_PLAYBOOK.md`, этап D). Файла под эту запись до 01.08 не
существовало — правило жило в регламенте и не исполнялось ничем.

**Зачем это нужно на практике, а не для порядка.** Без записи вопрос «сломает ли
правка бэкенда тот фронт, который сейчас у зрителей» решается реконструкцией по
датам коммитов вместо сверки с тем, что реально отдаёт CDN. Один раз это уже
стоило нам ответа: 01.08 нельзя было подтвердить, что магазин RimWorld сломан у
зрителей, — подтвердил владелец по памяти, а не архив.

---

## 0.0.5 — ПЕРЕСОБРАНА 2026-09-07, в кабинет НЕ загружена

Действующий кандидат. Предыдущая сборка (`9200f6c2…`) отменена: в ней панель не
отличала «не авторизован» от нуля.

| Поле | Значение |
|---|---|
| Файл | `dist/shedlink-0.0.5.zip` (в worktree `codex-public-release`), копия `dist/releases/shedlink-0.0.5-hosted-20260907-739666b9.zip` |
| SHA-256 | `739666b937f0ba3620ae0c0256f992dca6dd17ec9cd31a95905eeb09c77c83b9` |
| MD5 | `ec40d2b1494db6b8028e1ff3dc09511e` |
| Размер | 246 348 байт, 24 файла + VERSION, 879.0 КБ до сжатия |
| Cache-bust | `?v=202609071851` в обеих оболочках |
| Исходники | ветка `codex/public-release-readiness`, коммит `3391139` |

**Что изменилось против `9200f6c2…`.** Панель перестала принимать `HTTP 200` за
успех. Бэкенд, не сумев опознать зрителя, отвечает 200 с
`{"status":"unauthorized"}` без поля `points`, а фронт делал `data.points || 0`
и рисовал НОЛЬ — баланс, доход, квесты, кейсы, а бейдж роли падал в «Зритель».
07.09 так выглядела панель владельца, у которого в базе лежало 11 058 327💎.
Теперь неизвестные значения показываются прочерком, панель сначала пробует
опознаться заново молча и только потом просит нажать «Войти», а бейдж роли при
неудаче пустой, а не врущий.

**Сверка архива с деревом:** расхождений НОЛЬ сверх вставленного
`<meta name="shedlink-version">` в трёх оболочках.

**Причина того дефекта чинится на бэкенде** (миграция M122, выкачена 07.09):
связка «Twitch ID → логин» теперь живёт в базе и переживает перезапуск. До
этого каждый деплой ослеплял все открытые панели до перезагрузки страницы.

**Что делает владелец:** загрузить этот архив в кабинет вместо предыдущего и
сверить MD5. Версия ещё не подана на ревью, поэтому замена архива стоит
нескольких кликов.

---

## 0.0.5 — Hosted Test 2026-09-06 (сборка `9200f6c2…`, ОТМЕНЕНА 07.09)

Первая сборка с ночными исправлениями: в `0.0.4` их нет. Пересобрана после
того, как обновилось раскрытие шансов (см. ниже) — сумма ниже актуальная,
предыдущая (`0230ab1c…`) отменена и в кабинет НЕ загружалась.

Последняя сборка загружена в Dev Console и повторно сверена 07.09: кабинет
показал MD5 `2e891a30588a28a3f85ca87c92c50cdb` — он совпал с действующим локальным
архивом, размер в кабинете 239,32 КБ. Все поля «Сведения о версии» и четыре
картинки скопировались с прошлой версии, три allowlist-домена (`https://shedoy23.ru/`
для изображений, медиа и запросов) на месте. Версия переведена в **Тест на
сервере**: базовый URI сменился на CDN Twitch
`https://02andzqle1efhi123xhg44s4wd1ldd.ext-twitch.tv/`. **Review НЕ отправлялся** —
ждёт решения владельца.

| Поле | Значение |
|---|---|
| Исходники frontend | `b05d3ac` и текущий HEAD `a380ad7` (после `b05d3ac` frontend не менялся) |
| Архив | `dist/shedlink-0.0.5.zip`, неизменяемая копия `dist/releases/shedlink-0.0.5-hosted-20260906c-9200f6c2.zip` |
| SHA-256 | `9200f6c229c17615032f27ef1c1a27ff16fe269203b36f1997ee66fb56204ee6` |
| MD5 | `2e891a30588a28a3f85ca87c92c50cdb` — совпал с кабинетом 07.09 |
| Размер | 245 066 байт при лимите 1 МБ, 24 файла frontend + VERSION |

**Фактический статус 07.09:** `0.0.5` находится в «Тесте на сервере», текущий
архив лежит на CDN Twitch, кнопка «Отправить на проверку» доступна. Review ещё
не отправлен. Streamer Allowlist пуст, а Testing Account Allowlist заполнен;
три CSP allowlist содержат `https://shedoy23.ru/`.

**Архив пересобирался 06.09 несколько раз; в кабинете лежит действующая сборка
`9200f6c2…`.** Первая
(`490fc586…`) устарела через несколько часов: во фронт добавилась загрузка
каталога товаров ShedColony с сервера. Вторая (`80702723…`) — тем же вечером:
фронт стал слать JWT в запрос конфига, чтобы сервер понимал, чья это сборка.
Обе были сверены в кабинете по MD5 и обе отменены; действует третья. Пока
Review не отправлен, замена архива стоит трёх кликов и дешевле, чем везти
новую версию ради одного файла.

**Правка от 06.09, вечер (после внешнего обзора).** Первая починка упаковщика
оказалась неполной, и это разобрано ниже — здесь важно, что в архиве изменилось:
восстановлены HTML-комментарии в `extension.html` и `mobile.html`, которые
портила штамповка версии. Впервые сверка архива с деревом даёт НОЛЬ расхождений
сверх вставленной строки версии — прежние сборки её проходили только потому, что
проверка была слепа.

**Найдено при сверке третьей сборки — дефект упаковщика, живший три версии.**
Штамповка номера версии удаляла прежнюю метку голым `re.sub` по всему файлу и
заодно выедала упоминание `<meta name="shedlink-version">` из HTML-КОММЕНТАРИЯ:
в архивах 0.0.3–0.0.5 два шелла отличались от рабочего дерева не только
вставленной строкой. Ущерба не было — испорчен комментарий, — но правка
разметки вслепую однажды съест значимое. Хуже сам факт: **моя прежняя сверка
«архив совпадает с деревом с точностью до строки версии» этого не видела**,
потому что вырезала ту же подстроку с обеих сторон — проверка была слепа ровно
к тому дефекту, который искала. Починено: метка снимается только в `<head>` и
только со своей строки, перевод строки берётся из файла, а в саму сборку
встроен затвор. **И этого оказалось мало.** Внешний обзор в тот же вечер
показал, что починка неполная: если тег версии стоял отдельной строкой ВНУТРИ
комментария, аккуратный regex всё равно его удалял, а затвор этого не видел —
он применял ту же чистку к эталону. То есть я повторил ровно ту ошибку, про
которую сам написал урок часом раньше.

Вывод, который стоило сделать сразу: **пока функция что-то удаляет, проверять её
нечем.** Теперь она не удаляет ничего. Настоящий тег в исходнике (комментарии из
поиска исключены) останавливает сборку с объяснением «убери метку из дерева» —
в рабочем дереве её быть и не должно, метку ставит сборка. Затвор вырезает
вставленное ПО ПОЗИЦИИ и сравнивает с оригиналом буква в букву, без всякой
нормализации. Показано в двух состояниях: тег отдельной строкой в комментарии —
комментарий цел и сборка идёт; настоящий тег в дереве — сборка падает и
называет причину. Тот же файл прежним regex портился.

Заодно архив пишется во временный файл: падение больше не оставляет на месте
ZIP огрызок в 22 байта, который можно по ошибке залить.

**Что нового относительно 0.0.4:**
- кнопки «Лечить» и «Воскресить» подняты под строку статуса RimWorld;
- цены всех четырёх модулей приходят с сервера, во фронте только запасные;
- двойной клик не глотает второй призыв, кулдаун призыва показывается для
  каждой стороны отдельно;
- 33 кода отказа Bannerlord получили человеческие фразы;
- кнопка «Открыть все» в кейсах;
- списки товаров ShedColony (выдать / на склад / закрепить в запасе) панель
  берёт с сервера — снять испортившийся товар теперь можно деплоем, а не
  подачей новой версии; список у каждого стримера свой, поэтому запрос
  конфига идёт с JWT;
- **раскрытие шансов приведено в соответствие с кодом**: у ежечасной раздачи
  свои веса (69/25/6/0.1), и до этой правки панель показывала для неё чужие
  70/25/4/1. Это compliance-текст §5.3, и держит его теперь
  `backend/tests/test_odds_disclosure.py`.

**Проверено при сборке:** 24 файла архива побайтно совпадают с рабочим деревом
(html — с точностью до строки версии), те же файлы совпадают с тем, что отдаёт
прод по HTTPS, посторонних файлов нет, cache-bust одинаков в обеих оболочках,
версия видна в шапке.

**Проверено после переезда на CDN** (именно это ломается при смене хоста —
фронт перестаёт быть на одном домене с бэкендом): прод отдаёт
`access-control-allow-origin: https://02andzqle1efhi123xhg44s4wd1ldd.ext-twitch.tv`
и `allow-credentials: true`, а все четыре серверных конфига цен
(`/api/core/config`, `/api/shedcolony/config`, `/api/bannerlord/config`,
`/api/rimworld/config`) отвечают 200 с этим Origin. Без этого версия 0.0.5
показала бы запасные цены из фронта и молчаливые отказы.

**Дальше руками владельца:**
1. Открыть расширение на своём канале в режиме Hosted Test и пройти чек-лист
   `Расширение/docs/REVIEWER_WALKTHROUGH.md` — руками остаются вход, мобильная
   оболочка, «Открыть все» и вид блоков раскрытия шансов; игровые сценарии
   закрываются свежим постстрим-триажом.
2. Вписать Testing instructions для ревьюера: канал, окно доступности игры по
   UTC, что именно будет запущено, запасной контакт. Эти данные знает только
   владелец.
3. Сказать «отправляй» — Submit for Review делается только с явного согласия.

---

## 0.0.4 — Hosted Test 2026-09-05

Сначала по указанию владельца обновлён frontend для https://shedoy23.ru/dev,
затем собран и загружен новый ZIP. В Dev Console подтверждён статус
«Тест на сервере»; Review ещё не отправлялся. На момент проверки 0.0.3 тоже
находилась в Hosted Test; прежние записи о Review ниже — история, а не текущий статус.

| Поле | Значение |
|---|---|
| Исходники | рабочая копия HEAD `654b84b793bc7abbbfa498fda9f17ca8a3827d9a` с незакоммиченными изменениями frontend |
| Архив | `dist/releases/shedlink-0.0.4-hosted-20260905-709d381c.zip` |
| SHA-256 | `709d381c88c5660bc2577db8d18c9549b939deb4a5f39ae5668b9799e99c85bc` |
| MD5 в Twitch и локально | `b47ec60e553163d0e0a0f73743386aa0` |
| Размер | 239739 байт, 24 файла frontend + VERSION |
| Свидетельства | `dist/release-evidence/twitch-0.0.4-20260905/verification.json`, рядом diff исходников |

Проверки: 22 загружаемых файла /frontend совпадают по HTTPS с локальными;
/dev превью загрузило профиль и данные. JS lint пройден. Общий backend-прогон:
68/69, единственный сбой — разные cache-bust метки; после синхронизации
меток `test_frontend_module_lifecycle.py` прошёл 15/15. Содержимое ZIP сверено
с исходниками с учётом добавленной метки версии HTML, CRC исправны.
Backend совпадал с сервером, не выкатывался и не перезапускался.
Резервная копия frontend: `/root/shedlink-frontend-before-dev-20260905T061143Z.tar.gz`.
Живые игровые сценарии через Twitch Hosted Test пока не проверены.

---

## 0.0.1 — Released 2026-07-28 — ⚠️ АРХИВ УТРАЧЕН

| Поле | Значение |
|---|---|
| Twitch version | 0.0.1 |
| Git commit | **не зафиксирован** |
| ZIP path | **не сохранён** |
| SHA-256 | **неизвестна** |
| Built at UTC | неизвестно |
| Статус | Released 28.07, публично у зрителей |

Пробел обнаружен 30.07. Тега подачи нет, архива в `dist/` нет.
**Закрывается одним действием владельца:** скачать released `0.0.1` из Dev
Console в `dist/`, дописать сюда SHA-256 и предполагаемый commit. Отложено:
`DEFERRED.md` §C0-decies.

---

## 0.0.3 — ПОДАНА НА РЕВЬЮ 2026-09-02, архив ПЕРЕСОБРАН в тот же день

> ⚠️ **ПОД НОМЕРОМ 0.0.3 СУЩЕСТВУЮТ ДВА РАЗНЫХ АРХИВА.** Владелец перезаливает
> второй поверх первого. Это ровно тот класс, на котором мы горели с jar-ом
> ShedColony: номер один, содержимое разное, и по имени файла не отличить.
> Поэтому обе суммы записаны здесь, и вопрос «что именно смотрел ревьюер»
> остаётся отвечаемым.
>
> | Сборка | SHA-256 | Что в ней |
> |---|---|---|
> | **№1**, залита ~09:00 | `3B5E4366…` (commit `b7b7edc`) | канат 1×1, но правила в модалке схлопывались от опроса раз в 2 секунды; стартовые экраны трёх мини-игр в разнобой |
> | **№2**, пересобрана ~13:00 — **актуальная** | `25899878C2662AD0A8FF1D947D59345CC6943FE27BF473B4B7D7775E62A107DB` (commit `89602c4`) | то же плюс: свёрнутые блоки живут в оболочке и переживают опрос, форма стартового экрана одинакова у крестиков, каната и дуэлей |
>
> Полные суммы и неизменяемые копии — в `dist/releases/`:
> `shedlink-0.0.3-submitted-20260902-3B5E4366.zip` (№1) и
> `shedlink-0.0.3-submitted-20260902b-25899878.zip` (№2).
>
> **Правило на будущее:** если пересборка случается ПОСЛЕ отправки на ревью,
> первым делом сюда добавляется строка с новой суммой — до того, как файл уедет
> в кабинет. Иначе через неделю никто не скажет, какую версию завернули.

## 0.0.3 — ПОДАНА НА РЕВЬЮ 2026-09-02

**Это ВТОРАЯ сборка под номером 0.0.3.** Первая (01.09) держалась на расхождении
оболочек — кубики и кейсы резались только из мобильной — и была отозвана в тот же
день по решению владельца («отдельную версию для мобилки не хочу»), её архивы
удалены с диска. В кабинет Twitch ни одна из них не загружалась, поэтому номер
переиспользован, а не сожжён. Ниже — состав того, что реально надо грузить.

| Поле | Значение |
|---|---|
| Twitch version | 0.0.3 |
| Git commit | `89602c4d8c23a2930f1d90c0d14357e82fc542ac` (сборка №2) |
| ZIP path | `dist/shedlink-0.0.3.zip` |
| SHA-256 | **актуальная сборка №2:** `25899878C2662AD0A8FF1D947D59345CC6943FE27BF473B4B7D7775E62A107DB` (сборка №1 — `3B5E4366…`, см. врезку выше) |
| Размер | 233.0 КБ в архиве, **0.84 МБ** распакованным — под лимитом правила 3.2 (1 МБ) |
| Файлов | 25 (24 файла фронта + `VERSION`) |
| Статус | **загружена владельцем и отправлена на Review 2026-09-02.** Ждём вердикт. Владелец: «заебало ждать твич» — ответа на письмо от 24.08 так и не было, подали не дожидаясь |
| Неизменяемая копия | `dist/releases/shedlink-0.0.3-submitted-20260902b-25899878.zip` (обе сборки сохранены) |

**Проверено ВНУТРИ архива, а не в рабочей копии:** `tugofwar.js` на месте,
`dice.js` отсутствует; в обеих оболочках есть карточка каната и блок официальных
правил конкурсов, кубиков нет ни в одной. Оболочки совпадают по составу действий
и скриптов — отдельной версии для мобильного вида НЕТ.

**Что изменилось против поданной 0.0.2**

| Изменение | Зачем |
|---|---|
| **Кубики заменены «Канатом»** — дуэль 1×1 с очередью подбора, ELO, сезоном и призами топ-3 | правило 3.5: в кубиках случайность неустранима. В канате её нет ни одной, а исход целиком в руках двоих — сезонный приз стоит на той же почве, что у крестиков и дуэлей |
| Кубики заморожены и на бэкенде: `play-vs-bot` отвечает отказом, `dice` убран из очереди матчей | кнопки нет, но адрес остался бы открыт — класс дыры с атрибутами (27.07) |
| Официальные правила конкурсов в обеих оболочках | App Store Review 5.3.2 — отдельное основание для отказа, которое нам ещё не предъявляли |
| Турнир без случайного трофея; «🎁 Случайный товар» убран целиком | наше заявление «единственная случайность — тир кейса» иначе неверно; заголовок читался как лутбокс |
| Слово «рандом» снято с надписей; автоход крестиков стал детерминированным | правило 3.5 про то, как поверхность читается; «игра на навык» должна быть правдой в коде |
| Панель настроек: предупреждение об уходе с Twitch, ссылка подписана как место модерации озвучки, убрана заглушка «скоро» | правила 4.6.2 и 7.2 |

**Что НЕ вошло и почему.** Кейсы остались как есть: их случайность (тир дропа)
раскрыта в интерфейсе, а вырезать их значило бы оставить квесты, серии и просмотр
без потребителя. Переделка «случайность → выбор» (Scope B) не делалась — владелец
решил сначала пробовать пройти с тем, что есть; спека лежит готовая.

**Отдельно к письму ревьюеру:** в прошлом стоит фраза «the only random element is
which tier drops». Она неверна — случайны также выбор навыка и выбор супруги, обе
за внутриигровое золото. Оговорить явно
(`docs/audits/TWITCH_REJECTION_SURFACE_2026-09-01.md` §1).

**CSP-домены в кабинете (сверено с содержимым архива 2026-09-02).** Во вкладке
Capabilities во всех трёх списках стоит `https://shedoy23.ru/` — картинки
(`img-src`), медиа (`media-src`) и запросы (`connect-src`). Этого достаточно и
ничего лишнего: разбор ВСЕХ ссылок внутри поданного архива даёт ровно пять
хостов — `shedoy23.ru` (10 упоминаний), `api.twitch.tv`, `extension-files.twitch.tv`,
`supervisor.ext-twitch.tv`, `www.twitch.tv` — плюс `www.w3.org`, и это пространство
имён SVG, а не сетевой запрос. Твичевые хосты разрешены по умолчанию, ссылка на
сам Twitch разрешена правилом 4.6.

Проверка воспроизводима одной командой (перечисляет хосты внутри ZIP):

```bash
python -c "import zipfile,re,collections;z=zipfile.ZipFile('dist/shedlink-0.0.3.zip');c=collections.Counter(h for i in z.infolist() if i.filename.endswith(('.js','.html','.css')) for h in re.findall(r'https?://([a-z0-9.*-]+)', z.read(i.filename).decode('utf-8','replace')));print(sorted(c.items()))"
```

Зачем это записано: без домена картинок в белом списке питомцы у ревьюера
превращаются в битые иконки, и расширение выглядит сломанным — при том что код
исправен. Проверять надо было ДО подачи, а не после отказа.

**Не забыть при подаче:** вкладка **Access → Streamer Allowlist**. Это второе
требование из отказа 24.08, без него завернут независимо от содержимого; список
после одобрения меняется только через новое ревью — вносить всех сразу.

---

## 0.0.2 — IN REVIEW с 2026-08-16, ждём вердикта Twitch

| Поле | Значение |
|---|---|
| Twitch version | 0.0.2 |
| Git commit | `5767bff29a6d6ec4816d441c5d4b2a2fdaa78c16`, тег `submit/0.0.2` |
| ZIP path | `dist/shedlink-0.0.2.zip` |
| SHA-256 | `E5A2B41143D2CCBE874B61AA5FC8056644BB00037EC174C491090981095B06EF` |
| Built at UTC | 2026-08-05 12:40 |
| Размер | 229 054 байта (223.7 КБ), 22 файла + `VERSION` |
| cache-bust | `202608051239`, одинаков в `extension.html` и `mobile.html`, совпадает с выкаченным на наш сервер |
| Статус | отправлена в Twitch Review 2026-08-16; ожидаем решение |
| Неизменяемая копия | `dist/releases/shedlink-0.0.2-submitted-20260805-E5A2B411.zip` |

---

### 🔒 ЗАМОРОЗКА до вердикта ревьюера

**СНЯТА 2026-09-05.** Review так и не отправлялся: в Dev Console и `0.0.3`, и
`0.0.4` числятся в Hosted Test, вердикта ждать не от кого. Заморозка держалась
по инерции формулировки «подана на ревью», из-за чего `deploy.ps1` блокировал
выкат фронта на прод — флаг `$FrontendReviewOpen` снят в тот же день.

**Взвести обратно в день, когда версия действительно уйдёт на Review** — вместе
с флагом в `deploy.ps1`. Ниже — как это работает и почему.

**Механика (писалась под 0.0.2, номер архива менялся на 0.0.3 — `3B5E4366…`).

С момента загрузки этого архива в кабинет и до ответа Twitch **фронт на прод не
деплоим**. Причина механическая: `deploy.ps1 -Frontend` проставляет НОВЫЙ
cache-bust в оба шелла, и рабочее дерево немедленно перестаёт соответствовать
поданному архиву. Тогда вопрос «то же самое ревьюер видит или уже другое»
теряет ответ — а именно ради ответа на него этот файл и существует.

**Что продолжаем делать:** Manager, backend и игровые моды не входят в Twitch
ZIP и могут развиваться своим циклом. Backend-деплои обязаны оставаться обратно
совместимыми одновременно с публичной `0.0.1` и поданной `0.0.2` и не менять
смысл проверяемого reviewer flow. Файлы Twitch Extension, cache-bust и
зафиксированный ZIP не трогаем до вердикта.

**Если правка фронта всё-таки понадобится** (ревьюер вернул с замечанием):
править → деплоить → пересобирать архив → загружать НОВЫЙ в кабинет → сюда
новую строку с новой SHA-256. Старый архив не удалять: он остаётся ответом на
вопрос, что показывали в прошлый раз.

**Неизменяемая копия поданного архива:**
`dist/releases/shedlink-0.0.2-submitted-20260805-E5A2B411.zip` — отдельным
именем потому, что `pack-extension.py` пишет всегда в `dist/shedlink-0.0.2.zip`
и следующая же пересборка затёрла бы поданный файл.

### Сверка заморозки 2026-08-16 (проверено командами, не на слово)

| Что проверял | Чем | Результат |
|---|---|---|
| Поданный архив не подменён | `sha256sum` неизменяемой копии | `e5a2b411…95b06ef` — совпал с таблицей выше |
| Фронт не поехал после подачи | `git diff submit/0.0.2 HEAD -- Расширение/frontend` | пусто |
| cache-bust не разъехался | `grep viewer.js?v=` в обоих шеллах, на теге и сейчас | `202608051239` во всех четырёх местах |

**Бэкенд с 05.08 по 16.08 изменился** (M109–M112, `module_liveness`, Manager
pairing, оба игровых манифеста, `routes/viewer.py`, `routes/streamer.py`), то
есть ревьюер смотрит фронт от 5 августа против бэкенда от 16-го. Изменения
`viewer.py` разобраны: правка `_module_on_air` карточку первого шага чинит, а
не ломает — до неё отметка «мод на связи» жила в памяти процесса и после
каждого деплоя обнулялась. Правка `streamer.py` добавляет `return_to` для
Manager pairing, дефолтный редирект на дашборд сохранён — шаг 8 Verification
(модерация озвучки) не задет.

**Открытый вопрос — этапы C/E/F и шаги Verification 1–8.** Прогонял ли их
владелец руками перед подачей, не записано нигде, а «не записано» здесь читаем
как «не прогонял». Ответ вписать сюда строкой с датой в обе стороны. Подробнее
— `CHANGELOG_0.0.2.md`, врезки от 16.08.

---

**Порядок сборки, чтобы хеш не разъезжался:** сначала деплой на наш сервер
(`deploy.ps1 -Frontend` сам проставляет новый cache-bust в оба шелла), и только
ПОТОМ упаковка. Иначе архив уедет с одним номером кэша, рабочее дерево с другим,
и запись «архив совпадает с деревом» перестанет быть правдой. Промежуточная
сборка того же дня (`E848DAC7…`, bust `202608051115`) вышла именно так и
замещена этой.

**Что изменилось против сборки 04.08** (SHA была
`01D86E56…B1B801B4B62EB`, 225 569 байт, cache-bust `202608040521`):
переключение игры больше не затирает название расширения в шапке — «ShedLink»
остаётся на месте, активная интеграция ушла подписью строкой ниже, а рядом с
названием печатается версия сборки. Источник версии один — метка
`<meta name="shedlink-version">`, которую ставит упаковщик; своей копии номера
во фронте нет, иначе шапка начнёт врать. Незапакованный фронт (наш сервер,
страница `/dev`) метки не имеет и честно пишет `dev`.

Попутно в той же правке: `deploy.ps1` сбрасывал кэш только для `*.js`, поэтому
правка `viewer.css` не доехала бы до зрителя вообще (её `?v=` стоял вручную с
15 июня) — regex расширен на `*.css`. И `pack-extension.py` падал с
`UnicodeEncodeError` при запуске из PowerShell до того, как что-либо собрать
(консоль cp1251, вывод скрипта русский) — печать переведена в UTF-8 явно.

**Проверено при сборке (не на слово скрипту):**
- в архиве нет `.env`, `.db`, `.py`, source maps, секретов и токенов;
- `overlay.html` НЕ входит — это виджет для OBS, а не Twitch video-overlay
  (правило: регламент, пункт 4-тер; упаковщик клал его до 01.08 вопреки правилу);
- строка версии `<meta name="shedlink-version" content="0.0.2">` стоит в обоих
  шеллах;
- все 22 файла архива побайтово совпадают с рабочим деревом (html — с точностью
  до вставленной строки версии). На 05.08 дерево ЧИСТОЕ и всё запушено, поэтому
  происхождение сборки — обычный commit SHA, без оговорки про несохранённые
  правки, как было 04.08;
- в архиве есть место под карточку первого шага (`id="first-step-card"`), а её
  содержимое приходит с бэкенда — он уже выкачен и отдаёт `first_step`.

**Заметка о воспроизводимости:** побайтово пересобрать этот же ZIP нельзя —
файл `VERSION` содержит время сборки с точностью до минуты, и хеш меняется.
Сверять надо не хеш пересборки, а содержимое против commit SHA, как выше.

**Что в этой версии для зрителя** — `CHANGELOG_0.0.2.md`. Коротко: магазин
RimWorld оживает (у зрителей сломана бо́льшая часть, подтверждено владельцем),
причины отказа доходят текстом, цены на кнопках настоящие, четыре необратимых
действия спрашивают подтверждение.

Добавлено 05.08, уже после того как changelog был написан: в шапке видно
название расширения и версию сборки, активная игра — подписью под ним; на
главной вкладке появилась карточка первого шага — приглашение завести
персонажа для новичка и переход «Твой герой · имя» для остальных. Если игра у
стримера не запущена, карточка честно об этом пишет, но остаётся рабочей:
расширение проходимо целиком и без запущенной игры (это важно ревьюеру).
Причина появления карточки — разбор воронки, `DEFERRED.md`. Пункты дописаны
в `CHANGELOG_0.0.2.md` отдельным блоком — ревьюер читает именно его.

⚠️ **Сборка ≠ готовность к подаче.** Собранный и проверенный ZIP закрывает
ровно этап D регламента — и ничего больше. На 02.08 **не начаты этапы C
(Local Test), E (Hosted Test на самом CDN-артефакте) и F (независимый аудит
версии + diff к Released)**, а трек B проверен только на деньгах: эффекты в
игре не смотрел никто. Полный список — `STATUS.md`, раздел «Поправка 02.08».

**Почему эта строка здесь:** 01.08 наличие готового архива было прочитано как
«осталось только поиграть». Архив — это один этап из девяти, и запись о сборке
не должна создавать впечатление финиша.
