# Roadmap: стабилизация → уверенный уровень

> **Last updated: 2026-06-11.** Личный план действий. Что было построено неверно,
> что обязательно сделать в ближайшие дни/недели, и по каким критериям понять,
> что проект вышел на уверенный уровень. Обзор проекта — [OVERVIEW.md](OVERVIEW.md).
>
> Формат: каждый пункт — чекбокс + зачем + примерное время + **готовый промпт
> для Клода** (рабочий инструмент — сессии с Клодом, промпты можно копировать
> как есть).
>
> **Этот документ — план стабилизации (в основном закрыт).** То, что мы решили
> отложить по ходу обычной работы, живёт отдельно: [DEFERRED.md](DEFERRED.md) —
> журнал «отложили сознательно, вот почему, вот что разблокирует», включая
> раздел «решено НЕ делать», чтобы не возвращаться к закрытым вопросам.

---

## 0. Диагноз: что изначально построено неверно

Корневой паттерн один: **«сначала строю — ограничения проверяю потом»**.
Он уже оплачен четыре раза:

| # | Ошибка | Доказательство | Цена |
|---|---|---|---|
| 1 | Казино построено → правила Twitch проверены потом | вырезано целиком (Phase 1 compliance) | недели работы в корзину |
| 2 | Модель скопирована с BLT → лицензия (LGPL) «потом» | clean-room аудит до сих пор **блокер публичного релиза** | проект не может выйти на Twitch, пока не закрыт |
| 3 | «Работает у меня» → секьюрити потом | аудит 2026-04-30: **13 CRITICAL**; JWT доделывался в июне | повезло, что нашли сами |
| 4 | Один канал → мультитенант потом | `m1_multitenant.py` — ретрофит всей схемы | большая миграция + хвост channel_id-багов |

Структурные следствия «наития» (не фатально, но тормозит каждый день):

- **Тестов де-факто нет** — проверка = «задеплоить в прод посреди стрима и
  смотреть логи». Каждый баг находят зрители. Падающий тест (exit 2) никого
  не останавливал — сигнал мёртв.
- **viewer.js ~7k строк** — один файл, две игры, без сборки. Каждая правка —
  риск регресса в соседней игре (FLICKER-FIX дошёл до «v7»).
- **Площадь фич растёт бесконтрольно** — питомцы, дуэли, кубики, TTT, TTS,
  кейсы, гильдии… Каждая фича — вечная аренда (её надо чинить при каждом
  изменении), а данных «что реально юзают» нет.
- **Прод = дев** — один VDS, деплой во время стрима, offsite-бэкапа нет,
  игровой ПК = стримерский ПК, версия движка прибита руками (инцидент 1.4.5).

Что построено **правильно** (не трогать, поддерживать): Module API и
мультитенантное ядро, outbox+рефанды, data-driven баланс (ребаланс миграцией
без пересборки мода), защитные Harmony-финализаторы, deploy.ps1 + pre-commit
lint, CONTEXT-доки, привычка «прогнать агентов по логам после стрима».

---

## 1. P0 — обязательно в ближайшие ДНИ

### [x] 1.1 Offsite-бэкап БД ✅ 2026-06-11
Самый дешёвый катастрофический риск: умер VDS — умерло всё (поинты зрителей,
герои, экономика).
**Сделано:**
- ПК ежедневно тянет последний `.zst`-снапшот с прода (`scripts/pull-backup.ps1`),
  хранит 14 копий в `%USERPROFILE%\shedstream-backups\`. Планировщик Windows
  `shedstream-db-backup-pull` (daily 13:00 + StartWhenAvailable). Проверено
  end-to-end (LastResult 0x0).
- **Бонус-находка:** прод-cron бэкапа был **мёртв 10 дней** (деплой ~1 июня
  снёс флаг +x → `Permission denied`). Починено: `chmod +x` + cron переведён
  на `/bin/bash …` (иммунно к потере +x). In-process `backup_loop.py` всё это
  время прикрывал, так что данные не были голыми.
- **Остаётся (→ 2.2):** реальный restore-drill — развернуть бэкап и убедиться,
  что он рабочий.

### [x] 1.2 Тесты-гейт ✅ 2026-06-11
`test_multi_tenant_isolation.py` (на деле мега-сьют на 2577 строк) падал (exit 2)
из-за протухших игровых тестов — сигнал был мёртв неделями.
**Сделано:**
- Тесты разделены: **CRITICAL** (tenant/security/инфра — 11 тестов, 107 ассертов,
  все зелёные) vs **GAME** (мини-игры/соц — чаще ломаются при реворках, НЕ гейтят).
- Флаг `--critical` гоняет только инварианты безопасности; `deploy.ps1` **прогоняет
  его перед каждым backend-пушем — красный = деплой прерывается** (проверено dry-run).
- Каждый тест обёрнут в try/except → один краш не обрывает прогон, видны ВСЕ поломки.
- Починены 2 tictactoe-теста (были под 3×3, игра давно 4×4; ELO_START 1100→1000).
  guilds/voting/pets/bannerlord оказались зелёными (их не было видно за обрывом).
- **Бонус:** прогон вскрыл, что весь игровой раздел сгнил со Sprint 5.24 — «мёртвый сигнал».

### [x] 1.2b Переписать 2 протухших dice-теста (СДЕЛАНО 2026-06-11)
`test_dice_game_logic` / `test_dice_match_flow` падали `ImportError` — dice
переработан в v2 (BO3, `_resolve_winner`→state-based `_resolve_pvp_winner`).
Переписаны под v2: фазовая машина (`_new_dice_state`/`_compute_phase`/
`_recompute_totals`), 3-раундовый прогон, cross-channel + cross-player
isolation, lexicon-scrub. Полный набор: **PASSED 1335, FAILED 0**.

### [x] 1.3 Стейджинг на том же VDS (СДЕЛАНО 2026-06-11)
Чтобы перестать тестировать на живых зрителях. Реализовано on-demand (не ест
ресурсы постоянно — VDS всего 961MB RAM / 1 CPU).

**Что есть:**
- `/root/twitch-extension-staging/` — отдельная папка, своя `viewers.db` (копия
  прод-снапшота), свой `.env`. Делит venv с продом (не дублируем зависимости).
- supervisor-сервис `twitchbot-staging` на порту **8001**, `autostart=false`
  (по умолчанию ВЫКЛЮЧЕН — включаешь только на тест, выключаешь после).
- **Защита от конфликта с прод-ботом:** env-флаг `DISABLE_LIVE_INTEGRATIONS=1`
  в `main.py` отключает ровно исходящие-в-Twitch задачи (IRC-чат-бот, auto-
  message, EventSub-регистрацию, PubSub-drain, OAuth-refresh). БД-циклы
  (points/drops/matchmaking/voting) работают — их и тестируем. Без флага прод
  работает в полном режиме на 8000.
- Перед добавлением 1GB swap на VDS (swappiness=10) — RAM-страховка, чтобы
  staging на 1 ядре не уронил прод по OOM во время теста.

**Как пользоваться:**
```
# залить текущий код в staging и поднять его (прогонит critical-тесты сам):
pwsh scripts/deploy.ps1 -Staging
# проверить вручную:  curl http://127.0.0.1:8001/docs  (через ssh на VDS)
# выключить после теста (важно — это on-demand):
ssh root@31.130.132.224 'supervisorctl stop twitchbot-staging'
```
**Проверено 2026-06-11:** staging поднялся на :8001, лог показал
`🧪 STAGING MODE: live Twitch integrations DISABLED`, миграции прошли, прод
(uptime 1 день) не задет. 107 critical-тестов зелёные.

**Новое правило с этого дня: на прод — только после стрима, не во время.**
(Исключение — hotfix падающего прода.)

### [x] 1.4 BLT clean-room аудит (ЗАКРЫТ 2026-06-12: проходы 1–2 + ремедиация + риск-решение)
Это **потолок проекта**: пока не закрыт — публичный релиз невозможен. Полный отчёт:
`docs/BLT_CLEANROOM_AUDIT_2026-06-11.md`.

**Сделано 2026-06-11:**
- Проход 1 (горячий кластер) + проход 2 (полный sweep ~70 файлов, 7 агентов).
- Найдено и **устранено 6 настоящих COPIED/идентичных мест** (AoE-формула,
  RelativeLevelScaling, hex-цвета+overlap нейметегов, loot-формула, sort-key,
  CLASS_SKILL_WEIGHT). Мод пересобран — build OK.
- Экономика убийств обезличена от «BLT × 0.5» (числа переназначены — нужна
  проверка баланса на стриме).

**Хвост тоже сделан (2026-06-11):**
- ✅ Совпадающие имена локальных переменных переименованы (Summon/LeaveClan/Marry/KillReward).
- ✅ Дословная BLT-цитата убрана; точные пины `// BLT X.cs:NNN` сняты в Fief/KingdomTax/Marry;
  `OneShotEffect` — именованные комментарии к аргументам.
- ✅ `reference/` **gitignored** → LGPL-исходник не шипится → `NOTICE.md` для продукта не нужен.
- Build OK после всех правок.

**Остаётся (НЕ нарушения — низкий риск):**
- Общие `// BLT pattern (X)` атрибуции (~18 файлов) — легитимная прозрачность, опц. полировка.
- STRUCTURAL-скелеты — engine-forced API движка (защитимы как API-ONLY).
- (Не lice) баг: `VassalAutoFollowBehavior` без mercenary-ветки — отдельная задача.

**Юрист как gate — СНЯТ риск-решением владельца 2026-06-12** (непропорционален: физлицо
в СНГ, BLT заброшен, реальный вектор — жалоба в Twitch, защита — аудит-док + git-история;
триггер пересмотра — заметные деньги). Детали: `docs/BLT_CLEANROOM_AUDIT_2026-06-11.md`
§«Принятие риска». Изначальная цель работы с BLT всегда была «пример, не копия» —
аудит это подтвердил.

Итог: **крупных красных флагов clean-room не осталось — 1.4 закрыт полностью.**

### [x] 1.5 Compliance-проход по живым механикам (СДЕЛАНО 2026-06-11)
Прогон `/twitch-compliance` по правилам Twitch (получены 2026-06-11). Отчёт:
`docs/TWITCH_COMPLIANCE_2026-06-11.md`.

**Итог:** из 6 механик **5 ✅ COMPLIANT** (кейсы/лутбоксы §5.3, дуэли — ставка уже
убрана, промокоды, валюты без cash-out, sub/boosty-гейтинг без регресса).
**1 риск устранён:** ставки на турнир (wager-на-исход + запрещённый лексикон) →
переделаны в **no-loss прогноз** (бесплатно, верный прогноз → бонус 500⦷ из
платформы, лексикон вычищен). Попутно: UI призов дуэлей выровнен под реальные
выплаты. Тесты 1335/0, build/syntax OK.

**Задеплоено на прод 2026-06-11** (проверено: прогноз-UI отдаётся, ставок нет).
Косметика на потом: убрать неиспользуемые `min_bet`/`max_bet` из API. Юрист-gate
снят (см. 1.4 «Принятие риска») — 1.5 закрыт полностью.

**Отложенный deep-dive (когда будет время):** разобраться, что РЕАЛЬНО можно для
prediction/stake-механик в стороннем расширении на *своей* валюте. Проверено: запрет
wagering (§6.2.6) — только для Bits; Twitch Predictions разрешён как first-party фича,
не прецедент для нас. No-loss выбран как перестраховка, не как «нельзя иначе». Возможно
вернуть настоящие ставки (вар. B) или отдать нативным Twitch Predictions (вар. C). Детали:
`docs/TWITCH_COMPLIANCE_2026-06-11.md` §«Уточнение… ОТЛОЖЕНО».

---

## 2. P1 — ближайшие 2–4 НЕДЕЛИ

### [x] 2.1 Закрыть хвост секьюрити — RimWorld auth (ЗАКРЫТО 2026-06-12, строгий режим LIVE)
Было: 13 RimWorld mod-ingest эндпоинтов БЕЗ авторизации (любой по URL мог стереть
пешки / подменить цены / инжектить пешки). Закрыто module-token'ом по образцу
Bannerlord (`verify_module_token`). **На проде `RIMWORLD_REQUIRE_TOKEN=1` (строгий).
Проверено end-to-end: без токена → 401, мод с валидным токеном → 200, мусор → 401.**
Попутно: починен 500 стримерского дашборда (f-string скобки) + добавлены кнопки
копирования module-токенов в дашборд.

**Сделано (код + тесты):**
- Backend (`rimworld.py`): зависимость `rimworld_mod_auth` на всех 13 mod-эндпоинтах.
  **SOFT-режим по умолчанию** (нет токена → пускает + логирует, НЕ ломает); строгий —
  по env `RIMWORLD_REQUIRE_TOKEN=1` (тогда 401). Проверено: soft/strict/cross-module.
- RimLink C# (`PriceSettings`/`RimLinkMod`/`RimLinkAPI`): поле `ModuleToken` в настройках
  мода + заголовок `Authorization: Bearer` на всех запросах. Build OK.

**Rollout (ручные шаги, по порядку — ничего не ломается):**
1. Задеплоить backend (soft-режим) — безопасно, RimWorld продолжает работать.
2. Обновить RimLink-мод в игре (новый `RimLink.dll` из `RimLink/Assemblies/`).
3. Стримеру: получить rimworld-токен (`GET /api/streamer/module-token?module_id=rimworld`
   из дашборда, или admin-эндпоинт) → вставить в настройки мода (вкладка «Основные»).
4. Убедиться по логам, что мод шлёт валидный токен (нет SOFT-warning) → выставить
   `RIMWORLD_REQUIRE_TOKEN=1` в прод `.env` + рестарт → строгий режим, дыра закрыта.

**Rollout выполнен полностью** (деплой → новый DLL в игре → токен в моде → флип флага).
**Осталось (отдельной сессией):** быстрый ре-аудит «что ещё из 13 CRITICAL не закрыто».

### [x] 2.2 Тест восстановления из бэкапа (СДЕЛАНО 2026-06-13)
Бэкап, из которого никогда не восстанавливались — это не бэкап, а надежда.
**Дрилл выполнен:** offsite-копия (`viewers.daily.2026-06-11.db.zst` с ПК) →
распакована → `integrity_check=ok` → подняла ТЕКУЩИЙ бэкенд на staging (миграции
прошли на реальных данных) → данные целы (1 канал / 42 зрителя / 2.46M💎) → API
отдаёт. Пошаговая процедура (3 сценария + репетиция через staging) — в
`docs/RESTORE_PLAYBOOK.md`.

**Находка (1.1-follow-up):** offsite-pull (задача `shedstream-db-backup-pull`) иногда
убивается `0xC000013A` (ПК спал посреди scp) → offsite отставал на день. Скрипт сам
рабочий (ручной прогон тянет ок); подтянул 06-12 вручную. Хардинг задачи (wake-таймер /
fail-fast scp) — в backlog.

### [x] 2.3 Метрики использования фич (СДЕЛАНО 2026-06-13)
Счётчик `feature_usage` (per channel/feature/day, m72) + best-effort
`record_feature_use()`; инструментирован bannerlord buy_action (единая точка).
Отчёт: `GET /api/streamer/feature-usage?days=N` → топ-10/анти-топ-10. Бэкафилл
истории из `module_actions`.

**Первый дата-инсайт (бэкафилл 16.05–10.06):** ядро = power.activate (5788) +
player.spawn (5086) + detachment-команды. А «династия/экономика»
(create_kingdom=**1**, create_clan=17, marry=16, sell_workshop/caravan=7–11) —
почти не используется. Прямые кандидаты на заморозку (сократит viewer.js +
площадь BLT-аудита). RimWorld/платформа — инструментировать тем же хелпером (1 строка).

### [x] 2.4 Распил viewer.js (ЗАВЕРШЁН 2026-06-13 — все фичи вынесены, инфра оставлена осознанно)
> **Итог:** viewer.js 7141 → 2453 строки (−66%). RimWorld → `viewer-rimworld.js` (368),
> Bannerlord фичи → `viewer-bannerlord.js` (~4500). В core осталась Bannerlord-инфра
> («мост»: диспетчер/polling/хелперы/`switchIntegrationModule`) — оставлена осознанно как
> integration-слой (вынос = большой риск, малая выгода; план — ниже, если когда-то захочется).
> ⚠️ Проверка техническая (node-check/единственность/прод-200/ноль мохибейка); финальное
> подтверждение — живой in-game клик на стриме.
> **Контекст:** фронт уже распилен на 14 feature-файлов (pawn/shop/xenotype/
> cases/duels/…). `viewer.js` (6952→6866) — это «ядро + Bannerlord (~4.8k) +
> остаток RimWorld». Куски ниже вытягивают RimWorld из ядра в `viewer-rimworld.js`
> (грузится последним — безопасный сток). Главная масса впереди — Bannerlord.

7k строк, две игры, один файл. Цель: `viewer-core.js` (платформа) +
`viewer-bannerlord.js` + `viewer-rimworld.js`. Без изменения поведения, по куску.

**Кусок 1 (2026-06-13):** RimWorld online-status + identity-запрос → новый
`viewer-rimworld.js` (грузится после viewer.js в обоих шеллах). node --check ок,
функции определены 1 раз, подтверждено в браузере (статус-бейдж + гейт работают).
Попутно: deploy.ps1 кэш-бастит все `viewer*.js`; main.py авто-регистрирует роуты
`viewer-*.js` (поймали 404 — фронт раздаётся хардкод-словарём, не статик-маунтом).

**Кусок 2 (2026-06-13):** RimWorld events-блок (магазин ивентов) → тот же
`viewer-rimworld.js`: loadRimworldEvents/onEventsSearch/renderEvents/buyEvent +
EVENT_COOLDOWN_MS + event-tick. node --check ок, 1 определение, вызовы в viewer.js
(234/457/1039) все рантайм. Задеплоено на прод (verified: код в новом файле, из
viewer.js убран, ?v= бампнут, 200).

**Кусок 3 (2026-06-13):** RimWorld черты/гены (removeMyTrait/startPawnRefresh/
buyTrait/buyGene/removeMyGene +_pawnRefreshTimer) → `viewer-rimworld.js`. Вызовы
из shop.js/pawn.js на рантайм-кликах. node --check ок, 1 определение по всему
фронту, задеплоено (verified: 5 в новом файле, 0 в viewer.js, ?v= бампнут, 200).

**Кусок 4 (2026-06-13):** остаток RimWorld → `viewer-rimworld.js`: parseRimColor
(rich-text), SKILL_LABELS_RU+localizeSkill, showCreatePawnModal+createPawn. node --check
ок, 1 определение по фронту, задеплоено. **После этого в viewer.js НЕТ ни одного
`/api/rimworld/` вызова и ни одной RimWorld-функции — RimWorld полностью вынесен**
в `viewer-rimworld.js` (~370 строк). viewer.js: 7141 → 6810.

**Bannerlord-распил начат (2026-06-13).** Новый `viewer-bannerlord.js` (грузится после
viewer.js, до viewer-rimworld.js в обоих шеллах; glob-роут + кэш-баст уже покрывают его).
- **Кусок 1 (tournament):** loadBannerlordTournament + render + predict + TOURNAMENT_*
  → `viewer-bannerlord.js`. Полностью изолирован. node --check ок, 200, verified.
  viewer.js: 6810 → 6659.
- **Кусок 2 (progression):** loadBannerlordProgression + BNR_SKILLS/BNR_SKILL_LABELS_RU/
  BNR_ATTRIBUTES/BNR_ATTR_*/BNR_FOCUS_TIER_COSTS/BNR_ATTRIBUTE_COST → bannerlord.js.
  ⚠️ `BNR_SKILL_LABELS_RU` теперь шарится кросс-файлово: hero-card (viewer.js, пока
  в core) ссылается на неё в рантайме. Безопасно (top-level const видна всем скриптам,
  hero рендерится после загрузки bannerlord.js). Станет внутрифайловой, когда hero-card
  переедет. viewer.js: 6659 → 6505.
- **Куски 3-6 (2026-06-13, через `_autotest/movechunk.py` — байт-точный перенос по
  line-range с assert границ, ноль ручного воспроизведения кириллицы):**
  - **3 workshops:** _BNR_WORKSHOP_TYPES + loadBannerlordWorkshops + _renderBuyWorkshopInline.
  - **4 fiefs/caravans/inheritance:** loadBannerlordFiefs/Caravans(+inline)/CaravanRescues/Inheritance.
  - **5 vassals/party/diplomacy/ransom:** +_BNR_POLICIES +inline-рендеры. ⚠️ вклиненный
    core-хелпер `_smartInnerHTML` корректно ОСТАВЛЕН в core.
  - **6 daily/heirs/family:** loadBannerlordDaily(+claim)/Heirs/Family(+_fam* handlers).
  Все: self-contained, core `_bnr*`/`_smartInnerHTML` forward, callers рантайм
  (loadBannerlordHero / _startBannerlordPolling). viewer.js: 6505 → 4796.
- **Кусок 7 (battle status):** loadBannerlordBattleStatus + _renderBannerlordDetachmentPanel
  + _renderBannerlordStance + _renderBannerlordBattleBanner. Ноль общих констант. Стейт
  `_bannerlordBattle` остался в core; hero-card + polling зовут перенесённые рендеры
  кросс-файлово (рантайм). viewer.js: 4796 → 4583. (Анализ безопасности — субагентом.)
- **Кусок 8 (shop):** loadBannerlordShop. Форвард-зовёт core/cluster-2 рендеры
  (renderBannerlordRandomEquipHtml/CurrencyHtml/_bind*). viewer.js: 4583 → 4512.
- **Кусок 9 (classes/active-powers/summon):** loadBannerlordClasses + class-picker +
  active-powers + summon. ⚠️ Общие символы ОСТАВЛЕНЫ в core (форвард): `_bannerlordClassesCache`
  (стейт, читают currency+hero-card), `BNR_POWER_LABELS`/`PRICES` (юзает и buffs).
  viewer.js: 4512 → 4313.
- **Кусок 10 (buffs HUD):** loadBannerlordBuffs + _renderBannerlordBuffs. Форвард
  `BNR_POWER_LABELS` (core). viewer.js: 4313 → **4268**.
  Каждый кусок 7-10: байт-точный mover + node-check + единственность + ноль мохибейка
  + прод-200 + кросс-файл-callers проверены. viewer.js: 7141 → 4268 (−40%).
- **Куски 11-15 (2026-06-13):** 11 status-badge · 12 random-equip/retinue/currency
  (`_formatBigGold/Price` оставлены в core) · 13 dynasty A (clan-upgrades/forge/achievements/
  gender/profile/family/clan+kingdom mgmt — `_bnrConfirm`/`_bnrShowSimpleModal` оставлены) ·
  14 part-B create-kingdom/join/clan inline + bind-random-equip (`_stopBannerlordPolling` оставлен) ·
  **15 hero-card (loadBannerlordHero, 611 строк, гигант)** — все саб-рендеры уже в bannerlord.js,
  `BNR_SKILL_LABELS_RU` кросс-ссылка из чанка 2 РЕЗОЛВИЛАСЬ (внутрифайл). Каждый verified на проде.
  **viewer.js: 7141 → 2453 (−66%). bannerlord.js: ~3000 строк.**

### Статус 2.4: ВСЕ ФИЧИ Bannerlord+RimWorld вынесены. Осталась только инфра.
В viewer.js остаётся **связная Bannerlord-инфраструктура** (1060-1824, ~760 строк):
state-глобалы, `_bnr*` хелперы (price/afford/settlement/balances), `BNR_REFUSE_REASON_RU`+refund,
`_bnrConfirm`/`_bnrShowSimpleModal`, `HERO_GOLD_TIER_COSTS`, `BNR_POWER_LABELS/PRICES`,
`_formatBigGold/Price`, cooldown-система (`_bnrInflight`/`_bnrCd*`/`_bnrActionCdTick`/`_bnrAffordTick`
+ **parse-time 1s тикер**), диспетчер `_bannerlordBuyAction`, polling (`_start/_stopBannerlordPolling`),
`switchIntegrationModule` (роутер — настоящий core). **Это «мост», не фичи.** Дробить дальше —
самый рискованный шаг (parse-time тикер, двунаправленная связь со всеми фичами, guard для polling
при выносе из switchIntegrationModule). **Рекомендация: оставить как integration-слой ИЛИ выносить
отдельной свежей сессией** (`viewer-bannerlord-core.js`), не в хвосте марафона. Диспетчер не зовётся
из core (только из bannerlord.js-фич) — он выносим первым, если решим продолжить.

**Осталось вынести (Bannerlord) — это ЗАПУТАННЫЕ куски, делать осторожно в свежей сессии
(не в хвосте марафона). Анализ зависимостей — субагентом, см. ниже:**
- **shop** (loadBannerlordShop) — зовёт currency/random-equip рендеры; двигать вместе или
  кросс-файл-ссылкой.
- **currency / random-equip** — ⚠️ вклинена retinue-секция (часть hero-card) + `_formatBigGold`
  шарится с hero-card. Исключать аккуратно (как `_smartInnerHTML`).
- **classes / active-powers / summon  ↔  buffs** — ⚠️ ШАРЯТ `BNR_POWER_LABELS`. Двигать
  ОБА вместе (и `BNR_POWER_LABELS` с ними), иначе кросс-файл-ссылка. Не по-отдельности.
- **gender / profile / clan-mgmt / kingdom-mgmt + модалки** — ⚠️ `_bnrConfirm`/`_bnrShowSimpleModal`
  вклинены и нужны hero-card → оставить в core (как `_smartInnerHTML`).
- **hero-card (loadBannerlordHero, ~600 строк)** — САМАЯ рискованная: центральный оркестратор,
  зовёт ~8 саб-рендеров + dialog-хелперы. `BNR_SKILL_LABELS_RU` станет внутрифайловой.
- **В самом конце** — диспетчер `_bannerlordBuyAction` + cooldown-тикер + polling
  (`switchIntegrationModule` зовёт `_startBannerlordPolling` — решить guard `typeof===function`).

**В CORE ОСТАЮТСЯ навсегда/пока:** `_bnr*` хелперы, `_smartInnerHTML`, `_bnrConfirm`/
`_bnrShowSimpleModal`, `_formatBigGold/Price`, state-глобалы, `switchIntegrationModule`.

Инструмент `_autotest/movechunk.py` (НЕ в гите, untracked) — переиспользуемый байт-точный
mover (job-JSON: src/dst/start/end/assert_first/last/after/header/pointer). Всегда
перепроверять текущие номера строк грепом перед каждым переносом (сдвигаются).

**В CORE остаются (НЕ выносить — зовутся при переключении модуля / диспетчер):**
`switchIntegrationModule`, `_startBannerlordPolling`/`_stopBannerlordPolling`,
`_bannerlordBuyAction` (диспетчер всех действий), state-глобалы `_bannerlord*` (1060-1073),
shared price-хелперы `_bnr*` (пока — много потребителей).

**Следующие куски (по карте, порядок):** progression-модалка (NB: `BNR_SKILL_LABELS_RU`
шарится с hero-card — решить: оставить в core или тащить) → buffs/battle HUD → shop +
`_bnr*` хелперы → hero-card (большая) → семья/кланы/королевства/дипломатия → в самом конце
диспетчер. Гейт цены учитывать (create_kingdom = 5M). Главная масса ещё впереди (~4.6k).
**При распиле сразу выносить/замораживать анти-топ-фичи** (метрики 2.3), а не тащить
в новые модули. Учитывать гейт цены (create_kingdom мало юзают из-за 5M, не «не нужно»).

### [x] 2.5 Распил мега-функции buy_action (ЗАКРЫТО 2026-06-14 — тест + фаза-распил)
⚠️ **КОРРЕКЦИЯ исходной формулировки** (была «дисптач-таблица»): анализ показал, что
`_bannerlord_buy_action_locked` (~1410 строк) — это НЕ if/elif-диспетчер, а **security-критичный
атомный конвейер** («касса»: проверь цену → списать под `BEGIN IMMEDIATE` → enqueue моду).
~40 действий вкрапляют кусочки в РАЗНЫЕ фазы; часть нарочно стоит так, чтобы чинить прошлые
race-баги двойного списания (задокументировано в комментах). Наивный «дисптач-дикт» их вернёт.

**Сделано:** характеризующий тест `tests/test_bannerlord_buy_action.py` — касса БЫЛА без покрытия;
тест фиксит charge/idempotency/insufficient/refuse, зелёный (26/26), изолирован (temp-БД, set_db).
Ремень перед рефактором.
**Рефактор СДЕЛАН** (фаза-распил, НЕ дисптач): `_bannerlord_buy_action_locked` **1411 → 72-строчный
оркестратор**; фазы вынесены в `_prepare_action`/`_enforce_price`/`_resolve_perks`/`_resolve_cooldown`/
`_charge_execute_enqueue`/`_post_commit_side_effects` — **байт-в-байт** (diff = только границы функций +
return-связки + оркестратор; тела фаз unchanged context). Zero behavior change: load-bearing порядок
(idempotency+dedup внутри BEGIN IMMEDIATE, атомный `UPDATE … WHERE points>=?`, set_cooldown после commit,
equip_trophy separate-conn) сохранён. Гейты: тест 26/26 + compileall зелёные; diff-ревью подтвердил
verbatim. Оркестратор диспетчит по `isinstance(result, dict)` (refusal=dict, success=не-dict).

### [x] 2.6 Мониторинг прода (ЗАКРЫТО 2026-06-14)
- `/health` (GET/HEAD, no-auth, проверяет БД `SELECT 1` → 200/503; main.py) задеплоен —
  `https://shedoy23.ru/health` → 200 `{"status":"ok","db":"ok"}`.
- Внешний монитор **UptimeRobot** пингует /health каждые 5 мин (независим от VDS → ловит
  полное падение), алерт на **почту** (Telegram у них ушёл в платный тариф). Подтверждено
  в логах: `GET /health … 200 OK` от IP UptimeRobot (84.17.46.76). О падении узнаёшь от
  системы, а не от зрителей.
```
Промпт: «Сделай health-мониторинг: каждые 5 минут проверка /health прода,
при падении — уведомление мне (telegram-бот или почта). Хостить можно на
этом ПК или на самом VDS через cron.»
```

### [x] 2.7 Документ «процедура обновления движка» (СДЕЛАНО 2026-06-14)
Инцидент 1.4.5 (Steam auto-update убил сейв) повторится при следующем патче.
→ `docs/ENGINE_UPDATE_PLAYBOOK.md`: золотое правило (не обновляйся сразу), Steam
auto-update off, бэкап → ждать совместимых модов → обновить + пересобрать `deploy.ps1 -Mod`
→ тест на КОПИИ сейва → потом стрим; откат + кросс-ссылки на dev-env/compat-matrix/testing/restore.
```
Промпт: «Напиши docs/ENGINE_UPDATE_PLAYBOOK.md: что делать при выходе новой
версии Bannerlord — пошагово (не обновлять сразу, ждать моды, бэкап сейва,
пересборка, прогон на staging-сейве).»
```

---

## 3. Постоянные правила (вставить в CLAUDE.md и требовать исполнения)

Это главный рычаг: ты управляешь проектом через промпты и CLAUDE.md.
Наитие генерирует «что строить», но не видит «что проверить ДО» и «что
чинить ПОСЛЕ» — эти две вещи должны быть правилами.

1. **Новая механика = сначала проверка, потом код.** Перед любой новой фичей:
   (а) compliance-чек по правилам Twitch, (б) лицензионный чек если идея из
   BLT, (в) короткая спека на согласование. Код — только после «ок».
2. **«Готово» = доказательство.** Не «сделал», а вывод тестов / строка лога /
   скрин. Красный тест = стоп.
3. **На прод — после стрима.** Во время стрима только hotfix падающего прода.
4. **Фича входит — фича выходит.** Перед добавлением новой механики назвать
   одну старую из анти-топа метрик, которую замораживаем/убираем.
5. **Раз в месяц — аудит-сессия вместо фич** (секьюрити / дохлый код /
   техдолг / зависимости). Апрельский аудит окупился лучше любой фичи.
6. **Каждое значимое изменение — в CONTEXT-док.** (Уже делается — не бросать.)

---

## 4. Чего НЕ делать сейчас

- **Не добавлять новые крупные механики**, пока не закрыты 1.4 (BLT) и 1.5
  (compliance) — каждая новая фича увеличивает объём аудита и риск переделки.
- **Не переезжать с SQLite / не переписывать на «правильный» стек.** Для
  текущего масштаба всё работает; переписывание — самый дорогой способ
  ничего не улучшить. Вернуться к вопросу, когда появится второй-третий
  реальный канал.
- **Не полировать то, что не болит** (рефакторинг ради красоты). Болит:
  тесты, compliance, бэкапы, viewer.js.

---

## 5. Критерии «уверенного уровня» (когда можно выдохнуть)

Проект на уверенном уровне, когда ВСЕ пункты — да:

- [x] BLT-аудит закрыт, по каждому пункту — переименовано/изменено/обосновано. (1.4, 2026-06-12)
- [x] Compliance-проход пройден, рискованные механики исправлены. (1.5)
- [x] CI зелёный, и красный тест физически блокирует деплой. (1.2 — deploy-gate прогоняет critical-тесты)
- [x] Есть staging; на прод во время стрима не деплоим. (1.3 + правило)
- [x] Offsite-бэкап работает + восстановление из него один раз реально прогнано. (1.1 + 2.2)
- [x] Из 13 CRITICAL аудита закрыто всё (включая RimWorld mod-ingest). (2.1)
- [x] Прод мониторится; о падении узнаёшь от бота, а не от зрителей. (2.6, UptimeRobot→/health, 2026-06-14)
- [~] По каждой фиче есть цифры использования; анти-топ заморожен. (метрики — 2.3 ✅; саму заморозку анти-топа — когда накопятся данные + по решению; не блокер стабилизации)
- [x] viewer.js распилен; правка Bannerlord-кода не трогает RimWorld. (2.4, 2026-06-13)
- [x] Правила из раздела 3 живут в CLAUDE.md и реально применяются. (пополняются по ходу)

После этого — следующий горизонт: Twitch submission, второй реальный
стример-канал (форсирующая функция для всего мультитенантного), и только
потом — новые крупные механики.

### Пре-релизная подготовка (НЕ срочно — пока в Local Test катаем свободно)
Релиз меняет правила: фронт переедет на CDN Twitch → мгновенный `scp`-хотфикс фронта
кончится (правка фронта = новая версия + ревью Twitch, дни). Бэкенд остаётся твоим и
хотфиксится всегда. Отсюда подготовка к этой реальности:

- [ ] **Тонкий фронт: балансовые числа — на бэк (дедуп).** Цены/кулдауны/лимиты сейчас
  зашиты display-копией во фронте И enforce'ятся на бэке — одно число живёт в 2-3 местах
  (напр. focus-cost `30000`: `BNR_FOCUS_TIER_COSTS` в `viewer-bannerlord.js` + `FOCUS_TIER_COSTS`
  в `bannerlord.py` + C#-мод). Бэк должен ОТДАВАТЬ эти числа (в `/my-hero` или `/config`),
  фронт — рисовать присланное, свои консты удалить. Тогда смена цены = 1 правка на бэке,
  мгновенно у всех, без релиза фронта (а у релизного фронта на CDN «замёрзшая» цена иначе
  даёт mismatch: бэк берёт новую, фронт показывает старую → «не хватает» при «хватает»).
  Делать постепенно (трогаешь цену — выноси) или одним проходом перед submission; не big-bang.
  Кандидаты: `BNR_FOCUS_TIER_COSTS` / `BNR_ATTRIBUTE_COST` / `RETINUE_TIER_DINARS` /
  `RECRUIT_PRICE_BASIC` / `EVENT_COOLDOWN_MS` / trait-gene display-цены. (Принцип — в CLAUDE.md gotchas.)
- Полезно знать: **две версии** расширения (релизная на CDN + тестовая для себя в Local Test,
  быстрый scp) — штатный update-флоу Twitch (см. `docs/extensions/life-cycle`), а не два
  отдельных расширения.

---

## 5b. Принцип развития: глубина связи с героем, а не число кнопок

**Статус: черновик, обсудить на свежую голову (записан 2026-07-23, ~04:00).**

Направление, выбранное владельцем: развивать то, ради чего проект и существует —
**у зрителя есть свой персонаж, который живёт между стримами** (обоснование и
разбор конкурентов: `Расширение/docs/MARKET_LANDSCAPE.md`). Ни Crowd Control, ни
Chaos Tricks этого не дают: у них «дёрнул рычаг → эффект», без памяти.

Формулировка владельца: «давать зрителю как можно больше возможностей
взаимодействий». Уточнение к ней — **больше возможностей ≠ больше связи**.
Двадцать кнопок, из которых понятны три, ощущаются беднее пяти понятных.
Углубляют связь не кнопки, а:

1. **Последствия, переживающие стрим.** Купил меч — действие. Женился, родил
   наследника, потерял феод в войне — история, которую пересказывают. Уже есть,
   это наша сила.
2. **Видимость своего персонажа.** Зритель должен узнавать своего на экране
   (культурные скины классов, 2026-07-21 — пример такого вложения).
3. **Возвращаемость.** Причина зайти завтра — не «могу купить ещё предмет», а
   «мой герой в плену, надо выкупить», «идёт вотум за войну, нужен мой голос».
   Незакрытые сюжеты, а не расширенный магазин.

**Критерий для новой механики:** не «даёт ли ещё одну возможность», а
**«появится ли у зрителя причина рассказать об этом или вернуться завтра»**.
Если нет — это кнопка в поддержку, а не глубина.

**Честная поправка (владелец, 2026-07-23):** не приписывать «раздутости фич» то,
что ею не является. RimWorld-механики месяцами не проверялись, потому что
владелец в это время **не играл в RimWorld** — а не потому что механик много.
При этом законы Bannerlord висели сломанными с 17 июня в игре, в которую играли
постоянно. Значит корень — **отсутствие проверки эффекта**, а не число фич.
Лечится аудитом работы (§6), а не только правилом «фича входит — фича выходит».

## 6. Структура работы (чтобы недели не уходили в пустоту)

### Пять типов сессий — называй тип в начале

Главный источник «пустоты» — сессии без рамки: начали с фичи, свернули в баг,
закончили рефакторингом, ничего не доведено. Лечится одним словом в начале:

| Тип | Когда | Стартовый промпт |
|---|---|---|
| **Фикс-сессия** | после каждого стрима, 15–30 мин | «Запусти агентов по вчерашним логам, разбери, почини найденное» |
| **Долг-сессия** | 1–2 вечера в неделю | «Берём следующий пункт из ROADMAP P0/P1: …» |
| **Фича-сессия** | только когда P0 пуст | «Хочу механику X. Сначала compliance/лицензия/спека, код после моего ок» |
| **Код-аудит** | раз в месяц | «Месяц прошёл — аудит КОДА: секьюрити / дохлый код / техдолг» |
| **Аудит работы** | раз в месяц / перед релизом | «Аудит РАБОТЫ: прогони каждую платную механику от кнопки до эффекта на боевых данных» |

Правило: **одна сессия = один тип = один результат.** Если посреди фичи
нашёлся баг — записать в список, не переключаться (кроме горящего прода).

**Почему код-аудит и аудит работы — РАЗНЫЕ типы (урок 2026-07-22).** Четыре
дорогих бага (очередь RimWorld мертва с миграции m1, магазин уходил в питомцев,
законы висели «на голосовании» вечно, счётчик покупок не откатывался) прошлые
аудиты НЕ поймали — и не могли. Код-аудит **читает код** и ищет, что в нём
неправильно: дыры, атомарность, compliance. Но эти баги в коде не видны —
каждый файл по отдельности корректен. Они видны только на живой системе:
пустая таблица на проде, порядок загрузки двух файлов, запрос в боевую базу.

Аудит работы — другая дисциплина: **проверка, а не ревью.** Берёшь каждую
платную механику и прогоняешь её целиком, глядя на НАБЛЮДАЕМЫЙ эффект (не на
ответ эндпоинта): списались очки → произошло в игре → отразилось в базе → видно
зрителю. Ровно принцип RUNBOOK «успех подтверждается внешним признаком».
Что ловит именно этот тип и никакой другой:
- механики, которые «работают» по коду, но мертвы в проде;
- баги, рождённые ПОЗЖЕ последнего код-аудита (код-аудит — фото на дату);
- расхождения кода со схемой миграции, порядком загрузки, живыми данными;
- платные действия с тихим no-op (списание без эффекта).
Перед каждой подачей в Twitch — обязателен: именно он поймал бы мёртвый
магазин RimWorld до того, как тот уехал в поданную версию.

### Ритм недели (ориентир)

- **После стрима:** фикс-сессия по логам (то, что мы уже делаем — это работает).
- **1–2 вечера:** долг-сессии по этому файлу, сверху вниз. Не выбирать
  «что интереснее» — выбирать следующий незакрытый чекбокс.
- **Фичи:** в очередь (ниже), реализация — когда P0 закрыт.
- **Раз в неделю, 5 минут:** открыть этот файл, отметить сделанное, посмотреть
  что следующее. Файл живой — устарел пункт, попроси Клода обновить.

### Как формулировать задачи (анти-пустота)

1. **Цель, а не решение.** «Зрители не понимают, почему действие не сработало»
   лучше, чем «добавь тост». Решение Клод предложит сам — и часто дешевле.
2. **Одна задача — одна сессия.** «Сделай X, и ещё Y, и заодно Z» = три
   полусделанных вещи. Большое — попроси разбить: «разбей на шаги, делаем по одному».
3. **Для крупного — сначала план.** «Покажи план/спеку, код после моего ок».
   Отменить план — бесплатно; отменить написанное — больно (казино).
4. **В конце сессии — сводка.** «Что изменилось, что где задеплоено, что мне
   сделать руками». (Теперь это правило #7 в CLAUDE.md — Клод обязан сам.)
5. **Идеи — в очередь, не в работу.** Пришла идея механики → одной строкой в
   список идей (или сюда), а не «давай сделаем сейчас». Раз в неделю-две —
   разобрать очередь и выбрать одну по данным использования.

### Чем «уверенный уровень» отличается от текущего — в одну строку

Сейчас: *что-то сломалось → узнаём от зрителей → чиним на живую.*
Цель: *ломается на staging → тесты краснеют → зрители ничего не заметили.*
Каждый пункт P0/P1 — шаг этого перехода.
