# Послебоевое знакомство и краш 22.09

## Проверка смены

Основа 5c78924/72d7cd5, после последней записи HANDOFF чужих коммитов нет.
CodeGraph не инициализирован; пользователь ранее отказался от инициализации.
Декомпиляция текущего движка использована только для чтения; незакоммиченная
папка decompile-campaignsystem не включается в изменения.

## Диалог

09:41:42 завершена миссия; 09:41:52 лог фиксирует разговор, Stop и встречу
с дезертирами. На скриншоте Сорегун благодарит за спасение и единственный
ответ — «Меня зовут EdwardVII». Native LordConversationsCampaignBehavior
регистрирует его как ally_thanks_meet; условие требует выигранный бой и
дружественного ещё незнакомого героя. Последующая реплика штатно применяет
отношения и закрывает разговор.

Реплика была известна только мирному маршруту, который исключает активный
MapEvent/Battle. Маршрут боя удерживал разговор с другим собеседником.
Теперь точная доступная ally_thanks_meet обрабатывается перед боевым маршрутом,
вместе со штатными послебоевыми освобождениями, с прежней паузой между шагами.
Не добавлен случайный выбор дипломатии, ручное закрытие встречи или награда.

## Дамп

Источник: C:/ProgramData/Mount and Blade II Bannerlord/crashes/2026-09-22_04.55.52/dump.dmp.
Время каталога — создание отчёта; игра перестала писать лог около09:46:15.
Thread37 / OS0x54d4: System.NullReferenceException, HResult80004003.
Стек: IncidentsCampaignBehaviour.<InitializeIncidents>b__25_553 →
Incident.CanIncidentBeInvoked → Campaign.Tick → MapState.OnTick.
Это не диалог Сорегун и не стек нового восстановления перековки.

Объект Incident0x24f07dcf158 имеет StringId incident_hammer_of_the_sun.
Главный отряд0x24f04427020: _currentSettlement=null.
MapState0x24f09130430: _nextIncident всё ещё указывает на этот Incident.
Нативный IL берёт CurrentSettlement в local0 и без проверки читает Name
(IL00db–00dc) для TOWN_NAME. Выбранный CharacterObject — aserai_recruit,
не null; наличие у обычного солдата HeroObject=null здесь не причина.
Последние строки автопилота: найм в Аскаре, перестать ждать, LeaveSettlement,
Finish, цель Вадар. Данные доказывают потерянный контекст города при проверке
условия события. Они не сохраняют точный момент постановки события в очередь.

Ранее HoldExitForQueuedIncident проверял очередь лишь до меню. Между проверкой
и LeaveSettlement вызывается меню wait_leave, которое может менять состояние.
Добавлена повторная проверка непосредственно в LeavePeacefulSettlement:
новое событие удерживает отряд и сохраняет отложенную цель до его обработки.
Это закрывает воспроизведённый промежуток; точный callback постановки из этого
дампа не доказан, поэтому живой повтор последнего save остаётся обязательным.
Не добавлялось глобальное подавление исключений/отключение случайных событий.

## Проверки

Диалог: старый код 562ok/1FAIL; после исправления563/0. В тесте исправлена
сама модель изменения struct-реплики: новое значение записывается обратно
в список. Повтор на старом поведении снова дал exit1.
Новая очередь внутри callback выхода: до защиты565/1, после566/0.
Итог AuditRegression566/0, ContractCheck494/494, Release0errors/0warnings.
Логи D:/shedlink-build/ally-thanks-*.log, incident-boundary-red.log.
Материалы дампа D:/shedlink-build/crash-20260922-{fault,objects,incident,party}.txt.
Два теста сначала закоммичены красными. Повтор в настоящей игре не выполнен.
