# Постстрим-триаж 2026-09-22

Методика: `Расширение/docs/POSTSTREAM_TRIAGE.md`. Разбор сделан 23.09.
Днём 22.09 параллельные смены уже разобрали два краша
(`docs/BANNERLORD_CRASH_2026-09-22.md`, `CRASH_PEACE` в ветке Codex) и
вассалов общего клана (`DEFERRED.md`). Здесь итог за весь эфир целиком и
то, что за день не смотрели: 65 минут на новой DLL после 23:00.

## Что за эфир

| | |
|---|---|
| Сессия | `2026-09-22 04:37:08 → 19:07:03 UTC` (09:37–00:07 по UTC+5) |
| Действий | 4903 (4372 `acked`, 531 `failed`) |
| Зрителей с движением крустиков | 29 |
| Потрачено / начислено | 3 939 515💎 / 3 925 513💎 |
| Запусков игры | 12, из них 4 без штатной выгрузки (см. п. 5) |

## 1. Прод не падал (ТОЧНО)

- `twitchbot` RUNNING, страницы `/ /extension.html /overlay.html /privacy.html
  /terms.html /health` → 200.
- 5xx в `out.log.1` (сутки 22.09) — 0; свежих `Traceback` в `err.log` — 0.
- Прод перезапускался во время эфира (~07:19 UTC, выкат смены Codex с
  наследниками) — факт, не находка.

## 2. Деньги сошлись (ТОЧНО)

| проверка | результат |
|---|---|
| зависшие `queued`/`dispatched` старше 10 мин | 0 |
| `failed` без маркера возврата | 0 |
| разрывы в `points_ledger` | 0 |
| `bannerlord_policy_requests` в `pending` | 0 |
| сумма возвратов vs сумма цен отказанных действий | 243 525💎 = 243 525💎 |

`REFUNDED:0 (no_price)` — все строки законны: у `join_kingdom`, `join_clan`,
`recruit_troops` и остальных в `data` лежит `price: 0` (платят динарами).

Механик «попытки есть, успехов нет» среди Bannerlord-типов нет.

## 3. 🟠 Зеркало героя `ksiandil` не обновлялось весь эфир (ТОЧНО, фикс выкачен 23.09)

`player.state_update` падал 426 раз с `UNIQUE constraint failed:
bannerlord_vassals.channel_id, vassal_clan_id`
(`_adapter.py:1302 → bannerlord_vassals.py:111`), ровно по 17–44 в час с
04:36 до 19:07 UTC. Жертва одна: `ksiandil` (`last_sync 10:10:59`, у всех
остальных — конец эфира). Механизм и фикс — `DEFERRED.md`, «22.09 — вассалы
общего клана»; там было записано 126 — это счёт посреди эфира, за сутки 426.
**Выкачено 23.09** (`routes/bannerlord_vassals.py`, откат `.rollback-20260923`).

## 4. 🔴 Бессмертие выключено — первый час показал цену (ТОЧНО)

Новая DLL (`fe40bb5`) работала 23:00:59–00:06:16. Доказательство включения:
`Harmony patch SKIP (safe-rollback): ...KillCharacterAction_ApplyInternal_Patch`
(23:01:29), `[DeathProtect] BLOCKED` — 13 до 23:00, 0 после.

За 65 минут — **11 смертей зрителей** (за 13 часов до этого — 0):
banderaslv ×2, analniyshekotyn ×5, igotpaws (старость), fikoos418, logistick1,
nikita__g345 (старость). Наследник подхватил в трёх случаях
(`AUTO-SUCCESSION OK`).

Сценарий `analniyshekotyn`, по логу:
1. 23:08 — герой 36-го уровня с легендарным оружием (перековка 3×20 000💎 за
   день) погиб: `gold=197442` → `0`. Наследник `Феан` L17 получил 5000💰 и
   состоит в ЧУЖОМ клане (`де Мерок`, Вландия) — клан зрителя `АНАЛ_КАРНАВАЛ`
   и королевство `Shed` к нему не перешли. В 23:15:25 мёртвый герой L36 снова
   прислал состояние под ником зрителя (`L36 gold=36904 clan=АНАЛ_КАРНАВАЛ`) —
   на один ник два героя; не разбиралось, ВЕРОЯТНО устаревшая ссылка в
   синхронизации.
2. 23:34 — наследник погиб, наследников нет.
3. 23:37, 23:40, 23:41, 23:44 — новый герой L5, `equipment stripped`, `0/11
   filled`, сразу призван в тот же бой за 50💎 — и погибает через 1–2 минуты.
   Четыре раза подряд.

`banderaslv` — то же: с 905 204💰 до нового героя с 1500💰, второй раз убит
через 2 минуты после создания.

Это решение владельца, риск назван заранее (`DEFERRED.md`, «бессмертие
ВЫКЛЮЧЕНО»). Новое — факты: (а) голый герой L5, призванный в идущий бой, живёт
1–2 минуты, то есть покупка призыва после смерти почти всегда впустую;
(б) динары погибшего до наследника почти не доходят (197 442 → 5 000), а
наследник из чужого клана уводит зрителя из его клана и королевства.

## 5. Крашей 4, разобраны 3 (ВЕРОЯТНО по четвёртому)

Запуски без предшествующего `unloaded`: 09:35→10:05, 11:30→12:26,
12:26→14:15, 14:15→14:26.
- 09:46 — автопилот `incident_hammer_of_the_sun` (STATUS/HANDOFF автопилота);
- 12:09 и 13:10 — одинаковый стек `DefaultDiplomacyModel.GetRelationScore` в
  суточном голосовании о мире; 13:10 доведён до причины (`leave_clan` оставлял
  клан с чужим лидером), фикс в DLL с 22:59;
- 14:15 — сессия прожила 31 секунду (последняя строка 14:15:41), причина
  неизвестна. Папка `C:\ProgramData\...\crashes` к 23.09 удалена целиком,
  дампов больше нет. Скорее всего тот же краш на загрузке испорченного сейва
  (док 13:10 предсказывал повтор на следующем игровом дне), но это вывод.

После 14:26 падений нет.

## 6. Багрепорты за 22.09

| id | автор | суть | вывод |
|---|---|---|---|
| 63 | endorphine13 | вышел из королевства — не пускает в другое | **подтверждено**: 5 отказов `already_in_kingdom 'Вландия'` 10:33–10:42. Это диагноз Codex 22.09 (флаги уничтожения); ремонт клана в DLL с 22:59 (`[clan-repair] '[BLink] Рой пчёл': снят флаг` в 23:01:55). Живьём повтор join не проверен |
| 64 | kuro_gothic | после победы герой отдаёт вещи отряду, себе только коней | не проверяемо: мод не пишет раздачу трофеев. Не разобрано |
| 65 | shedoy23 | активка не сработала из-за оружия, а таймер пошёл | **не воспроизводится**: 78 отказов `required_weapon_not_wielded`, деньги вернулись все; мод ставит кулдаун только после успеха (`ActivatePowerHandler.cs:185`), бэкенд снимает свой при отказе (`_adapter.py` `clear_cooldown`), панель опрашивает таймеры каждые 2,5 с. Видимый таймер — это отсчёт, запущенный кнопкой сразу по ответу сервера, до ответа игры; должен пропадать на следующем опросе. Если висел дольше — нужен скриншот/время |
| 66 | analniyshekotyn | перекованное оружие пропадает при смене на новое | **не установлено**. 17:42 перековал `aserai_mace_5_t4` до Legendary, 19:01 надел другую булаву в тот же слот. По коду (`EquipmentShopHandlers.cs`, `equip_owned`) снятая вещь кладётся в `hero.PartyBelongedTo.ItemRoster` С модификатором — то есть в инвентарь ТОЙ партии, где герой сейчас состоит; если это не его партия, вещь уезжает чужому. Проверить уже нечем: герой погиб в 23:08 (п. 4), зеркало инвентаря — стартовое. ВЕРОЯТНО, не доказано |

## 7. Мелочи

- `hero.detach` 12 отказов из 18: все — «уже отсоединён» (`IsDetached`),
  9 из 12 — один зритель. Деньги возвращены (10💎).
- Сверхусилие (`ed5b1b9`, порог 6700 очков) после 23:00 не сработало ни разу,
  максимум 3341,9 — крупного боя не было. Не проверено.
- Перековка «откатилась после перезагрузки» (#58, 21.09): на трёх перезапусках
  этого эфира повтора не видно, но и прямого теста (тот же предмет до и после
  загрузки) в логе нет.

## Что остаётся открытым

1. ~~Выкат бэкенда с фиксом зеркала `ksiandil`~~ — выкачен 23.09, проверить на эфире.
2. Бессмертие: владелец 23.09 оставил как есть (голый призыв приемлем; волна
   смертей — герои по ~90 лет). Записано, не в работе: наследник из чужого
   клана (п. 4) и «мёртвый герой шлёт состояние под ником» (23:15:25).
   Остальные пункты владелец велел не трогать.
3. #64 и #66 — нужны строки в логе мода (раздача трофеев; куда ушла снятая
   вещь), иначе следующий случай снова будет не доказать.
4. Краш 14:15 — дамп утрачен; при повторе сохранить папку `crashes`.
