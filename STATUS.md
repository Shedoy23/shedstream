<!-- deployment 2026-09-21 -->
21.09.2026: по команде пользователя «Выкати все» обе DLL/PDB установлены с rollback-20260921-231804 и проверкой SHA256. BannerlordLink MD5 6C8CF6DF87EA09FD26B6C5C95D420C6C, Autopilot AEA589923E5A6864F022EB08F9C4DF1F. Серверные 6 файлов магазина обновлены; twitchbot PID 2568080, public health ok/db ok, JS и обе HTML 200 с совпадающими SHA256. Backup /root/shedlink-deploy-backups/no-party-20260921-2321. Twitch CDN и живая кампания не проверены/не обновлены соответственно: CDN не обновлён, покупки и оборона в кампании ещё не проверены.

**21.09 — магазин без клана/багажа реализован ЛОКАЛЬНО.**
Купить и надеть; подтверждённая продажа старой вещи, точная доплата/остаток.
Основа — актуальный BannerlordLink с выплатами x2; исходные DLL сохранены.
20+48+21 проверок мода, выплаты68/68, backend/UI OK, Release0/0.
Новая DLL: D:/shedlink-build/no-party-equipment-20260921/bin; НЕ установлена.
См. docs/BANNERLORD_NO_PARTY_EQUIPMENT_2026-09-21.md.

**21.09 — баланс по реальным боям УСТАНОВЛЕН при закрытой игре.**
DLL/PDB совпали с проверенной сборкой62AB4851…86488; резерв проверен.
Сравнение декомпиляций: только3 коэффициента награды. Максимумы прежние.
Оверлей уже выложен; живой бой после нового запуска пока не проверен.
См. docs/BANNERLORD_BATTLE_PAYOUT_BALANCE_2026-09-21.md.

**21.09 — оверлей средней оценки золота ВЫЛОЖЕН.**
Только overlay.html, резерв и атомарная замена; SHA256 файла и localhost HTTP совпали.
В бою одна ≈сумма, после фактический итог. Открытый источник OBS нужно обновить.
Игра/OBS/сервер не перезапускались. Блок SSH обойдён штатным клиентом Paramiko.
См. docs/BANNERLORD_OVERLAY_ESTIMATE_2026-09-21.md.

**21.09 — боевые награды перекалиброваны по реальным результатам, ЛОКАЛЬНО.**
Прежние максимумы; P/(P+4000), свита R/(R+2000), участие до8000 очков.
В бою19:00 реальные28→76k, 59–65→123–138k с учётом урона/свиты.
62/62, проводка7/7, Release0/0. Актуальная сборка payout-observed-balance-20260921.
Предыдущие локальные варианты заменены; игру не закрывали, DLL не установлена.
См. docs/BANNERLORD_BATTLE_PAYOUT_BALANCE_2026-09-21.md.

**21.09 — максимум боевой награды возвращён по уточнению владельца, ЛОКАЛЬНО.**
Прежние потолки249600/399360; участие50k с плавным ростом до40000 очков.
54/54, Release0/0. Сборка payout-balance-original-cap-20260921; прежняя устарела.
Игра не закрывалась, DLL не установлена. Подробности: docs/BANNERLORD_BATTLE_PAYOUT_BALANCE_2026-09-21.md.

**21.09 — оверлей: средняя оценка золота готова, выкат заблокирован SSH.**
В бою ≈среднее min/max, после фактическое +золото120с; тесты и синтаксис прошли.
Серверная замена не подтверждена: reset/banner timeout. Игра/OBS не перезапускались.
См. docs/BANNERLORD_OVERLAY_ESTIMATE_2026-09-21.md.

**21.09 — перебалансировка оплаты боя подготовлена ЛОКАЛЬНО, НЕ установлена.**
Участие 50k→10k; насыщение личного вклада/свиты отодвинуто к 16000 очков.
30→130 условных врагов: 23.4k→65.8k в обычной победе. Выплаты существенно снижены.
51/51 policy/ledger, проводка7/7, Release0/0. Игра не закрывалась, DLL не заменялась.
Артефакт D:/shedlink-build/payout-balance-20260921/bin; состав сборки перед выкатом сверить.
См. docs/BANNERLORD_BATTLE_PAYOUT_BALANCE_2026-09-21.md; живая проверка впереди.
**21.09 18:39 — оплата v2 выложена на сервер, журнал оплаты установлен в игру.**
DLL/PDB SHA256 5A8585B7…06C4989, rollback .rollback-20260921-183443.
Сервер: точечный патч _adapter.py + viewer-bannerlord.js, кэш обеих оболочек обновлён.
twitchbot PID2560330 RUNNING, локальный /health status=ok db=ok после рестарта.
SSH/публичный HTTPS нестабильны; внешний health дал timeout. Живой бой не проверен.
OBS overlay.html не обновлялся; итоговая карточка находится в панели зрителя, итоговый текст — в журнале игры.

**21.09 — итог оплаты добавлен в игровой журнал, DLL пока НЕ установлена.**
После успешного начисления положительной суммы: @ник, итог, участие/герой/свита.
Ошибка сообщения отделена от начисления. wiring7/7, Release0/0.
Готовая DLL/PDB: D:/shedlink-build/payout-journal/bin. Игра запущена (PID30424),
поэтому замена не выполнялась. Фронтенд/сервер/OBS-оверлей этой правкой не менялись.

**21.09 18:29 — новый ZIP 0.0.5 собран по запросу владельца.**
Архив/кэш/журнал проверены; dist/shedlink-0.0.5.zip, SHA256 9e35ba1c…564f368.
Twitch не загружен. Сервер оплаты v2 не обновлён; OBS-оверлей пока не показывает итог v2.

**21.09 18:22 — запрет самостоятельной смены королевства кланами зрителей установлен.**
Пять решений дипломатического ИИ заблокированы для лидеров-зрителей; ручные команды сохранены.
Фронтенд не меняли. Harmony 22/22, пять реальных методов движка, save/load6/6, Release0/0.
DLL 18C69DB7…28E460, rollback .rollback-20260921-182251. Живая кампания не проверена.
Подробности: docs/BANNERLORD_VIEWER_KINGDOM_AI_2026-09-21.md.

**21.09 18:08 — оплата боя v2: DLL установлена; серверный выкат заблокирован SSH.**
Участие + личный вклад + свита50%; старые денежные kill/вехи/доплата заменены.
36/36 policy/ledger, wiring5/5, UI/backend, Release0/0, save/load6/6.
DLL/PDB установлены, SHA256 совпал; rollback .rollback-20260921-180816.
Серверные файлы не изменены: SSH banner timeout/reset. HTTP health старого сервиса ok.
Живая экономика не проверена.
Формула/ограничения: docs/BANNERLORD_BATTLE_PAYOUT_V2_2026-09-21.md.

**21.09 — экономика зрителей: аудит, без изменений.**
В логе боевых выплат ~41% вехи, ~42% помощь отстающим; найден разрыв доплаты
на 50k. Предложена плавная модель участия/вклада и заработок действиями на карте.
Подробности и ограничения: docs/BANNERLORD_ECONOMY_REVIEW_2026-09-21.md.

**21.09 — RBM усилен по просьбе владельца: сильно агрессивный профиль.**
FrontlineAttackWeight=3 (максимум UI), BackStep=0.15, FindAlly=0.5.
Обе стороны; только тактика передней линии. Живой бой не проверен.
Предыдущий умеренный config.xml сохранён рядом в rollback-копии.

**21.09 17:00 — по разрешению владельца DLL приказов УСТАНОВЛЕНА; RBM настроен.**
Игра закрыта; DLL/PDB совпали с локальной сборкой. SHA256 DLL:
343908BDA2127EAC2EBBFE0A9A2AE4D7B43282795AE17CE5C30527A2324CCAE0.
6/6 save/load; в DLL проверен BuildSnapshotForMaster. Живой бой не проверен.
RBM FrontlineAttackWeight 1→1.5, FrontlineBackStepWeight 1→0.65; остальные без изменений.
Копии DLL/PDB и RBM config.xml рядом с оригиналами: .rollback-20260921-170027.
Настройки влияют на давление передней линии обеих сторон, не на приказ всей армии атаковать.

**21.09 — «Перестрелка» доработана ЛОКАЛЬНО, без выката.**
Максимум 3 отхода за приказ, точка в 8 м, таймаут 4 с, пауза 3 с после остановки.
52/52 проверок; живой бой не проверен. docs/BANNERLORD_SKIRMISH_LOCAL_2026-09-21.md.

**21.09 — боевые приказы улучшены ЛОКАЛЬНО; владелец запретил выкат.**
Пеший Charge выбирает достижимую пехоту, не гоняется за отдельными лошадьми,
ограничивает погоню за кавалерией. Исправлены Gate/отказы/rollback, добавлены
статусы панели. 43/43 проверок приказов, UI/backend, Release и 6 save/load — OK.
DLL только D:\shedlink-build\battle-orders-local\bin; установленная не изменена.
Живой бой не проверен. См. docs/BANNERLORD_BATTLE_ORDERS_LOCAL_2026-09-21.md.

**21.09 — аудит боевых приказов зрителей, без исправления/выката.**
Найдены подмена ворот лестницей, изменение управления до отказа и ложный успех
при ошибке движения. Предложены точные статусы и отдельный этап осадного ИИ.
В игре качество движения не проверялось. Сохранение/вассалы не менялись.
См. docs/BANNERLORD_BATTLE_ORDERS_AUDIT_2026-09-21.md.

**21.09 — исправлено сохранение при зрительском голосовании война/мир.**
Оба типа решений зарегистрированы в SaveSystem. Старая DLL воспроизводит
ошибку из лога; новая проходит 6 проверок сохранения/загрузки на DLL движка.
Release: 0 ошибок/предупреждений. Установленная DLL совпала с проверенной:
SHA-256 `370925D8…78668`; резерв предыдущей версии проверен.
Нужен запуск игры и сохранение кампании с ожидающим голосованием; живой
повтор ещё не проверен. См. docs/BANNERLORD_DIPLOMACY_SAVE_2026-09-21.md.

**21.09 15:40 +05 — два пути основания королевства выкачены.**
Независимый клан может создать королевство без феодов. Клан внутри чужого
королевства поднимает восстание только с двумя кланами-сторонниками: личными
вассалами либо кланами, чьи лидеры имеют отношение к герою от +50. Основатель
и сторонники переходят со своими владениями; правящий, нейтральные,
наёмнические и уничтоженные кланы не учитываются. Панель показывает прогресс
и имена сторонников. Harness, Release-сборка, синтаксис фронта и покрытие
текстов отказов прошли. DLL установлена (SHA-256 `f4946a5c…`, совпал),
backend/frontend выкачены: PID 2555714, local/public health 200, публичный
viewer JS совпал SHA-256 `18b7ae37…`. Живое восстание в игре не проверено.

**21.09 15:00 — два зрительских отчёта, локально, без выката.**
Убран ошибочный запрет Endorphine13 создавать королевство без феодов; цена 5 млн
и остальные проверки сохранены. `action.failed` теперь снимает cooldown; тесты зелёные.
У AnalniyShekotyn свежий celtic_dagger не улучшался и не пропал: production
snapshot показывает его в партийном инвентаре, count=1. Старый легендарный
«Сакс» 17.09 относился к другому save_id. См.
docs/BANNERLORD_REPORTS_2026-09-21_1500.md.

**21.09 15:40 +05 — исправление турнира установлено вместе с общей сборкой.**
Полная очередь из save, идемпотентная запись, защита от старых снимков и очистки ожидающих.
SQLite/C# проверки и Release сборка прошли. Вассалы: production зеркало пусто,
восстановления из save нет; наличие прежних 13 кланов в текущем save не доказано.
DLL/backend/frontend выкачены по прямой команде владельца; живой повтор записи
зрителя после запуска игры ещё не выполнен. См. docs/BANNERLORD_TOURNAMENT_VASSALS_2026-09-21.md.

**21.09 — призывы: исправлен запуск приёма команд после неудачного ping.**
DLL установлена после закрытия игры; SHA-256 20fa69d5…bdc723 совпал.
Startup-тест и 133 проверки прошли; живой призыв ждёт запуска игры.
Очередь не менялась. См. docs/BANNERLORD_POLLER_STARTUP_2026-09-21.md.

**21.09 14:09 +05 — синхронизация героев выкачена:** backend/frontend,
health OK, RUNNING PID 2553136, хэши совпали. Исправлены конфликт hero_id
и ложная подсказка для старых сборок. DLL/Twitch ZIP/CDN не менялись.
Живой повтор: 26 героев восстановлены; AnalniyShekotyn и shedoy23 имеют
ready=true у сборки. Для shedoy23 ограничения объясняются пленом.
Подробности: docs/BANNERLORD_HERO_SYNC_2026-09-21.md.

**21.09 — Twitch ZIP 0.0.5 пересобран:** `dist/shedlink-0.0.5.zip`,
MD5 `4779eba8ffb0bffc957638b5ffb08db8`, 264 400 байт. Включает безопасный
настоящий инвентарь; прежняя сборка сохранена. В Twitch ещё не загружен.
Подробности: `Расширение/docs/RELEASE_RECORD.md`.

**21.09 10:48 +05 — инвентарь Bannerlord опубликован:** исключён багаж пленителя;
надетые вещи, багаж своего отряда и старое хранилище разделены. Неизвестные
данные больше не показаны как пустые. 16+48 C# проверок, три backend-набора и
два браузерных набора прошли; DLL установлена с резервом и сверкой SHA-256.
M127/backend/frontend выкачены после staging; health и запрос инвентаря HTTP 200.
Twitch ZIP/CDN не менялись; проверка в игре ожидает загрузки сохранения.
Подробности и ограничения: `docs/BANNERLORD_NATIVE_INVENTORY_2026-09-21.md`.

Автопилот 16.09, 18:52: установлен bdb5982 — случайный выбор в MapIncident, случайные диалоги включены по умолчанию. 234/234 локально; живой повтор ожидается. См. BannerlordAutopilot/review/RANDOM_WINDOWS_2026-09-16.md.

Автопилот 16.09, 17:37: установлена 8d27395 — проверка добычи перенесена до штатных обработчиков закрытия; убрано ложное выключение при изменении инвентаря после Готово. 233/233, контракт 350; игровой повтор не проверен. См. BannerlordAutopilot/review/LOOT_CLOSE_2026-09-16.md.

Автопилот 16.09: сбор вещей после боя добавлен и установлен (4701cf9, MD5 D0B6DC3C14B13F35AE34B0A9042B02C1). Локально 231/231, BattleMission 22, контракт 350; живой прогон новой добычи не проверен. См. BannerlordAutopilot/review/LOOT_2026-09-16.md.

# ShedLink — что сейчас

**Bannerlord, 17.09 — DLL установлена:** после закрытия игры и лаунчера
пересобрана и установлена правка покупки/удаления вещи из хранилища во время
боя. SHA-256 локальной и установленной DLL совпадает:
`7cd350db79ecba20a6e63b583cfd028d53d38d8d945dda9cf4cb57049cb7ba3f`.
Предыдущая DLL сохранена рядом как
`BannerlordLink.dll.rollback-2026-09-17-6C797E00` (SHA-256 `55fa1166…`).
52/52 локальных проверок прошли до установки; живой игровой повтор ещё не
проверен. Изменение загрузится при следующем запуске Bannerlord.

**Bannerlord, 17.09 — вес вещей:** магазин и «Мои вещи» показывают игровой вес
предмета; фронт опубликован на `/dev`. Локальный ZIP 0.0.5 обновлён: MD5
`7609ebb5d5deaa7c785e3c4b32472838`, SHA-256 `5c6407b4…`, 262 053 байта,
метка `202609170946`. В Twitch Dev Console этот архив ещё не загружен.

**Bannerlord, 17.09 — покупка в бою:** покупка и удаление вещи из хранилища
разрешены во время миссии; надеть, снять или удалить надетую вещь по-прежнему
нельзя. Первоначально установка была отложена владельцем; выполнена после
закрытия игры, как записано выше.

**Bannerlord, 17.09 — свита в «Герое»:** свита перенесена из «Инвентаря» перед
прогрессией и оформлена раскрывающейся карточкой с рамкой. Фронт опубликован
на `/dev`; локальный ZIP 0.0.5 пересобран: MD5
`48fa45e030f271982d5ed4c3d9af6881`, SHA-256 `3251a818…`, 261 917 байт,
метка `202609170852`. В Dev Console этот архив ещё не загружен; Hosted Test
точного архива не проверен.
Это предыдущая локальная сборка; копия сохранена в `dist/releases/`.

**Twitch ZIP 0.0.5, 17.09 — обновлённый предревью-архив:** на `/dev` и бэкенде
опубликован ограниченный конфиг оформления Bannerlord. Порядок и видимость
четырёх секций боя, подписи и цвета тиров задаются серверным JSON без правки
клиента. Новый локальный архив `dist/shedlink-0.0.5.zip`: MD5
`f162beb0ea1287fa1bb3d3657de04563`, SHA-256 `baf0a648…`, 261 849 байт,
метка `202609170751`; копия в `dist/releases/`. В Dev Console этот архив ещё
не загружен, Hosted Test точного архива не проверен. Настройка и ограничения:
[BANNERLORD_UI_CONFIG.md](Расширение/docs/BANNERLORD_UI_CONFIG.md).
Это предыдущая локальная сборка; неизменяемая копия сохранена в `dist/releases/`.

**Twitch ZIP 0.0.5, 17.09:** локальный `dist/shedlink-0.0.5.zip` пересобран из
опубликованного на `/dev` фронта `fe0b4cb`: MD5
`4b9b8fd1b83d0e74b1f06aaa26d3c26f`, SHA-256 `9b37e9fb…`, 259 433 байта,
метка `202609170657`. `verify-candidate.py` прошёл; новая копия сохранена в
`dist/releases/`. В Dev Console этот архив ещё не загружен. Ранее загруженный
Hosted Test ZIP с MD5 `059806841c3b84790af97c1056c4c3f5` сохранён отдельно.
Новый архив не является доказательством его установки на Twitch CDN.
Этот предыдущий локальный архив теперь хранится только в `dist/releases/`.

**Bannerlord, 17.09 — frontend /dev и backend опубликованы:** магазин за динары в нижнем
«Инвентаре», свободное снаряжение, одна специализация и одна оружейная активка.
Стартовый набор выдаётся один раз; сила активки растёт с навыком, общий кулдаун
90 секунд. Перенос старых героев отменён владельцем. На Review ещё не отправлено;
DLL установлена 17.09 в 01:38 +05 с резервной копией и сверкой SHA-256;
позже обновлена: установленная DLL считает цены вещей ×10 от игровой стоимости,
старая DLL сохранена. Сервер покажет новые цены после запуска игры и обновления каталога;
живой прогон остаётся. Сервер/миграции/публичные хэши
проверены: [выкат](docs/BANNERLORD_FREE_BUILDS_DEPLOY_2026-09-17.md).
[Реализация и проверки](docs/BANNERLORD_FREE_BUILDS_2026-09-17.md).

**ShedLink, 16.09 22:53 +05: установлен фикс массовых 429 панели (c9c125a).** Опрос и
presence считаются на JWT-зрителя, действия — на канал; frontend 0.0.5 не менялся.
Регрессия воспроизводится на текущем production-файле; исправление, frozen-client
и 1335 tenant-проверок проходят. Production-файл и SHA-256 проверены, сервис
перезапущен, local/public health 200; живой повтор панели ещё не проверен.
Детали и резервная копия: `docs/VIEWER_RATE_LIMIT_FIX_2026-09-16.md`.

**Автопилот, 16.09 после краша 13:23:** завершение расстановки перенесено из
обхода обработчиков в опрос приложения. Добавлены приём пленных до лимита,
«Готово» и подтверждение оставшихся. Локально 219+22 проверки, контракт 333;
живой повтор ещё не выполнен. Установка — `BannerlordAutopilot/HANDOFF.md`.
DLL 855917a установлена при закрытой игре в 14:46, резерв и MD5 проверены.

**Автопилот 16.09:** реализованы помощь защитникам осаждённого поселения и
зачистка известных убежищ полноценным дневным штурмом. Локально 203+20 проверок,
контракт установленной игры 307; игровой прогон ещё не выполнен.
Подробности и состояние установки — `BannerlordAutopilot/HANDOFF.md`.
DLL 9186e54 установлена в 13:17 при закрытой игре; резерв и хэши проверены.

**Диалоги 15.09:** по решению владельца добавлены принятие сдачи бандитов и
отдельный случайный режим `autopilot.random_dialogs on|off`. Тесты 180/180;
новое поведение в игре ещё не проверено. Подробности — BannerlordAutopilot/HANDOFF.md.
DLL a39470b установлена в 23:41 при закрытой игре, резерв сохранён, MD5 сверен.

**15.09, исправление автопилота:** дамп подтвердил NullReferenceException при
включении без PlayerEncounter. Проверка перенесена перед getter; 153/153,
контракт 262/262, сборка чистая. DLL 17aa26a установлена в 23:15 с резервной
копией, MD5 сверен. Игровой повтор после фикса ещё не выполнен.
Разбор: `BannerlordAutopilot/review/CRASH_NULL_ENCOUNTER_2026-09-15.md`.

Владелец подтвердил исправление вылета. Следом добавлены разговор с преследуемыми
бандитами и выход после завершённого боя: 165+15 проверок, контракт 278.
Живой повтор ещё не проверен: `BannerlordAutopilot/review/BATTLE_FLOW_2026-09-15.md`.
DLL 1dd17cf установлена при закрытой игре в 23:32, резерв сохранён, MD5 сверен.

Обновлено: 2026-09-12 · снимок предыдущей версии: `docs/archive/STATUS_THROUGH_2026-08-24.md`

Текущий контур публичного выпуска и единый release checklist:
`docs/PUBLIC_TWITCH_RELEASE_PLAN_2026-09-07.md`.

- **UI /dev 10.09:** в каталоге RimWorld описание раскрывается под товаром по
  нажатию, отдельно от покупки; одновременно открыта одна карточка. Проверены
  вещь, черта и ген в dev; локально 12 DOM-проверок, JS lint и consistency lint.
  Опубликованы только shop.js, viewer.css и две HTML-оболочки без рестарта backend.
  **Кандидат пересобран 10.09 по слову владельца:** MD5
  `059806841c3b84790af97c1056c4c3f5`, sha256 `6f7e115b…`, 249 294 байт,
  метка кэша `?v=202609101017`. Фронт выкачен на прод ДО упаковки, поэтому
  архив, рабочее дерево и прод несут одну метку и один `shop.js` — md5 сверен
  во всех трёх местах; `verify-candidate.py` зелёный, все четыре проверки.
  Прежний архив (`34bad42f…`) остался в `dist/releases/` как след того, что
  лежало в Hosted Test до 10.09. Предложение по дальнейшей навигации и проверки:
  `docs/VIEWER_UI_PLAN_2026-09-10.md`. Код закоммичен (`81aa332`) — до этого
  прод стоял на нём, а в git его не было, и любой фронт-деплой стёр бы его молча.
  **Раскрытая карточка была пустой не по вине фронта.** Мод присылает к каждой
  позиции готовую подсказку («✦ Сознание: +15%»), но она одна из всего каталога
  жила в файле `backend/tooltip_cache.json` рядом с кодом — а `deploy.ps1`
  исключает из архива только базу и `.env`. Мой же выкат бэкенда привёз туда
  пустую копию с этой машины: в логе прода `Каталог обновлён: 2913 предметов,
  тултипов: 2913` при заливке и `Тултипы загружены: 0` после рестарта. Файловый
  кэш не имел ни арендатора, ни бэкапа. **Починено M124:** подсказка — колонка
  в `shop_catalog`, как все остальные поля каталога; выкачено через staging,
  прод отвечает 200, каталог из 2913 позиций цел. **Подсказки вернутся при
  следующем запуске RimWorld** — мод заливает каталог при загрузке сохранения,
  новая DLL не нужна. Запрос для проверки после запуска лежит в
  `RUNBOOK.md` §7 «Ловушки, на которых мы уже спотыкались».

- **На проде:**
  **Квесты и «сегодня» перешли на московские сутки (12.09, на проде).** Было:
  день кончался в 00:00 UTC, то есть в 03:00 МСК — в час ночи зритель ждал
  новые квесты и ещё два часа видел вчерашние (багрепорт #48, подтверждён и
  закрыт). Переведены и квесты, и активность, и чат, и разбивка истории по
  дням; ночные записи теперь попадают в правильный день. Выкат сделан днём —
  в это время московский и UTC день совпадают, поэтому ничьи данные не
  сместились. Держит `tests/test_viewer_day_moscow.py`, виден красным.
  backend с воронкой онбординга; на CDN живёт `0.0.1`; **Manager alpha.16 (06.09) ставит актуальные моды: ShedColony 0.1.4, Bannerlord 0.1.3, RimWorld 0.1.3** — постоянная ссылка `/download/manager` ведёт на него. **03.09 закрыт хвост 28.08:** в папке загрузок прода лежал сломанный `shedcolony-0.1.0.jar` (12 из 33 платных действий отсутствуют) — починенный 0.1.1 собран 28.08, но не публиковался. Теперь опубликован: 0.1.0 отдаёт 404, 0.1.1 скачивается, sha256 сходится, проверка действий зелёная, `install.md` обновлён. Ссылок на эту папку с сайта нет — попасть можно только по прямому адресу, поэтому задело узкий круг. Локальная сборка владельца тоже переведена на 0.1.1 и **проверена в игре**: колонист создан (`colonist.spawn`, citizen=4), а `colonist.equip_netherite` — одно из двенадцати действий, отсутствовавших в старом jar, — отработало (`equipped netherite armour`). Остальные 11 не проверены. Мод трижды за вечер не смог отправить событие по таймауту — разобрано: это периодические снимки, их потеря безобидна, исход платного действия идёт другим каналом и самозалечивается дедупом (`DEFERRED.md`).
- **Frontend 05.09 выкачен на прод-фронт (его показывает страница входа `/dev` → `/dev/preview`; отдельной среды `/dev` нет, маршрута `/dev/extension.html` не существует):** превью и совпадение файлов по HTTPS проверены; backend не перезапускался. Собран и загружен в Twitch ZIP **0.0.4**, статус **Hosted Test**, MD5 кабинета совпал с локальным. Перед Review нужен прогон на канале. Архив и проверки — `Расширение/docs/RELEASE_RECORD.md`. Текущий статус 0.0.3 в консоли также Hosted Test, прежнее указание Review устарело.
- **«Канат» на проде с 02.09:** дуэль 1×1 с очередью подбора, ELO, сезоном и призами топ-3 — устроен как крестики и дуэли, за отдельный матч крустики не платятся, случайности в механике нет ни одной. Кубики заморожены с обеих сторон (карточка, `play-vs-bot`, очередь матчей). Сезон каната заведён на проде, ошибок после рестарта нет.
- **Twitch: Review НЕ отправлялся; `0.0.5` в Hosted Test.** История сборок,
  хеши и неизменяемые копии — `Расширение/docs/RELEASE_RECORD.md`; ворота и план —
  `docs/PUBLIC_TWITCH_RELEASE_PLAN_2026-09-07.md`. Платная озвучка с 07.09 на проде
  имеет терминальный исход: отказ возвращает ровно списанное, заявка без решения
  сама возвращает деньги через 30 минут. **08.09 проведён аудит «кто решает
  правило — фронт или бэкенд»** (`docs/FRONTEND_BACKEND_AUTHORITY_AUDIT_2026-09-08.md`).
  Нашлось: две платные кнопки Bannerlord (мастерская 2500💎, караван 4000💎) в
  подсказке называли цену нулевой — имя `_bnrPrice` было объявлено дважды, и
  форматтер молча умер; плюс четыре места, где окно подтверждения и клиентская
  проверка «хватает ли денег» держали замороженное число, пока подпись рядом уже
  брала цену с сервера. Деньги нигде не терялись — списывает сервер, — но панель
  называла платное бесплатным, а при первой же правке цены на сервере не пустила
  бы тех, кому хватает. **Закрыто целиком** (`f3ac607`, `f8a27e9`): цены и лимит
  озвучки приходят с сервера, `null` в конфиге больше не превращается в цену 0,
  `player.give_item` отказывает сразу на неизвестном типе предмета. Заведены
  четыре фронт-гейта и контрактные тесты, **каждый видел красным** на предыдущем
  коммите; suite 87/87, линтер и `compileall` зелёные. **Ранее загруженный в Hosted Test архив 10.09:**
  SHA-256 `6f7e115b…`, MD5 `059806841c…`, 249 294 байт, метка кэша
  `202609101017`; тег `candidate/0.0.5` (`559f9bf`). Прежние архивы 08–09.09
  (`ab685aae…`, `3d7bc695…`) — уже история, в кабинет идёт только этот.
  **12.09 CI лежал красным с 11.09 — починен.** Две ложные поломки (проверка
  даты RUNBOOK не работает при обрезанной истории; самопроверка менеджера
  сторожила номер версии мода вместо правила) держали job красным, а за
  красным шагом GitHub помечает остальные `skipped` — то есть бэкенд-тесты
  и валидация каталога установки в CI НЕ выполнялись вовсе. Починка шла
  слоями, каждая открывала следующую поломку: нашлось, что манифесты
  установки 0.1.3/0.1.4 несли номер версии от предыдущего выпуска. Сейчас
  CI зелёный; локальный release-аудит 14.09 прошёл 100/100 backend-тестов.
  **12.09 заморозка закрыта с двух сторон.** Выкат фронта на прод заперт до
  вердикта Twitch, и замок теперь доказан исполнением: тест запускает настоящий
  `deploy.ps1` в пробном режиме и был виден красным на старом скрипте — раньше
  пробный запуск обходил замок целиком и печатал обычный план выката.
  Бэкенд при этом деплоится как обычно, это осознанно. И появился контракт
  замороженного клиента: тот самый ZIP распаковывается и исполняется в тесте
  против настоящих обработчиков бэкенда (42 проверки), так что правка сервера,
  которая сломает клиента на CDN, теперь валит CI — до этого «не сломаем»
  держалось на чтении кода. **От тебя:** загрузить архив
  в кабинет, сверить MD5 и пройти `Расширение/docs/REVIEWER_WALKTHROUGH.md` —
  руками остаются вход, мобильная оболочка, «Открыть все», блоки раскрытия и
  config view; там же впиши канал и окно доступности игры по UTC. Submit for
  Review жмётся только по твоему слову. **Ловушка:** код живёт в ветке
  `codex/public-release-readiness`, `main` отстаёт на 44 коммита — деплой из
  `main` молча вернёт закрытые дыры. Деплоить только из ветки.
  **09.09, внешняя перепроверка нашла две вещи и обе закрыты.** Лимит пяти
  детей вчера объявили защищённым ошибочно: зритель мог прислать в теле
  запроса `max_alive_children: 20` и поднять предел впятеро — мод принимал
  это поле из payload'а, а бэкенд его не ставил вовсе. Проверено
  исполнением: прислано 20, в очередь уходило 20. Теперь предел ставит
  сервер; гейт шлёт враждебное тело и виден красным. Вчерашний гейт был
  зелёным потому, что искал текст константы в исходнике, а не поведение —
  урок в `LESSONS.md`, правило в `CLAUDE.md`. Второе: проверка кандидата
  падала в обычной консоли Windows на кириллице, до единой проверки; я не
  видел этого, потому что гонял её со своей переменной окружения. Обе
  правки **бэкендовые и фронта не касаются** — ревью Twitch не требуют.
  **Выкачены на прод 09.09:** deploy-gate 107/107, сервис RUNNING
  (pid 2228000), `/health` и пять страниц 200, старт без ошибок. Что живёт
  именно новый код, проверено по ответу сервера: `/api/core/config` отдаёт
  `tts_max_len`, которого в прежней сборке не было. Архив `ab685aae…` не
  затронут — фронт не деплоился, метка кэша прежняя.
  **Дальше по подсказке обзора проверил весь канал «клиентское поле → мод»**
  (мод читает 80 полей, а `payload = dict(data)` копирует тело зрителя как
  есть) — нашлись ещё две дыры, обе закрыты и **ждут выката**: способности
  принимали от покупателя свою длительность и силу (`duration_s`/`value` у
  десяти активок), а `player.modify_attribute` за 50💎 давал сколько угодно
  очков характеристик — при том что соседний живой путь берёт 50 000💰 ЗА
  ОЧКО и зажат в 1..10. Двойник закрыт целиком (фронт его не вызывал);
  решение перепроверь — `DEFERRED.md`. Оба гейта видел красными.
  **Выкачено 09.09** (pid 2228813, deploy-gate 107/107, health и страницы 200,
  маркеры обоих фиксов на проде). Связь рвалась, деплой доводил руками —
  ловушка описана в `RUNBOOK.md` §7 «Ловушки».
  **Следом закрыта гонка ответов панели** (пункт 3 того же обзора): состояние
  грузится из тридцати мест без счётчика поколений, и панель закрашивал тот
  ответ, что пришёл последним. Доказано исполнением: баланс, отправленный ДО
  покупки, затирал послепокупочный («деньги не списались» → жмёт ещё раз), а
  устаревший `unauthorized` гасил панель и показывал карточку входа уже ПОСЛЕ
  успешного восстановления. Починено счётчиком с проверкой после каждого
  `await`; гейт `test-frontend-state-race.mjs` виден красным на обоих
  сценариях. **Фронт выкачен на прод** (метка `202609090817`, прод отдаёт
  исправленный `viewer.js` — проверено ответом сервера) и **собран новый
  кандидат**: SHA-256 `c9894cff…`, MD5 `bf845f2d…`, 247 662 байта. Прежний
  `ab685aae…` снят — в нём гонка не исправлена.
  **Пункт 2 обзора тоже закрыт:** у черт и генов цена растёт с каждой покупкой,
  и между отрисовкой каталога и нажатием она могла измениться — зритель
  соглашался на 3000, платил 6000, отказа не было. Панель теперь шлёт цену,
  которую показала; сервер её НЕ списывает, а сверяет и при расхождении
  отказывает, ничего не сняв. Старый CDN-клиент `0.0.1` поле не шлёт и
  работает как прежде. **Пункт 4 проверен — дефекта нет:** возврат идемпотентен
  по маркеру `REFUNDED:` внутри транзакции, отказ сразу делает строку
  терминальной (поздний ACK становится no-op), повторный ACK не запускает
  второй возврат, залипшие действия подметают два сторожа. Шесть профильных
  тестов зелёные.
  **Исторический кандидат 09.09:** SHA-256 `513ff646…`, MD5 `34bad42f…`,
  247 938 байт; отменён архивом 10.09 `6f7e115b…` / `059806841c…`.
  **09.09 отдельным заходом — надёжность транспорта модов**
  (`docs/MOD_TRANSPORT_RELIABILITY_PLAN_2026-09-09.md`): зеркало состояния героя
  замерзало навсегда после любой сетевой ошибки — снимок помечался доставленным
  ДО отправки, а результат отправки выбрасывался. Заодно нашлось, что мод считал
  подтверждением общий `status:ok`, хотя отказ события лежит внутри ответа: тем
  же способом **денежные события молча удалялись из надёжной очереди**. Всё
  починено, DLL собран и **установлен в игру** (md5 `FAC97389`, откат
  `AD517D85` сохранён). ShedColony проверен — дефектов нет. **От тебя: запустить
  игру и прогнать пять платных действий**, живьём ничего не проверялось.
  **Догон по внешней проверке (09.09, вечер):** клиентской сериализации было
  мало — по HTTP-таймауту мод не знает, применил сервер запрос или нет, и
  задержавшийся снимок мог перезаписать свежий уже НА СЕРВЕРЕ. Теперь порядок
  защищает бэкенд: миграция `M123` добавила `state_ts`, и обработчик отбрасывает
  снимок старше применённого. Плюс событийный `Push` больше не ходит мимо
  общего трекера. **Выкачено через staging на прод** (M123 применилась, колонка
  в боевой БД есть, health и страницы 200), DLL переустановлен (`FBC96819`).
  Гейты подключены в CI циклом — новый подхватывается сам.
  **Постстрим-триаж 09.09** (`docs/POSTSTREAM_TRIAGE_2026-09-09.md`): эфир
  13:50–17:19 UTC на RimWorld, 12 зрителей, 89 платных действий. Деньги сошлись
  полностью — 0 разрывов журнала, оба отказа вернули ровно списанное в ту же
  секунду; трассировок за окно эфира ноль; часовые кейсы отработали 5 раундов.
  **Найден и починен дефект:** отказ в RimWorld возвращал крустики МОЛЧА —
  зритель видел уход и возврат денег без объяснения и жал снова (так и сделал
  один из зрителей). Bannerlord и ShedColony объясняют, RimWorld не умел вовсе.
  Гейт виден красным. **Выкачено на прод 10.09:** оба файла побайтно совпали с локальными (md5 сверен), служба поднялась, все три модуля обнаружены, трассировок после старта ноль; фразы отказа проверены исполнением на самом проде, неизвестный код даёт общую фразу, а не пустоту. **Внешний обзор 10.09 нашёл в защите порядка снимков (M123) два дефекта, оба подтверждены исполнением, первый починен полностью.** Проверка свежести и запись снимка жили в РАЗНЫХ транзакциях, и второй конверт успевал пролезть между ними: в базе оставались данные старого снимка под меткой нового — база начинала утверждать, что держит версию свежее той, что держит на самом деле, и после этого отвергала все настоящие снимки между ними. Зеркало зрителя замирало до перезапуска игры, то есть ровно тот отказ, ради которого защита и заводилась. Теперь условие перечитывается внутри той же транзакции, которая пишет. Второй дефект — порядок снимков выведен из настенных часов игровой машины — закрыт пока только СМЯГЧЕНИЕМ: метка дальше пяти минут вперёд от часов сервера означает «порядок неизвестен», снимок применяется, метка им не отравляется. Худший исход (молчаливая заморозка зеркала от прыжка часов) снят, верный порядок не получен: он чинится монотонным счётчиком отправок из самого мода — счётчик там уже есть, но до сервера не доезжает, и это требует новой DLL. Подробности и цена — `DEFERRED.md`, «Порядок снимков Bannerlord висит на настенных часах». Оба гейта видели красными, выкат прошёл через staging, suite 93/93. **Постстрим-триаж 10.09** (`docs/POSTSTREAM_TRIAGE_2026-09-10.md`): игра во время эфира не была запущена — доказано логом мода и пульсом на сервере; прод не падал, журнал денег непрерывен. **Найдена дыра про деньги:** платное действие RimWorld при выключенной игре списывало крустики, отвечало «✨ Пешка создаётся!» и держало деньги без срока — у старой очереди не было сторожа, а на проде стоит тех-режим `RIMWORLD_REQUIRE_STREAM_LIVE=false`, при котором не проверялось ничего. **Починено и выкачено 11.09:** отказ до списания при молчащей игре, 30-минутный сторож с возвратом для обеих очередей RimWorld, объяснение каждого возврата. Застрявшие 550 💎 вернулись сами на первом проходе сторожа (14:16:47 UTC): `laitru` +350, `ethanenok` +200, каждому уведомление. Тех-режим оставлен твоим решением — нужен для тестов вне эфира (`DEFERRED.md`). **Внешний обзор 11.09 нашёл P1 в моей же правке, и он оказался шире:** у платной заявки было три гонки «проверка в одной транзакции, действие в другой» — сторож мог вернуть деньги за заявку, которую игра уже взяла (все три модуля; ShedColony и Bannerlord с июля, RimWorld подключил я), опрос мода — отдать игре заявку, за которую уже вернули деньги, а сторож `dispatched` — вернуть в очередь подтверждённую. Итог у всех один: и эффект, и деньги. Починены все три, тест видел их красными, выкачено через staging (`0050f18`), разбор — §9 отчёта триажа 10.09.
- **На проде с 28.08:** розыгрыш кейсов больше не отдаёт их брошенным вкладкам; сверка колонии сама вычистит четыре призрачные привязки при первом снимке; **заведён журнал движения крустиков** — теперь возврат можно доказать строкой в базе, а не строкой в ротируемом логе (`RUNBOOK.md` §6 «Проверки»). **Закрыто эфиром 28.08:** доставка `channel.follow` **работает** — `2026-08-28 09:18:25 ... eventsub: принято type=channel.follow` и следом `👋 follow-greet @dawesladenkuifurfemboihik` в `/var/log/twitchbot.out.log`. Гипотеза «пересоздание подписки 22.08 починило» подтверждена наблюдением; рейды и биты в тот эфир не приходили вовсе, поэтому про них по-прежнему ничего не известно (ноль событий ≠ поломка). **Побочно найдено 01.09:** «скачивания» в воронке онбординга считают краулеров — 9 из 10 последних это Amazonbot/MJ12bot и сканеры, `robots.txt` нет (`DEFERRED.md`).
- **Bannerlord переехал на 1.4.8 (02.09):** **19 патчей из 19**, рукопожатие с бэкендом есть, 16 героев на месте; вылет оказался чужим `BanditBlackHole`. Платный путь подтверждён: `give_item`, `add_skill`, `army_create` применились и ACK'нулись; отказ `propose_peace` вернул 1500 отдельной проводкой. Исправлены явные исходы этих трёх обработчиков и `party_order_set/release`, пакетная экипировка подтверждена 16/16 ACK. Полный аудит кнопок: 60 покупаемых действий = 56 C# + 4 backend-only, потерянных обработчиков нет; 1318 ссылок action-handler'ов на API TaleWorlds 1.4.8 разрешаются, 0 битых. Найденный drift `Clan._heroes` → `_heroesCache` исправлен. DLL установлена, MD5 `5CD9DA51…`; сборка 0 ошибок, harness 133/133. **Живой прогон кнопок 03.09 (браузер + лог мода):** «Призвать за стримера» (50💎) отработал целиком — герой + свита 5/5, награды за убийства идут; «Призвать против стримера» (100💎) списывал деньги и не делал ничего, если герой зрителя уже на поле боя — **починено и подтверждено в бою 03.09**: отказ сработал дважды, 100💎 вернулись оба раза, возрождение мёртвого героя не сломано. DLL в игре (md5 `73F19D14…`), бэкенд с текстом отказа выкачен на прод. Стойка боя применилась верно (`stance=aggressive` при «Натиске») — вчерашняя гипотеза о расхождении не воспроизвелась. Под вопросом стойка боя: панель показывает «Натиск», мод при спавне записал `stance=defensive` — не подтверждено, лог однократный. Доказательства и варианты починки — `DEFERRED.md`.
- **Ждёт твоих рук:**
  **14.09 автопилот Bannerlord (отдельный тест-мод, ShedLink не затронут) запущен в
  игре и с 02:16 играет сам:** за два часа 10 заездов и выходов, 9 нанятых, одна
  остановка на 161 с — окно случайного события (подтверждено); автопилот под ним
  действовал — исправлено в коде (`5f31d82`), выбор варианта события за тобой
  (`DEFERRED.md`). Партия сама ездит, ждёт в поселениях и уходит по
  решению штатного AI; в городе и деревне продаёт обычных пленных, покупает еду и
  нанимает — с резервом денег и потолками трат. Находки независимых проверок
  исправлены или записаны как ограничения (`DEFERRED.md`); стенд 118/0, контракт на
  DLL 1.4.8 — 196/196. **В игре с 04:34 стоит версия с защитой от окон** (`5f31d82`,
  md5 `A9F220C0…` сверен; ещё не запускалась); прежние DLL —
  `D:\shedlink-build\autopilot-dll-rollback`, сохранения —
  `D:\shedlink-build\bannerlord-saves-backup-20260914`. Защита от окон подтверждена
  игрой в 13:55. **Днём Кодекс** добавил покупку еды по живой цене (верно, в игре
  работает), полноценный бой (в игре не было; после боя сцена ждёт TAB — игра встанет)
  и свои поправки патруля; его DLL сейчас в игре. Разбор —
  `BannerlordAutopilot/review/CODEX_REVIEW_2026-09-14.md`. **Тебе:** решить, оставлять
  ли бой до доводки выхода из сцены, нужны ли свои оценки патруля, выбирать ли
  варианты событий.
  **12.09 менеджер починен и выпущен: alpha.17.** alpha.16 не мог поставить
  НИ ОДИН из трёх актуальных модов: манифесты вшиты в сборку, а в них размер
  остался от предыдущего выпуска и подпись была под старый размер — установка
  падала. Переподписаны настоящим ключом, выпущен RimLink 0.1.4 (с фиксом
  протезов), собран и опубликован alpha.17, постоянная ссылка переключена и
  проверена ответом сервера. Новый гейт `scripts/check-published-artifacts.py`
  качает то, что реально лежит на сервере, и сверяет размер, sha и ПОДПИСЬ —
  11 манифестов зелёные. **Тебе: скачать менеджер по обычной ссылке и
  переустановить моды** — тогда же проверится фикс протезов (#50).
  **12.09 багрепорт #50 разобран и починен в моде RimWorld,
  но в игре не проверен.** «Не у всех отображает бонусы к параметрам» — это
  протезы: каталог их намеренно продаёт, а подсказка описывала только
  импланты со статами. DLL пересобрана (md5 `e97f9945…`), нужен твой запуск
  с новой DLL — каталог заливается при загрузке сохранения. Проверка после
  запуска: `SELECT COUNT(*) FROM shop_catalog WHERE category='implant' AND
  tooltip LIKE '%✦%'` на проде — до фикса протезы в это число не попадали.
  Багрепорт держим открытым до твоего подтверждения.
  **06.09 менеджер догнал работу:** он ставил моды двухдневной давности — ShedColony 0.1.1 (без причин отказа и без сверки каталога) и моды 0.1.2 (без уведомления о неполной свите, элиты по жалованью и своего файла лога). Проверено содержимым опубликованных ZIP, а не номерами версий. Выпущены **ShedColony 0.1.4, Bannerlord 0.1.3, RimLink 0.1.3 и Manager alpha.16**: архивы опубликованы, скачаны обратно, sha256 сошлись с манифестами, метки правок внутри бинарников на месте; постоянная ссылка переключена и проверена ответом сервера. **Тебе — просто скачать менеджер по обычной ссылке и переустановить моды.** Матрица R3 (0 из 10) на аккаунте `Семья`; **Manager запускать только из РАСПАКОВАННОЙ папки** — 28.08 он был запущен прямо из окна архиватора и поэтому писал «Release-каталог недоступен или повреждён» (`RUNBOOK.md` §7 «Ловушки»). **Микрофон QuadCast S с 26.08 потерял дескриптор** (`VID_0D8C&PID_016C` вместо `0951:171D`) — помехи и провалы в звуке, гарантии нет; разбор и варианты починки — `RUNBOOK.md` §14 «Стрим-железо». Перед эфиром проверить источник звука в OBS: он должен стоять на «По умолчанию». Биты закрыты 24.08: scope выдан, `channel.cheer` зарегистрирована, 10 подписок `enabled`.
  **Живой боевой прогон 04.09:** прошли призыв героя + свиты 5/5, ярость, взрывные стрелы и пять команд отряда; стены/ворота в полевом бою корректно отказали с возвратом. Бой закончился победой, награды и state sync прошли. Найден нулевой платный heal при полном HP; исправлен на `health_full` + refund, а `288/287` — единым округлением. DLL установлена (SHA-256 `F8801E0A…`), текст отказа на проде, health 200; сборка 0/0, harness 133/133. Остался live-retest лечения после перезапуска игры.
- **Ночь 05→06.09, коротко:** триаж двух эфиров закрыт (деньги сошлись, 103 367💎 возвращены; разбор — `docs/POSTSTREAM_TRIAGE_2026-09-05-evening.md`). Сделано и выкачено: свой файл лога у мода RimWorld, jar ShedColony **0.1.2** с причиной отказа, все цены четырёх модулей переехали на серверные конфиги, кнопка **«Открыть все»** для кейсов и **часовая раздача** кейсов активным. Перед подачей дочищено: молчаливый отказ кнопок (замок склеивал стороны призыва) и 33 кода отказа Bannerlord получили человеческие фразы. Каждый гейт видел красным. Подробности и оставшиеся хвосты — `DEFERRED.md`. **06.09 днём:** начат курс «каталог берётся из игры везде» — на проде серверный чёрный список каталога RimWorld (мёртвый товар снимается деплоем, а не релизом DLL), Bannerlord больше не решает на бэкенде, какие навыки существуют, и заведено событие `action.partial` — свита призыва пришла не вся, зритель узнаёт. Доведена работа параллельной сессии по треку активности: минута начисления атомарна, heartbeat не начисляет время, починен второй BotCore без чата; **твоим решением возвращены** 50% пассивного дохода, обычный кейс лёркеру и имя `watch_half`. Аудит совместимости — `docs/MOD_COMPAT_AUDIT.md`. Пункты 1–3 плана «каталог из игры везде» закрыты: чёрный список RimWorld и белые списки Bannerlord на проде, свита сообщает о неполноте (`action.partial`), элитный рекрут определяется жалованьем, а не именем юнита — DLL установлен (md5 `AD517D85`), **нужен перезапуск игры и живой прогон двух кнопок**: «Призвать за стримера» и «Нанять войска». **Пункт 4 (каталог ShedColony) закрыт целиком.** Списки товаров (выдать колонисту / на склад / закрепить в запасе) больше не живут двумя копиями: объявлены на бэкенде один раз и приезжают в панель с сервера — снять испортившийся товар стоит деплоя, а не подачи новой версии в Twitch. Сверх того **мод 0.1.3 сверяет этот каталог с реестром предметов ТВОЕЙ сборки** и сообщает, чего в ней нет: такие позиции исчезают из панели, и никто не платит за предмет, который выдать нечем (тот самый класс «сабли Wolfein»). Свой список мод не держит — спрашивает бэкенд. Молчание мода = торгуем полным каталогом. Всё на проде, миграция m121 применена; гейты `test_shedcolony_item_catalog.py` и `test_shedcolony_catalog_check.py` видели красным. **Внешний обзор вечером нашёл в этой работе дыру, и он прав:** мод спрашивал каталог у той же ручки, которая уже вычла отсутствующее, — проверял только оставшееся, отвечал «у меня всё есть», и скрытые товары возвращались в продажу. Дальше мигали через сверку. Починено: мод просит полный каталог, фильтрация только для зрителя. Заодно закрыт второй вход — покупка отсутствующего товара теперь отклоняется ДО кассы, а не через списание с возвратом. **Jar 0.1.4 опубликован** (sha256 `2A1F1CDB…` сверен после скачивания), **0.1.3 снят с публикации** — в нём та самая самоотменяющаяся сверка. **От тебя:** поставить 0.1.4 в сборку и перезапустить сервер Minecraft. Заодно свели двойные пути записи: время и доход теперь пишутся из одного места, гейт `test_single_write_path.py` не даёт вернуться второму; урок про двойной опыт (19 096 минут при 16 352 минутах эфиров) — в `LESSONS.md`, правило — в `CLAUDE.md`. **Закрыт ночной фарм чата:** сообщения раз в 11 минут при выключенном стриме закрывали чат-квесты — за ночь 1700💎 и два кейса. Антифрод отработал штатно, но он проверяет частоту и повторы, а не время суток; проверки «идёт ли эфир» не было ни у бонуса, ни у квестов. Теперь награды за чат требуют живого стрима, неизвестный статус = не платим, а само сообщение вне эфира по-прежнему учитывается в статистике и пуле голосования. Гейт `test_chat_rewards_need_stream.py` (видел красным). Ничего ни у кого не отнимал.
- **Постстрим 06.09:** 24/24 RimWorld-действия терминальны, два отказа вернули
  10 150💎 полностью; backend и БД чистые. №49 закрыт живым сценарием: две пешки
  умерли, трупы исчезли, имя/мир сохранились, `is_alive=0`. Найдена повторная
  фантомная stream session из-за EventSub live-cache без Twitch id; локальный
  фикс и красно-зелёный тест готовы. Полный suite дополнительно поймал две
  регрессии текущего прода: лёркер выпал из случайных дропов и watch-time
  квестов. **Все три правки выкачены 06.09 в 17:35 UTC:** deploy-gate 107/107,
  целевые тесты на проде 3/3, сервис RUNNING, health/pages 200. Telegram-анонс
  старта не ушёл из-за известной сетевой недоступности; PubSub 404 повторился.
  **Исправлено 07.09 по логам:** прежняя запись «на скриншоте было `ShedLink
  dev`, значит 0.0.5 не подтверждён» неверна — владелец переключил версию с
  локального теста на Hosted прямо в эфире. Логи nginx это подтверждают: 3796
  запросов панели за окно эфира пришли с CDN-домена
  `02andzqle…ext-twitch.tv` (это и есть Hosted Test), запросов с локального
  теста в тот день нет вовсе. То есть эфир шёл на сборке 0.0.5.
  Разбор: `docs/POSTSTREAM_TRIAGE_2026-09-06.md`.

**Живой прогон ShedColony 04.09:** игра в `E:\lexplosion` загрузила 0.1.1, мод увидел колонию и 5 жителей; локальный JAR побайтно совпадает с опубликованным (`DDCC3400…`) и содержит 33/33 обработчика. От кнопки до лога/ACK прошли 11 применённых действ; 9 неприменимых/повторных покупок дали `ACK FAIL` и возврат ровной суммы, включая 50 000💎. Gradle, backend money/empty-roster и consistency-проверки зелёны; ошибок ShedColony в логах и на сервере нет.

## Правило обновления

Здесь только витрина: что на проде, что ждёт выката, что горит, что от
владельца, ближайшая дата. **Разрослось больше 10 пунктов — значит, содержимое
уехало не в свой файл** (история → `docs/archive/`, операционка → `RUNBOOK.md`,
план → `ROADMAP.md`, отложенное → `DEFERRED.md`). Держит линтер
`check_status_is_a_window`.

