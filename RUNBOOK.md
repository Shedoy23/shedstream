# RUNBOOK — как здесь всё устроено на самом деле

Один файл на всё, что нужно знать, чтобы не гадать: где что живёт, чем
проверять, и что выглядит сломанным, но таковым не является.

**Чем отличается от соседей:** `OVERVIEW.md` — что за проект. `ROADMAP.md` —
план стабилизации (июнь, закрыт). `DEFERRED.md` — что отложено и почему.
`docs/CONTEXT*.md` — статус по областям. **Этот файл — операционная правда:
пути, команды, ловушки, способы доказать что работает.**

Последняя ревизия: 2026-07-29.

---

## Содержание

1. [Главный принцип: доказательство вместо рапорта](#1-главный-принцип)
2. [Прод: что где лежит](#2-прод)
3. [Бэкапы и восстановление](#3-бэкапы)
4. [Деплой](#4-деплой)
5. [Проверки: как доказать, что работает](#5-проверки)
6. [Ловушки, на которых мы уже спотыкались](#6-ловушки)
7. [Моды Bannerlord: сборка, логи, публикация](#7-моды)
8. [Выглядит сломанным, но это не так](#8-выглядит-сломанным)
9. [Ограничения, которые нельзя обойти](#9-ограничения)
10. [Локальная копия и поддельный мод](#10-локальная-копия-и-поддельный-мод)
11. [Вопросы владельца и регулярная уборка](#11-вопросы-владельца-и-регулярная-уборка)

---

## 1. Главный принцип

**Успех подтверждается внешним признаком, а не сообщением исполнителя.**

За один день 2026-07-22 этот класс ошибки всплыл четыре раза:

| Что рапортовало | Что было на самом деле |
|---|---|
| Mass Upgrade: «нужен перезапуск» (подразумевая «сохранил») | не сохранял ни у кого с мая |
| Загрузчик Workshop: `Finished...` | не опубликовал ничего |
| Мод RimWorld: команда поставлена в очередь | очередь молча теряла все строки с миграции m1 |
| Расширение: «закон на голосовании» | закон давно принят, статус не обновлял никто |

Отсюда правило на каждую проверку: не «сервис пишет ок», а **запрос в боевую базу /
дата на странице / строка в логе игры / вывод теста**. Ниже под каждую подсистему
дана конкретная такая проверка.

---

## 2. Прод

**Сервер:** `root@31.130.132.224`, домен `https://shedoy23.ru`, supervisor-сервис
`twitchbot`, рабочая папка `/root/twitch-extension/backend`.

### Базы данных

| Что | Путь | Комментарий |
|---|---|---|
| **БОЕВАЯ** | `/root/twitch-extension/backend/viewers.db` | 81 МБ, 106 таблиц (сверено запросом 2026-07-29; станет 107 после выката M102 — она ещё в ветке) |
| staging | `/root/twitch-extension-staging/backend/viewers.db` | живая и настоящая, но **не прод** |
| корзина | `/root/_trash_20260722/` | перенесённый мусор, можно сносить |

**Не гадать по путям — спросить у живого процесса:**
```bash
PID=$(supervisorctl pid twitchbot)
ls -l /proc/$PID/fd | grep -oE '/[^ ]*\.db' | sort -u
```

Раньше рядом лежали **нулевые пустышки** (`/root/viewers.db`,
`/root/twitch-extension/viewers.db`, `backend/data.db`) — открываешь, видишь
пустоту, делаешь вывод «данных нет». Убраны 2026-07-22 в корзину. Одна осталась
внутри `backend.bkp-20260528_104928/` — папка явно бэкапная, не ловушка.

### nginx

Конфиг: `/etc/nginx/sites-enabled/twitchbot`, проксирует на `127.0.0.1:8000`.

`client_max_body_size 20m` — добавлено 2026-07-22. **Зачем:** по умолчанию 1 МБ,
и каталог RimWorld (2559 предметов с описаниями) не влезал → мод получал `413`,
магазин зрителей замер на старом срезе. Бэкапы конфига: `/root/twitchbot.nginx.bak.*`.

**Правило:** менять конфиг только так — `nginx -t` (проверка синтаксиса) **до**
`systemctl reload nginx`. 2026-07-22 моя же правка сломала синтаксис, и поймала
это именно проверка перед перезагрузкой.

### Логи

- бэкенд: `/var/log/twitchbot.out.log`, `/root/twitch-extension/logs/`
- бэкап-cron: `/root/twitch-extension/logs/backup.log`

---

## 3. Бэкапы

**Три независимых механизма, все проверены 2026-07-22 — рабочие.**

| # | Кто | Куда | Формат | Хранит |
|---|---|---|---|---|
| 1 | cron 05:00, `backup_db.sh` | `/root/twitch-extension/backups/viewers.daily.*.db.zst` | сжатые ~8.8 МБ | 15 дневных + 7 недельных |
| 2 | in-process `backup_loop.py` | `/root/twitch-extension/backend/backups/backup_*.db` | **несжатые 65 МБ** | ~15 штук = 910 МБ |
| 3 | **офсайт** | `%USERPROFILE%\shedstream-backups\` на ПК владельца | сжатые | 18 копий (сверено 2026-07-27, задача LastTaskResult=0) |

### Офсайт устроен как ЗАБОР, а не отправка

Задача Windows **`shedstream-db-backup-pull`** (ежедневно 13:00,
`scripts/pull-backup.ps1`) тянет свежий `.zst` с прода на локальный компьютер.

**Искать офсайт grep'ом по серверу бесполезно — его там нет и быть не должно.**
2026-07-22 я на этом ошибочно объявил дыру; владелец поправил.

Проверка:
```powershell
Get-ScheduledTaskInfo -TaskName "shedstream-db-backup-pull"   # LastTaskResult должен быть 0
Get-ChildItem "$env:USERPROFILE\shedstream-backups" | Sort-Object LastWriteTime -Desc | Select -First 3
```

### Тест отработал, но не завершился — закрывай пул соединений

Симптом: тест печатает `PASSED: N FAILED: 0` и **висит вечно**. В CI это хуже
падения — деплой-гейт ждёт бесконечно, красного никто не видит, а по ощущениям
«всё тормозит». Именно так 2026-07-26 съелся час: тесты отрабатывали за секунду
и потом висели, а я ждал.

Причина: пул соединений держит потоки, которые интерпретатор обязан дождаться
перед выходом. Прод закрывает пул сам (`main.py` на shutdown), тестам этого
никто не делал.

Лечение — в конце теста:
```python
if getattr(db, "_pool", None):
    await db._pool.close()
```
**У каждого `Database` СВОЙ пул.** Глобальный `close_db_pool()` тут ни при чём и
ничего не чинит: замер показал 2 потока до него и 2 после, а после
`db._pool.close()` — ноль.

Проверять надо по коду САМОГО `timeout`, а не по коду конвейера:
```bash
timeout 90 python tests/X.py > out.txt 2>&1; echo $?   # 124 = висит
```
`timeout 90 python ... | grep PASSED` вернёт 0 от grep даже у повисшего теста —
на этом я едва не отрапортовал успех.

## Как доказать, что бэкап рабочий (а не просто лежит)

**Проще всего — одной командой, с ПК владельца:**
```bash
python scripts/verify-backup.py        # свежий срез;  --all = все 14
```
Разжимает, гоняет `integrity_check`, проверяет что `viewers` не пуста.
Коды выхода: **0** восстановим · **1** битый, разбираться сейчас ·
**2** проверить не вышло (нет пакета `zstandard`) — это НЕ «бэкап плохой».
Разводить 1 и 2 обязательно: ежедневная ложная тревога приучает не смотреть
в лог, и тогда настоящую поломку тоже пропустят.

**Запускается само:** с 2026-07-25 `pull-backup.ps1` вызывает эту проверку
после каждого забора и пишет результат в `pull-backup.log` строкой
`verify: restorable` / `VERIFY FAILED` / `verify: SKIPPED`. До этого
единственной проверкой было «файл больше 1024 байт» — её проходит и архив,
скачанный наполовину. Разовый дрилл 2026-06-13 доказал восстановимость той
копии, но ничего не говорил про скачанную сегодня.

**Вручную, на сервере:**
```bash
# на сервере, где есть zstd
zstd -d -q -f /root/twitch-extension/backups/viewers.daily.YYYY-MM-DD.db.zst -o /tmp/rc.db
python3 -c "
import sqlite3; c=sqlite3.connect('/tmp/rc.db')
print(c.execute('pragma integrity_check').fetchone()[0])
print('зрителей:', c.execute('select count(*) from viewers').fetchone()[0])"
rm -f /tmp/rc.db
```
Плюс сверить MD5 локальной копии с серверной — докажет, что перенос не исказил файл.

Результат теста 2026-07-22: `integrity_check: ok`, 106 таблиц, 76 зрителей,
5 731 423 очков, 22 героя. MD5 совпал.

### Известное к улучшению

Механизм №2 пишет **несжатые** копии: 910 МБ против 355 МБ у cron за тот же охват.
Сжатие сократило бы до ~130 МБ. Не срочно (диск занят на 51%).

---

## 4. Деплой

```powershell
pwsh scripts/deploy.ps1 -Backend     # только сервер
pwsh scripts/deploy.ps1 -Mod         # сборка + копия DLL в игру + сверка md5
pwsh scripts/deploy.ps1 -All         # мод + бэк + фронт
pwsh scripts/deploy.ps1 -DryRun      # ничего не менять, показать план
```

Если `pwsh` не найден: `powershell -NoProfile -ExecutionPolicy Bypass -File scripts\deploy.ps1 -Backend`.

**Что делает сам:** прогоняет критические тесты (красный тест = деплой не пойдёт),
тарит без `*.db`/`.env`, рестартит supervisor, проверяет `RUNNING` + `/health` 200.

**Правила:**
- `deploy.ps1 -Frontend` обновляет наш сервер, но **не обновляет публичный
  Twitch CDN-фронт**. Публичный фронт выпускается только новой версией через
  `Расширение/docs/TWITCH_UPDATE_RELEASE_PLAYBOOK.md`;
- пока новая версия в Review, submitted ZIP неизменяем; backend-деплой обязан
  оставаться совместимым и с текущей Released-, и с новой submitted-версией;
- перед `-Mod` **закрыть игру** — иначе DLL занят;
- прод-деплой во время стрима — только хотфикс падающего.

**Безвредный транзиент:** `tar: timestamp … in the future` — локальные часы впереди
серверных, файлы уже распакованы. Проверить curl'ом и жить дальше.

---

## 5. Проверки

### Бэкенд жив
```bash
curl -s -o /dev/null -w '%{http_code}\n' https://shedoy23.ru/health   # 200
ssh root@31.130.132.224 "supervisorctl status twitchbot"
```

### Миграция реально применилась (а не «скрипт отработал»)
```python
select name, applied_at from migrations_applied where name like 'M9%'
```

### Действие зрителя реально сработало
Смотреть **следствие в боевой базе**, а не ответ эндпоинта. Примеры сегодняшнего дня:
классы — `select class_key from bannerlord_classes where deprecated=0`;
законы — `select status, count(*) from bannerlord_policy_requests group by status`.

### Мод Bannerlord реально что-то сделал
Лог мода: `Документы\Mount and Blade II Bannerlord\Configs\ModLogs\bannerlordlink_ГГГГММДД.txt`
(~6 МБ за стрим, новый файл на день). Полезные метки:
- `[diplo-policy] ... ENACTED policy` — закон применён;
- `[CONTRIB итог]` — заработок за бой по каждому зрителю (киллы/урон/поглощение/очки);
- `[power.stealth]`, `[CrashGuard]`, `[StubbornAssault]` — активки и патчи.

### Пост-стрим триаж — методика целиком

Полный порядок проверок, ловушки и формат находок:
**`Расширение/docs/POSTSTREAM_TRIAGE.md`**. Документ самодостаточный — его можно
отдать другой модели или человеку целиком.

Главное из него в одну строку: **ищи не сообщения об ошибках, а несостоявшиеся
события** — «когда эта механика работала последний раз».

### Открытые баг-репорты зрителей
```sql
select id, message, created_at from bug_reports where status='open' order by id desc
```
Помечать `resolved` сразу по факту фикса, не копить.

### Заготовки, записанные авансом (вассалы и заявки на мир)

Появилось 28.07 вместе с фиксами ворот выпуска. Обе записи создаются ДО того,
как мод сходит в игру, и раньше не закрывались никогда. **Доказательство, что
уборка работает — по этим маркерам в логе:**

| Маркер | Что означает |
|---|---|
| `[VAS-CLEANUP]` | отказ снёс заготовку вассала |
| `[PEACE-CLEANUP]` | отказ закрыл заявку на мир по её `action_id` |
| `[PEACE-EXPIRE]` | суточный сторож закрыл заявки без отказа (в т.ч. до M101) |

Сколько зависшего осталось прямо сейчас:
```sql
select count(*) from bannerlord_vassals where vassal_clan_id like 'pending\_%' escape '\';
select count(*) from bannerlord_peace_offers where status='pending'
  and offered_at < datetime('now','-24 hours');
```
Оба должны стремиться к нулю. Ненулевой первый — норма только если строки
моложе 10 минут (идёт создание). Ненулевой второй дольше часа = сторож не
работает, смотреть `[PEACE-EXPIRE]` в логе.

**Чего эти числа НЕ значат.** Ноль фантомов не означает, что вассалы создаются:
lookup наследника в моде до сих пор не починен, и создание падает штатно. Ноль
здесь значит только «неудача не оставляет следов».

---

## 6. Ловушки

Полный список того, обо что уже спотыкались. Каждая стоила часов.

### Тихое проглатывание ошибок
- **`INSERT OR IGNORE`** прощает НЕ ТОЛЬКО дубликат — любое нарушение целостности.
  Так очередь команд RimWorld молча теряла все строки с миграции m1 (нарушение
  `NOT NULL` на `channel_id`). Подавление ошибки должно быть **узким**.
- **Финализатор, который глотает исключение**, меняет вылет на молча сломанную
  функцию. Сначала декомпиль и прицельный фикс, глушить — последнее средство.

### Событие мода не объявлено в манифесте
Новый `PostEventAsync("bannerlord", "hero.X", …)` **молча не работает**, пока
`- hero.X` не добавлен в `modules/bannerlord/manifest.yaml`. Кусало трижды
(`hero.properties_snapshot`, `hero.restore_profile`, `hero.policy_result`).
Симптом: мод пишет «отправил», бэкенд ничего не делает.

### ACK мод-действия не доказывает выполнение
`ActionPoller` подтверждает `success=true` **до** реальной работы. Судить об
исходе по `module_actions` нельзя — нужен отдельный результат-эвент.

**И даже `error_msg` в `module_actions` нельзя считать полным журналом
отказов.** На стриме 28.07 мод отправил 20 `action.failed`, а в БД осталось
19 строк с ошибкой: поздний ACK одной из них стёр `REFUNDED:` и записал
`error_msg=NULL`. В постстрим-триаже сравнивать число `action.failed` в
мод-логе с числом отказов в БД и разбирать каждый diff по `action_id`.
Живой случай и запросы:
`Расширение/docs/POSTSTREAM_TRIAGE_2026-07-28.md`, PT-28-01.

### Статус без терминального состояния
Если статус может остаться промежуточным навсегда — его никто не закрывает, и это
баг ещё до того, как что-то сломается. Заявки на законы висели `pending` вечно, а
уникальный индекс по `pending` превратил это в полную блокировку механики.

### Файлы фронта делят ОДНУ глобальную область

`viewer.js` распилили на `pets.js` / `shop.js` / `duels.js` / …, но подключаются
они обычными `<script>` — значит все функции верхнего уровня валятся в общее
пространство. **Два файла с функцией одного имени — законный JavaScript: тот,
что грузится позже, молча заменяет первый.** Ни ошибки, ни предупреждения.

Реальный случай (2026-07-22, час диагностики): `pets.js` и `shop.js` оба
объявляли `async function _buyItem`. `pets.js` подключён позже
(`extension.html:469` против `461`) → **весь магазин RimWorld** уходил в
`/api/pet/purchase`, передавая URL вместо идентификатора, и получал «Предмет
не найден в каталоге» — при полностью исправном каталоге. В nginx-логе
33 запроса к питомцам и **ни одного** к RimWorld.

Чтением кода не ловится: каждый файл по отдельности корректен. Ловится
линтером — `check_frontend_global_collisions` (сверяет только те файлы,
что грузятся одной оболочкой; `const`/`let` не проверяет — их
переобъявление даёт громкую ошибку и потому не опасно).

**Правило:** функции модуля называть с префиксом модуля (`_rwBuyItem`,
`_bnrBuyAction`), а не общими словами.

### Деплой НЕ ставил зависимости (исправлено 2026-07-27)

Скрипт копировал код и перезапускал сервис, но `pip install` не запускал **никогда**.
Пакет попадал в `requirements.txt` и не попадал на сервер — падала ровно та ветка
кода, которая его импортирует.

Так и вышло: дашборд переехал на jinja2-шаблоны 26.07, пакет добавили в
requirements тогда же, на прод он не поехал. С деплоя 27.07 **все страницы
стримера отдавали 500**, включая возврат после авторизации Twitch. `/health` при
этом был зелёный: он проверяет базу, а не страницы. Нашли только когда владелец
зашёл руками.

Теперь `deploy.ps1` ставит зависимости (и падает, если не встали) и после
рестарта дёргает реальные страницы: `/`, `/extension.html`, `/overlay.html`,
`/privacy.html`, `/terms.html`. Последние две — URL из заявки в Twitch, их
падение равно отказу на ревью.

### Поломка, которая только пишет в лог, — не обработана

**Класс ошибки, стоивший трёх недель.** Обновление OAuth-токена канала сломалось
10 июля, честно поймало это и написало в лог. **448 раз.** И всё. Ни уведомления,
ни отметки в базе, ни видимого следа. Зрители тратили очки канала и не получали
крустики; узнали 27 июля случайно.

Почему не заметили автоматикой — механика важнее самого случая:
```
⚠️  OAuth refresh failed: 400 {"status":400,"message":"Invalid refresh token"}
```
Печатается обычным `print()` со значком, **уровня логирования нет**. Не содержит
ни `Traceback`, ни `[ERROR]` — то есть проверка «свежих ошибок» в `preflight`
честно показывала **0** при 448 таких строках. Она не врала: она ищет словарь,
а не severity.

**Вывод, применимый шире:** гоняться за всеми возможными формулировками
бессмысленно. Проверять надо ФАКТ, а не его описание в логе. Добавлено в
`preflight.ps1`: спросить у Twitch, жив ли токен и есть ли
`channel:read:redemptions`.

**Правильное лечение (ещё не сделано):** сбой продления должен всплывать туда,
где его видно — карточка в дашборде или сообщение в телеграм. Preflight помогает
только когда его запускают.

### Токен в базе ЗАШИФРОВАН — не проверяй его напрямую

`channels.oauth_access_token` хранится шифротекстом с префиксом `enc:`. Если взять
значение из базы и спросить у Twitch, жив ли токен, — придёт **401 на исправном
токене**. 2026-07-27 я на этом чуть не объявил рабочую авторизацию сломанной.

Правильно — брать через код приложения:
```bash
# на проде, из папки backend
../venv/bin/python -c "
import sys, asyncio; sys.path.insert(0,'.')
from dependencies import set_db; from database import Database
db = Database('viewers.db'); set_db(db)
from routes.streamer import get_fresh_oauth_token
async def m():
    await db.init_pool(); print(bool(await get_fresh_oauth_token(98319857)))
asyncio.run(m())"
```
Готовая проверка с вердиктом — `scripts/preflight.ps1`.

### Переавторизация канала Twitch — ДВА шага, второй обязателен

1. владелец открывает `https://shedoy23.ru/api/streamer/auth/start`,
   **залогинившись на Twitch под нужным каналом**, и подтверждает доступ;
2. **рестарт `twitchbot`** — подписки EventSub создаются ТОЛЬКО при старте
   (`main.py`, `register_eventsub_subscriptions` в startup-задаче). Без рестарта
   свежий токен просто лежит.

Рестарт рвёт коннекты зрителей → не во время стрима и не в окне ревью.
Успех виден в логе: `✅ eventsub: channel.channel_points_custom_reward_redemption.add registered`.

### Схема в коде разъехалась со схемой миграции
`CREATE TABLE IF NOT EXISTS` на уже мигрированной базе **не срабатывает** —
и код остаётся с досетевой схемой. Проверять обе.

### Мультитенантность
Каждый запрос к тенант-таблице обязан включать `channel_id`. Гейтится линтером,
но `rimworld.py` из проверки **исключён** (`tenant-lint: skip-file`) — и это
исключение спрятало реальный баг ровно того класса.

### Дашборд — обычный HTML-шаблон (было: f-строка Python)
**Изменилось 2026-07-26 — старое правило «дублируй каждую `{`/`}`» БОЛЬШЕ НЕ
ДЕЙСТВУЕТ и вредно:** в шаблоне скобки пишутся один раз, удвоение сломает CSS.

Разметка живёт в `backend/templates/streamer_dashboard.html`, рисуется через
`_render_template()` (jinja2). Функция `_dashboard_html` осталась, но теперь она
подставляет данные в шаблон, а не склеивает строку.

**Автоэкранирование Jinja выключено НАМЕРЕННО** — экранирует вызывающий код
(`html.escape` для HTML, `json.dumps` + защита от `</` для JS). Включить =
экранировать дважды.

Шаблон кэшируется → правка требует рестарта. Проверка перед деплоем:
```bash
python tests/test_dashboard_render.py
```

### Windows / оболочки
- в `cmd` `cd` **не меняет диск** — нужен `cd /d`; амперсанд в «Mount & Blade»
  рвёт команду без кавычек;
- PowerShell 5.1: нет `&&`, нет тернарника; `.ps1` без BOM читается как ANSI →
  скрипты держать ASCII-only;
- Python-запись файла без `close()` может не долететь до диска — использовать `with`.

---

## 7. Моды

### Сборка
```powershell
dotnet build BannerlordLink\src\BannerlordLink.csproj -c Release   # платформенный мод
dotnet build C:\Users\Edward\Desktop\SiegeSanity\src\SiegeSanity.csproj -c Release
dotnet build C:\Users\Edward\Desktop\MassUpgrade\src\MassUpgrade.csproj -c Release
dotnet build RimLink\Source\RimLink.csproj -c Release              # RimWorld
```
DLL подхватывается **только при старте игры**, горячей замены нет.

### Логи модов
| Мод | Файл |
|---|---|
| BannerlordLink | `Документы\...\Configs\ModLogs\bannerlordlink_ГГГГММДД.txt` |
| Siege Sanity | `Документы\...\Configs\SiegeSanity.log` |
| Mass Upgrade | `Документы\...\Configs\MassUpgrade.log` |
| RimLink | `AppData\LocalLow\Ludeon Studios\RimWorld by Ludeon Studios\Player.log` |

### Публикация в Steam Workshop — три способа тихого провала

Утилита `TaleWorlds.MountAndBlade.SteamWorkshop.exe` **рапортует `Finished...`,
не сделав ничего**. Все три грабли собраны 2026-07-22:

1. **XML-декларация или комментарий перед `<Tasks>`** — тул читает
   `xmlDocument.FirstChild` и перебирает его детей; если первый узел не `<Tasks>`,
   список задач пуст. Файл обязан начинаться с `<Tasks>`.
2. **Относительный `ModuleFolder`** — внутри вызывается `LoadWithFullPath`, нужен
   абсолютный путь.
3. **Превью больше 1 МБ** — Steam отвергает. Сжимать (1280px, jpg ~200 КБ).

Успех выглядит так: `Item created. Item ID is …` и `Uploading done!`.
**Проверять всегда по дате обновления на странице Workshop**, а не по выводу.

### Опубликованные моды
| Мод | ID | Репозиторий |
|---|---|---|
| Mass Upgrade | 3724776428 | github.com/Shedoy23/Mass-Upgrade |
| Siege Sanity | 3769206755 | github.com/Shedoy23/Siege-Sanity |

---

## 8. Выглядит сломанным

Список того, что вызывает вопросы, но работает как задумано (или уже объяснено).

| Что видно | Что на самом деле |
|---|---|
| У ассасина кнопка **«Стойкость»**, а он от неё исчезает | техническая заглушка: новый ключ силы без ярлыка во фронте = кнопки нет вообще. Одобрение получено 28.07 — переименование ушло в фронт-батч 0.0.2 (DEFERRED §A) |
| `rimworld_catalog` — 0 строк | мёртвый дубль; настоящий каталог живёт в `shop_catalog` |
| `damage_reflect_pct` есть в коде, но ни у кого не работает | пассивка не выдана ни одному классу — мёртвый груз, не баг |
| В магазине RimWorld 493 предмета вместо 2559 | лимит nginx снят 2026-07-22; обновится при следующем запуске игры |
| Зрителям доступно 12 законов из 32 | список зашит руками во фронте; линтер об этом предупреждает |
| Все пешки/колонисты RimWorld пустые | в последней сессии была новая игра, 0 пешек. **Не проверено** — нужен запуск с реальной колонией |
| Папка `_autotest/` не в гите | не наша, лежит с прошлых сессий, никто не трогал |
| `[lint][warn] policy-catalog: …20 unreachable` | намеренное напоминание, не ошибка |

---

## 9. Ограничения

### Публичный фронт версионирован Twitch
Первая заморозка закончилась 2026-07-28, но Released-версия на Twitch CDN
по-прежнему неизменяема по правилам платформы. Любая правка HTML/JS/CSS/assets
выходит как новая версия: Local Test → ZIP → Hosted Test → Review → Approved →
ручной Release. Подготовка следующей версии не затрагивает текущую публичную.

Канонический процесс, матрица «что требует ревью», совместимость EBS двух
поколений и аварийный план:
`Расширение/docs/TWITCH_UPDATE_RELEASE_PLAYBOOK.md`.

Бэкенд можно хотфиксить отдельно только с сохранением контракта текущего
CDN-фронта. В день Release старые endpoints/поля не удалять: уже открытые
вкладки некоторое время продолжают выполнять прежний JS.

### Две валюты, не путать
💎 крустики — платформенные очки (зарабатываются просмотром/чатом).
💰 динары — внутриигровое золото. 1 💎 = 5 динаров. Действие списывает **одну**
валюту по смыслу. Боевые награды — только динары и опыт (решение владельца).

### Платное действие обязано ловить тихий no-op
Движковое «сделай X» часто молча ничего не делает (кап, дубль, нет цели). Хендлер
отдаёт success → списание без эффекта. Детектить по **наблюдаемому** эффекту
(before/after публичными геттерами), не по копии внутренней формулы движка,
и на провале возвращать деньги.

### Заряд и эффект — в одной транзакции
`add_points`/`remove_points` открывают своё соединение и коммитят отдельно. Для
платных действий использовать `add_points_tx`/`remove_points_tx` на общем `conn`,
иначе крах в окне = деньги списаны без эффекта.

---

## Что проверять первым при «всё сломалось»

1. `curl https://shedoy23.ru/health` → 200?
2. `supervisorctl status twitchbot` → RUNNING?
3. `/var/log/twitchbot.out.log` — свежий Traceback?
4. Открыта ли процессом та база, что ты смотришь (см. §2)?
5. Если трогали фронт — совпадает ли cache-bust в `extension.html` и `mobile.html`?
6. Если мод — смотреть **лог мода**, а не догадки: он пишет и отказы, и причины.

---

### История: заморозка деплоя перед первым ревью Twitch

> Период ниже завершён 2026-07-28 после approval и Release `0.0.1`.
> `$FreezeUntil` должен быть удалён из `deploy.ps1`. Для следующих версий
> используем version-specific gate из
> `Расширение/docs/TWITCH_UPDATE_RELEASE_PLAYBOOK.md`, а не вечную заморозку
> всего проекта.

`deploy.ps1` сам не пустит на прод: `-Backend`/`-Frontend` выходят с кодом 1 и
печатают, что делать вместо этого. Владелец попросил это прямо своими словами —
«не дай мне сломать прод до аудита», а обещание не механизм.

**Что считается КРИТИЧНЫМ** (только ради этого лезем в прод):
прод лежит · деньги теряются или списываются дважды · данные текут между
каналами · само ревью не может состояться.
Всё остальное — на локальной копии, катим после вердикта.

Разрешено и во время заморозки:
```bash
./scripts/deploy.ps1 -DryRun      # показать, что было бы
./scripts/deploy.ps1 -Staging     # staging на :8001, не прод
./scripts/deploy.ps1 -Mod         # DLL в игру, прод не участвует
python scripts/local-setup.py     # полная копия на своём ПК
```

Настоящая авария — одним флагом, но с письменной причиной:
```bash
./scripts/deploy.ps1 -Backend -CriticalReason "что именно горит"
```
Причина обязательна намеренно: пока формулируешь, становится видно, авария это
или нетерпение. Все обходы пишутся в `scripts/deploy-freeze-overrides.log`.

**После вердикта** — снять `$FreezeUntil` из `deploy.ps1` (или сдвинуть дату
под следующее окно). Заморозка, про которую забыли, однажды остановит нужный
хотфикс.

## 10. Локальная копия и поддельный мод

Полный стек на своём ПК: тот же бэкенд, тот же фронт, база — копия боевой из
вчерашнего бэкапа. Twitch о ней не знает, прод не участвует.

```bash
python scripts/local-setup.py      # развернуть; печатает команды запуска
```
Кладёт базу в `%USERPROFILE%\shedstream-local\` — **вне репозитория, намеренно.**
Локальная база рядом с кодом 2026-06-25 положила прод (её журнал уехал в архив
деплоя и затёр боевой). `deploy.ps1` теперь такие файлы исключает, но держать
базу снаружи — вторая линия защиты, независимая от той строки.

Путь к базе бэкенд берёт из `DB_PATH` (по умолчанию `viewers.db` — на
проде поведение не меняется). `FRONTEND_PATH` обязателен, иначе статика не
монтируется и копия открывается пустой (404 на `/static/*`).
Проверка: `curl http://127.0.0.1:8000/health` → `{"status":"ok","db":"ok"}`,
`/static/extension.html` → 200.

### Поддельный мод — проверка модуля без запуска игры

`scripts/fake-mod.py` подключается по Module API как настоящий мод: здоровается,
шлёт события, забирает команды, подтверждает. Нужен потому, что у RimWorld нет
другого способа себя проверить — владелец в него не играет, и модуль полтора
месяца тихо расходился с реальностью.

```bash
python scripts/fake-mod.py --channel <id> --token <module-token>            # обычная работа
python scripts/fake-mod.py --channel <id> --token <t> --refuse-all          # проверка рефанда
python scripts/fake-mod.py --channel <id> --token <t> --drop-all            # зависшие команды
python scripts/fake-mod.py --channel <id> --token <t> --double-ack          # идемпотентность
python scripts/fake-mod.py --channel <id> --token <t> --send-event <тип>    # одно событие
```
Токен: из папки backend —
`python -c "import sys; sys.path.insert(0,'.'); from routes.streamer import issue_module_token; print(issue_module_token(98319857,'rimworld'))"`

Ловит класс «событие не объявлено в манифесте» мгновенно: отправка неизвестного
типа печатает `ОТВЕРГНУТО: event_not_in_manifest` и выходит с кодом 1. Этот
класс дважды жил в проде незамеченным.

**По не-локальному адресу не пойдёт** без явного `--i-know-this-is-prod`: он
пишет настоящие события в базу.

---

## 11. Вопросы владельца и регулярная уборка

### Три вопроса, которые не устареют

Они про суждение, а не про процесс, поэтому их нельзя автоматизировать —
и задавать их можно годами.

**1. «Что это будет стоить, если ты ошибся?»**
Определяет, сколько проверять ПЕРЕД действием. Локальная правка — ошибайся
свободно, переписывается. Прод, Steam Workshop, публичный релиз, миграция
данных — сначала думай. Самый дорогой класс ошибок проекта живёт именно тут:
казино построено и вырезано, механики по мотивам BLT и последующий аудит
лицензии, фронт, замёрзший на чужом CDN.

**2. «Чем докажешь?»**
Не «ты уверен» (на это я отвечу «да»), а «покажи». На втором либо показываю,
либо обнаруживаю, что показать нечем. Отсюда растут «видел красным?» и
«не протухло ли?».

**3. «Кому от этого станет лучше?»**
Живому зрителю на живом стриме — или только нам внутри? Второе иногда
необходимо, но если перевешивает, мы строим инфраструктуру вместо продукта.
Слово «платформа» очень удобно это оправдывает.

### Семь частных случаев — строительные леса

Каждый выведен из реального промаха и **должен со временем исчезнуть**: вопрос,
который приходится задавать месяцами, — признак несделанной автоматизации, а не
постоянный инструмент.

| Вопрос | Когда | Из какого промаха | Куда девается |
|---|---|---|---|
| «Что из этого уже есть?» | до того как я пишу новый инструмент | 2026-07-26: написал второй поддельный мод, не заметив первый в `tests/` | должно стать моей привычкой |
| «Это обратимо?» | всё, что уходит наружу | казино, BLT, замёрзший фронт | частный случай вопроса №1, останется |
| «Ты видел это красным?» | я отчитываюсь, что работает | 2026-07-26: едва не отрапортовал нерабочий фикс — смотрел на код конвейера, а он возвращал успех фильтра | **правило 2b в CLAUDE.md** |
| «Не протухло ли то, на что ссылаешься?» | я пересказываю статус из документа | 2026-07-25: три устаревших факта за день (офсайт, размер `viewer.js`, мониторинг) | **правило 8b в CLAUDE.md** |
| «Что из этого почувствует зритель?» | список дел похож на инфраструктуру | сползание в «платформу» | частный случай вопроса №3, останется |

**Если вопрос три раза подряд дал ответ «всё чисто» — убрать его.** Список
вопросов гниёт ровно так же, как набор тестов.

### Регулярная уборка — это ритуал, а не вопрос

У неё другой ритм: по календарю, а не по поводу. Раз в месяц-полтора.

**«Проведи проверку актуальности тестов».** Прохожу по всему набору и отвечаю по
каждому: что ловит · во что обошлась бы эта поломка · не проверяет ли он ФОРМУ
кода вместо поведения (такие мешают переделкам — худший вид) · ни разу не
краснел и стал структурно невозможен · не дешевле ли перенести в линтер.
Предлагаю удаления списком. Набор гниёт молча: медленный перестают гонять, а
потом пропускают и настоящую поломку.

**Независимый аудит сторонней моделью.** Раз в пару месяцев или перед
подачей на ревью. Бриф — `docs/EXTERNAL_AUDIT_BRIEF.md`, копируется целиком.
Смысл не в «второй модели», а в читателе, который НЕ писал этот код: свои
допущения изнутри не видны. Просить строго три класса — деньги, изоляция
каналов, авторизация; всё остальное даёт шум. Требовать по каждой находке
сценарий отказа и способ доказательства. **Основной помощник проверяет находки
на свой же код и заинтересован их отклонить — отклонение без доказательства не
принимается.**

*Почему это работает — механизм, а не «свежий взгляд» (зафиксировано 2026-07-28
после трёх прогонов).* Слепые пятна порождаются самим авторством:
- **код читается как задуманный, а не как написанный.** Про кассу было известно,
  что она списывает атомарно, — проверялось ровно это. Что путь ПОСЛЕ неё теряет
  цену, в голову не приходило: в модели автора цена уже учтена (PB-01, три
  месяца, 74 случая невозвращённых денег);
- **смотрят туда, где чинили.** Все внутренние прогоны шли по свежим правкам;
  старый «стабильный» код не перечитывает никто;
- **проверки наследуют допущения автора.** Тест кассы писал тот же, кто писал
  кассу, и он покрывает обычную ветку — потому что автор думал о кассе как об
  одной ветке. Такой тест структурно не может поймать непредставленное.

*Как использовать правильно, по результатам прогонов:*
- **давать ДАННЫЕ, а не только код.** Аудит по копии боевой базы дал 3 из 3 —
  лучший результат; аудит по одним исходникам давал 12 из 15 и один ложный
  блокер. Данные не разделяют авторских допущений вообще;
- **не перечислять заранее, что уже проверено** — иначе внешний наследует ту же
  рамку и перестаёт быть внешним;
- **в бриф класть, какие URL обслуживает бэкенд, а какие лежат в архиве.**
  Единственный ложный блокер (privacy/terms «отдают 404») родился именно из
  того, что в пакете этого не написали;
- просить сценарий отказа и способ проверки, а не мнение о качестве.

**«Что мы можем удалить?»** Мёртвый код, неиспользуемые механики, устаревшие
документы, дубли инструментов, мусор в рабочей папке — списком, с оценкой, что
сломается. Площадь поддержки растёт сама и сокращается только руками; без этого
ритуала правило «фича входит — фича выходит» не работает.
